# Executor-Next Command Layer Review — July 2026

**Scope:** `crates/executor-next` (103 `Command` variants, protocol DTOs, error boundary, metadata/IDL tooling, session model) and its fitness as the single standardized command layer for every downstream consumer: `cli-next`, the V1 Python SDK, the V1 Node SDK, and the V1 MCP server (`strata mcp serve`).
**Method:** 12 parallel dimension auditors (consumer parity × 5, design quality × 3, contract conformance × 4), each grounded in the executor charter (`docs/executor/executor-charter.md`), the V1 error contract, and CLAUDE.md hard rules. The old Python/Node/MCP surfaces in their sibling repos were used as *requirement sets* (evidence of what real consumers shipped), filtered against the V1-removed feature list. Every critical/high finding was adversarially re-verified by an independent default-reject agent that re-read the cited code.
**Findings:** 124 total — 3 critical, 41 high, 54 medium, 26 low. All 44 critical/high findings were verified: **17 confirmed, 21 downgraded, 6 rejected**. Medium/low findings are reported unverified.

**Status: findings report only — no fixes are made by this document.**

---

## Verdict

The executor core is genuinely well-engineered and honors its charter: `dispatch.rs` is a pure policy-free match, all 68 facade methods are provably lossless single-command wrappers, the error boundary is the strongest surface in the crate (every construction site lands on a registered `<class>.<area>.<detail>` code), and the wire format is disciplined (internally tagged commands, `deny_unknown_fields`, test-verified unknown-field rejection). The shared vocabulary — `PageInfo`, `CommitReceipt`, `MutationEffect`, `BatchResult`, uniform branch/space addressing — is legitimately Stripe-grade design.

What keeps it from being a shippable world-class surface is **completeness for the M9 consumers**, not craftsmanship. Five structural gaps dominate: (1) **no conditional write exists anywhere** — the state primitive was deliberately removed pre-V1 in favor of `txn.cas()`, but V1 then removed public transactions too, leaving no safe read-modify-write for the product's agent-memory positioning; (2) **execution is fully serialized** — `execute` takes `&mut self` on a non-cloneable handle, so a threaded SDK or MCP server cannot run two reads concurrently; (3) **no read-only open / access mode** despite being a plan deliverable; (4) **branch merge/diff have no commands** (and no engine implementation) although V1 explicitly defines strict-refusal merge semantics; (5) the **metadata/IDL pipeline is architecturally excellent but schema-less and unconsumed** — no output schemas, nothing reads the catalog, and cli-next hand-writes a parallel clap tree with no drift guard, which is exactly how two sources of truth diverge.

A second tier of confirmed wire-quality defects will be breaking changes if not fixed before SDK cutover: `Bytes` serializes as JSON integer arrays (not base64), `KvScan` always reports a terminal page even when it truncates, `EventGetByType` duplicates `EventList`, config is read-only, graph time travel is implemented in the engine but unreachable, and Arrow vector import hardcodes the Cosine metric. All of these are cheap now and expensive after M9.

---

## Adjudicated cross-unit themes

Several findings were raised independently by multiple units; the adversarial verifiers sometimes split. Resolutions:

**T1 — Conditional writes (state / CAS) — REAL GAP, adjusted framing.** Three units flagged the missing state primitive (PY-1, NODE-1, MCP-2). Verifiers established: state was formally removed pre-V1 (issue #1606 / PR #1861, 2026-03-24, rationale "redundant with KV+transactions and `txn.cas()`"), so the "silent removal" framing is false and PY-1/MCP-2 were rejected. But V1 *also* removed public manual transaction sessions — the documented successor — and no `put-if-absent` / `put-if-version` exists anywhere in executor-next or engine-next. NODE-1 (critical) is CONFIRMED on that residual: **no consumer can perform a safe concurrent read-modify-write.** The fix is a product decision (conditional-write options on the KV family), owned by engine semantics per the charter.

**T2 — Serialized execution — CONFIRMED (high).** NODE-11 confirmed; SESS-1 downgraded only because single-process CLI use masks it today. `execute(&mut self)` on a non-`Clone` handle means Python/Node SDKs and the MCP server serialize every operation, including pure reads. Needs an interior-mutability or handle-clone design before M9.

**T3 — Executor imports inference-next directly — REAL, downgraded (medium ×3).** ENGINE-4, THIN-1, DTO-3 all found the same thing: 13 `Inference*` commands dispatch straight into `strata-inference-next` and embed its types in the wire `Output`. CLAUDE.md rule 5 routes consumers through intelligence-next (M8). Verifiers downgraded because M8 doesn't exist yet and the dependency is transitional — but it must be re-pointed at intelligence-next when M8 lands, and the inference types must not freeze into the wire contract before then.

**T4 — `Bytes` on the JSON wire — CONFIRMED (high, ×2).** DSGN-5 and DTO-2 independently: `Bytes` serializes as an array of u8 integers in JSON output (`[104,101,...]`), no base64; the CLI reverse-engineers strings from it and JSON keys echo back as byte arrays. Human-hostile, bloats payloads ~4×, and every SDK would have to special-case it. Must be fixed before any wire consumer ships.

**T5 — Metadata model: excellent skeleton, missing organs — CONFIRMED (META-1, META-4); coverage complaints REJECTED.** The 32/103 IDL coverage (META-2, MCP-4) is documented slice sequencing, not a defect. What IS confirmed: the metadata model has **no argument/output schemas** (so MCP tool generation, SDK stubs, and `strata agents guide` — first-run design D3/D8 — cannot be generated from it), and **nothing consumes it** (cli-next hand-writes a parallel clap surface with no cross-check test). The schema gap is a model-shape decision that gets more expensive every slice it waits.

**T6 — KV bytes-only value model — NOT A DEFECT.** PY-2 and NODE-6 (both initially critical/high) were rejected with strong evidence: bytes KV + typed JSON capability is a documented deliberate V1 design (engine-kv-parity plan Design Decision 1; core-architecture assigns the canonical user value model to engine-next). Residual action: state the "KV is opaque bytes; use JSON for typed values" contract explicitly in consumer-facing docs so SDKs don't reinvent typed encodings.

**T7 — Planned-gap ledger.** Findings tagged with a milestone are absences with a credible planned home, judged only on whether the executor has a reviewed slot ready. Search/retrieval commands (M6 substrate, M8 stages), auto-embed surface (M8), config-write (M9), remaining IDL families (M9). The recurring recommendation: **reserve the wire shapes now** (a reviewed `Search` command signature, config-write key registry) so M8/M9 land into designed slots instead of inventing contracts late.

---

## Dimension summaries

| Unit | Dimension | Findings (C/H/M/L) | Verified (✓/↓/✗) | Verdict summary |
|---|---|---|---|---|
| PY | Python SDK parity | 2/4/3/3 | 1/3/2 | The executor-next command surface covers the old Python SDK's KV/JSON/event/vector/space/branch-lifecycle namespaces well (~78 of 118 public members map cleanly, with pagination and as_of time-travel reads generally superset-quality). Four holes are serious: the entire db.state namespace (including cas/init optimistic concurrency) has zero command coverage and no emulation path since nothing in executor-next or engine-next offers a conditional write; KvPut/KvGet moved to raw bytes so the SDK's typed-value contract cannot ride the wire without each consumer inventing a private encoding; config is read-only (no ConfigureSet), stranding set_auto_embed/configure_model/use_provider/set_embed_model; and time_range plus branch merge/diff vanished even though time travel and strict-refusal merge are declared V1 pillars. Search and the auto-embed pipeline are absent but credibly planned for M8. |
| NODE | Node SDK parity | 1/4/6/0 | 2/2/1 | The executor-next command surface cleanly covers ~75% of the old Node SDK: KV/JSON/event/vector CRUD, spaces, branch lifecycle/fork, and inference map well (often as supersets with better pagination, batch envelopes, and structured errors replacing the old "[CODE] message" string parsing). The serious holes are the silently-dropped state-cell primitive (no CAS/put-if-absent anywhere in 103 commands), 19 of 33 graph methods (ontology, BFS, analytics) with no command slots, and vector filtering collapsed from 8 operators to Eq-only. Node-specific: `execute(&mut self)` plus a non-cloneable Database forces full per-handle serialization of concurrent Promises; there is no progress/cancellation affordance for multi-GB model pulls; and Bytes serialize as JSON integer arrays on the generic dispatch path. Wire shapes are otherwise textbook TypeScript discriminated unions. Transactions, follower mode, bundles, flush/compact/retention were correctly filtered as V1-removed. |
| MCP | MCP server parity | 0/4/5/2 | 1/1/2 | The old MCP server registers 63 tools (not 61); after filtering V1-removed surfaces (txn, bundles, retention, flush/compact), 44 of them map cleanly onto executor-next Command variants, and the stateless branch/space-Option-per-command design elegantly replaces the old session switch tools. The §8 escape hatch is structurally ready today: Command derives Deserialize with serde tag="type"/snake_case/deny_unknown_fields, cli-next's run --command-json is exactly that machinery, and defaults.yaml pins mcp_name to strata_{family}_{op_slug}, so name+JSON-args dispatch is mechanical. The real risks are metadata, not vocabulary: the IDL catalog covers only 32/103 commands (kv+vector only) and carries no per-command input schemas, so neither the curated ~15-20 tool schemas nor the escape-hatch catalog can be generated yet; and the biggest semantic losses are cross-primitive search (planned M6/M8), branch diff/merge (planned M6), and the undocumented disappearance of the state primitive's CAS/init conditional writes. |
| CLI | CLI parity and thinness | 0/4/5/2 | 1/3/0 | Direction 1 is healthy: every implemented cli-next verb is a mechanical translation to exactly one Command, the --json success path is a faithful serde serialization of Output, and command run/print --command-json is a genuine canonical structured entrypoint (full Command vocabulary, deny_unknown_fields). Direction 2 is weaker: 25 of 103 Command variants (all 11 batch ops, EventBatchAppend, GraphBatchWrite, GraphBindingsForEntity, and all 12 inference commands) have no first-class verb, and GraphAddNode's binding field is hardcoded off. The structural risks are (a) two unreconciled sources of truth for the CLI surface — the executor's generated CLI command catalog already disagrees with the handwritten clap tree on names it ships as "stable" — and (b) split session-state ownership: the executor session model has only default_branch while space defaults live in CLI context, producing a concrete asymmetry where --space applies to verbs but is silently ignored by command run. |
| ENGINE | Engine capability reachability | 0/4/4/2 | 2/2/0 | The implemented engine-next D4 surface (admin, spaces, branches, KV/JSON/event/vector/graph) is reached almost 1:1 by the 103 commands with zero dispatch stubs and generally high output fidelity (commit receipts, generations, index diagnostics all carried). The real gaps are structural: the M6 buckets (branch power ops, retrieval/recipes/derived-state, IPC classification, clone artifacts) do not exist in engine-next at all and the Command enum has no slot for them; and two reachability holes where the engine is ahead of the executor - graph time travel and version-pinned reads are fully implemented in engine services but no command can invoke them. One contract violation: the 13 Inference* commands import inference-next directly, bypassing the intelligence layer required by hard rule 5. |
| DSGN | Design consistency (Stripe-grade) | 0/5/6/1 | 3/2/0 | The executor's shared vocabulary (PageInfo, CommitReceipt, MutationEffect, BatchResult, uniform branch/space addressing) is genuinely Stripe-grade design; verb deviations (JsonSet/VectorUpsert/EventAppend) are domain-justified. The failures are in adoption discipline: KV and Event only partially honor the pagination model (unbounded defaults, a false "Defaults to 100" doc on KvList, and PageInfo::terminal() emitted on truncatable results so has_more lies), catalog mutations use six different result envelopes including bare Bool with no commit facts, and Bytes serialize as integer arrays on the JSON wire — the reference CLI already mis-decodes them. One outright charter violation: EventGetByType is a strict subset of EventList, a second public surface for the same operation. |
| EVOL | Wire evolution and versioning | 0/3/4/1 | 0/3/0 | The wire format itself is in good shape: Command is internally tagged (`type` + snake_case + deny_unknown_fields, unknown-field rejection test-verified), Output is adjacently tagged (`type`/`data`), optional fields consistently use Option + serde(default) (the correct additive pattern), and roundtrip/name-lock/golden-fixture tests pin the contract. What is missing is everything around evolution: no runtime-discoverable protocol/schema version (only the engine package version string in Pong/Info/Describe, while the IDL's `strata.idl.v1` is never exposed), no structured error code for the unknown-command/unknown-field version-skew failure (CLI surfaces raw serde prose), no additive-change or deprecation playbook anywhere in docs/executor/ (IDL wire_status allows only stable\|transitional), zero `#[non_exhaustive]` in the crate (including ExecutorErrorClass, a rule-28 violation), and the IDL/SDK-generation substrate covers only 32 of 103 commands. Nothing here blocks shipping v1.0 SDKs, but v1.1 cannot ship additively without first closing the version-identifier, decode-error, and playbook gaps. |
| ERR | Error boundary | 0/2/2/4 | 2/0/0 | The executor error boundary is the strongest-engineered surface in the crate: every construction site in src/executor/ and src/arrow/ lands on a registered <class>.<area>.<detail> code, completeness is enforced by source-scanning tests on three crates plus a runtime fallback that preserves unregistered codes in details, the taxonomy is fully machine-actionable (class + per-code reviewed retry_policy + commit_outcome serialized on every status), redaction is clean and test-verified, and tests assert codes/classes, never prose. The gaps are hardening, not blockers: three public wire enums missing #[non_exhaustive] before format freeze, the engine source chain and reference-id log correlation silently dropped at the boundary, a lossy legacy second class taxonomy with no deletion condition, and the first-run 7.2 "errors that teach" pillar being ~90% done in the registry but 0% rendered on the CLI human channel. |
| THIN | Charter conformance (thinness / one model) | 0/2/6/4 | 1/1/0 | The executor core genuinely honors the charter: dispatch.rs is a pure policy-free match, services.rs is uniform validate-resolve-dispatch, all 68 facade methods are provably lossless single-command wrappers, converts are mechanical with exhaustive enum maps, and open_cache/open_durable_local carry no product OpenSpec policy (defaults live in engine option constructors). The real drift is at the edges: the inference family imports inference-next directly and embeds its serde types in Command/Output (hard-rule 5 violation pending M8), arrow vector import invents product policy (auto-create collection with hardcoded Cosine metric), and KV/JSON mutation effects are fabricated by racy executor pre-reads while the engine owns the same fact for vectors — split authority plus an N+1 read tax. Batch semantics are engine-adapted and declared via BatchMode, but duplicate-key policy, default list limits, and pagination authority diverge across families in executor-invented ways. |
| META | Machine-readable metadata readiness | 0/4/3/2 | 2/1/1 | The metadata pipeline (authored IDL overlay -> command-index.json -> embedded cli-command-index.json + runtime CliCommandCatalog) is architecturally excellent — deterministic, checksummed, CI-freshness-gated, with fixtures round-tripped through the real serde types — but it covers only 32 of 103 Command variants (kv + vector, a documented slice boundary) and contains zero machine-readable argument or output schemas, so none of the roadmap codegen targets (MCP inputSchema, SDK stubs, strata agents guide) can be generated from it yet. There is no exhaustiveness guard: adding a new Command variant fails nothing. Most concerning for rot: nothing user-facing consumes the catalog — cli-next hand-writes its entire clap surface with no cross-check against the metadata, despite the implementation plan claiming strata explain/commands are implemented. |
| SESS | Session and lifecycle model | 0/2/4/2 | 1/1/0 | The Executor lifecycle shape is fundamentally right for SDKs: one handle per database, reopen-after-close and execute-after-close (FailedPrecondition/ClosedHandle) are tested, double-close is idempotent by construction, and branch defaulting is uniform across all 103 variants with no manual-transaction leakage anywhere in the public surface. The two real gaps are session-model shrinkage versus the governing plan — the promised Session (branch + space + access mode) collapsed to a single default_branch string, so there is no read-only access mode (which the old Python SDK exposes at open) and no session space default — and an undeclared concurrency contract: Executor is Send+Sync (compile-verified) but fully &mut, so every command including reads serializes through one lock, with zero concurrent-use tests pinning any of it. Smaller seams: create_branch_from_head bypasses command dispatch, ConfigureGetKey is a redundant legacy-named projection of ConfigGet, and executor constructors expose no open options (memory budget) without a direct engine-next import. |
| DTO | Protocol DTO quality | 0/3/6/3 | 1/2/0 | The DTO layer is structurally strong: consistent {type,data} envelope, uniform pagination/commit/effect facts, found-flag results that distinguish null from missing, structured per-item batch errors, and inputs that move (not copy) payloads into the engine. The main defects are contract-hardening gaps rather than design flaws: KV keys/values have no size cap while storage's manifest reader hard-bounds keys at 4KB (write/read asymmetry), Bytes serializes as an int-array on the JSON wire (every JSON consumer pays 4x and the CLI heuristically re-decodes), zero non_exhaustive attributes anywhere (rule 28 violated verbatim, and every M8 addition breaks SDK builds), and Output embeds strata-inference-next types directly against hard rule 5. Read paths also deep-clone every value/embedding where a move is available. |

---

## Confirmed findings (17)

Critical/high findings whose evidence, impact, and severity survived independent adversarial verification.

### NODE-1 — State-cell primitive (incl. CAS and put-if-absent) silently dropped with no conditional-write replacement

- **Severity:** **critical**  |  **Category:** missing-core-operation  |  **Unit:** NODE (Node SDK parity)
- **Evidence:** ~/Documents/GitHub/strata-nodesdk/stratadb.d.ts:706-716 defines StateNamespace with `cas(cell, newValue, {expectedVersion})` and `init(cell, value)`. No State* variant exists in crates/executor-next/src/command.rs; engine-next has no state capability (crates/engine-next/src/data/ contains only event/graph/json/kv/vector; docs/architecture/engine-architecture.md:88 lists 'KV, JSON, event, vector, and graph'). State is absent from docs/architecture/v1-removed-surfaces.md. KvPut (command.rs:131-142) has no expected-version or if-absent precondition.
- **Consumer impact:** Node SDK cannot ship its db.state namespace (9 methods); Python SDK and MCP memory tools lose the same surface. Worse, no consumer can implement optimistic-concurrency agent state at all — there is no compare-and-swap or create-if-absent write anywhere in the 103 commands, so concurrent agents doing read-modify-write on KV silently lose updates.
- **Recommendation:** Either add conditional-write support to KV (expected_version / if_absent fields on KvPut, honoring MutationEffect) and document state-cells-over-KV as the migration story, or formally add state to the V1-removed-surfaces doc with the sanctioned alternative spelled out.

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

I independently verified every evidence claim: StateNamespace with cas/init exists in the Node SDK typings; command.rs has no State variants and KvPut (lines 131-142) has no precondition fields; engine-next/src/data/ holds only event/graph/json/kv/vector; and grep for expected_version/if_absent/cas/compare_and_swap/optimistic across executor-next and engine-next data returns nothing, so no conditional write exists anywhere in the 103-command surface. I read v1-removed-surfaces.md in full — state cells are absent from all 8 removed items, and the executor-command-migration-checklist never records a disposition for the state command family; the only trace of intent is a one-line note in v1-test-inventory.md:114 that statecell vocabulary is not V1 product language, which drops the name but supplies no replacement for CAS/init semantics. The impact is real, not theoretical: the old crates are already deleted from the workspace, so the M9 SDK/MCP cutover has no surface to rebuild db.state on, and no consumer can do a safe concurrent read-modify-write — a core operation for the product's agent-memory positioning. The recommendation is charter-compatible (semantics in engine, executor stays thin). Evidence, impact, and critical severity all hold for a missing core operation with no documented removal or migration story.

</details>

### CLI-4 — Session-default ownership is split between executor (branch) and CLI (space); --space is silently ignored by `command run`

- **Severity:** **high**  |  **Category:** session-model  |  **Unit:** CLI (CLI parity and thinness)
- **Evidence:** Executor session state is only `default_branch` (executor/mod.rs:167,208-219); space defaults per-command via DEFAULT_SPACE (input_common.rs:12). CLI injects scope.space into every verb-built Command (lib.rs, e.g. 267-268) but `command run` deserializes raw JSON untouched (lib.rs:855-885), so `strata --space app command run --command-json '{"type":"kv_put",...}'` writes to `default` while `strata --space app kv put ...` writes to `app`. REPL `use` mutates only CLI CommandContext (repl.rs:92-107), never the executor default.
- **Consumer impact:** Charter §2 assigns branch AND space session context to the executor; instead every frontend (CLI today, SDKs/MCP tomorrow) must reimplement space-default injection, and CLI users hit a silent wrong-space write when combining --space with the canonical structured entrypoint.
- **Recommendation:** Add `with_default_space`/session-scope to the executor and have the CLI (including `command run`) rely on executor-resolved defaults instead of per-command injection; at minimum error when --space is combined with `command run`.

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

I independently verified every cited location: executor session state holds only default_branch (executor/mod.rs:167, 208-219) with no space analog; space defaults per-command to DEFAULT_SPACE via product_space (input_common.rs:12) and Command's space fields are Option with serde(default). The CLI applies --branch to the executor via with_default_branch (lib.rs:74-77) but injects --space only into verb-built commands, while `command run` (lib.rs:143, 855-885) deserializes raw JSON with no scope applied — so `--space app command run` with space-less JSON silently writes to `default`, and REPL `use` (repl.rs:92-107) mutates only the CLI context. The executor charter §2 explicitly assigns branch context, space context, and current-session defaults to the executor, so the recommendation aligns with the charter rather than contradicting it, and spaces/command-run are not on the V1-removed list. The impact is a real silent wrong-space write on the canonical structured entrypoint plus a charter-violating split that every future frontend must re-solve, so high severity holds.

</details>

### DSGN-2 — KvScan has no pagination contract: always returns PageInfo::terminal() even when limit truncates, and inclusive start prevents overlap-free resumption

- **Severity:** **high**  |  **Category:** pagination  |  **Unit:** DSGN (Design consistency (Stripe-grade))
- **Evidence:** crates/executor-next/src/executor/kv.rs:114-117 returns page: PageInfo::terminal() unconditionally, so a limit-truncated scan reports has_more:false; command.rs:197-199 start is 'Optional inclusive start key' with no cursor param. This defeats the invariant-checked PageInfo model (types/common.rs:71-80).
- **Consumer impact:** Node SDK exposed kvListPaginated-style iterators (strata-nodesdk/index.d.ts:190); an SDK scan iterator built on KvScan either silently drops rows (trusting has_more) or must use the fragile 'returned < limit' heuristic plus manual key-successor arithmetic to avoid re-reading the boundary key.
- **Recommendation:** Give KvScan the same exclusive-cursor + has_more contract as KvList (it is the only bulk key+value read, so it must paginate honestly).

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

I read kv.rs:99-118 and confirmed execute_kv_scan returns PageInfo::terminal() unconditionally even when the engine scan truncated at limit; command.rs:197 confirms inclusive start with no cursor; engine service.rs shows scan returns a bare Vec with no has_more signal while list_page implements the honest limit+1 pattern the executor already uses for KvList. The v1 wire fixture (responses/v1/kv/scan_page.json) pins has_more/cursor on kv_scan_result, so the contract actively misreports completion rather than omitting pagination — a silent-data-loss trap for any client, and it is being frozen into the SDK-facing IDL now, making a later fix a breaking wire change. The finding's SDK cite (kvListPaginated) is list-based, not scan-based, so present-day consumer harm is prospective — but this is a pre-cutover design review of the only bulk key+value read, where a lying serialized field is exactly the class of defect severity 'high' should flag. Nothing in the executor charter or V1 out-of-scope list defers scan pagination, and the recommendation matches the existing KvList pattern in the same crate.

</details>

### DSGN-3 — EventGetByType is a strict functional subset of EventList — two public surfaces for one operation — and its page facts are fake

- **Severity:** **high**  |  **Category:** canonical-path  |  **Unit:** DSGN (Design consistency (Stripe-grade))
- **Evidence:** command.rs:799-817 EventGetByType{event_type,limit,after_sequence,as_of} vs command.rs:887-906 EventList{event_type?,limit,after_sequence,as_of}; both return Output::EventRecords. EventGetByType always emits PageInfo::terminal() (executor/event.rs:98-101) despite taking limit+after_sequence, and lacks the alias='cursor' that EventList has (command.rs:901 vs :813).
- **Consumer impact:** Violates CLAUDE.md rule 8 / executor charter 'one command protocol': every SDK must decide which of two commands to wrap for 'list events of type X', and the one with the type-required signature returns lying pagination facts (has_more:false on truncated pages).
- **Recommendation:** Delete EventGetByType (EventList with event_type covers it exactly), or if kept for wire compat, route it through list_page so page facts are honest.

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

I read both command variants myself (command.rs:799-817 vs 887-906) and confirmed EventGetByType duplicates EventList's type-filtered read with a required event_type, missing cursor alias (line 901), and the same Output::EventRecords. The fake-pagination claim is verified in code: executor/event.rs:98-101 returns PageInfo::terminal() while the engine's get_by_type_with_selector (engine-next event/service.rs:406-408) truncates to limit, so truncated pages report has_more:false — and get_by_type_at additionally truncates before applying the as_of filter (service.rs:213-220), unlike list_page (356-362), so the redundant command can also silently drop visible events. Both commands are live public surfaces (facade/event.rs:49-55, exercised by tests), events are core V1 scope (not on the removed list), and the impact is a real wire-contract correctness hazard for any paginating consumer plus a direct violation of the charter's "one command protocol" (executor-charter.md:48) and CLAUDE.md rule 8. The recommendation (delete or route through list_page) enforces rather than contradicts the charter, and high severity is warranted by the silent-data-loss pagination behavior combined with the hard-rule violation.

</details>

### DSGN-5 — Bytes serialize as JSON integer arrays on the wire; JSON document keys are echoed back as Bytes; the reference CLI decodes them with a lossy heuristic

- **Severity:** **high**  |  **Category:** wire-format  |  **Unit:** DSGN (Design consistency (Stripe-grade))
- **Evidence:** types/common.rs:11 Bytes(#[serde(with='serde_bytes')] Vec<u8>) → integer arrays in serde_json; executor/kv_json_convert.rs:189-195 json_write_output wraps the String doc key as Bytes::from(key), so json_set echoes key 'user:1' as [117,115,...]; cli-next/src/render.rs:269-274 bytes_from_json_array heuristically decodes ANY all-int-0..255 array to UTF-8, so a genuine stored JSON array like [72,105] renders as 'Hi'.
- **Consumer impact:** Every JSON-wire consumer (MCP tool results read by LLMs, Python/Node SDKs) must invent its own bytes convention, and the in-repo reference consumer already demonstrates the failure mode with a decoding that corrupts legitimate data.
- **Recommendation:** Pick one wire encoding for Bytes (base64 string is conventional), and echo JSON document keys as String in JSON-family outputs instead of reusing the KV WriteResult envelope.

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

I independently verified all three evidence locations: types/common.rs:11 uses serde_bytes over a serde_json-only wire (render.rs:11, lib.rs:195; no binary codec in executor-next or cli-next Cargo.toml), which yields integer arrays; kv_json_convert.rs:189-195 wraps the String document key as Bytes::from(key) and executor/json.rs:29,70 confirms json_set/json_delete flow through it, so keys echo as int arrays; render.rs:269-274 bytes_from_json_array is applied schema-blind via scalar_summary/raw_scalar (lines 248, 260) including the json_value path (print_maybe_json line 177), so a genuine stored JSON array like [72,105] renders as "Hi" in human/raw output. The impact is not theoretical — the in-repo reference CLI both emits the encoding and demonstrably corrupts legitimate data today, and every external JSON consumer (MCP, SDKs) must invent a decoding convention. The feature is core shipped executor surface, not on the V1-removed list nor M8-planned absence, and the base64/String-key recommendation contradicts neither the executor charter (which mandates no bytes encoding) nor CLAUDE.md (rule 35 wire-stability favors fixing before freeze). High severity holds for a wire-format finding: it affects every consumer, has a demonstrated corruption in the shipping reference CLI, and wire formats ossify.

</details>

### DTO-2 — Bytes serializes as an array of u8 integers on the JSON wire; no base64; CLI reverse-engineers it heuristically

- **Severity:** **high**  |  **Category:** value-model  |  **Unit:** DTO (Protocol DTO quality)
- **Evidence:** crates/executor-next/src/types/common.rs:10-11 'struct Bytes(#[serde(with = "serde_bytes")] Vec<u8>)' — under serde_json this emits [104,101,...]; deserialization also accepts a JSON string (asymmetric round-trip). crates/cli-next/src/render.rs:249,261,269 bytes_from_json_array(...).and_then(String::from_utf8) heuristically re-decodes int arrays as strings.
- **Consumer impact:** Every JSON transport consumer (MCP tools which emit JSON results, Node SDK bridging, CLI --format json users) sees KV keys/values as integer arrays: ~4x payload size, unreadable, and ambiguous (a genuine numeric array is indistinguishable from bytes, so the CLI heuristic can misrender data). Old consumers expect string keys / JSON values (strata-nodesdk/index.d.ts:59 kvPut(key: string, value: any)).
- **Recommendation:** Pick and freeze a canonical human-format encoding for Bytes — base64 with a tagged shape (e.g. {"b64": ...}) or UTF-8 string with escape fallback — via a custom Serialize keyed to format, and delete the CLI array-decoding heuristic. Keep compact binary encoding for binary codecs. Document in the IDL.

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

I read common.rs:10-11 and render.rs:242-274 and empirically reproduced the wire behavior in a scratch crate with the same serde stack: Bytes serializes to an integer array ([104,101,...]) under serde_json and deserializes from both an array and a JSON string, confirming the asymmetric round-trip. The CLI is a real shipped consumer: options.rs exposes --json/--format json and render.rs:11-18 serializes executor responses (whose KV keys/values are Bytes per kv_branch.rs) directly via serde_json, while render.rs:248-274 contains the exact bytes_from_json_array + String::from_utf8 heuristic, which would misrender a genuine numeric array (e.g. [72,105] → "Hi") — the ambiguity is demonstrable. The finding is not on the V1-removed list, and the recommendation (canonical tagged encoding, delete heuristic) aligns with CLAUDE.md rule 8 (one canonical path) and rule 35 (wire stability); the executor charter contains nothing contrary. The only weakness is the strata-nodesdk/index.d.ts citation, which does not exist in this repo, but it is supplementary to otherwise fully verified evidence. As a value-model defect on the public command boundary affecting every JSON consumer, high severity holds.

</details>

### ENGINE-1 — Graph time travel fully implemented in engine but unreachable from any command

- **Severity:** **high**  |  **Category:** missing-exposure  |  **Unit:** ENGINE (Engine capability reachability)
- **Evidence:** crates/engine-next/src/data/graph/service.rs:302,312,392,513,608,694 implement get_node_at/_at_version, list_nodes_at, get_edge_at, neighbors_at, bindings_for_entity_at; crates/executor-next/src/executor/dispatch.rs:496-652 - no Graph* command carries an as_of field ("Command::GraphGetNode { branch, space, graph, node_id }").
- **Consumer impact:** CLI cannot offer --as-of on graph commands and MCP/Python/Node SDKs cannot ship a uniform time-travel story: every other capability (KV/JSON/event/vector) exposes as_of, so graph reads silently become latest-only, breaking cross-capability temporal queries that are a headline Strata feature.
- **Recommendation:** Add as_of to GraphGetNode/GraphGetEdge/GraphNeighbors/GraphListNodes/GraphList/GraphGetMeta/GraphBindingsForEntity and route to the existing *_at engine methods; engine work is already done, this is command-DTO plumbing only.

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

I verified both sides of the gap myself: crates/engine-next/src/data/graph/service.rs implements get_node_at/get_node_at_version (302/312), list_nodes_at (392), get_edge_at (513), neighbors_at (608), bindings_for_entity_at (694), plus list_graphs_at (170) and graph_info_at (229), and they are exercised by engine-next/tests/engine_graph.rs — so this is shipped engine behavior, not M8-planned work and not on the V1-removed list. In crates/executor-next/src/command.rs every as_of field (12 occurrences) belongs to KvGet/KvList/JsonGet/JsonList/VectorGet/VectorQuery/VectorIndexQuery/EventGet/EventGetByType/EventLen/EventListTypes/EventList, while none of the 14 Graph* variants (lines 917-1128) carries one, and dispatch.rs 496-652 confirms no graph handler receives a temporal parameter; I also confirmed no alternate temporal surface covers graph. Consumer impact is real, not theoretical: cli-next already wires as_of 34 times across other capabilities and zero times for graph, so the CLI literally cannot offer --as-of on graph reads today, and I found no deferral note in docs/executor/* or the command migration checklist tracking this gap. The recommendation matches the charter — kv.rs and event.rs in executor-next already do exactly this pattern (Option<u64> → Timestamp::from_micros → *_at engine method), so it is thin plumbing with no executor business logic. High severity holds: an entire capability silently drops a headline cross-capability feature that the engine fully implements, with no tracking; the only workaround (BranchForkAtVersion to materialize past state) is heavyweight and non-uniform.

</details>

### ENGINE-2 — Branch power operations (merge/diff/revert/cherry-pick/promote) absent from both engine and executor

- **Severity:** **high**  |  **Category:** planned-gap  |  **Unit:** ENGINE (Engine capability reachability)
- **Planned milestone:** M6
- **Evidence:** crates/engine-next/src/branch/service.rs:36-234 - BranchService ends at list/get/create/fork_current/fork_at_version/fork_at_timestamp/delete; docs/architecture/strata-v1-implementation-roadmap.md:537-538 requires "compare, promote, copy, restore, revert, cherry-pick"; docs/executor/executor-minimal-surface-implementation-plan.md:304-308 promises diff/merge/merge-base/diff-three-way commands (EX4).
- **Consumer impact:** No downstream consumer (CLI, Python, Node, MCP) can ship the V1 branching workflow beyond fork/delete; even the V1 strict-refusal merge path has no surface to refuse from.
- **Recommendation:** Track as M6 engine work + EX4 executor slice; when adding, reserve Command variants for diff/merge/merge-base/revert/cherry-pick so the wire enum does not churn twice.

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

I independently read crates/engine-next/src/branch/service.rs (the service ends at list/get/create/create_from_head/fork_current/fork_at_version/fork_at_timestamp/delete — no merge/diff/revert/cherry-pick/promote) and grepped crates/executor-next/src/command.rs (branch variants are only BranchCreate/BranchForkCurrent/BranchForkAtVersion/BranchForkAtTimestamp/BranchDelete), so the evidence holds exactly as cited. The roadmap Phase 6 item 3 requires compare/promote/copy/restore/revert/cherry-pick and the executor plan EX4 promises diff/merge/merge-base/diff-three-way/revert/cherry-pick, and none of these appear on docs/architecture/v1-removed-surfaces.md (only branch bundles are removed); CLAUDE.md hard rule 20 makes strict-refusal merge mandatory V1 scope, so this is required work, not deliberately dropped. Consumer impact is real and structural: with no engine method and no wire Command variant, no CLI/SDK/MCP consumer can reach any branch operation beyond fork/delete, including the mandated merge refusal path. The finding correctly self-categorizes as a planned gap targeting M6/EX4, and its recommendation (engine owns semantics, thin executor adapter, reserve wire variants) is consistent with the executor charter's thin handlers/branch.rs guidance and CLAUDE.md rule 7. High severity is justified because an entire headline V1 workflow (hard rules 19-21) is absent from both layers.

</details>

### ERR-1 — Public wire enums ExecutorErrorClass, RetryPolicy, CommitOutcomeStatus are not #[non_exhaustive]

- **Severity:** **high**  |  **Category:** contract-violation  |  **Unit:** ERR (Error boundary)
- **Evidence:** crates/executor-next/src/error.rs:128 (ExecutorErrorClass, no attribute); crates/engine-next/src/diagnostics/error.rs:48 (RetryPolicy) and :73 (CommitOutcomeStatus) — both serde-serialized and re-exported through executor-next; only ErrorClass (:10) and EngineErrorClass (:205) carry #[non_exhaustive]. CLAUDE.md rule 28 and contract Source Chains rule 6: 'Public error enums should be #[non_exhaustive]'; the contract explicitly anticipates adding classes later.
- **Consumer impact:** Python/Node SDK bindings and any Rust embedder that matches exhaustively on retry_policy or commit_outcome (the fields the contract tells them to branch on) break at compile time when V1.x adds a variant — the enums freeze into the wire contract at V1 and can never grow.
- **Recommendation:** Add #[non_exhaustive] to ExecutorErrorClass, RetryPolicy, and CommitOutcomeStatus before the M9 surface freeze; it is a one-line change per enum now and a breaking change forever after.

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

I read all cited locations myself: ExecutorErrorClass (crates/executor-next/src/error.rs:130), RetryPolicy (crates/engine-next/src/diagnostics/error.rs:49), and CommitOutcomeStatus (:74) carry no #[non_exhaustive], while ErrorClass (:10) and EngineErrorClass (:206) do — a grep for non_exhaustive across both crates confirms those are the only two annotated enums. All three unannotated enums are on the D4 public surface: ExecutorErrorClass is re-exported at executor-next/src/lib.rs:27, and RetryPolicy/CommitOutcomeStatus are re-exported at lib.rs:33 and returned by public accessors ErrorStatus::retry_policy()/commit_outcome() (error.rs:248/260), the exact fields the contract tells consumers to branch on. The rule is real and binding: CLAUDE.md hard rule 28 and docs/architecture/v1-error-and-diagnostics-contract.md lines 655 and 727 state public error enums are #[non_exhaustive]. This is not on the V1-removed list and not a planned-milestone gap; the consumer impact holds for Rust embedders (exhaustive matches compile-break on any V1.x variant addition, and the fix becomes semver-breaking after the M9 freeze), though the Python/Node phrasing is slightly loose since serde string deserialization is unaffected by the attribute. The one-line recommendation contradicts nothing in the executor charter; severity high is justified because the violation permanently freezes the wire enums if not fixed before M9.

</details>

### ERR-2 — Engine error source chains are dropped at the boundary and reference ids correlate to no log anywhere

- **Severity:** **high**  |  **Category:** diagnostics-fidelity  |  **Unit:** ERR (Error boundary)
- **Evidence:** crates/executor-next/src/error.rs:481-485 — From<EngineError> reads only value.status(), discarding EngineError.source (engine-next/src/diagnostics/error.rs:233); error.rs:513 `impl Error for ExecutorError {}` exposes no source(). No tracing/log call exists in executor-next or cli-next src. Contract §Error Surface field 5 requires the boundary to write the reference id 'into both the rendered status and the correlated internal log line'; §Engine To Command Boundary rule 5: 'Keep source chains available for logs and diagnostics.'
- **Consumer impact:** CLI users are shown '(err_local_000042)' (cli-next/src/render.rs:283-291) that points to nothing — support and self-debugging via the reference id is impossible for every consumer (CLI, Python SDK, Node SDK, MCP), and the underlying root cause (e.g. the storage error behind internal.engine.persistence) is unrecoverable once the boundary is crossed. Ids also restart at 000001 per process (error.rs:515-520), so they collide across runs.
- **Recommendation:** At the From<EngineError>/render boundary, emit one structured internal log line (tracing) carrying reference_id + code + full source chain; make the default id source process-unique (timestamp or random prefix). Keep the status itself pure per the contract.

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

I verified every cited location: `From<EngineError>` at crates/executor-next/src/error.rs:481-485 calls only `value.status()`, discarding the `source: Option<Arc<dyn Error>>` field defined on EngineError (crates/engine-next/src/diagnostics/error.rs:233-237), and `impl Error for ExecutorError {}` at error.rs:513 exposes no source(); a grep for tracing/log across executor-next and cli-next src and Cargo.toml returned zero hits, so no log sink exists for the reference id to correlate to, while cli-next/src/render.rs:282-291 does print the id to users. Both contract quotes hold verbatim: v1-error-and-diagnostics-contract.md line ~329 requires the boundary to write the id "into both the rendered status and the correlated internal log line," and §Engine To Command Boundary rule 5 (line 579) requires keeping source chains available for logs — so this is a violation of explicit contract rules, not a missing feature; the executor implementation plan contains no planned logging slice. The id-collision claim is also real (SequentialErrorReferenceIdSource starts at AtomicU64::new(1) in a process-static OnceLock, error.rs:38, 515-520). The impact is not theoretical: every error surfaced through any consumer today shows a dangling reference id, and the root cause behind internal.engine.persistence-class errors is irrecoverably lost at the boundary; this is in-scope executor work, not on the V1-removed list and not M8 intelligence. The recommendation (structured log at the boundary, injectable id source kept) is consistent with the charter's thin-adapter role and with the contract, which prohibits only construction-time random ids, not boundary-minted process-unique ones. High severity is warranted within a diagnostics-fidelity review since the finding defeats the entire purpose of the reference-id mechanism for all consumers.

</details>

### MCP-3 — BranchDiff and BranchMerge missing from executor vocabulary though V1 keeps both

- **Severity:** **high**  |  **Category:** missing-operation  |  **Unit:** MCP (MCP server parity)
- **Planned milestone:** M6
- **Evidence:** Old tools strata_branch_diff / strata_branch_merge (~/Documents/GitHub/strata-mcp/src/session.rs:140-154); CLAUDE.md rule 20 keeps merge with strict refusal; roadmap Phase 6 item 3 'branch create, branch-from-version, branch-from-time, compare, promote, ... conflict handling'; no Diff/Merge/Compare/Promote variant in command.rs
- **Consumer impact:** The agent branching workflow (fork -> experiment -> diff -> merge back) is Strata's signature MCP story; without diff/merge commands, MCP, CLI, and both SDKs can only fork and abandon. Planned at M6, and the branch family has a credible slot (fork variants already exist).
- **Recommendation:** Track BranchDiff/BranchCompare and BranchMerge (strict-refusal semantics, typed conflict error code) as required executor additions when M6 branch semantics land; add them to the IDL branch family at the same time.

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

Verified all cited evidence directly: executor-next/src/command.rs contains only List/Get/Create/Fork*/Delete branch variants with no Diff/Merge/Compare/Promote; the old MCP session.rs:140-154 exposes diff_branches and merge_branch; roadmap Phase 6 item 3 (lines 537-538) commits V1 to compare/promote/conflict handling and CLAUDE.md rule 20 keeps merge with strict refusal, so this is not deliberately removed (only branch bundles are, and the finding does not request those). The finding correctly reports a planned-M6 vocabulary gap rather than a shipped bug — engine-next branch/service.rs also lacks merge/diff, matching the M6 framing. Impact is real, not theoretical: docs/executor/executor-minimal-surface-implementation-plan.md:302-309 requires a branch power-op adapter covering fork/diff/merge/merge-base/diff-three-way, yet docs/executor/executor-command-migration-checklist.md:165-166 already marks BranchDiff/BranchMerge as [x] migrated despite no code existing, meaning the ledger would let this gap ship silently. The recommendation (track for M6, engine-owned strict-refusal semantics with a typed conflict code, thin executor variants) is consistent with the executor charter and CLAUDE.md hard rules, and high severity holds for a missing required operation family in the product's signature branching workflow.

</details>

### META-1 — No machine-readable argument or output schemas — MCP tool schemas, SDK stubs, and 'strata agents commands' cannot be generated from this metadata

- **Severity:** **high**  |  **Category:** codegen-readiness  |  **Unit:** META (Machine-readable metadata readiness)
- **Planned milestone:** M9
- **Evidence:** cli_metadata.rs:254-257 — input is a bare variant ref ('Executor command variant reference' / `pub input: String` = "Command::KvPut"); idl_tooling.rs:1134-1147 forbids field declarations in command YAML ("command YAML may only reference executor DTOs"); no schemars/JSON-Schema derivation exists anywhere in executor-next. Spec requires "name, args, types, output shape, examples" (docs/design/first-run-experience.md:322) and MCP tools require inputSchema (~/Documents/GitHub/strata-mcp/src/tools/mod.rs:33).
- **Consumer impact:** MCP (61 tools need JSON Schema inputs), Python/Node SDK stub generation, and the strata agents commands catalog are all blocked: the only per-arg names/types/descriptions live in non-machine-readable doc comments on the Command enum. dto-inventory.yaml is a flat string allowlist with no structure behind response_model.
- **Recommendation:** Add a schema-derivation stage (schemars on Command/Output or a generated dto-schema.json validated against fixtures) plus per-arg descriptions sourced from the existing doc comments; keep the YAML overlay thin by referencing the derived schemas rather than authoring them.

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

I independently verified every cited location: cli_metadata.rs:254-257 exposes `input` as a bare String variant reference ("Command::KvPut") with no arg-level structure; idl_tooling.rs:1134-1147 actively rejects `fields:`/`schema:`/`properties:`/`request_schema:`/`response_schema:` in command YAML ("command YAML may only reference executor DTOs"); a grep of executor-next found zero schemars/JsonSchema usage (only an unrelated arrow infer_json_schema call); and dto-inventory.yaml is confirmed to be a flat string list of response_model names. The consumer impact is real, not theoretical: docs/design/first-run-experience.md §7 explicitly designates this metadata as the generation source for MCP tool schemas, SDK docstrings, and `strata agents commands` ("name, args, types, output shape, examples — generated from executor metadata"), and the existing strata-mcp repo has 63 hand-authored ToolDef::new calls each requiring a JSON inputSchema that cannot be derived from any current executor artifact — the only per-arg names/types live in Rust doc comments on the Command enum. The gap is architectural rather than merely incomplete, because the IDL validator forbids the schema data from ever entering the YAML while providing no derivation alternative. The finding is not on the V1-removed list, is not misreporting planned M8 intelligence work, and the recommendation (derive schemas from executor DTOs, keep YAML thin) is consistent with the executor charter's position that the executor defines the public operation model, and violates no CLAUDE.md hard rule. Severity high holds given the design doc calls this the centerpiece of the agent story and three planned consumers depend on it.

</details>

### META-4 — Nothing consumes the metadata — cli-next hand-writes a parallel clap surface with no cross-check, and the plan's 'Implemented' status for strata explain/commands is false

- **Severity:** **high**  |  **Category:** consumption  |  **Unit:** META (Machine-readable metadata readiness)
- **Planned milestone:** M9
- **Evidence:** Grep for cli_metadata|CliCommandCatalog across crates/ hits only executor-next itself; crates/cli-next/src/options.rs defines all ~103 subcommands by hand and help comes from clap (repl.rs:88 `Cli::command().print_long_help()`); no explain/commands subcommand exists. Yet v1-idl-overlay-cli-generation-implementation-plan.md:4-6 claims "Implemented for ... `strata commands`, `strata explain`, help rendering".
- **Consumer impact:** CLI users/agents get none of the discovery surface the metadata was built for, and two machine descriptions of the CLI (clap tree vs catalog paths like 'kv batch-get') can silently diverge — unconsumed metadata rots and undermines the one-canonical-description principle (charter/CLAUDE.md rule 8).
- **Recommendation:** Wire strata commands/strata explain into cli-next from CliCommandCatalog (the plan's stated deliverable), and add a conformance test asserting every metadata path_display resolves to a real clap subcommand path and vice versa for covered families; correct the plan's Status section.

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

I independently reproduced every evidence claim: grep confirms CliCommandCatalog/cli_metadata is consumed only inside executor-next itself; cli-next/src/options.rs hand-defines the full clap tree with help from clap (repl.rs:88), and no explain/commands subcommand exists in cli-next or the strata-idl bin. Git history strengthens the finding: commit 3ef7c331 actually implemented strata commands/strata explain in cli-next, then 3448fba0 ("Clean up v1 workspace") deleted the whole implementation with no documented descoping, and the plan's Status section still says "Implemented" — so the doc describes deleted code. The feature is not on the V1-removed list, and the recommendation aligns with the plan's own architecture, the executor charter, and CLAUDE.md rule 8 (one canonical description). Impact is concrete: ~1,900 lines of generated metadata plus a 669-line runtime module are dead code, the authoritative plan misleads consumers, and the hand-written CLI has no conformance check against the catalog, so high severity holds.

</details>

### NODE-11 — Executor::execute takes &mut self on a non-cloneable handle, serializing all concurrent SDK calls

- **Severity:** **high**  |  **Category:** async-concurrency  |  **Unit:** NODE (Node SDK parity)
- **Evidence:** crates/executor-next/src/executor/dispatch.rs:7 `pub fn execute(&mut self, command: Command)`; Executor owns `database: Database` (executor/mod.rs:165-170) and engine Database owns StoragePersistence/ControlPlane by value with no Clone (crates/engine-next/src/api/database.rs:137-144).
- **Consumer impact:** A Node NAPI binding must guard the handle with a Mutex, so `Promise.all` over reads executes strictly sequentially — a regression from the old SDK, which ran concurrent reads on the libuv thread pool ('All data methods are async — they run on a blocking thread pool', index.d.ts:50-51). Python (GIL-released threads) and any IPC server front-end hit the same single-lane bottleneck.
- **Recommendation:** Make read-path execution possible through &self (interior mutability below the boundary) or provide a cheap shareable/cloneable executor handle, so language bindings can dispatch concurrent reads.

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

I verified all three cited locations myself: dispatch.rs:7 takes &mut self, Executor owns Database by value with no Clone, and engine Database (database.rs:137-144) owns StoragePersistence/ControlPlane by value with no Clone — and the constraint is deep, since every read service accessor (kv, json, vectors, events, graphs at database.rs:173-273) also takes &mut self, so any multi-threaded consumer must serialize all operations behind one Mutex. Concurrency is not on the v1-removed-surfaces list or CLAUDE.md out-of-scope list, and no architecture doc documents a deliberate single-threaded handle; meanwhile the V1 IPC contract explicitly plans one handler thread per connection with read-only IPC clients allowed, which this handle would bottleneck to a single lane. The one weak evidence point is the old-SDK index.d.ts quote, which is not present in this repo (old crates deleted on the V1 branch), but the code-level claim stands independently. The recommendation (interior mutability below the boundary or a shareable handle) does not contradict the executor charter's thin-dispatch model or any CLAUDE.md hard rule. Impact is prospective (current cli-next is one-shot and unaffected), but this is an API-boundary review and the &mut propagates through the frozen-ish engine D4 surface, making it exactly the kind of high-cost-to-fix-later shape defect a boundary review exists to catch — high severity holds.

</details>

### PY-3 — No config-write command: configure_set and the entire provider/auto-embed config surface is stranded

- **Severity:** **high**  |  **Category:** missing-command-family  |  **Unit:** PY (Python SDK parity)
- **Planned milestone:** M9
- **Evidence:** Executor-next has only ConfigGet and ConfigureGetKey (command.rs:49-54). Old SDK ships configure_set/configure_model/configure_anthropic/openai/google/local/use_provider/set_embed_model/set_auto_embed (__init__.py:735-861); the legacy executor had ConfigureSet, ConfigureModel, ConfigSetAutoEmbed (docs/executor/executor-command-migration-checklist.md:227,246,248).
- **Consumer impact:** Python SDK cannot implement 8 shipped config methods; CLI 'strata config set' equivalents and MCP configuration tools are read-only. Users cannot enable auto-embed, pick an embed model, or configure providers through any V1 surface.
- **Recommendation:** Add a ConfigureSet command (sanitized key registry, redaction per rule 31) in the executor surface; provider/embedding keys can land with M8 but durability/product keys need it at cutover (M9).

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

I independently verified every evidence citation: executor-next's Command enum (crates/executor-next/src/command.rs:49-54, full variant list enumerated) contains only ConfigGet and ConfigureGetKey with no config-write variant anywhere; the shipped Python SDK (strata-python/python/stratadb/__init__.py:735-861) implements configure_set plus 7 wrapper methods that all require it; and the migration checklist (docs/executor/executor-command-migration-checklist.md:227,246,248) marks ConfigureSet/ConfigureModel/ConfigSetAutoEmbed as "[x] migrated and owned by the new executor" even though they are absent from the code — a ledger violation of the checklist's own rule that dropped commands need an explanatory note. The feature is not on the V1-removed list; the executor charter explicitly lists config as a thin command family belonging in executor, so the recommendation is charter-consistent, and the finding correctly separates M8 provider/embed keys from M9 keys like "durability" rather than misreporting planned intelligence work. Impact is concrete: inference/generation commands already exist in executor-next but no surface can configure a provider, embed model, or auto-embed, stranding 8 shipped SDK methods and any CLI/MCP config-write path. High severity holds for a whole missing command family with a stale migration ledger claiming it was ported.

</details>

### SESS-2 — Access mode dropped from the session model: no read-only open despite plan deliverable and old Python SDK surface

- **Severity:** **high**  |  **Category:** session-model  |  **Unit:** SESS (Session and lifecycle model)
- **Evidence:** docs/executor/executor-minimal-surface-implementation-plan.md:188-201 promises 'A minimal Session model covering: branch context, space context, access mode' with 'access-mode enforcement' tests; grep read_only over executor-next/src and engine-next/src/api = zero hits; old SDK exposes it: ~/Documents/GitHub/strata-python/python/stratadb/__init__.py:657 `def open(path, read_only=False)`.
- **Consumer impact:** The Python SDK cannot honor its existing `read_only=True` open flag and MCP cannot offer a safe read-only server mode; combined with the storage single-writer lock there is no way to open a durable database for inspection without acquiring full write authority. Read-only open is not on the V1 removed-features list.
- **Recommendation:** Either implement the planned access mode (a ReadOnly open option on DurableLocalOpenOptions surfaced through Executor, rejecting mutating commands with a stable failed_precondition.executor.read_only code) or explicitly amend the charter/plan to remove access mode from V1 so SDK authors drop the parameter deliberately.

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

I reproduced every evidence claim: the plan (executor-minimal-surface-implementation-plan.md:186-201) and charter (executor-charter.md:80-105) both promise session access mode and access-mode enforcement, while grep over executor-next and engine-next finds zero enforcement code — only per-command read/write doc-metadata strings — and DurableLocalOpenOptions (engine-next/src/api/options.rs:51) has no read-only field; the Python SDK read_only flag exists at stratadb/__init__.py:657. Read-only open is not on the V1 removed list; the roadmap product-coverage table (line 133) and engine-architecture.md runtime bucket (lines 345-419) explicitly commit read-only open and access-mode enforcement to V1. Critically, this is not merely planned-for-later: executor-command-migration-checklist.md:59 falsely claims the EX2 kernel "now owns ... access-mode enforcement", and downstream plans (executor-spaces-admin-test-plan.md:25,205; cli-next-implementation-plan.md:102) are deferred/conditional on a read-only handle that no slice owns delivering. The impact holds because storage-next takes an exclusive single-writer lock (local_fs.rs:1056 try_lock_exclusive), so without read-only mode a durable database cannot be opened for inspection without full write authority. The recommendation matches the charter and violates no CLAUDE.md rule, so severity high stands.

</details>

### THIN-2 — Arrow vector import auto-creates collections with hardcoded Cosine metric

- **Severity:** **high**  |  **Category:** policy-drift  |  **Unit:** THIN (Charter conformance (thinness / one model))
- **Evidence:** crates/executor-next/src/arrow/import.rs:249 'metric: VectorDistanceMetric::Cosine' inside create_vector_collection, invoked at import.rs:182-185 whenever the target collection does not exist, with dimension inferred from the first non-null row. Charter: import/export must not become a data-portability policy surface.
- **Consumer impact:** A CLI or MCP user importing euclidean or dot-product embeddings gets a silently-created Cosine collection; every subsequent vector_query returns semantically wrong rankings with no error anywhere. This is invented product policy (default metric + implicit DDL) living in executor.
- **Recommendation:** Require the collection to pre-exist (fail with a structured not_found telling the user to create it), or take an explicit metric parameter on ArrowImport; never default a semantic like distance metric in the executor.

<details><summary>Adversarial verification</summary>

**Verdict: CONFIRMED**

I read crates/executor-next/src/arrow/import.rs myself: line 249 hardcodes VectorDistanceMetric::Cosine in create_vector_collection, called from lines 182-185 whenever collection_exists (line 164) is false, with dimension inferred from the first row — the quoted evidence is exact. I verified Command::ArrowImport (command.rs:1141) and the CLI options (cli-next/src/options.rs:976-987) expose no metric parameter, so a real CLI/MCP consumer with euclidean or dot-product embeddings gets a silently Cosine collection and semantically wrong vector_query rankings with no error, exactly as claimed. Notably, the canonical Command::VectorCreateCollection (command.rs:492) requires an explicit metric with no serde default, so the import path smuggles a semantic default around a deliberate requirement — textbook policy drift under the executor charter (docs/executor/executor-charter.md lines 289-295, 376: arrow/ must not be a second policy surface) and CLAUDE.md rule 7. The feature is not on the V1-removed list and the recommendation aligns with, rather than contradicts, the charter; the silent-wrong-results failure mode plus implicit DDL justifies keeping severity at high.

</details>

---

## Downgraded findings (21)

Real issues whose severity was reduced on verification — typically because the harmed consumer does not exist until M8/M9, or a mitigating behavior exists. They remain work items at their corrected severity.

### CLI-1 — 13 shipped batch/binding Command variants have no CLI verb, including 7 the executor's own catalog marks as stable CLI paths

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** surface-gap  |  **Unit:** CLI (CLI parity and thinness)
- **Evidence:** KvBatchPut/Get/Delete/Exists, JsonBatchSet/Get/Delete, VectorBatchUpsert/Get/Delete, EventBatchAppend, GraphBatchWrite, GraphBindingsForEntity all dispatch in crates/executor-next/src/executor/dispatch.rs:81-99,144-158,365-398,629-652 but have no verb in crates/cli-next/src/options.rs; the generated catalog crates/executor-next/idl/v1/generated/cli-command-index.json defines `kv batch-put`, `vector batch-upsert` etc. with wire_status "stable".
- **Consumer impact:** CLI users have no ergonomic bulk ingest/read path (only hand-writing --command-json blobs), so scripted loading — the CLI's core embedded-DB workflow — is far worse than the Python/Node SDKs which get typed batch calls.
- **Recommendation:** Add batch-* verbs (with --file JSONL/JSON input) for kv/json/vector/event/graph plus `graph bindings`, matching the catalog's already-published paths.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

Evidence verified: all 13 batch/binding variants dispatch at the cited lines in crates/executor-next/src/executor/dispatch.rs, cli-next has zero batch verbs, and the catalog marks 7 batch paths wire_status=stable. However, docs/architecture/cli-command-coverage.md explicitly classifies these as "Raw Only — Available through `strata command run` but not ergonomic yet; Ergonomic batch syntax remains a follow-up," and I confirmed `strata command run --command-json/--command-file` (options.rs:1095-1110) reaches all batch commands, so no consumer is actually blocked. The parity plan (cli-next-user-experience-parity-implementation-plan.md, Slice 4 step 2) already schedules exactly the recommended fix. The gap is real (catalog publishes stable CLI paths the CLI does not expose; SDKs get typed batch calls the CLI lacks) but it is documented, deliberately deferred, and has a working raw workaround — so high severity for an unnoticed shipped surface gap is overstated.

</details>

### CLI-2 — Two sources of truth for the CLI surface: handwritten clap tree ignores the executor-generated CLI command catalog and already diverges from it

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** contract-violation  |  **Unit:** CLI (CLI parity and thinness)
- **Evidence:** cli-next has zero references to CliCommandCatalog (grep); catalog says `vector metadata update` and `vector index query` (cli-command-index.json) while the CLI implements `vector update-metadata` (options.rs:546) and `vector query --diagnostics` (lib.rs:508-541); catalog covers only 32/103 commands (kv+vector families only). No parity test exists (cli-next has no tests/ directory).
- **Consumer impact:** Every consumer that trusts the executor's generated CLI metadata (docs, MCP tool descriptions, error docs links, fixtures) gets command paths that do not exist in the real binary — a broken discoverability contract and a standing charter violation (one public model, CLAUDE.md rule 8).
- **Recommendation:** Pick one authority: either generate/verify the clap tree from the catalog with a conformance test, or extend the catalog to all 103 commands and rename CLI verbs to match; add a CI parity test either way.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

I reproduced the core evidence myself: cli-next has zero CliCommandCatalog references and no tests dir; the catalog's path_display values are "vector metadata update" and "vector index query" while options.rs defines UpdateMetadata (kebab-cased to update-metadata) and lib.rs:508-541 routes `vector query --diagnostics` to VectorIndexQuery — the verb divergence is real. However, the 32/103 coverage gap and the handwritten clap tree are explicitly planned: v1-idl-overlay-cli-generation-implementation-plan.md scopes the catalog to kv+vector only and defers both remaining families and full command-parser generation. The claimed consumer impact is theoretical today — the catalog's only consumers are executor-next's own module/tests and the strata-idl dev tool; no docs, MCP, SDK, or explain surface consumes it yet. What survives is a real unplanned path divergence between two checked-in artifacts with no parity test, a latent contract bug rather than a shipped high-severity breakage, so medium is the correct severity.

</details>

### CLI-3 — All 12 inference Command variants are CLI-invisible even on inference-enabled builds

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** surface-gap  |  **Unit:** CLI (CLI parity and thinness)
- **Planned milestone:** M9
- **Evidence:** cli-next/Cargo.toml forwards inference features to strata-executor-next, and dispatch.rs:695-765 executes InferenceGenerate/Embed/ModelsList etc., but options.rs defines no models/generate/embed/rank/tokenize verbs — only `command run --command-json` reaches them.
- **Consumer impact:** The CLI cannot demo or operate Strata's AI-database differentiator (model pull, generate, embed) — the flagship `strata models list` / `strata generate` workflows from the old CLI and the MCP requirement set have no V1 CLI equivalent.
- **Recommendation:** Add an inference/models verb family in the M9 CLI slice; the executor surface is complete, so this is pure clap+mapping work.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

I verified the evidence directly: executor-next/src/command.rs:1196-1272 defines exactly 12 inference Command variants dispatched at executor/dispatch.rs:696-749, cli-next/Cargo.toml:14-18 forwards the inference features, and cli-next/src/options.rs contains no inference verbs — only the `command run --command-json`/`--file` escape hatch (options.rs:1100-1122). The feature is not on the V1-removed list and the recommendation does not conflict with the executor charter, which explicitly keeps inference commands as public executor wrappers (executor-charter.md:282-284). But two facts reduce severity: the commands ARE reachable today via the documented raw-command escape hatch, so no consumer is hard-blocked; and docs/architecture/cli-command-coverage.md already records "Inference/model commands" as intentionally "Raw Only ... First-class CLI syntax should be added once UX is settled" — a tracked, deliberate deferral, not an unnoticed surface defect. The gap is real and worth an M9-era slice (the coverage doc itself says the escape hatch is not the target UX), but "high" overstates a documented, workaround-available, planned item; medium is appropriate.

</details>

### DSGN-1 — KvList with limit omitted enumerates the entire keyspace while its doc claims 'Defaults to 100'; JSON/Vector/Graph lists apply a real 100 default

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** pagination  |  **Unit:** DSGN (Design consistency (Stripe-grade))
- **Evidence:** crates/executor-next/src/command.rs:182 doc says 'Optional item limit. Defaults to 100.' but crates/executor-next/src/executor/kv.rs:89-96 calls service.list(prefix) returning ALL keys when limit is None; contrast executor/json.rs:236, vector.rs:189, graph.rs:52 which apply DEFAULT_*_LIST_LIMIT=100 (executor/mod.rs:93-95). Cursor-without-limit also unbounded: executor/batch.rs:20 unwrap_or(usize::MAX).
- **Consumer impact:** MCP is worst hit: strata-mcp/src/tools/kv.rs:48-50 advertises 'Use cursor and limit for pagination through large result sets' — an LLM omitting limit on a 1M-key namespace blows the tool-result context. SDKs that trust the documented default generate wrong client docs and wrong behavior.
- **Recommendation:** Apply DEFAULT_KV_LIST_LIMIT=100 in execute_kv_list matching the doc, or fix the doc and add an explicit server-side max; make cursor-without-limit use the default, not usize::MAX.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

I verified the core evidence myself: command.rs:182 documents 'Optional item limit. Defaults to 100.' for KvList, but kv.rs:81-96 applies no default — limit=None calls service.list(prefix) returning all keys, and cursor-without-limit routes through batch.rs:20's unwrap_or(usize::MAX), also unbounded; json.rs:236, vector.rs:189, and graph.rs:52 all apply the real 100 defaults from mod.rs:93-95, so the doc/behavior contradiction and cross-primitive inconsistency are CONFIRMED. However, the headline consumer impact does not hold: there is no strata-mcp crate in this repo (crates/ contains only cli-next, core-next, engine-next, executor-next, inference, inference-next, storage-next), and the cited strata-mcp/src/tools/kv.rs:48-50 text is unfindable anywhere in the tree, so 'MCP is worst hit' is unreproducible here. Additionally, output.rs deliberately distinguishes Output::Keys (full list) from Output::KeysPage, suggesting the unlimited path may be intentional and the defect may be a doc error plus inconsistency rather than a straightforward pagination bug; the executor charter says nothing about list limits, so the recommendation does not conflict with it or CLAUDE.md. A real, fix-worthy inconsistency with a wrong doc comment, but with the primary consumer harm unverified in-repo, high is overstated — medium is appropriate.

</details>

### DSGN-4 — Catalog mutations use six different result envelopes; VectorDeleteCollection and JsonDropIndex return bare Bool with no MutationEffect or CommitReceipt

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** output-envelope  |  **Unit:** DSGN (Design consistency (Stripe-grade))
- **Evidence:** executor/vector.rs:50 Output::Bool(service.delete_collection(...)) and executor/json.rs:301 Output::Bool for drop_index — both classified writes (command.rs:1417-1426) yet carry no effect/commit; VectorCreateCollection returns a one-item VectorCollectionList with terminal page (vector.rs:34-39); GraphCreate returns GraphInfo with no commit facts (graph.rs:22); vs SpaceCreateResult/SpaceDeleteResult/GraphDeleteResult full envelopes (output.rs:46-84,454-482); BranchCreate/fork return bare Output::Branch with neither (admin_branch.rs:123-170).
- **Consumer impact:** SDK codegen cannot offer a uniform write receipt: a Python SDK returning CommitReceipt from every mutation has holes for collection/index/branch/graph creation and deletion, so audit/version-pinning workflows (fork-at-version after a write) cannot be built on those operations.
- **Recommendation:** Define one CatalogMutationResult{subject, created/deleted, effect, commit?} and use it for all container create/delete ops.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

I reproduced every cited location: vector.rs:50 and json.rs:301 do return bare Output::Bool for VectorDeleteCollection/JsonDropIndex, both are in the is_write list in command.rs, VectorCreateCollection returns a one-item VectorCollectionList whose VectorCollectionInfo has zero commit facts, BranchCreate/fork return bare Output::Branch, while SpaceCreate/SpaceDelete/GraphDelete carry full effect+commit envelopes — the six-envelope inconsistency is real and the recommendation aligns with the charter's 'one output protocol' rule. However, two things overstate the finding: GraphCreate's GraphInfoData actually carries created_version/created_timestamp/updated_version/updated_timestamp, so 'no commit facts' is wrong for that op, and the bare-Bool cases mirror engine-next signatures (delete_collection/drop_index return EngineResult<bool>), so the gap originates in the engine surface, not an executor translation choice. Consumer impact is real for uniform SDK receipt codegen but narrow: only rare catalog mutations lack receipts, no consumer is blocked from performing them, and all data-row writes (the usual audit/version-pinning targets) carry full receipts. Real pre-freeze design debt, but medium, not high.

</details>

### DTO-1 — No KV key/value size caps at the boundary; storage read path hard-bounds keys at 4KB (write/read asymmetry)

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** boundary-validation  |  **Unit:** DTO (Protocol DTO quality)
- **Evidence:** crates/engine-next/src/data/kv/types.rs:74-84 KvKey::new only rejects empty; KvValue::new (kv/types.rs:118-122) is infallible. crates/storage-next/src/format/table_manifest.rs:18 'const MAX_KEY_BYTES: usize = 4_096' enforced only via read_bounded_bytes at :826-829; no write-side bound in encode_table_manifest (:556) or executor input_common.rs. JSON/event payloads ARE capped (json_document_too_large, event_payload_too_large) — KV is the outlier.
- **Consumer impact:** Any consumer (Python SDK, MCP tool, CLI script) can write a >4KB key or multi-GB value; the key case plausibly produces a durable table manifest that fails to decode on reopen (database wedge), and the value case flows unbounded garbage into WAL/commit with no resource_exhausted error. SDKs cannot document stable limits because none exist.
- **Recommendation:** Add explicit max key bytes (aligned with storage MAX_KEY_BYTES minus physical prefix overhead) and max value bytes to KvKey/KvValue::new with invalid_argument.engine.kv_key_too_large / kv_value_too_large codes; add a write-side assertion in table-manifest encoding; add an integration test writing a 5KB key and reopening the database.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

I verified the core evidence myself: KvKey::new (crates/engine-next/src/data/kv/types.rs:76-85) rejects only empty keys, KvValue::new (:121) is infallible, executor-next/src/executor/input_common.rs:16/:24 forwards with no cap, MAX_KEY_BYTES=4096 exists in table_manifest.rs:18, and JSON/event payloads are capped while KV is not — plus grep confirms no key-size check anywhere on the WAL/memtable/table-block write path. KV is core, not on any removed/planned list, so the asymmetry is a shipped gap. However, the high-severity mechanism is refuted by the code: TableManifestTableBounds::new (table_manifest.rs:340-357) enforces MAX_KEY_BYTES at construction and the production flush path (lifecycle/table_manifest.rs:766, :849) goes through it, so an undecodable manifest can never be written and there is no decode-fails-on-reopen wedge. The real failure mode is weaker but genuine: a >4KB key commits durably to WAL, then every subsequent flush/manifest publication fails with failed_precondition.lifecycle.table_manifest_publication — the database reopens and serves reads, but the durable pipeline for that branch is persistently degraded, a delayed and hard-to-diagnose failure from ordinary consumer input with no invalid_argument at write time. The recommendation (engine-side caps with typed error codes) is consistent with CLAUDE.md authority rules, so the finding is real but overstated: medium, not high.

</details>

### DTO-3 — Output embeds strata-inference-next types directly; executor depends on inference crate against hard rule 5

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** dependency-direction  |  **Unit:** DTO (Protocol DTO quality)
- **Planned milestone:** M8
- **Evidence:** crates/executor-next/src/output.rs:496-536 variants carry strata_inference_next::{ModelInfo, PullModelOutput, InferenceCapability, GenerateResponse, EmbedResponse, RankResponse, ModelCacheStatus}; Cargo.toml:36 'strata-inference-next = { path = ../inference-next, optional = true }'; lib.rs:36 re-exports them. CLAUDE.md rule 5: executor consumes intelligence, never imports inference directly.
- **Consumer impact:** The wire contract for inference commands is defined by an internal provider crate that has no DTO discipline obligations (no serde stability guarantees, no non_exhaustive policy) — any inference-next refactor silently changes the CLI/SDK wire shape. Also bypasses the intelligence-next mediation layer the architecture mandates.
- **Recommendation:** Mirror the needed inference outputs as executor-owned DTOs (as done for every other domain) and, when intelligence-next lands (M8), route inference commands through it, dropping the direct dependency. If the passthrough is intentionally transitional, mark it with a deletion condition per PR discipline rule 4.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

I read all cited locations myself: output.rs:496-536 does embed seven strata_inference_next types in Output variants, Cargo.toml:35 has the optional dependency, lib.rs:36-43 re-exports them, and the inference-next structs (runtime.rs, lib.rs:132, registry/mod.rs:88) are plain serde derives with no non_exhaustive or DTO discipline — so the evidence and the wire-stability concern both hold, and this literally contradicts CLAUDE.md hard rule 5. But the severity is overstated: crates/executor-next/tests/inference_guards.rs contains a guard test that deliberately asserts this exact facade dependency and forbids reaching past the facade (no registry::, provider internals, or API keys), and the executor charter (docs/executor/executor-charter.md:282-288) intentionally places inference commands in executor pending the "real intelligence layer," which cannot be routed through because crates/intelligence-next does not exist yet (M8). No CLI/SDK consumer is broken today — the impact is prospective wire-shape drift risk plus a missing transitional/deletion-condition marker (I grepped; none exists), not a shipped defect. Real, actionable (mirror executor-owned DTOs now, mark transitional per PR rule 4), but an intentional guarded interim design with a planned M8 resolution warrants medium, not high.

</details>

### ENGINE-4 — Executor imports inference-next directly for 13 Inference* commands, violating dependency hard rule 5

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** contract-violation  |  **Unit:** ENGINE (Engine capability reachability)
- **Planned milestone:** M8
- **Evidence:** crates/executor-next/Cargo.toml:35 "strata-inference-next = { path = \"../inference-next\", optional = true }"; crates/executor-next/src/executor/mod.rs:169 "inference: strata_inference_next::InferenceRuntime"; dispatch.rs:695-768 routes InferenceGenerate/Embed/Rank etc. straight to it. CLAUDE.md hard rule 5: "Executor and CLI consume intelligence; never import inference directly."
- **Consumer impact:** All consumers bind to a wire contract for inference commands whose backing path must be rewired at M8 (intelligence-next); if request/response shapes shift when the intelligence layer is inserted, Python/Node/MCP inference surfaces break post-ship.
- **Recommendation:** Mark the direct dependency as transitional with an explicit M8 deletion condition (PR discipline rule 4), and freeze the Inference* command/output DTOs so the M8 rewiring is behavior-neutral on the wire.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

I verified all three evidence locations myself: crates/executor-next/Cargo.toml line 35 declares `strata-inference-next = { path = "../inference-next", optional = true }`, src/executor/mod.rs:169 holds `inference: strata_inference_next::InferenceRuntime` behind the `inference` feature, and dispatch.rs:695-768 routes the Inference* commands (I count 12, not 13) directly to that runtime — so the literal violation of CLAUDE.md hard rule 5 is real, and I found no transitional marker or deletion condition anywhere in the crate (PR discipline rule 4 gap). However, `crates/` contains no intelligence-next crate at all — it is M8 work that has not started — so the direct wiring is the known pre-M8 sequencing state, and the verification instructions say planned M8 intelligence absence must not be reported as a shipped bug; docs/executor/executor-charter.md and the minimal-surface plan (EX5) already anticipate rewiring these commands through the intelligence layer. The consumer impact is forward-looking: no CLI/SDK/MCP consumer is broken today, executor-next has not shipped to main, and "DTOs may shift at M8" is a risk, not an observed breakage. The recommendation (mark transitional with an M8 deletion condition, freeze the Inference* DTOs) is consistent with both the charter and CLAUDE.md, so the finding survives as a real but process-level gap — downgraded from high to medium because the defect is an unmarked transitional dependency and wire-freeze risk, not a shipped contract break.

</details>

### EVOL-1 — Unknown-command / command-decode failures have no structured error code; decode is transport-owned

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** wire-contract  |  **Unit:** EVOL (Wire evolution and versioning)
- **Evidence:** crates/cli-next/src/lib.rs:884 `Ok(serde_json::from_str(&text)?)` -> lib.rs:896 `Json(serde_json::Error)` rendered as bare prose (lib.rs:911); crates/executor-next/src/error_registry.rs has no unknown_command/command_decode code; no executor-owned JSON decode entry point exists
- **Consumer impact:** A v1.1 Python/Node SDK or MCP client sending a new command variant or new optional field to a v1.0 binary gets unstructured serde prose (e.g. "unknown variant `x`") instead of a stable `<class>.<area>.<detail>` code, so clients cannot programmatically detect version skew and say "upgrade Strata"; each transport (CLI today, MCP/FFI next) also re-implements decode and error mapping, guaranteeing divergent failure shapes.
- **Recommendation:** Add an executor-owned `Command` decode helper (e.g. execute_json / Command::from_json) that maps serde failures to registered codes such as invalid_argument.executor.unknown_command and invalid_argument.executor.command_decode (distinguishing unknown variant vs unknown field vs malformed JSON), and route cli-next --command-json and all future transports through it.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

I reproduced every evidence claim: cli-next lib.rs:884 decodes Command via bare serde_json, CliError::Json (lib.rs:896) renders as raw serde prose (lib.rs:911) and bypasses the structured render_error envelope even in --format json (lib.rs:43-50), executor-next/src/error_registry.rs contains no unknown_command/command_decode code, and no executor-owned decode helper exists. The gap is aggravated by deny_unknown_fields on Command (command.rs:20) and the recommendation is consistent with the executor charter's "one error protocol / stable shared contract" ownership and CLAUDE.md rules 8 and 27, and the feature is not on any removed/deferred list. However, the high-severity consumer impact (SDK/MCP version-skew detection) concerns transports that do not yet exist — cli-next is currently the sole wire consumer, decode failures occur only on the error path of --command-json, and exit code 2 vs 1 gives a coarse programmatic signal today. Real wire-contract defect worth fixing before M9 transports replicate decode, but no shipped consumer is blocked now, so severity is medium rather than high.

</details>

### EVOL-2 — No protocol/wire schema version a client can check; IDL versions exist but are never exposed

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** versioning  |  **Unit:** EVOL (Wire evolution and versioning)
- **Evidence:** types/admin.rs:42-43 and :174-175 expose only "Engine package version"; output.rs:21-24 Pong carries the same; idl_tooling.rs:18-19 defines SUPPORTED_COMMAND_SCHEMA_VERSION = "strata.idl.v1" and cli_metadata.rs:14 "strata.cli.v1", neither reachable via any Command
- **Consumer impact:** SDKs and the MCP escape hatch cannot negotiate or even verify command-vocabulary compatibility with the binary they are driving — the only recourse is parsing the engine semver string (which does not promise wire-schema mapping) or trial-and-error against EVOL-1's unstructured errors.
- **Recommendation:** Expose a protocol schema version (and ideally the supported command-name list or command-index hash) in Describe/Info output, sourced from the IDL schema_version constants, and document its bump policy.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

I verified every cited location: types/admin.rs:42-43 and 174-175 document only "Engine package version", output.rs:21-24 Pong carries the same, and SUPPORTED_COMMAND_SCHEMA_VERSION ("strata.idl.v1", idl_tooling.rs:18) and "strata.cli.v1" (cli_metadata.rs:14) are private consts; a grep over executor-next confirms no Command or Output exposes any schema_version, so the evidence holds and the recommendation is compatible with the charter's "public protocol types" end state. However, the consumer impact is overstated: executor-next has no IPC/network transport (the only bin is the strata-idl tooling binary), V1 explicitly excludes network server mode, and the CLI, PyO3 SDK, and in-binary MCP all compile against the executor and ship in the same versioned artifact — so runtime command-vocabulary skew between a client and "the binary it is driving" is not a shipped V1 scenario, and the engine package version effectively pins the IDL version per release. The gap becomes real only for externally generated SDKs or a future out-of-process transport, and MCP clients already get runtime vocabulary discovery via tools/list. Genuine protocol-hygiene gap worth fixing cheaply, but nothing is blocked today, so high is not justified.

</details>

### EVOL-3 — No additive-change or deprecation playbook; wire_status vocabulary has no 'deprecated' state

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** evolution-policy  |  **Unit:** EVOL (Wire evolution and versioning)
- **Evidence:** grep of docs/executor/*.md finds no additive/deprecation/compat policy (charter's only compat section, executor-charter.md:414, covers the rewrite); idl_tooling.rs:1329-1331 `match wire_status { "stable" | "transitional" => Ok(()) ... }`; one ad-hoc alias exists (legacy `is_valid` accepted, tests/command_contract/shapes.rs:196-207)
- **Consumer impact:** When v1.1 needs to add a field, rename one, or retire a command, there is no written rule for what is legal (Option+default fields, alias policy, variant addition, wire_status transitions), so SDK maintainers for Python/Node/MCP cannot rely on any stability promise, and deprecations cannot be communicated through the IDL that is supposed to drive their codegen.
- **Recommendation:** Write a short wire-evolution contract in docs/executor/ (additive fields must be Option+serde(default); new variants are minor; deny_unknown_fields means new request fields require a binary that understands them; alias policy), and extend wire_status with deprecated plus since/replacement metadata in ResolvedCommand.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

Evidence fully holds on my own inspection: grep of docs/executor/*.md finds no additive/deprecation/wire-evolution policy (charter line 414's compat section covers only the rewrite); idl_tooling.rs:1329-1336 accepts exactly "stable"|"transitional" with no deprecated state (test at line 1741 enforces it); shapes.rs:191-208 shows the undocumented legacy is_valid alias; and command.rs:20 has deny_unknown_fields on Command, so additive request fields genuinely break older binaries, making the missing policy a real technical constraint rather than pure documentation. The recommendation is charter- and CLAUDE.md-compatible and the topic is not on the V1-removed list. But the consumer impact is prospective, not current: executor-next is pre-cutover (M9 incomplete, -next suffix intact), no shipped SDK/MCP codegen consumes the v1 IDL yet, and no command needs deprecation today, so nothing is blocked until the first post-V1 evolutionary change. A real gap that should be closed before v1.1 planning, but "high" overstates a policy-doc-plus-enum-extension fix with no present breakage; medium is appropriate.

</details>

### MCP-1 — No cross-primitive search command — the flagship agent tool has no executor slot

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** missing-operation  |  **Unit:** MCP (MCP server parity)
- **Planned milestone:** M8
- **Evidence:** ~/Documents/GitHub/strata-mcp/src/tools/search.rs:18-21 'strata_search — Search across multiple primitives... Use this to find data when you don't know which primitive contains it'; no Search/Retrieve variant among the 103 in crates/executor-next/src/command.rs; engine substrate planned at docs/architecture/strata-v1-implementation-roadmap.md:541 'Implement search/retrieval substrate, recipes, BM25 rows'
- **Consumer impact:** MCP is the surface where search matters most — first-run-experience.md §8.3 puts search in the curated ~15-20 tool set, and an agent that cannot search across primitives loses Strata's core value prop. The tagged Command enum gives a credible slot, so this is planned (engine deterministic retrieval M6, model stages M8), but MCP serve (M9) is blocked on it.
- **Recommendation:** When M6 lands engine retrieval, add a Search command variant with keyword mode first (engine-owned, rule 22), then extend with expand/rerank flags at M8; keep the old SearchQuery field set (query, k, primitives, time_range, mode, expand, rerank) as the requirement checklist.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

I verified every citation: strata-mcp/src/tools/search.rs:18-21 contains the exact quoted strata_search description with the full SearchQuery field set; crates/executor-next/src/command.rs has 103 variants with no Search/Retrieve match (the '[x] Search' in docs/executor/executor-command-migration-checklist.md is the pre-V1 executor ledger, not executor-next); roadmap M6 item 5 plans the search/retrieval substrate as quoted; and the executor charter (docs/executor/executor-charter.md:264-274) explicitly reserves public search commands as thin executor dispatch, so the recommendation is charter- and rule-22-compliant. The feature is not on the V1-removed list (the removed autosearch optimizer is distinct from cross-primitive search). However, the gap is expected mid-roadmap state, not a defect: engine retrieval is M6, intelligence stages M8, and the executor/MCP cutover M9, so no current consumer is broken today — the shipping MCP binary runs against the old stack. Real and worth tracking as an M9 blocker checklist item, but 'high' severity for a planned, already-sequenced feature overstates it; downgrade to medium.

</details>

### META-3 — No exhaustiveness drift guard — adding a Command variant without metadata fails nothing

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** drift-prevention  |  **Unit:** META (Machine-readable metadata readiness)
- **Evidence:** idl_tooling.rs:1282-1289 validate_executor_ref only checks referenced variants EXIST in the enum, never that all enum variants are referenced; tests pin the IDL side only (idl_kv_vector_overlay.rs:64 `assert_eq!(ids.len(), REQUIRED_KV.len() + REQUIRED_VECTOR.len())`). No test diffs enum_variants(command.rs) against covered inputs.
- **Consumer impact:** Once generators ship, a new executor command lands with green CI while the agents guide, MCP tool list, and SDK stubs silently omit it — exactly the drift the roadmap says must be "a CI failure" (first-run-experience.md §7/§10.2).
- **Recommendation:** Add a coverage test: enum_variants(command.rs) minus the union of resolved `input` refs must be empty (with an explicit allowlist for intentionally-unpublished variants) so every new variant forces an IDL entry or a conscious exemption.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

The evidence is accurate: I read idl_tooling.rs:1282-1289 and confirmed validate_executor_ref only checks that referenced variants exist in the enum, and idl_kv_vector_overlay.rs:64 pins the IDL-side count only; I searched the entire executor-next crate and found no test diffing enum_variants(command.rs) against IDL-covered inputs (the only exhaustiveness test, command_names_cover_every_variant in tests/command_contract/roundtrip.rs, checks the hand-built contract-test list, not IDL metadata). However, the severity is overstated: the IDL is explicitly a partial KV/vector overlay (32 commands) while the Command enum has ~60+ variants across families not yet authored, and the test idl_tooling_does_not_add_downstream_generators shows generators are deliberately not shipped — the finding's own consumer_impact is conditional on future generators, so no CLI/SDK/MCP consumer is hurt today. The first-run-experience.md "drift is a CI failure" requirement (lines 331-333, 465) is real but attaches to the not-yet-landed generation workstream (D3 notes "full IDL (#5) refines later"). Real gap worth a coverage test with an allowlist (the recommendation does not conflict with the executor charter or CLAUDE.md), but it is a prospective drift-prevention hole in mid-rollout tooling, not a high-severity shipped defect.

</details>

### NODE-3 — 19 of 33 old graph methods unmappable: ontology (13), BFS traversal, and 5 analytics have no command slots

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** missing-core-operation  |  **Unit:** NODE (Node SDK parity)
- **Planned milestone:** M6
- **Evidence:** ~/Documents/GitHub/strata-nodesdk/stratadb.d.ts:781-824 (defineObjectType…nodesByType, bfs, wcc/cdlp/pagerank/lcc/sssp). No corresponding Command variants. Ontology and traversal ARE contracted engine-next V1 behaviors (docs/architecture/engine/primitive-implementation-contract.md:686-691: 'Ontology object and link types… Traversal and neighbor query behavior. Temporal traversal behavior.'); analytics are 'Optional… otherwise feature-gate' (docs/architecture/engine/public-api-and-cli-surface-cleanup-checklist.md:148). None appear in the executor plans' command lists.
- **Consumer impact:** Node SDK graph namespace loses more than half its methods; MCP graph tools and the old CLI graph commands have no target. GraphAddNode's binding replaces entityRef but objectType typing is gone, so typed-knowledge-graph workloads built on the old SDK cannot migrate.
- **Recommendation:** Ontology and BFS: track as M6 engine work with explicit executor command slices (they are already contracted). Analytics: make the keep/feature-gate/drop decision now and record it in v1-removed-surfaces.md if dropped.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

I verified the raw facts: the old SDK (~/Documents/GitHub/strata-nodesdk/stratadb.d.ts:781-824) does expose 13 ontology methods, bfs, and 5 analytics; executor-next's Command enum (crates/executor-next/src/command.rs) has only 14 graph variants with none of these; the contract (primitive-implementation-contract.md:686-691) does list ontology and traversal as V1 graph behaviors; and v1-removed-surfaces.md does not mention graph analytics. However, the finding's key claim that these operations are "unmappable" with "no command slots" and "None appear in the executor plans" is wrong: executor-graph-core-command-contract-implementation-plan.md:160-184 enumerates every one of the 19 commands by name as intentionally excluded ("These names should remain absent from executor-next until their own engine plans exist"), and engine-graph-core-implementation-plan.md:13 states "Ontology is intentionally a separate follow-up" — this is a documented, conditioned deferral, not a forgotten gap. The residual real issue is medium: no follow-up engine plan file yet exists for contracted ontology/traversal behaviors, and the analytics keep/feature-gate/drop decision (cleanup-checklist "Optional") remains unrecorded, so a V1 cutover without them would breach the contract; but nothing has shipped, no consumer is blocked today, and the recommendation largely restates what the plans already say.

</details>

### PY-4 — Cross-primitive search command absent

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** planned-gap  |  **Unit:** PY (Python SDK parity)
- **Planned milestone:** M8
- **Evidence:** SDK db.search(query, k, primitives, time_range, mode, expand, rerank) (__init__.py:865-884); no Search variant among the 103 commands; legacy executor had Search (migration checklist line 228); roadmap line 541 places search/retrieval substrate in engine-next M6 with intelligence stages in M8.
- **Consumer impact:** Python SDK's flagship retrieval method (and MCP search tools, CLI search) cannot ship until a Search command exists; the command enum has a credible slot but no reserved shape today, risking last-minute wire design at M8.
- **Recommendation:** Record the planned Search command shape (query, k, primitives, time_range, mode, expand, rerank) now so M8 lands into a reviewed slot instead of inventing the wire contract late.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

I independently verified all cited evidence: the Python SDK's search() at strata-python/python/stratadb/__init__.py:865-884 matches the quoted signature; executor-next/src/command.rs contains no Search variant at all; migration checklist line 228 lists Search as a legacy command; and the roadmap places the search/retrieval substrate in M6 with intelligence stages in M8. The feature is in V1 scope (only the autosearch optimizer is removed) and the executor charter explicitly assigns public search commands to executor as thin adapters, so the recommendation is charter-consistent. However, the gap is correctly self-labeled as planned M8 work and no consumer is blocked today — the V1 SDK/CLI/MCP ship in M9, after M8, so milestone ordering already covers it; the only present risk is late wire-contract design. Real and worth tracking, but "high" overstates a scheduled absence with no current breakage.

</details>

### PY-5 — Auto-embed pipeline surface (set_auto_embed, embed_status) absent; embed() has a model-argument mismatch

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** planned-gap  |  **Unit:** PY (Python SDK parity)
- **Planned milestone:** M8
- **Evidence:** SDK set_auto_embed/auto_embed_enabled/embed_status (__init__.py:691-733); no commands; legacy executor had ConfigSetAutoEmbed, AutoEmbedStatus, EmbedStatus (checklist lines 234,248,249). InferenceEmbed/InferenceEmbedBatch exist but require an explicit model spec (command.rs:1242-1255) while SDK embed(text) uses the database-configured embed model.
- **Consumer impact:** Python SDK cannot ship its auto-embed onboarding story (enable, watch progress, embed); consumers must hardcode model specs client-side, which will drift from the database's configured embed model and trigger failed_precondition.embedding_model_mismatch at retrieval time.
- **Recommendation:** Land auto-embed toggle/status commands with M8 intelligence orchestration, and give InferenceEmbed/EmbedBatch an optional model (default = database-configured embed model) so consumers do not resolve defaults themselves.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

All three evidence citations verified by direct read: SDK set_auto_embed/auto_embed_enabled/embed_status exist at strata-python __init__.py ~691-733; executor-next command.rs 1240-1255 shows InferenceEmbed/InferenceEmbedBatch requiring an explicit model String with no auto-embed/embed-status variants anywhere in the enum; checklist lines 234/248/249 confirm the legacy commands. Auto-embed is in V1 scope (intelligence-next, M8), not on the removed list, so the gap is legitimate to track. But the finding itself labels it a planned M8 gap, executor-next has not cut over to any consumer (SDK cutover is M9 and the Python SDK still runs on the legacy executor where these commands work), so no real consumer is blocked today — the impact is prospective. The InferenceEmbed "mismatch" is a raw inference passthrough coexisting with a not-yet-built M8 database-configured embed surface, not a defect. Real, charter-compatible recommendation, but "high" severity overstates a tracked planned gap.

</details>

### PY-6 — time_range discovery command missing despite time travel being a V1 pillar

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** missing-command  |  **Unit:** PY (Python SDK parity)
- **Evidence:** SDK property db.time_range returns {oldest_ts, latest_ts} (__init__.py:1035-1041); no TimeRange command; AdminDatabaseInfo/AdminMetrics (crates/executor-next/src/types/admin.rs:41-95) carry no timestamp bounds; grep for time_range/oldest_ts in executor-next and engine-next returns nothing; legacy executor had TimeRange (checklist line 219).
- **Consumer impact:** SDKs cannot validate or suggest as_of values for db.at() snapshots, and CLI/MCP temporal tooling cannot show the valid time-travel window, even though every read command takes as_of and BranchForkAtTimestamp exists.
- **Recommendation:** Add a TimeRange (or extend Describe with branch timeline bounds) command backed by an engine-next temporal-bounds query; it is a small read-only surface consistent with the existing admin family.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

The factual evidence holds: I grepped executor-next and engine-next src and found no TimeRange/time_range/oldest_ts anywhere, crates/executor-next/src/types/admin.rs carries no timestamp bounds, and command.rs has as_of on many reads plus BranchForkAtTimestamp (line 117), so there is genuinely no temporal-bounds discovery surface. However, the finding misses that this absence is deliberate and guard-enforced: crates/executor-next/tests/error_and_guards.rs line 625 (`executor_admin_surface_excludes_deferred_old_commands`) explicitly lists `Command::TimeRange` among deferred admin commands (alongside Flush/Compact/Retention) and fails if any source exposes it — this is a known deferral, not an overlooked missing command, so no current consumer is blocked (SDK/CLI cutover happens later and the cited __init__.py is the legacy SDK, not in this repo's V1 path). TimeRange is not on the CLAUDE.md V1-removed list, and the recommendation (small read-only admin command) does not contradict the charter. The real residual defect is narrower than claimed: docs/executor/executor-command-migration-checklist.md line 219 marks TimeRange `[x]` meaning "migrated and owned by the new executor," which contradicts the guard test and violates the checklist's own rule that intentionally-dropped commands need an explanatory note — the ledger meant to prevent this command from being forgotten already misrepresents it. That tracking inconsistency plus the eventual need for the command justifies medium, not high.

</details>

### SESS-1 — Fully serialized execution: &mut self on all 103 commands, no concurrent-read path, no concurrency tests

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** concurrency  |  **Unit:** SESS (Session and lifecycle model)
- **Evidence:** crates/executor-next/src/executor/dispatch.rs:7 `pub fn execute(&mut self, command: Command)`; 101 &mut self methods vs 1 &self across executor/; engine services all &mut (crates/engine-next/src/api/database.rs:173-277); durable storage is single-writer-locked (crates/storage-next/src/service/cache_mode_absence_tests.rs:40 `SingleWriterLock`) so one process = one handle; zero thread::spawn in executor-next/tests. Executor IS Send+Sync (compile-verified, scratchpad/sendcheck).
- **Consumer impact:** A threaded Python SDK (PyO3 releasing the GIL), Node SDK worker threads, or any long-lived MCP host must wrap the single handle in Mutex<Executor>, serializing all reads behind writes — unlike SQLite/DuckDB peers which allow concurrent readers. Nothing pins Send+Sync in a test, so a future Rc/RefCell field would silently break every threaded embedding at SDK compile time.
- **Recommendation:** Declare the concurrency contract explicitly: add a static assertion test (fn _assert() { fn ok<T: Send + Sync>(){} ok::<Executor>(); }), document the serialized-execution model in the D4 surface docs, and record whether a shared read path (&self reads or an internal lock) is planned so SDK authors can design around it.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

Evidence verified: dispatch.rs:7 is `pub fn execute(&mut self, ...)`, my grep found 169 `&mut self` vs 1 `&self` in executor-next/src/executor/, every engine-next service accessor (branches/kv/json/vector/event/graph/spaces/admin, database.rs:173-277) takes `&mut self`, `BackendCapability::SingleWriterLock` exists in storage-next, and executor-next has zero thread::spawn or Send/Sync assertions in src or tests. The finding is not on the V1-removed list (in-process threading is distinct from the removed network-server mode), and the concern is sharpened by the legacy executor explicitly providing `new_handle()` "for multi-threaded use" (executor-crate-map.md:157) — a capability the V1 stack drops with no documented replacement or stated contract in the charter or executor docs. However, the impact is prospective, not a shipped defect: no SDK/MCP consumer is built on executor-next yet (M9 in flight), correctness is unaffected (a Mutex wrapper works, only read concurrency suffers), and the serialized-command model may be deliberate V1 scoping — the doc comment says "Executes one serialized command". The recommendation (Send+Sync static assertion, document the concurrency contract in D4 docs) is cheap, does not contradict the executor charter (which is about authority, not threading) or CLAUDE.md rules. Real contract-documentation gap and design risk worth recording before SDK work lands, but "high" overstates a not-yet-consumed surface with no failing behavior today — medium.

</details>

### THIN-1 — Executor imports inference-next directly and embeds its types in the wire protocol

- **Severity:** ~~high~~ → **medium** (downgraded)  |  **Category:** dependency-direction  |  **Unit:** THIN (Charter conformance (thinness / one model))
- **Planned milestone:** M8
- **Evidence:** crates/executor-next/Cargo.toml:35 'strata-inference-next = { path = ../inference-next, optional = true }'; command.rs:1219 'request: strata_inference_next::GenerateRequest'; output.rs:512 'InferenceGeneration(strata_inference_next::GenerateResponse)'; lib.rs:36-43 re-exports 12 inference types; mod.rs:192 'strata_inference_next::InferenceRuntime::default()' constructed inside from_database. Violates CLAUDE.md hard rule 5 (executor consumes intelligence, never inference directly).
- **Consumer impact:** Every consumer (CLI, Python SDK, Node SDK, MCP) serializes Command/Output whose inference payload shapes are owned by inference-next, so an inference-next refactor silently changes the public wire contract; the M8 intelligence cutover will be a breaking protocol change instead of an internal swap. Default InferenceRuntime construction in from_database is bootstrap policy consumers cannot observe or control except via with_inference_runtime.
- **Recommendation:** Mark the direct dependency transitional with an explicit deletion condition (per PR discipline rule 4), define executor-owned protocol DTOs for generate/embed/rank requests and responses now so the wire shape survives the M8 rehoming, and move runtime construction out of from_database into an explicit injection point.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: medium)

Every cited evidence location holds on direct read: Cargo.toml:35 declares the optional strata-inference-next dep, command.rs:1219/1246/1262 and output.rs:499-536 embed inference-next request/response types in the serde-derived Command/Output wire enums, lib.rs:36-43 re-exports 11 inference types, and executor/mod.rs:192 builds InferenceRuntime::default() inside from_database — so hard rule 5's letter is violated. However, the finding omits that crates/executor-next/tests/inference_guards.rs deliberately codifies this as a facade-only boundary ("executor must depend on the optional inference facade", internals forbidden), and intelligence-next does not exist yet (M8 unshipped; the EX5 plan explicitly defers thin adapters over "the real intelligence layer"), so this is a designed interim state on the planned M8 path, not an accidental leak. No consumer is hurt today — cli-next works and with_inference_runtime provides injection — the impact is prospective wire-contract churn at the M8 cutover, plus a genuine PR-discipline gap: no transitional marker or deletion condition exists anywhere in the source (I grepped). The recommendation is sound and consistent with the executor charter's "adapt to the real intelligence layer" stance, but high severity treats a guard-tested planned-interim arrangement as a shipped defect; medium is appropriate for the missing transitional marker and DTO debt.

</details>

### ENGINE-3 — Retrieval/search substrate, recipes, and derived-state manifests have no engine implementation and no command slot

- **Severity:** ~~high~~ → **low** (downgraded)  |  **Category:** planned-gap  |  **Unit:** ENGINE (Engine capability reachability)
- **Planned milestone:** M6/M8
- **Evidence:** grep recipe|retrieval|search over crates/engine-next/src returns zero functional hits (only VectorSearchMatch names); none of the 103 Command variants is a search command; docs/executor/executor-charter.md:264-274 ("Search commands belong in executor") and roadmap Phase 6 item 5 (roadmap.md:541-542) assign the substrate to M6, model stages to M8.
- **Consumer impact:** MCP (which has search tools among its 61) and both SDKs cannot expose search/RAG at all; because the Command enum has no reserved slot, adding it later is a wire-surface expansion every consumer must absorb.
- **Recommendation:** Record as planned; when M6 lands the engine retrieval bucket, add the thin Search command adapter per charter EX5 rather than growing executor-side orchestration.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: low)

I re-ran the grep over crates/engine-next/src and confirmed zero recipe/retrieval-substrate hits (only vector-similarity code in data/vector/), verified crates/executor-next/src/command.rs has ~107 variants with no search command beyond VectorQuery/VectorIndexQuery, and confirmed both citations: executor-charter.md ~264-274 ("Search commands belong in executor... thin dispatch") and strata-v1-implementation-roadmap.md:541-542 (Phase 6 item 5 assigns the search/retrieval substrate, recipes, and derived-state manifests to M6, model stages to M8). The gap is real, not on the V1-removed list, and the recommendation aligns with the charter and CLAUDE.md rules 22-23. But "high" severity is overstated: this is a milestone-scheduled planned gap with no shipped consumer currently blocked (V1 MCP/SDKs are not cut over yet), and the "wire-surface expansion" impact is weak because Command is a serde tag-discriminated enum, so adding a search variant later is additive and costs non-adopting consumers nothing. Correctly recorded as planned-gap, but it warrants low severity as a tracking item, not high.

</details>

### NODE-9 — Vector metadata filtering collapsed from 8 operators to Eq-only; per-query metric override dropped

- **Severity:** ~~high~~ → **low** (downgraded)  |  **Category:** argument-mismatch  |  **Unit:** NODE (Node SDK parity)
- **Evidence:** Old contract: `MetadataFilter { op: 'eq'|'ne'|'gt'|'gte'|'lt'|'lte'|'in'|'contains' }` and `vectorSearchFiltered(..., metric?, filter?, asOf?)` (~/Documents/GitHub/strata-nodesdk/stratadb.d.ts:233-237, index.d.ts:197). New: `VectorFilterOp { Eq }` only (crates/executor-next/src/types/vector_input.rs:57-60); VectorQuery (command.rs:669-688) has no metric field.
- **Consumer impact:** Node SDK filtered vector search (and MCP vector tools) loses range, negation, set-membership, and substring filtering — common RAG metadata patterns (date ranges, category exclusion). Consumers must over-fetch and post-filter client-side, breaking k semantics.
- **Recommendation:** Extend VectorFilterOp toward the old operator set (VectorScalar already models the value types) or document the reduction as a deliberate V1 cut with the over-fetch guidance.

<details><summary>Adversarial verification</summary>

**Verdict: DOWNGRADE** (corrected severity: low)

I verified both sides of the evidence myself: the new surface is indeed Eq-only (crates/executor-next/src/types/vector_input.rs:57-60, mirrored in engine-next/src/data/vector/types.rs:398-401) and VectorQuery (crates/executor-next/src/command.rs:669-688) has no per-query metric, while the old Node SDK exposed 8 operators and a metric parameter (~/Documents/GitHub/strata-nodesdk/stratadb.d.ts:233-237, index.d.ts:197). However, this is a documented deliberate staging decision, not an argument-mismatch bug: docs/architecture/archive/implementation-plans/pinecone-vector-surface-reference.md:373-378 explicitly directs "Support top-level scalar equality first. Add comparison and set-membership operators only after equality behavior, deletion, search, and count tests are stable," and the engine vector parity test plan codifies equality-only filters. The metric drop is also by design — metric moved to collection schema (VectorCreateCollection carries metric: VectorDistanceMetric at command.rs:492; vector-indexing-design.md makes metric part of collection identity with cosine stored L2-normalized, which makes per-query metric switching incoherent). Consumer impact (over-fetch plus client-side post-filter for range/set/negation patterns) is real for migrating SDK/MCP consumers, so the finding is not fully rejectable, but it describes a planned V1 first-slice cut whose own recommendation ("document the reduction as a deliberate cut") is already satisfied by the authoritative design docs. Downgrade to low: worth tracking as a migration-notes item, not a high-severity contract defect.

</details>

---

## Unverified findings (80, medium/low)

Reported by a single auditor, not adversarially verified (verification was applied to critical/high only). Treat as credible leads.

### PY — Python SDK parity

- **PY-7** (medium, planned-gap) *(planned: M6)* — **Branch merge and diff have no commands although V1 defines merge semantics.** Python SDK's branch workflow (fork → experiment → merge) dead-ends at fork; CLI branch merge/diff also unshippable. SDK's last_writer_wins default must additionally be documented as an intentional V1 semantic break (strict-only).
  - Evidence: SDK merge(source, strategy) and diff(a, b) (__init__.py:1125-1131); no BranchMerge/BranchDiff variants; CLAUDE.md rule 20 'Branch merge is strict refusal on divergent concurrent history (V1)'; engine-architecture.md:145 lists 'fork, merge, diff, restore, revert, cherry-pick' as engine branch semantics but grep shows no merge/diff impl in crates/engine-next yet.
  - Recommendation: Track BranchMerge (strict refusal) and BranchDiff as M6 engine slices with executor exposure at M9; note the strategy-argument removal in the SDK migration notes.
- **PY-8** (medium, missing-capability) *(planned: M9)* — **Read-only open mode absent from executor and engine.** Python SDK cannot honor read_only=True — analytics/inspection processes lose their safety guarantee against accidental writes; CLI read-only inspection flows equally blocked.
  - Evidence: SDK Strata.open(path, read_only=False) (_stratadb.pyi:73-80); grep for read_only/access_mode across crates/executor-next/src and crates/engine-next/src returns nothing; Command::is_write() exists (command.rs:1399) so the enforcement hook is credible; roadmap Phase 10 exit criteria require product-path tests covering 'read-only'.
  - Recommendation: Add an access-mode to the executor handle (rejecting is_write() commands) backed by an engine read-only open; roadmap already requires it before promotion.
- **PY-9** (medium, argument-mismatch) — **VectorQuery lacks the per-query metric override the SDK exposes.** Python SDK users who override the distance metric per query (e.g., dot_product rerank over a cosine collection) get no equivalent; the SDK must either drop the parameter or error, a visible parity regression.
  - Evidence: SDK Collection.search(..., metric=None) routes to vector_search_filtered(collection, query, k, metric=..., filter=...) (_stratadb.pyi:514-533); VectorQuery command (command.rs:669-688) has query/k/filter/as_of but no metric field — metric is fixed at VectorCreateCollection.
  - Recommendation: Either add an optional metric override to VectorQuery, or declare per-query metric override out of V1 scope and document the SDK behavior change.
- **PY-12** (low, missing-command) — **Durability counters diagnostics have no command and Metrics does not carry them.** Python SDK loses its WAL observability method; operators diagnosing fsync stalls (WAL halt is a V1 hard rule) have no surface via SDK or CLI.
  - Evidence: SDK durability_counters() returns wal_appends/sync_calls/bytes_written/sync_nanos (_stratadb.pyi:820-824); legacy executor had DurabilityCounters (checklist line 220); AdminMetrics (types/admin.rs:82-95) contains only target/durable/open/branch_count/space_count/control_status.
  - Recommendation: Fold WAL counters into AdminMetrics output rather than adding a new command — keeps the surface minimal while restoring the diagnostics.
- **PY-10** (low, consistency) — **No BranchExists command — asymmetric with every other family's Exists variant.** Python SDK must implement db.branches.exists via BranchGet + not_found error-class handling — workable but forces error-driven control flow on the hottest existence check and breaks the surface's own symmetry.
  - Evidence: SDK branch_exists(name) (_stratadb.pyi:564); commands include SpaceExists, KvExists, JsonExists, VectorExists, EventExists but only BranchGet for branches (command.rs:90-94).
  - Recommendation: Either add BranchExists for symmetry or document BranchGet+not_found as the canonical existence idiom for all SDKs.
- **PY-11** (low, argument-mismatch) — **InferenceTokenize wire default for add_special is false; old SDK default was true.** Raw-wire consumers (CLI scripts, MCP) omitting the field get different token counts than the old SDK's documented default; Python SDK must remember to always send add_special=true to preserve behavior.
  - Evidence: command.rs:1229-1230 '#[serde(default)] add_special: bool' (defaults false); SDK tokenize(model, text, add_special_tokens=None) documents 'Whether to add special tokens (default: True)' (_stratadb.pyi:770-775).
  - Recommendation: Make the wire default match the documented product default (true) or require the field explicitly so no silent default exists.

### NODE — Node SDK parity

- **NODE-2** (medium, planned-gap) *(planned: M6/M9)* — **Branch diff and merge commands missing (merge is an explicit V1 semantic).** Node SDK branch namespace ships without diff/merge, and the branch-then-merge agent-experimentation workflow (a headline Strata story) is not expressible from any consumer.
  - Evidence: Old: `diffBranches(a,b)`, `mergeBranches(source, strategy)` (~/Documents/GitHub/strata-nodesdk/index.d.ts:134-136). No BranchDiff/BranchMerge variants. CLAUDE.md rule 20 defines V1 merge (strict refusal); engine-architecture.md:86-88 lists diff as a core capability; executor plan EX4 lists diff/merge/merge-base/diff-three-way (docs/executor/executor-minimal-surface-implementation-plan.md:304-307). Clone-artifact replacement for removed bundles also has no command yet (contract exists: docs/architecture/engine/dataset-clone-artifact-contract.md).
  - Recommendation: Land BranchDiff/BranchMerge command slices when M6 branch semantics arrive; note the merge `strategy` argument will not carry over (strict refusal only).
- **NODE-4** (medium, planned-gap) *(planned: M8)* — **Search, model configuration, auto-embed controls, and embed pipeline status absent (intelligence-dependent).** Node SDK's flagship cross-primitive `search()` and the auto-embed configuration surface cannot ship until M8; SDKs must temporarily hold a client-side default embedding model to emulate embed(). configureSet has no slot at all — the new config surface is read-only (ConfigGet/ConfigureGetKey).
  - Evidence: Old: `search(query, {k, primitives, timeRange, mode, expand, rerank})`, `configureModel`, `setAutoEmbed`/`autoEmbedEnabled`, `embedStatus`, `configureSet` (~/Documents/GitHub/strata-nodesdk/index.d.ts:223-227, 278-286). No corresponding commands; EX5 plan reserves the slot (docs/executor/executor-minimal-surface-implementation-plan.md:343-352). InferenceEmbed exists but requires an explicit model spec, whereas old embed() used the DB-configured model.
  - Recommendation: Track under M8 with the EX5 thin-adapter slot; decide explicitly whether generic config writes (configureSet) return or are formally removed.
- **NODE-10** (medium, argument-mismatch) *(planned: M6)* — **Graph read commands lack as_of, breaking db.at() snapshot parity and the temporal contract.** Node's `db.at(timestamp)` time-travel snapshot cannot include the graph namespace; time-travel debugging of relationship state (an advertised capability) is impossible from any consumer.
  - Evidence: GraphGetNode/GraphListNodes/GraphNeighbors/GraphGetMeta (crates/executor-next/src/command.rs:984-1109) have no as_of field while KV/JSON/event/vector reads all do. docs/architecture/engine/temporal-context-and-timeline-resolver-contract.md:606-618 requires graph nodes, edges, bindings, and traversal results to honor temporal selectors. Old snapshot surface: GraphSnapshotNamespace (~/Documents/GitHub/strata-nodesdk/stratadb.d.ts:870-890).
  - Recommendation: Add as_of to the graph read commands when graph temporal resolution lands (contracted for M6), keeping field naming identical to the other primitives.
- **NODE-5** (medium, missing-operation) — **TimeRange command dropped; no way to discover valid as_of bounds.** Node's db.at() and every as_of parameter have no anchor: SDKs and MCP agents cannot tell users what historical range is queryable, and BranchForkAtTimestamp calls are blind guesses.
  - Evidence: Old: `timeRange(): Promise<{oldestTs, latestTs}>` (~/Documents/GitHub/strata-nodesdk/index.d.ts:264); legacy `TimeRange` command existed (docs/executor/executor-command-migration-checklist.md:219). No new command; BranchItem carries generation/created_at version but no data-timestamp bounds (crates/executor-next/src/types/kv_branch.rs:80-89); AdminDescribe/AdminMetrics have no timestamp facts.
  - Recommendation: Add oldest/latest visible timestamps to BranchGet/Describe output or restore a lightweight time_range command.
- **NODE-7** (medium, wire-contract) — **Bytes serialize as JSON integer arrays on the generic dispatch path, with asymmetric input/output.** The Node SDK's generic execute(name, args) path (shipped in the old SDK, index.d.ts:229-244) and any JSON/IPC consumer (MCP) pay ~4x size inflation on values and get keys back in a shape that is unusable in TypeScript without decoding; round-tripping a cursor works but is unreadable and asymmetric with what was sent.
  - Evidence: crates/executor-next/src/types/common.rs:11 `pub struct Bytes(#[serde(with = "serde_bytes")] Vec<u8>)` — serde_json renders output as an array of u8 numbers (`"key": [104,101,121]`) for KvValue, key lists, scan/sample items, and even PageInfo<Bytes> cursors (output.rs:111-188), while input accepts JSON strings (serde_bytes str visitor): send "foo", get back [102,111,111].
  - Recommendation: Give Bytes a human-safe JSON representation (e.g. UTF-8 string with base64 escape form) or document the in-process typed-Command path as the only supported SDK route and constrain the JSON form.
- **NODE-8** (medium, async-semantics) — **No progress or cancellation affordance for long-running commands (model pull, generate, Arrow import/export).** A world-class Node SDK cannot wire AbortSignal or emit progress events for `modelsPull` (the old SDK's setup()/modelsPull UX), so a 5-minute download is an opaque hanging Promise; the CLI consumer cannot render progress bars either. Streaming generation is legitimately post-V1, but pull progress/cancel is not covered by that exclusion.
  - Evidence: InferenceModelsPull triggers multi-GB downloads but `pull_model` returns only a final PullModelOutput (crates/inference-next/src/runtime.rs:240-252); InferenceGenerate, ArrowImport/ArrowExport (command.rs:1141-1194), and SpaceDelete{force}/VectorDeleteAll are similarly unbounded one-shot request/response; the charter/protocol has no progress, cancellation, or job concept.
  - Recommendation: Add a minimal progress/cancel affordance for model pulls (polling command over a pull-job id, or a host-level callback hook on the executor handle) before SDK GA.

### MCP — MCP server parity

- **MCP-5** (medium, metadata-coverage) — **No machine-readable input schema per command — MCP tool inputSchema cannot be generated.** MCP protocol requires a JSON Schema per tool. The old server hand-wrote schemas per tool (e.g. search.rs:22-41); the V1 plan says schemas come from shared metadata, but the metadata has no parameter-level facts, so strata mcp serve would regress to hand-written schemas that can drift from the serde shapes.
  - Evidence: ResolvedCommand.input is just a variant-name string ('Command::KvPut') and fixtures are example JSON, not schemas (idl_tooling.rs:121-168, 196-206); CliCommandEntry (idl_tooling.rs:264-306) likewise has no parameter facts; Command has no schemars derive
  - Recommendation: Add parameter facts to the IDL (name/type/required/doc per field) or derive JSON Schema from Command via schemars and snapshot it in generated/, so MCP tool schemas, CLI help, and SDK docstrings share one source.
- **MCP-6** (medium, error-contract) — **Escape-hatch decode failures are unstructured — no error-registry code for malformed/unknown command JSON.** For MCP the escape hatch IS the wire: when an agent passes a bad tool name or wrong args to strata_command, it should get a stable structured code it can react to (retry vs re-read schema), not a serde prose message. Every consumer of run --command-json (CLI scripts, future MCP serve) currently gets contract-violating unstructured errors on this path.
  - Evidence: cli-next/src/lib.rs:866-885 raw_command_from_sources -> serde_json::from_str -> CliError::Json rendered as prose (lib.rs:911); executor-next/src/error_registry.rs has no invalid_argument.*.command_decode/unknown-command entry; v1-error-and-diagnostics-contract requires <class>.<area>.<detail> and CLAUDE.md rule 29 forbids asserting on prose
  - Recommendation: Register an invalid_argument.executor.command_decode (and arguably not_found.executor.unknown_command) code and provide an executor-owned decode helper (JSON -> Command -> ErrorStatus) so cli-next and mcp serve share one canonical decode path instead of each wrapping serde_json.
- **MCP-7** (medium, consistency) — **No BranchExists command despite Exists variants for every other family.** MCP serve and both SDKs must implement exists-by-probing-BranchGet-and-classifying-not_found, an asymmetry against the five sibling Exists commands; agents hit an avoidable error-path round trip when validating a branch context switch.
  - Evidence: Old server relies on Command::BranchExists for branch_switch validation (~/Documents/GitHub/strata-mcp/src/session.rs:86-95); executor-next has SpaceExists, KvExists, JsonExists, EventExists, VectorExists but no BranchExists in command_variants.txt / command.rs
  - Recommendation: Add BranchExists returning Output::Bool to close the family; it is trivially adapted from BranchGet.
- **MCP-8** (medium, missing-operation) — **No time-range/timestamp-bounds command — agents cannot discover valid inputs for time travel.** BranchForkAtTimestamp and EventRangeByTime exist, but an MCP agent has no way to learn the database's valid timestamp window, so it must guess-and-retry on out-of-range errors. Python SDK exposes time_range as a property today, so both MCP and SDK parity break.
  - Evidence: Old tool strata_db_time_range (~/Documents/GitHub/strata-mcp/src/tools/database.rs:42); no equivalent variant, and AdminDescribe/Info/Metrics (executor-next/src/types/admin.rs:173-192) expose no min/max timestamps; not listed in v1-removed-surfaces.md
  - Recommendation: Expose min/max retained timestamp (and version) either as a small TimeRange command or as fields on AdminDescribe/AdminMetrics — the latter also enriches the orientation story.
- **MCP-9** (medium, agent-orientation) *(planned: M9)* — **Orientation is data-adequate via Describe, but the strata_guide/agents-guide vocabulary orientation has no home yet.** An agent connecting via MCP can orient on what data exists (Describe) but not on what operations exist or how to use the escape hatch; the guide meta-tool is the plan's answer and it depends on MCP-4/MCP-5 metadata plus M9 packaging. Without it the escape hatch's long tail is undiscoverable.
  - Evidence: AdminDescribe returns version, target, default_branch, branches, spaces, primitive summaries, config, capabilities (executor-next/src/types/admin.rs:173-192) — good data orientation; first-run-experience.md §8.3 requires a strata_guide meta-tool and §6 a strata agents guide, but cli-next/src/options.rs has no mcp or agents subcommand and no executor/IDL artifact carries guide text
  - Recommendation: At M9, generate the agents guide from the (backfilled) IDL and serve it both as strata agents guide and as the strata_guide tool; consider a lightweight CommandCatalog output (names + summaries + access mode) so orientation works even over bare executor transports.
- **MCP-10** (low, missing-operation) *(planned: M8)* — **No command to configure the embedding model (old strata_configure_model); config surface is read-only.** An MCP agent cannot select/install the embedding model for autoembedding from the tool surface; acceptable pre-M8 since intelligence-next owns installation, but MCP serve needs some write path (or documented CLI-only flow) for model configuration by V1.
  - Evidence: Old tool strata_configure_model (~/Documents/GitHub/strata-mcp/src/tools/config.rs); executor-next has only ConfigGet and ConfigureGetKey (command.rs:49-54), both reads; CLAUDE.md rule 10: intelligence installs implementations per database at M8
  - Recommendation: Decide at M8 whether model configuration is an executor command (fits 'same verbs everywhere') or a CLI/open-options-only concern, and record it in the IDL either way.
- **MCP-11** (low, missing-operation) *(planned: M6)* — **Clone artifacts (the bundle replacement) have no executor commands yet.** Bundles are correctly filtered, but the agent workflow they served (hand a dataset to another database/session) needs the replacement surface; MCP/SDK parity for export-import currently rests on Arrow only, which drops branch/version semantics.
  - Evidence: Old bundle_export/import/validate tools (~/Documents/GitHub/strata-mcp/src/tools/bundle.rs); bundles are V1-removed and replaced by clone artifacts (CLAUDE.md; roadmap Phase 6 item 8 'dataset clone artifact validation and export/import substrate'); no Clone*/Artifact* variant exists — ArrowImport/ArrowExport are the only data-movement commands
  - Recommendation: When M6 lands the clone-artifact substrate, add export/import/validate commands and IDL entries; until then document Arrow as the interim data-movement path.

### CLI — CLI parity and thinness

- **CLI-5** (medium, correctness) — **Bare invocation silently opens the current directory as a durable database.** CLI users scatter accidental database files into arbitrary directories (e.g. $HOME); this contradicts the recorded distribution decision that bare CLI commands refuse without a path (docs/design/first-run-experience.md).
  - Evidence: open.rs:30-33: `(None, None) => std::env::current_dir()?` then `Executor::open_durable_local(path)` — `strata kv put a b` with no --db creates a database layout in cwd.
  - Recommendation: Refuse with a usage error pointing at --db/--cache when no path is given (except `init`/`command print`), or require an explicit opt-in flag for cwd.
- **CLI-6** (medium, wire-contract) — **--json stream mixes faithful Output serialization with three CLI-invented wire shapes.** Scripts and agents (including MCP wrappers shelling out to the CLI) parsing --json cannot rely on a single executor-defined vocabulary; the error envelope shape can drift per frontend since each surface invents its own.
  - Evidence: `{"type":"init",...}` (init.rs:16), `{"type":"context",...}` (repl.rs:96-104) mimic the Output tag envelope with non-Output tags; the error envelope `{"error": <status>}` is a CLI-local struct (render.rs:26-31 ErrorEnvelope) with no executor-owned definition — the v1 error contract defers wire encoding to the command boundary, which never defines it.
  - Recommendation: Move the error envelope into executor-next as the canonical serialized failure shape (charter: one error protocol), and either add Output variants for init/context or clearly namespace CLI-local shapes.
- **CLI-7** (medium, correctness) — **REPL silently ignores per-line global flags (--json/--raw/--db/--cache/positional path).** An interactive CLI user typing `--json kv list` gets human output with no warning and no error — flags that work at the shell prompt silently no-op inside the REPL.
  - Evidence: repl.rs:139-144 re-parses each line with the full Cli grammar, but handle_line (repl.rs:108-115) consumes only cli.branch/cli.space/cli.command — cli.json, cli.raw, cli.format, cli.db, cli.db_path, cli.cache parse successfully and do nothing.
  - Recommendation: Either honor per-line output-format overrides or reject open/format flags in REPL lines with a usage error.
- **CLI-8** (medium, session-model) — **REPL `use` is a CLI-composed session operation with invented validation and output.** The one stateful session verb the CLI offers is built from two commands plus prose errors instead of an executor session operation, so Python/Node/MCP session-switch semantics will each be reinvented and can diverge (and REPL users lose their space context on branch switch).
  - Evidence: repl.rs:173-201 validates by chaining Command::BranchGet + Command::SpaceExists and hand-rolls the refusal message; success prints CLI-invented `{"type":"context"}`; `use <branch>` without a space silently resets the space override (repl.rs:159-163 + context.set_space(None)).
  - Recommendation: Once executor session scope exists (see CLI-4), make `use` a thin call to it; preserve the space unless explicitly changed, and surface structured error codes.
- **CLI-9** (medium, surface-gap) — **Graph entity-binding feature is entirely unreachable from CLI verbs.** CLI users cannot exercise or inspect graph-to-primitive bindings (a promoted engine capability), making the CLI unusable for debugging binding-backed data even though the executor fully supports it.
  - Evidence: graph add-node hardcodes `binding: None` (lib.rs:715) despite Command::GraphAddNode carrying an optional entity binding (command.rs:979), and GraphBindingsForEntity has no verb — write and read sides both CLI-invisible.
  - Recommendation: Add `--binding <json>` to graph add-node and a `graph bindings <primitive> <key>` verb.
- **CLI-10** (low, consistency) — **`json set` silently coerces malformed JSON to a string, diverging from the canonical structured entrypoint.** A CLI user with a typo'd document (`{"a":1` ) silently stores the broken text as a string value instead of erroring — data quality hazard, and two public entrypoints give different semantics for the same operation.
  - Evidence: parse_relaxed_json_argument (input.rs:32-39): `serde_json::from_str(&text).unwrap_or(Value::String(text))`; the same payload via `command run` fails deserialization. Help text documents the coercion (options.rs:357).
  - Recommendation: Require valid JSON by default and gate coercion behind an explicit `--as-string` flag.
- **CLI-11** (low, polish) — **Deferral message labels permanently-removed features as 'not available yet'; structured-command parse errors bypass the error contract.** CLI users are told removed features are pending (wrong expectations), and structured-entrypoint failures give unparseable prose instead of the code-based errors the v1 diagnostics contract promises every consumer.
  - Evidence: deferred_command (lib.rs:178-182) emits "...not available in the V1 CLI surface yet" for V1-removed txn/flush/compact/tag/note alongside genuinely-planned search/recipe; malformed or feature-absent `command run` JSON surfaces as raw serde prose exit 2 (CliError::Json, lib.rs:884) with no error code and no use of the executor's CliCommandCatalog::suggestions.
  - Recommendation: Split deferral wording into removed vs planned, and map command-JSON parse failures to a structured invalid_argument error, using catalog suggestions for unknown types.

### ENGINE — Engine capability reachability

- **ENGINE-5** (medium, missing-exposure) — **Version-pinned reads (get_at_version) implemented across all capabilities but unreachable.** SDK/CLI outputs hand consumers commit versions (CommitReceipt, Getv history rows) they cannot dereference into a read - a consumer must fork a whole branch at a version just to read one key at that version.
  - Evidence: crates/engine-next/src/data/kv/service.rs:86, json/service.rs:180, vector/service.rs:288, graph/service.rs:302 all implement get_at_version; every command as_of is timestamp micros (e.g. executor/kv.rs:38 "Timestamp::from_micros(as_of)"); only BranchForkAtVersion consumes a CommitVersion.
  - Recommendation: Either add an at_version variant/parameter to the Get commands or document timestamp-only reads as the V1 contract and stop surfacing versions consumers cannot use for reads.
- **ENGINE-6** (medium, planned-gap) *(planned: M6)* — **IPC classification and clone artifacts absent from engine-next (planned M6).** CLI `strata clone` and same-machine IPC sharing have no substrate; Health/Info outputs cannot tell a consumer whether its handle is local or IPC-backed, which the diagnostics contract requires before CLI cutover.
  - Evidence: grep -rn "Ipc|clone" crates/engine-next/src = 0 functional hits; runtime/mod.rs is 1 line; roadmap.md:545-546 Phase 6 items 7-8 ("engine command classification for local and IPC-backed handles", "dataset clone artifact validation"); arch doc line 933 wants diagnostics to show "whether a handle is local or IPC-backed".
  - Recommendation: Planned M6 work; when landing, add the local/IPC fact to AdminHealthSummary/AdminDatabaseInfo so existing Info/Health commands carry it without new variants.
- **ENGINE-7** (medium, flattening) — **JsonGet switches Output variant and drops commit metadata when as_of is set.** Node/Python SDKs must type one command's result as a union of two shapes depending on a parameter, and lose version/timestamp metadata exactly on the time-travel path where provenance matters most.
  - Evidence: crates/executor-next/src/executor/json.rs:44-57 - as_of path returns Output::JsonValue via get_at (bare value); latest path returns Output::JsonVersionedValue via get_versioned. KV keeps versioned output on both paths (kv.rs:38 get_versioned_at). Root cause: JsonService has no get_versioned_at (data/json/service.rs:198 get_at is unversioned).
  - Recommendation: Add JsonService::get_versioned_at mirroring KV and return JsonVersionedValue on both paths of execute_json_get.
- **ENGINE-8** (medium, missing-exposure) — **Space usage introspection (SpaceUsageSummary) is D4-exported but no command reaches it.** CLI/MCP cannot answer "what is in space X" (row counts per capability) without deleting it (SpaceDeleteResult.deleted_rows is the only usage fact exposed); catalog/inspection tooling in MCP is degraded.
  - Evidence: crates/engine-next/src/api/space.rs:303 "pub fn usage(&mut self, space: &ProductSpace) -> EngineResult<SpaceUsageSummary>"; grep usage in crates/executor-next/src/executor = 0 hits; Describe reports primitives only "for the default space" (api/admin.rs:195) and Command::Describe takes no space argument.
  - Recommendation: Add a SpaceStats command (or a space parameter on Describe) mapping to SpaceService::usage.
- **ENGINE-9** (low, surface-hygiene) — **ControlDiagnostics exported at D4 but unreachable - should be testkit-gated or folded into Health.** No consumer harm today, but it is a second, unreachable public diagnostics path alongside AdminService.health - drift risk against hard rule 8 (one canonical path) and rule 33 (pub only for D4 items).
  - Evidence: crates/engine-next/src/api/database.rs:280 pub fn control_diagnostics (ungated, unlike the adjacent #[cfg(any(test, feature = "testkit"))] methods at lines 289-362); ControlDiagnostics/SpaceCatalogDiagnostics re-exported in lib.rs:32,49; zero executor callers.
  - Recommendation: Gate control_diagnostics behind testkit or route its facts through AdminHealthSummary detail and remove the standalone export.
- **ENGINE-10** (low, planned-gap) *(planned: M6/M10)* — **Diagnostics-bucket reporting (retention, derived-state status, degradation) not yet implemented; Metrics is 6 fields.** CLI `metrics`/`health` give operators little beyond liveness; branch-retention blockers and degraded-capability reasons (which the architecture says diagnostics must make visible) cannot be surfaced by any consumer.
  - Evidence: grep retention in crates/engine-next/src = 0; AdminMetricsSummary is target/durable/open/branch-count/space-count/health (api/admin.rs:80-93); arch doc lines 905-950 assign retention reports, derived-state status, and degradation reports to the diagnostics bucket.
  - Recommendation: Planned M6/M10 hardening; extend AdminHealthSummary/AdminMetricsSummary rather than adding new command families.

### DSGN — Design consistency (Stripe-grade)

- **DSGN-6** (medium, pagination) — **Entire event read family is unbounded by default: EventList/EventRange/EventRangeByTime with limit omitted materialize the full log.** MCP event tools (strata-mcp/src/tools/event.rs uses as_of/limit heavily) and CLI reads of long-lived event logs return unbounded payloads unless every caller remembers a limit — the family most likely to grow monotonically is the one without a guardrail.
  - Evidence: engine-next/src/data/event/service.rs:345-362 list_page with limit None retains all rows; :228-277 range materializes the whole window; executor passes Option through with no default (executor/event.rs:85,131,157,203). Json/Vector/Graph families default to 100; events default to everything.
  - Recommendation: Apply the same default limit (100) to EventList/EventRange/EventRangeByTime; they already return honest has_more/cursor so pagination just works.
- **DSGN-7** (medium, output-envelope) — **Write/delete acknowledgement shapes diverge per family: Vector/Event/Graph duplicate version+timestamp at top level, KV/JSON bury them in commit; deleted flag presence varies.** SDK and IDL generators must special-case where the commit version lives for each family and how to detect a no-op delete (effect.kind for KV/JSON, deleted bool elsewhere) — exactly the per-surface divergence the executor charter exists to prevent.
  - Evidence: output.rs:139-146 WriteResult{key,effect,commit} vs output.rs:218-233 VectorWriteResult adding top-level version/timestamp/vector_revision, output.rs:326-339 EventAppendResult and output.rs:416-452 Graph*WriteResult duplicating version+timestamp; KvDelete/JsonDelete DeleteResult (output.rs:148-156) has no deleted bool while Vector/Graph/Space/Branch deletes all carry one.
  - Recommendation: Pick one rule (version/timestamp live only in CommitReceipt; every delete result carries deleted: bool) and normalize the five write-ack variants to it.
- **DSGN-8** (medium, output-envelope) — **Output variant depends on input parameters: KvList returns Keys or KeysPage (byte-identical shapes), JsonGet silently drops version metadata under as_of while KvGet keeps it.** Consumers must match two tags for one command (CLI, MCP JSON handling), and a Python SDK exposing .version on get() results has it present for KV time-travel reads but None for JSON time-travel reads — an inexplicable asymmetry to users.
  - Evidence: output.rs:123-137 Keys and KeysPage have identical fields, selected by whether limit/cursor was passed (executor/kv.rs:81-96); executor/json.rs:43-56 JsonGet returns unversioned JsonValue when as_of is set but JsonVersionedValue otherwise, while executor/kv.rs:35-45 KvGet returns KvVersionedValue on both paths.
  - Recommendation: Collapse Keys into KeysPage, and make JsonGet as_of return JsonVersionedValue like the latest path.
- **DSGN-9** (medium, capability-symmetry) — **Graph is the only data family with zero as_of time-travel parameters (14 variants), and the only one without exists operations.** Old MCP had no graph tools so no demonstrated downstream demand, but the V1 MCP/SDK cannot offer 'query the graph as of the fork point' even though BranchForkAtTimestamp makes timestamp addressing a core product concept — an internal-symmetry hole that will read as arbitrary in docs.
  - Evidence: command.rs:916-1139: no graph variant carries as_of, while KV/JSON/Vector/Event reads do (e.g. command.rs:153-155, :324-326, :566-568, :783-785); graph also has no GraphNodeExists/GraphEdgeExists while Kv/Json/Vector/Event/Space all expose Exists.
  - Recommendation: Add as_of to graph read variants (GetNode/GetEdge/Neighbors/ListNodes/GetMeta) in an engine slice, or document the exclusion as intentional in the executor IDL.
- **DSGN-10** (medium, batch-consistency) — **Batch semantics diverge: duplicate keys are a whole-command error for KV but last-write-wins for JSON; KvBatchExists bypasses the shared BatchResult envelope.** SDK batch helpers cannot document one duplicate-key rule ('KV raises, JSON overwrites, Vector unspecified'), and KvBatchExists results cannot be processed by the same generic batch-result path SDKs build for the other nine batch commands.
  - Evidence: executor/kv.rs:142,222 reject_duplicate_valid_keys errors the whole KvBatchPut/KvBatchDelete (batch.rs:262-276), while executor/json.rs:109-123 accepts duplicate doc keys and adjusts effect via written_docs BTreeSet; executor/kv.rs:256 returns Output::BoolList(Vec<bool>) instead of BatchResult, losing per-item index/status that every other batch op carries (types/common.rs:174-397).
  - Recommendation: Pick one duplicate-key policy across families and return BatchResult<bool-like item> from KvBatchExists.
- **DSGN-11** (medium, capability-symmetry) *(planned: M6)* — **JSON secondary-index lifecycle (Create/Drop/ListIndexes) ships with no query verb that consumes the indexes.** A Python/MCP consumer can create a numeric index on .age but has no command to run a filtered lookup with it; until the query surface lands, the three index commands are dead weight on the public protocol.
  - Evidence: command.rs:444-478 exposes JsonCreateIndex/JsonDropIndex/JsonListIndexes, but there is no JsonQuery/JsonFind variant among the 103, and grep of crates/engine-next/src/data/json/ shows no query/find service method — the indexes are write-only from the public surface.
  - Recommendation: Track the JSON index-query command as a planned engine-retrieval slice; if it is post-V1, consider deferring the index-lifecycle commands too so the V1 surface has no orphaned capability.
- **DSGN-12** (low, naming) — **Assorted naming/addressing warts: ConfigureGetKey, EventLen + Uint-vs-EventLength counts, ArrowImport.file_path vs ArrowExport.path, GraphBatch UpsertNode vs single-op AddNode, required direction params.** Each wart is small, but together they force SDK/IDL generators and docs to carry a list of exceptions (CLI users type event_len not event_count; MCP schemas must mark direction required only on two families), eroding the 'guessable API' quality bar.
  - Evidence: command.rs:51 ConfigureGetKey ('configure_get_key') vs :49 ConfigGet; :819 EventLen vs Kv/Json/VectorCount, with outputs Output::Uint (kv.rs:291) vs EventLength{count} (event.rs:116); :1149 file_path vs :1177 path; types/graph.rs:180-209 batch ops named UpsertNode/DeleteNode vs single-op AddNode/RemoveNode/GraphDelete; EventRange/GraphNeighbors direction is wire-required despite enum #[default] (types/event.rs:8) under deny_unknown_fields; JSON path has no '$' default though old SDK defaulted it (strata-python __init__.py:209).
  - Recommendation: One rename/normalization sweep before the wire format freezes: config_get_key, event_count with Uint, unify path field names, align graph batch verbs with single-op verbs, add serde defaults for direction, default JSON path to '$'.

### EVOL — Wire evolution and versioning

- **EVOL-4** (medium, api-stability) — **Zero #[non_exhaustive] in executor-next; ExecutorErrorClass violates hard rule 28 and all wire enums are exhaustive.** Rust-level consumers — the PyO3 and Node FFI bridge crates that must match on Output/ExecutorErrorClass — break at compile time on every additive variant, turning each v1.1 addition into a coordinated breaking release; ExecutorErrorClass is a literal violation of CLAUDE.md hard rule 28.
  - Evidence: grep -rn non_exhaustive crates/executor-next/src/ matches only a Debug finish_non_exhaustive() (error.rs:62); ExecutorErrorClass at error.rs:130 is a public Serialize/Deserialize error enum without the attribute; Command (command.rs:21), Output (output.rs:19) and ~19 public wire enums (BatchStatus, MutationEffectKind, AdminHealthStatus, BranchStatus, GraphDirection, ...) are all exhaustive
  - Recommendation: Add #[non_exhaustive] to ExecutorErrorClass (rule 28) now, and decide explicitly (in the EVOL-3 playbook) which wire enums — at minimum Output and status/kind enums — take #[non_exhaustive] before SDK FFI crates are cut, since adding it later is itself breaking.
- **EVOL-5** (medium, sdk-generation) *(planned: EX5-EX7 (executor plan tail))* — **IDL overlay and request fixtures cover only 32 of 103 commands.** The IDL is the designated substrate for SDK/MCP/docs generation (it carries mcp_name, cli_path, per-command fixtures, errors), so Python/Node SDK and MCP tool codegen cannot be driven from it for ~70% of the surface — json, event, graph, branch, space, admin, arrow, and inference families have no authored contract entries or request goldens.
  - Evidence: crates/executor-next/idl/v1/commands/ contains only kv.yaml (13 ids) and vector.yaml (19 ids); tests/fixtures/requests/v1/ has only kv and vector dirs, while responses/v1/ covers 14 families (83 fixtures)
  - Recommendation: Finish authoring IDL command entries and request fixtures for the remaining families before SDK codegen starts; treat the command-index generator as the single source for SDK stubs and MCP tool schemas.
- **EVOL-6** (medium, wire-ownership) — **Inference Output variants embed strata-inference-next types, putting part of the wire outside the executor's contract governance.** A serde change inside strata-inference-next silently changes the executor wire that SDKs and the MCP escape hatch depend on, bypassing the golden-fixture/IDL regime that protects every other family; feature-gating also means the command vocabulary differs per build, discoverable only via the AdminCapabilities.inference boolean.
  - Evidence: output.rs:496-536 — InferenceModels/InferenceGeneration/InferenceEmbeddings etc. carry strata_inference_next::{ModelInfo, GenerateResponse, EmbedResponse, RankResponse, ModelCacheStatus} directly under #[cfg(feature = "inference")]
  - Recommendation: Either mirror these payloads into executor-owned DTOs (like every other family) or pin them with executor golden fixtures and an explicit note that inference-next's response types are frozen wire contracts; keep the capability flag as the documented probe for feature-gated vocabulary.
- **EVOL-7** (medium, newtype-stability) — **Bytes newtype deviates from hard rule 35 (no repr(transparent)/serde(transparent)) without documentation or guard.** The deviation is deliberate (serde(transparent) conflicts with the serde_bytes field attr) but unrecorded, so a future 'rule-35 cleanup' could silently flip the KV key/value wire encoding from number-array to plain array/string and break every pinned SDK; FFI consumers of ExecutorError lack the repr guarantee rule 35 promises.
  - Evidence: types/common.rs:11 `pub struct Bytes(#[serde(with = "serde_bytes")] Vec<u8>);` — neither attribute; JSON wire shape is an array of numbers (tests/command_contract/shapes.rs:20 asserts json!([110,101,119])); ExecutorError (error.rs:354) has serde(transparent) but not repr(transparent)
  - Recommendation: Add a rationale comment on Bytes stating the rule-35 exemption and that the number-array JSON shape is frozen (it is already golden-tested — reference the fixture), and add #[repr(transparent)] to ExecutorError.
- **EVOL-8** (low, forward-compat) — **No unknown-variant tolerance story for clients decoding Output from a newer binary.** A pinned v1.0 SDK talking to a v1.1 binary (e.g. system-installed strata driven over CLI JSON) hard-fails on any new output variant even for commands it never issues; embedded SDKs that version-lock the binary are safe, which is why this is low rather than high.
  - Evidence: output.rs:19 adjacently tagged enum with no #[serde(other)] catch-all; unknown `type` tag is a hard deserialize error; no documented SDK posture for tolerating new output variants
  - Recommendation: Document in the EVOL-3 playbook that generated SDK decoders must treat unknown output `type` tags as a typed 'unrecognized output (upgrade client)' result rather than a parse panic, and consider guaranteeing new outputs are only ever returned for new commands.

### ERR — Error boundary

- **ERR-3** (medium, duplicate-surface) — **Second lossy class taxonomy (ExecutorErrorClass) lives on the same error with no deletion condition.** SDK authors (Python/Node) binding the error surface must choose between two class enums; picking class() silently mislabels an access-denied or quota error as 'unavailable', producing wrong retry behavior in consumer code. Nothing marks the legacy enum transitional.
  - Evidence: crates/executor-next/src/error.rs:128-149 defines legacy ExecutorErrorClass alongside public ErrorClass; executor_class_for_status (error.rs:751-760) collapses AccessDenied | Unsupported | ResourceExhausted | Io | FailedPrecondition all into ExecutorErrorClass::Unavailable. Both class() and public_class() are pub on ExecutorError. CLAUDE.md rule 8: no two public surfaces for the same behavior; PR discipline requires transitional paths to carry a deletion condition.
  - Recommendation: Mark ExecutorErrorClass and ExecutorError::class() deprecated/transitional with an explicit deletion slice before M9B rename, or delete now — every in-repo consumer can use public_class().
- **ERR-4** (medium, hint-rendering) — **CLI human output drops hint, docs_url, and suggested_fix that the registry already provides (first-run 7.2).** CLI users and agents driving the CLI in human mode get no actionable next step on failure — the entire 'errors that teach' P4 pillar is unshipped on the highest-traffic channel even though executor-next supplies everything. JSON channel is complete, so SDK/MCP consumers are unaffected.
  - Evidence: crates/cli-next/src/render.rs human_error_line renders only '{code}: {message} ({reference_id})' while ErrorStatus carries per-code suggested_fix (non-empty for all ~190 codes, enforced by tests/error_and_guards.rs:266-301) and docs_url. docs/design/first-run-experience.md §7.2: 'CLI human output renders the hint'.
  - Recommendation: Extend human_error_line to a two-line render: code+message+ref, then 'hint: {suggested_fix} — {docs_url}'. Pure CLI change; no executor work needed.
- **ERR-5** (low, hint-rendering) — **Default ref URL shape and domain diverge from the decided stable-slug format.** Every consumer bakes strata.dev anchor-style URLs into logs, test fixtures (tests/error_and_guards.rs:129 asserts the strata.dev URL), and user-visible errors; changing the domain later churns fixtures and any cached agent knowledge.
  - Evidence: crates/executor-next/src/error.rs:17 DEFAULT_DOCS_BASE_URL = 'https://strata.dev/docs/errors' rendering '<base>/registry#<code>'; docs/design/first-run-experience.md §7.2 specifies short stable slugs like 'stratadb.org/e/<code>' (and the distribution decision fixed the org/domain as stratadb). with_error_render_config, the injection point, has zero consumers (only the lib.rs re-export).
  - Recommendation: Decide the final ref format now, switch DEFAULT_DOCS_BASE_URL to the stratadb.org short-slug form, and have cli-next install the render config explicitly so the injection path is actually exercised.
- **ERR-6** (low, correctness) — **RetryPolicy::Never is used as an 'unset' sentinel, so a deliberate Never can be silently upgraded to retryable.** An SDK or IPC layer proxying an ErrorStatus through executor types can observe the retry policy flip from never to retryable, causing a client to retry an operation the producer explicitly marked non-retryable.
  - Evidence: crates/executor-next/src/error.rs:654-662 override_retry_policy replaces supplied Never with the registry default; ExecutorError::from_status re-normalizes deserialized statuses (error.rs:400-404), so a wire status carrying retry_policy=never for a code whose registry default is same_request/after_state_change is mutated on ingest — round-trip is not identity.
  - Recommendation: Use Option<RetryPolicy> (or a distinct Unset marker) for 'defer to registry' instead of overloading Never; only apply the registry default when the caller supplied nothing.
- **ERR-7** (low, registry-hygiene) — **Executor registry has dead entries and no reverse-completeness test; fallback code name diverges from the contract.** Dead registry entries surface in `strata agents errors` style dumps and generated docs as codes no consumer can ever trigger; the contract/code name mismatch means anyone implementing against the contract doc matches on the wrong internal fallback code.
  - Evidence: crates/executor-next/src/error_registry.rs:19,28 register failed_precondition.executor.runtime_closed and .space_not_empty which no executor source ever emits (only matched in error.rs:743-748); engine-next has an every_registered_code_appears_in_source test (engine registry.rs:683) but executor-next has only the forward direction (error_and_guards.rs:304). Contract line 583 names the unknown-code fallback internal.command_invariant; implementation uses internal.executor.unregistered_code (error_registry.rs:226) without the contract's required boundary log.
  - Recommendation: Add the reverse dead-entry test to executor-next, delete or justify the two dead entries, and reconcile the fallback code name with the contract (update whichever side is wrong).
- **ERR-8** (low, hint-quality) — **Layout-error hints instruct users to 'run the required migration' — tooling V1 explicitly does not ship.** A first-run user or agent hitting a pre-V1 database follows the hint to a nonexistent migration command and dead-ends — the exact failure mode the errors-that-teach design exists to prevent, surfaced verbatim through CLI, Python, Node, and MCP.
  - Evidence: crates/engine-next/src/diagnostics/registry.rs:464,483: suggested fix for failed_precondition.engine.layout_version et al. is 'Open the database with a compatible Strata version or run the required migration.' CLAUDE.md rules 41-42: no migration tooling for pre-V1 databases; pre-V1 databases are rejected with structured errors.
  - Recommendation: Reword the layout-family suggested fixes to the actual V1 remedy: open with a compatible version or recreate the database; drop the word 'migration'.

### THIN — Charter conformance (thinness / one model)

- **THIN-3** (medium, split-authority) — **KV/JSON mutation effects fabricated by non-atomic executor pre-reads; engine owns the same fact for vectors.** SDK/MCP consumers displaying created-vs-updated can receive a wrong effect whenever another writer lands between the exists probe and the write; JSON batch set pays N+1 engine reads on a hot path. Two different authorities own the same product fact depending on family (CLAUDE rule 7).
  - Evidence: kv.rs:21-23 'let effect = upsert_effect(service.exists(&key)?); let outcome = service.put(...)'; kv.rs:150-155 batch_exists then put_batch; json.rs:27-28 same for set; json.rs:110-127 per-entry service.exists() inside the batch-set loop (N+1 engine reads). Contrast vector.rs:121 where the engine outcome reports created() natively.
  - Recommendation: Have engine KV put/put_batch and JSON set/batch_set_or_create return created/updated in their outcomes (as vector upsert already does) and delete the executor pre-reads.
- **THIN-4** (medium, one-canonical-path) — **Executor reimplements pagination and flips output shape for KvList.** Node/Python SDKs must union-handle two output variants for one command; time-travel listing loads the entire keyspace into executor memory; executor-side cursor semantics can drift from engine list_page semantics since two implementations own paging.
  - Evidence: batch.rs:9-39 page_or_keys materializes the full key list then filters 'key.as_bytes() > cursor.as_bytes()' and truncates; used for as_of (kv.rs:77-79) and cursor-without-limit (kv.rs:89-92) while engine list_page (kv.rs:83) serves only limit-without-as_of. Same command returns Output::Keys or Output::KeysPage depending on argument presence (kv.rs:84-96).
  - Recommendation: Add engine list_page_at (as_of-aware paging) so the executor always delegates, and collapse KvList to a single paged output variant.
- **THIN-5** (medium, consistency) — **Default list-limit policy diverges by family: JSON/vector/graph silently truncate at 100, KV is unbounded.** An MCP tool or SDK iterating JsonList without a limit silently sees at most 100 documents (page.has_more must be checked) while the identical KvList call returns everything — an executor-invented, family-divergent default that surprises consumers building generic listing code.
  - Evidence: mod.rs:93-95 DEFAULT_JSON_LIST_LIMIT/DEFAULT_VECTOR_LIST_LIMIT/DEFAULT_GRAPH_LIST_LIMIT = 100; json.rs:236 'optional_limit(limit)?.unwrap_or(DEFAULT_JSON_LIST_LIMIT)'; kv.rs:89-96 KvList with no limit returns the full key list.
  - Recommendation: Pick one policy (default page size everywhere, applied uniformly) and document it on the Command variants; the default constant should ideally come from one place, not three per-family constants.
- **THIN-6** (medium, one-canonical-path) — **create_branch_from_head is a second public path that bypasses execute().** Violates the charter's one-public-model rule (rewrite rule 4) and CLAUDE rule 8: a Rust caller has a richer/different path than the command surface for the same operation, and it skips dispatch-level branch-name/session handling that execute() applies.
  - Evidence: mod.rs:228-239: pub fn create_branch_from_head calls 'self.database.branches()?; branches.fork_current(&source, branch)?' directly, duplicating Command::BranchForkCurrent and facade::branch_fork_current, and discards the fork outcome. Sole consumer: executor-next/tests/kv_behavior.rs:170.
  - Recommendation: Delete it and port the one test to execute(Command::BranchForkCurrent) or the existing facade method.
- **THIN-7** (medium, consistency) — **KV-only duplicate-key batch rejection is executor-invented policy inconsistent across families.** SDK authors writing generic batch helpers get three different duplicate semantics: KV hard-fails the entire batch, JSON last-write-wins with synthesized effects, vector is engine-defined. Nothing in the output contract explains why.
  - Evidence: batch.rs:262-283 reject_duplicate_bytes fails the WHOLE command with 'invalid_argument.executor.kv_batch_duplicate_key'; applied only at kv.rs:142 (batch put) and kv.rs:222 (batch delete). JSON batch set instead tolerates duplicates and fakes effects via a written_docs BTreeSet (json.rs:109,121-122); vector batch upsert allows duplicates with no special handling.
  - Recommendation: Decide one duplicate policy (per-item rejection fits the Itemwise contract better than whole-command failure) and apply it to every batch family, or move duplicate semantics into the engine batch APIs.
- **THIN-8** (medium, split-authority) — **Vector batch paths duplicate engine semantic validation with executor-owned error codes.** Consumers asserting on error codes (as the V1 contract requires) see different codes for the same dimension-mismatch or missing-collection failure depending on whether they used the single or batch command; there is also a TOCTOU between the info pre-read and the batch write.
  - Evidence: vector.rs:358-374 batch upsert pre-fetches collection info and rejects per-item dimension mismatch with 'invalid_argument.executor.vector_dimension' (vector_convert.rs:131-136); vector.rs:416,454 batch get/delete pre-check 'not_found.executor.vector_collection'. Single-item upsert (vector.rs:101-123) performs no such pre-check and surfaces the engine's own error for the identical failure.
  - Recommendation: Let the engine batch APIs report per-item dimension/collection errors and pass them through, so one authority produces one code for one failure.
- **THIN-9** (low, contract-documentation) — **Itemwise BatchMode contract does not state that engine-application failure aborts the whole command.** An SDK cannot tell from the contract whether a returned error means nothing was applied (KV/JSON/vector) or whether it should have inspected per-item results (Event); retry logic written against one family is subtly wrong for another.
  - Evidence: types/common.rs:141-143 documents Itemwise as 'Valid items are applied together while invalid items are reported'; in KV/JSON/vector the single engine batch call is '?'-propagated (e.g. kv.rs:155 'service.put_batch(engine_entries)?'), so valid items are all-or-nothing, whereas Event itemwise outcomes genuinely come per-item from the engine (event.rs:21-28).
  - Recommendation: Document on BatchMode::Itemwise that input-invalid items fail positionally while the valid subset commits atomically, and note the Event family exception, or unify by adopting the Event model everywhere.
- **THIN-10** (low, surface-growth) — **68-method facade is lossless but has effectively zero consumers and no mechanicalness guard.** No current harm — it complies with charter rewrite rule 4 — but ~900 LOC of unused pub surface is exactly the substrate the legacy Strata API regrew from, and nothing stops a future method from adding a default or a second engine call unnoticed.
  - Evidence: executor/facade/* contains 68 pub methods, every one verified as build-one-Command + self.execute (e.g. facade/kv.rs:5-16); workspace grep finds a single call site outside the facade (tests/event_behavior.rs:351); cli-next uses execute() exclusively (cli-next/src/lib.rs:110-143).
  - Recommendation: Either prune the facade to the handful of methods a real consumer needs, or add a guard test that each facade method's Output equals executing the equivalent Command, locking in mechanicalness.
- **THIN-11** (low, batch-semantics) — **Arrow import is non-atomic and discards progress counts on mid-file failure.** A CLI user whose import fails midway has partially imported data with no report of rows_imported/batches_processed, making cleanup or resume guesswork.
  - Evidence: arrow/import.rs:88-95: each record batch is committed via execute(Command::KvBatchPut)? inside the loop; an error on batch N+1 returns Err, dropping the ImportCounts accumulated for batches 1..N which remain durably committed.
  - Recommendation: On failure, return a structured error carrying the partial ImportCounts (the error contract's details vector fits this), and document that import commits per record batch.
- **THIN-12** (low, conversion-hygiene) — **Minor conversion/feature blemishes: silent usize saturation and feature-dependent arrow error identity.** Practically unreachable saturation, but it contradicts the crate's own no-silent-defaulting discipline; the arrow feature-ordering quirk means a consumer probing a non-arrow build gets a misleading input error instead of failed_precondition/unimplemented for the missing feature.
  - Evidence: kv_json_convert.rs:52-54 'u64::try_from(value).unwrap_or(u64::MAX)' silently saturates (the crate's only silent-defaulting conversion, vs input_common.rs:77-96 which errors); arrow_commands.rs:48-54 (cfg not-arrow) returns invalid_argument.executor.arrow_input_missing for a missing file BEFORE the feature-disabled error, so the error code for the same input depends on build features.
  - Recommendation: Make usize_to_u64 infallible-by-construction with a rationale comment or an expect, and return the feature-disabled error unconditionally before touching the filesystem.

### META — Machine-readable metadata readiness

- **META-5** (medium, accuracy) — **Argument accuracy is pinned only by single example fixtures; optional fields unverified and examples not embedded.** A renamed optional field on a covered command would not break CI; and the agents guide / MCP examples cannot be rendered from the shipped catalog because fixture bytes live only in the repo test tree.
  - Evidence: idl_tooling.rs:1426-1435 deserializes each request fixture as Command (deny_unknown_fields, command.rs:20) — strong for fields present — but e.g. fixtures/requests/v1/kv/put.json contains only key+value, so optional args are never exercised; fixture refs in the embedded catalog are repo-relative test paths (cli_metadata.rs:281-289) whose content does not ship.
  - Recommendation: Require fixtures (or an alternate fixture per command) exercising all optional fields, and embed request/response example content into the generated CLI index so examples ship with the binary.
- **META-6** (medium, codegen-readiness) *(planned: M9)* — **MCP name/description resolved but dropped from the embedded catalog, and default MCP descriptions are boilerplate.** An MCP generator cannot use the runtime catalog (must run repo-side with the idl-tooling feature against command-index.json), and unless every prose file authors mcp_description, generated MCP tools ship with useless one-line descriptions — a direct quality regression vs the 61 curated tools in strata-mcp.
  - Evidence: idl_tooling.rs:816-842 cli_entry_from_resolved omits the mcp field when building CliCommandEntry; idl_tooling.rs:1048-1053 defaults mcp description to `format!("Run the {} command.", command.id)` when prose frontmatter omits mcp_description.
  - Recommendation: Either carry mcp.{name,description} into the CLI/embedded artifact or define the MCP-flavored generated artifact now; lint that mcp_description is authored (non-default) for every command before MCP generation lands.
- **META-7** (medium, codegen-readiness) — **Error metadata is code+URL only (no hint/class/retry join) and hardcodes the wrong docs domain (strata.dev vs stratadb.org).** The strata agents errors cheat sheet and SDK exception docs need hint+ref per code (first-run-experience §7.2 D4); out-of-process consumers of the JSON artifact cannot recover them, and every generated artifact would ship dead-domain error links.
  - Evidence: Per-command errors carry only {code, docs} (cli_metadata.rs:269-276); the registry's message_template/suggested_fix/class/retry_policy (crates/engine-next/src/diagnostics/registry.rs:13-28) are not joined into the artifact. URLs hardcode https://strata.dev/docs/errors/registry# (idl_tooling.rs:1196, cli_metadata.rs:431, error.rs:17) while the product plan uses stratadb.org (first-run-experience.md §7.2, install flows).
  - Recommendation: Join class/retry_policy/message_template/suggested_fix into the error refs at generation time, and centralize the docs base URL in one place aligned with the stratadb.org decision.
- **META-8** (low, drift-prevention) — **Enum-variant validation source is fragile text parsing of command.rs/output.rs.** A formatting or nesting change to command.rs silently changes the validated variant set — validation could degrade (accepting stale refs) without any test failing, weakening the drift guarantee the IDL relies on for covered commands.
  - Evidence: idl_tooling.rs:1254-1280 enum_variants() keys on exactly-4-space indentation and a leading uppercase character to harvest variant names from source text.
  - Recommendation: Replace with a compile-time source (e.g. a strum-derived VARIANTS const on Command/Output, or a serde-based probe) so the validator sees the enum the compiler sees.
- **META-9** (low, versioning) *(planned: M9)* — **No stability/deprecation annotations beyond wire_status, and exact-match schema versioning makes all evolution breaking.** SDK/MCP/docs generators cannot express command maturity or deprecation timelines in generated artifacts, and once external consumers pin strata.cli.v1 every metadata enrichment (which META-1/6/7 all require) is a coordinated breaking bump.
  - Evidence: wire_status admits only stable|transitional (idl_tooling.rs:1329-1336); no since/deprecated/experimental fields exist; cli_metadata.rs:305-316 rejects any schema_version or generator_version other than the exact supported strings, and CliCommandIndex is deny_unknown_fields (cli_metadata.rs:174), so even additive fields break older readers.
  - Recommendation: Before M9 generators consume the artifact, decide the evolution policy: add a stability field, and either tolerate unknown fields in readers or define a minor-version negotiation.

### SESS — Session and lifecycle model

- **SESS-3** (medium, dual-path) — **create_branch_from_head bypasses command dispatch — second public path for BranchForkCurrent.** Violates charter/CLAUDE rule 8 (one canonical path per operation): SDK authors binding the Rust surface see two ways to fork a branch, and the bypass skips execute()'s uniform validation/output/error envelope, so behavior can drift from the Command path unnoticed.
  - Evidence: crates/executor-next/src/executor/mod.rs:228-239: public method calls `self.database.branches()?.fork_current(...)` directly instead of dispatching Command::BranchForkCurrent; only consumers are executor-next's own tests (tests/kv_behavior.rs:170 etc.).
  - Recommendation: Delete the method and have tests use execute(Command::BranchForkCurrent) or the facade branch_fork_current wrapper (which already delegates through execute).
- **SESS-4** (medium, session-model) — **No session default space: with_default_branch exists but space is hardwired to the DEFAULT_SPACE constant.** SDKs building tenant- or space-scoped sessions (e.g. a Python `db.space("tenant_a")` handle, MCP per-agent space isolation) must thread the space string into every one of the ~90 space-scoped calls themselves; the branch/space session asymmetry makes the wrapper code irregular.
  - Evidence: crates/executor-next/src/executor/input_common.rs:11-13 `product_space(space)` always falls back to constant DEFAULT_SPACE; Executor holds only `default_branch: String` (executor/mod.rs:167); charter lists 'space context' under Session Semantics (docs/executor/executor-charter.md:86) and the plan's Session deliverable includes it (executor-minimal-surface-implementation-plan.md:190).
  - Recommendation: Add `with_default_space` (and a getter) mirroring with_default_branch, resolving via the same command-field-wins rule, or document that space context is intentionally per-command in V1.
- **SESS-5** (medium, dual-path) — **ConfigureGetKey is a redundant, legacy-named projection of ConfigGet, and unknown keys return null instead of an error.** Every SDK and MCP tool catalog carries two config commands where one suffices, with an inconsistent Config/Configure naming split baked into the frozen wire contract; a typo'd key ('durible') silently returns null instead of an error, which is a debugging trap for CLI and MCP users.
  - Evidence: engine config_value serves a closed 4-key allowlist (target/created/durable/default_branch) that is exactly ConfigGet's summary (crates/engine-next/src/api/admin.rs:326-351); unknown key → `_ => Ok(None)` (admin.rs:349); the 'Configure' prefix survives from the dropped ConfigureSet write family (docs/executor/executor-command-migration-checklist.md:226,246-247).
  - Recommendation: Before wire freeze, collapse to `ConfigGet { key: Option<String> }` (or rename ConfigureGetKey → ConfigGetKey), and return invalid_argument for keys outside the closed allowlist.
- **SESS-6** (medium, lifecycle) — **Executor constructors expose no open options; memory budget and default branch at open require a direct engine-next dependency.** Python/Node SDKs cannot expose the memory_budget knob (central to the unified-scale story) or an at-open default branch through the executor surface alone — they must import engine-next and use Database::open_* + from_database, creating two open paths; the CLI currently has no memory-budget flag at all.
  - Evidence: crates/executor-next/src/executor/mod.rs:174-183 hardcodes `CacheOpenOptions::new()` / `DurableLocalOpenOptions::new()`; the options carry `with_memory_budget` and `with_default_branch` (crates/engine-next/src/api/options.rs:25-79); executor lib.rs re-exports neither Database nor the options types (lib.rs:32-34); cli-next opens only via Executor::open_* (crates/cli-next/src/open.rs:20,33).
  - Recommendation: Add option-taking constructors (e.g. `open_durable_local_with(path, DurableLocalOpenOptions)`) or re-export the engine open-options types from executor-next so one crate dependency yields the full open surface.
- **SESS-7** (low, session-model) — **Default branch is builder-only; no mutable setter for SDK checkout/on_branch semantics.** A PyO3/napi wrapper implementing checkout()/on_branch() must either Option::take-and-replace the whole Executor or shadow-track the current branch and inject it into every command — workable (all scoped variants take Option<branch>) but awkward and easy to get subtly wrong in the context-manager restore path.
  - Evidence: crates/executor-next/src/executor/mod.rs:208 `pub fn with_default_branch(mut self, ...) -> ExecutorResult<Self>` consumes self; old SDK mutates branch context on a live handle: ~/Documents/GitHub/strata-python/python/stratadb/__init__.py:1118 `def checkout` / :1154 `def on_branch` context manager.
  - Recommendation: Add `set_default_branch(&mut self, branch) -> ExecutorResult<()>` alongside the builder, reusing the same name validation.
- **SESS-8** (low, test-coverage) — **No executor-level tests for double-close idempotence or the Send+Sync/serialized-execution contract.** SDK finalizers (Python __del__ plus explicit close(), Node GC hooks) routinely double-close; today's safety is an untested engine implementation detail that a refactor could regress without any executor gate catching it.
  - Evidence: Double-close is safe only via engine internals (crates/engine-next/src/api/database.rs:368-375 last_close short-circuit) — no executor test calls close() twice; no test in crates/executor-next/tests/ uses std::thread or asserts auto-trait bounds.
  - Recommendation: Add tests: close() twice returns Ok both times, and a static Send+Sync assertion; optionally a smoke test executing commands from a spawned thread through Arc<Mutex<Executor>>.

### DTO — Protocol DTO quality

- **DTO-4** (medium, wire-stability) — **Zero #[non_exhaustive] in the crate: ExecutorErrorClass violates rule 28 verbatim; Command/Output growth breaks Rust consumers.** Python SDK (PyO3) and Node SDK (NAPI) layers match on Output/ExecutorErrorClass in Rust; every M8 search/RAG variant addition and every new error class becomes a breaking compile error downstream instead of a graceful fallthrough. Output currently has no slot for M8 retrieval results, so growth is certain.
  - Evidence: grep non_exhaustive over crates/executor-next/src returns only a Debug finish_non_exhaustive (error.rs:62). Public error enum ExecutorErrorClass at error.rs:130, Command at command.rs:21, Output at output.rs:19, VectorFilterOp (vector_input.rs:57, single variant Eq), JsonIndexType, GraphBatchOperation — none marked non_exhaustive.
  - Recommendation: Add #[non_exhaustive] to ExecutorErrorClass (rule 28), Output, Command, and all growing DTO enums (VectorFilterOp, JsonIndexType, BatchItemStatus, GraphBatchOperation, Arrow enums) in one slice before any external consumer pins the crate.
- **DTO-5** (medium, boundary-validation) — **No batch-size or limit caps: unbounded Vec entries and usize::MAX default limits flow to engine.** An MCP tool or SDK caller can submit a 10M-entry batch or limit=2^63 list and the executor materializes it all in one engine commit/scan — no invalid_argument or resource_exhausted at the protocol edge, so misbehaving agent loops degrade the whole embedded process. SDKs also cannot document a supported max batch size.
  - Evidence: command.rs:205-236 KvBatchPut/Get/Delete take uncapped Vec<...>; executor/batch.rs:20 'optional_limit(limit)?.unwrap_or(usize::MAX)' for KvList paging; executor/vector.rs:288-292 k validated only as fits-usize (invalid_argument.executor.vector_limit); input_common.rs:77-88 optional_limit checks platform fit only. Duplicate batch keys ARE rejected (batch.rs:268-283).
  - Recommendation: Add a protocol-level max batch item count (e.g. 10k, invalid_argument.executor.batch_too_large) and a max/clamped page limit; replace the usize::MAX default in page_or_keys with the same DEFAULT_*_LIST_LIMIT=100 convention used by vector/json/graph.
- **DTO-6** (medium, efficiency) — **Every read deep-clones payloads (values, embeddings, metadata) from owned engine results instead of moving.** Python/Node SDK read throughput pays one full extra copy per row: a 1536-dim f32 embedding is 6KB copied per vector get/batch-get item/search match, and KV gets copy the whole value. On batch reads and top-k searches this is the dominant avoidable cost in the executor layer.
  - Evidence: executor/kv.rs:44-46 'service.get_versioned(&key)?.as_ref().map(versioned_value)' with kv_json_convert.rs:211 'value.value().clone().into_inner()'; executor/vector.rs:142,437 '.as_ref().map(vector_versioned_data)' with vector_convert.rs:140-141 'entry.embedding().as_slice().to_vec()' + metadata clone; vector_convert.rs:189 clones metadata per search match. The engine values are owned by the executor and dropped immediately after conversion.
  - Recommendation: Change converters to consume owned engine values (into_-style accessors: VectorEmbedding::into_values, KvValue::into_bytes, metadata into_inner) and map with .map(versioned_value) on the owned Option instead of .as_ref().
- **DTO-7** (medium, consistency) — **KvList returns two wire shapes (Output::Keys vs Output::KeysPage) for one command depending on parameters.** Every machine consumer (MCP tool schemas, SDK decoders) must handle two type tags for one operation, and the tag choice leaks a request detail into the response contract — violating the charter's one-command/one-output model and complicating generated bindings from the IDL.
  - Evidence: executor/kv.rs:84-96 returns Output::KeysPage when limit is set but Output::Keys otherwise; executor/batch.rs:10-39 page_or_keys picks the tag from cursor/limit presence; the shapes are identical ({items, has_more, cursor}). arrow/export.rs:152-155 must match both.
  - Recommendation: Collapse to a single Keys variant with PageInfo (terminal pages already express the unpaginated case) and delete the other tag before the wire freezes.
- **DTO-8** (medium, api-hazard) — **BatchItem<T> Deref panics on failed/miss items; PageInfo::new asserts.** SDK glue iterating a Partial batch result and touching *item (the ergonomic path Deref invites) panics the embedding process — in PyO3/NAPI that aborts the host application, the worst failure mode for an embedded database. PageInfo::new asserts convert an executor bug into a crash rather than an internal error status.
  - Evidence: types/common.rs:290-298 'impl Deref for BatchItem<T>' with expect("batch item primitive result payload is present") — result is None for every Error item (common.rs:199-203 clears it) ; common.rs:72-79 PageInfo::new uses assert! for cursor invariants.
  - Recommendation: Remove the Deref impl (keep result()/into_result()), and replace PageInfo::new asserts with a debug_assert plus a fallible constructor or normalization.
- **DTO-9** (medium, self-description) — **Generic Bool/Uint/BoolList output variants reused across ~10 unrelated commands; VectorCollectionList doubles as create/stats output.** A machine consumer sees {"type":"bool","data":true} with no indication of what was affirmed, weakening MCP tool result schemas and forcing SDKs to carry per-command output-mapping tables instead of deriving them from the tag. Collection-create returning a list is a surprising shape for every binding generator.
  - Evidence: grep shows Output::Bool/Output::Uint returned by kv exists, json exists/drop_index, event exists, vector delete_collection/exists/count, space exists (e.g. executor/vector.rs:50,97,174; json.rs:94,259,301); executor/vector.rs:34-39 and 84-87 wrap create_collection and collection_stats results in a 1-element VectorCollectionList.
  - Recommendation: Prefer purpose-named thin variants (Existence {exists}, Count {count}, IndexDropped {dropped}) or add an operation discriminator; give collection create/stats a singular VectorCollection variant.
- **DTO-10** (low, newtype-hygiene) — **Bytes newtype lacks repr(transparent)/serde(transparent) (rule 35).** Wire shape is effectively transparent today via serde newtype delegation, so impact is future-proofing only: adding a second field would silently change the wire encoding without the transparent markers making the contract explicit.
  - Evidence: types/common.rs:10-11: '#[derive(...)] pub struct Bytes(#[serde(with = "serde_bytes")] Vec<u8>);' — no #[repr(transparent)], no #[serde(transparent)]; it is the only wire newtype in types/.
  - Recommendation: Add #[repr(transparent)] and document that serde_bytes supplies the transparent encoding, or restructure as #[serde(transparent)] over a serde_bytes::ByteBuf.
- **DTO-11** (low, consistency) — **EventChainVerification carries prose error string and a renamed/aliased field; GraphBatchItemResult.operation is free-form String.** Consumers asserting on chain-verification failures must parse prose (violating the tests-assert-on-codes rule downstream), and graph batch consumers get an unvalidated operation string that can drift from the input enum vocabulary.
  - Evidence: types/event_records.rs:227-231 '#[serde(rename = "valid", alias = "is_valid")] is_valid: bool' plus 'error: Option<String>' (every other DTO uses structured ErrorStatus); types/graph.rs:559 'operation: String' rather than the GraphBatchOperation snake_case tag enum.
  - Recommendation: Replace error:Option<String> with Option<ErrorStatus> or a typed reason enum; type operation as a shared GraphOperationKind enum; drop the alias before wire freeze.
- **DTO-12** (low, d4-discipline) — **All output-DTO constructors are pub, bloating the D4 surface (29-positional-arg VectorIndexDiagnostics::new).** SDK authors can fabricate executor outputs (undermining the executor as the single authority) and the 29-arg constructor is a same-typed-argument transposition hazard for the executor itself; the protocol surface reviewers must approve (rule 32-33) is much larger than the protocol needs.
  - Evidence: types/vector.rs:333-363 pub fn new with 29 positional arguments; error-plumbing helpers like BatchItemResult::failed_error(ExecutorError) (kv_branch.rs:245), VectorBatchItemResult::failed_* are pub; lib.rs:44-64 re-exports the entire types module. unreachable_pub (workspace Cargo.toml:33, warn) cannot catch reachable-but-unneeded items.
  - Recommendation: Make output-DTO constructors pub(crate) (consumers only deserialize/read), and replace mega-positional constructors with a builder or struct literal within the crate.

---

## Rejected findings (6)

Raised by an auditor but refuted on verification. Kept for the record — several rejections carry residual actions noted in the themes above.

### PY-1 — State cell primitive has no command family and no CAS successor *(rejected)*

- **Original severity:** critical  |  **Unit:** PY
- **Original claim:** Python SDK cannot ship db.state (10 methods incl. Snapshot.state); MCP and Node SDK state tools are equally stranded. Agent checkpointing built on state.cas optimistic concurrency has no successor: KV emulation loses init/cas semantics entirely, so concurrent agents silently race.
- **Why rejected:** The raw evidence checks out — I read ~/Documents/GitHub/strata-python/python/stratadb/__init__.py (StateNamespace with set/get/init/cas/delete/list/history/batch_set), confirmed command_variants.txt (103 variants) has zero State* entries, and confirmed engine-next's KvService (crates/engine-next/src/data/kv/service.rs) offers only unconditional put/put_batch with no CAS/init/conditional write anywhere in engine-next. However, the finding's key claim that "state is not on the V1-removed list" is false: docs/product/strata-v1-feature-inventory.md lines 1227-1228, under "Remove Or Redesign Before V1", explicitly says "State-cell as an advertised V1 primitive unless a real V1 product surface is designed and implemented", and docs/architecture/v1-test-inventory.md:114 archives statecell fixtures as "not V1 product language" — so the absence of a State command family is a documented deliberate scope decision, not a missing family. Consumer impact is not real for shipped code: the Python SDK ships against the pre-V1 architecture, V1 is a declared clean break with SDK cutover at M9, and CLAUDE.md hard rule 11 defines the V1 capabilities as KV/JSON/event/vector/graph only. The residual observation that KV lacks a conditional-write successor for CAS workflows is a product-design follow-up already governed by the feature inventory's "unless a real V1 product surface is designed" clause, not a defect in executor-next.

### PY-2 — KV commands are bytes-only; the SDK's typed value contract cannot ride the wire *(rejected)*

- **Original severity:** critical  |  **Unit:** PY
- **Original claim:** Every consumer (Python, Node, MCP, CLI) must invent a client-side encoding to preserve db.kv.put('n', 42) → get('n') == 42. Divergent encodings make data written by one consumer unreadable as typed values by another and create a second public semantic model per client, violating the charter's one-canonical-model rule.
- **Why rejected:** I verified the raw evidence holds: `Command::KvPut` carries `value: Bytes` (crates/executor-next/src/command.rs:131-142), `Bytes` is a serde_bytes Vec<u8> wrapper (types/common.rs:11), engine-next `KvValue` is byte-oriented (data/kv/types.rs), no `pub enum Value` exists in engine-next/core-next, and the old core on main had the frozen 8-variant `Value` enum. However, bytes-only KV is a documented, deliberate V1 design decision, not an unresolved contract hole: engine-kv-parity-implementation-plan.md Design Decision 1 states "Parity means operational parity, not copying the old product value model... Typed product values can be layered above byte KV later"; executor-kv-command-contract-implementation-plan.md lines 54-55 repeat this and line 214 lists typed product value semantics as explicitly out of scope; the IDL overlay documents kv.put as writing a "binary value". The finding's demanded decision has also already been made: core-architecture.md §User Value Model assigns the canonical value type to engine-next, with executor/CLI/SDKs consuming or re-exporting it — precluding the "divergent client-side encodings" scenario. No consumer is hurt today: the V1 SDKs are M9 work that does not yet exist, and cli-next follows the documented bytes contract, so this is planned deferred work being reported as a critical shipped defect, analogous to reporting M8-planned absence as a bug.

### NODE-6 — KV value model changed from typed JSON to opaque Bytes with no canonical encoding convention *(rejected)*

- **Original severity:** high  |  **Unit:** NODE
- **Original claim:** Each SDK must invent its own value codec (JSON.stringify? MessagePack? raw UTF-8?). A database written by the Node SDK and read by the CLI or Python SDK will not round-trip typed values unless all consumers coordinate an encoding out-of-band — contradicting 'engine owns semantics' (CLAUDE.md rule 7) by pushing value semantics into every consumer.
- **Why rejected:** The quoted evidence is accurate (KvPut takes Bytes at crates/executor-next/src/command.rs:138-141, output is KvValue(Option<Bytes>) at output.rs:111, CLI wraps raw UTF-8 via bytes() at cli-next/src/lib.rs:887-889), but the finding misattributes the design decision: engine-next itself defines KV values as opaque bytes (KvValue(Vec<u8>) at crates/engine-next/src/data/kv/types.rs:116), and the primitive-implementation-contract describes KV as "the thinnest facade over the row substrate" with only a "basic text projection where values are text-like" — typed structured values are deliberately the JSON capability's job, which the executor exposes separately (JsonValue(MaybeJsonValue)). So the executor is faithfully mirroring engine-owned semantics, exactly what CLAUDE.md rule 7 and the executor charter's thin-adapter mandate require; the recommendation to add a typed-value KV variant at the executor layer would itself violate that charter. The old Node SDK's kvPut(value: any) is pre-V1 behavior, and V1 is an explicit clean break with no compatibility shims; V1 SDKs do not ship until M9, so the cross-consumer round-trip scenario is speculative, not a shipped defect — the only live consumer (cli-next) writes and reads bytes consistently. This is a deliberate value-model change (bytes KV + typed JSON capability), not a wire-contract bug; at most a one-line note in the executor contract stating "KV values are opaque bytes; use JSON for typed values" would be a docs nicety, which does not sustain the finding.

### MCP-2 — State primitive removed silently — no CAS or set-if-absent conditional write anywhere in the 103 commands *(rejected)*

- **Original severity:** high  |  **Unit:** MCP
- **Original claim:** MCP agents (and Python/Node SDKs, which expose db.state) used CAS for optimistic concurrency on shared agent memory — multiple sessions writing counters/locks/checkpoints. Plain KvPut is last-writer-wins; with public transactions also removed, V1 offers no safe read-modify-write at all. Set/get/list/history degrade to KV, but cas/init have no substitute.
- **Why rejected:** The headline claim of silent removal is false: git commit 0306cf59 (issue #1606, PR #1861, 2026-03-24) explicitly removed the State Cell primitive from the pre-V1 architecture with a documented rationale ("redundant with KV+transactions and txn.cas()"), deleting all State commands, docs, and formats; I confirmed main's old executor command.rs already has no state commands and main's engine primitives are the 5 without state. State is absent from v1-removed-surfaces.md because it was removed from the product months before V1 began — the finding's baseline (the stale strata-mcp repo) reflects a pre-March-2026 product, so no current MCP/SDK consumer loses anything in the V1 cutover. The secondary claim (no public read-modify-write) is a deliberate documented decision: public manual transaction sessions — the carrier of txn.cas() — are item 2 on v1-removed-surfaces.md and CLAUDE.md's out-of-scope list, and v1-test-inventory.md:175 explicitly recasts the old cas_operations tests as "Public transaction runtime removed; atomic writes remain." I did verify the raw facts (no CAS/if-absent variant among executor-next's commands, no 'state' in engine-next/src/data), but the finding mischaracterizes a tracked pre-V1 product removal plus an explicitly listed V1 removal as a silent V1 regression, so the impact as reported is not real.

### MCP-4 — IDL/MCP metadata covers only 32 of 103 commands (kv and vector families only) *(rejected)*

- **Original severity:** high  |  **Unit:** MCP
- **Original claim:** first-run-experience.md §8.3-8.4 requires curated tools + a strata_command escape hatch over 'any cataloged command', with teaching-text descriptions 'generated from the same metadata'. Branch, space, event, json, graph, admin, arrow, and inference families have no MCP names, descriptions, error lists, or fixtures — MCP serve (and llms.txt, agents guide, SDK docstrings per §6) cannot be generated until the IDL is backfilled. The 71 uncataloged commands would also be invisible to or undocumented in the escape hatch.
- **Why rejected:** The raw facts hold (I verified: idl/v1/commands/ has only kv.yaml and vector.yaml; command-index.json has 32 entries across families ['kv','vector']; command.rs enum Command has 103 variants; defaults.yaml:2 defines mcp_name template). However, the gap is deliberate staged work, not a defect: docs/architecture/v1-idl-overlay-strategy.md 'Later Slices' explicitly schedules the remaining families (JSON, event, graph, branch, space, admin, Arrow, inference) after the KV/vector slice, and v1-idl-overlay-kv-vector-implementation-plan.md:495 mandates 'Do not expand IDL coverage beyond KV/vector' for the current slice. No shipping consumer is hurt: there is no MCP server, llms.txt, or SDK generator in the workspace — the only MCP reference (executor-next/src/idl_tooling.rs) is doc-commented 'Future MCP metadata'. The finding reports the documented stopping point of a phased rollout as a high-severity bug, and its recommendation merely restates the existing plan; a coverage guard test would currently fail by design against the active slice's own constraint.

### META-2 — Metadata covers only 32 of 103 Command variants — json, event, graph, branch, space, admin, arrow, and inference families entirely absent *(rejected)*

- **Original severity:** high  |  **Unit:** META
- **Original claim:** Any consumer of the catalog — strata agents guide (D3), MCP tool generation (D8), SDK docstrings (D9), CLI explain — covers 31% of the surface; 71 commands including all of json/event/graph/inference would be invisible to agents and generators.
- **Why rejected:** The evidence is factually accurate — I verified the hardcoded ["kv.yaml","vector.yaml"] list at idl_tooling.rs:572, command_count 32 in the generated index, 103 Command enum variants, and kv/vector-only request fixtures. But the missing families are explicitly and deliberately out of scope: the implementation plan calls itself Slice 2, "intentionally narrower", and lists "JSON, event, graph, branch, space, admin, Arrow, or inference command families" as out-of-scope item 5, while the overlay strategy doc already plans json.yaml and the other family files as follow-on work. The claimed consumer impact is not real today: I confirmed the only consumer of CliCommandCatalog in the repo is executor-next's own tests; cli-next executes all families via hand-written parsing without reading the catalog and has no explain command wired, and the cited D3/D8/D9 consumers (agents guide, MCP generation, SDK docstrings) are themselves deferred by the same plan. This is documented slice sequencing tracked for M9, not a shipped defect, so per the rubric (deliberately out of scope, impact not real) it is rejected.

---

## Coverage notes per unit

**PY — Python SDK parity:** Enumerated all 118 public members of the stratadb package: the Strata wrapper (__init__.py) plus the full native _Strata surface (_stratadb.pyi) reachable via __getattr__ delegation, all 7 namespace objects (kv/state/events/json/vectors/branches/spaces), Collection, Snapshot + 5 snapshot namespaces, Transaction, module-level setup() and the 8-class exception hierarchy. Verified every mapping against the 103 Command variants in crates/executor-next/src/command.rs, argument-by-argument, plus supporting types (Bytes, VectorMetadataFilter, GenerateRequest, EmbedRequest, AdminMetrics/AdminDatabaseInfo) and the engine-next data layer for emulation feasibility (KV service has no CAS; no typed Value enum in engine-next/core-next; no time_range or read_only anywhere). V1-removed filter applied: transactions (begin/commit/rollback/txn_info/txn_is_active/in_transaction/Transaction), branch bundles (export/import/validate), flush/compact/retention_apply — excluded from findings. TTL/expiry: the old SDK exposes none, commands expose none — no gap. Time-travel as_of coverage verified per-command (KvGet/KvList/JsonGet/JsonList/VectorGet/VectorQuery/EventGet/EventList/EventLen/EventListTypes/EventGetByType all carry as_of). Pagination verified: JsonList exactly matches SDK cursor contract; KvList/EventList are supersets. Not verified: runtime behavior (no builds run, per instructions) and cli-next consumption patterns (other units' scope). The legacy migration checklist (148 commands, all checked) tracks the OLD executor rewrite — no reconciled ledger exists for the 103-variant executor-next surface, so the deltas above (TimeRange, ConfigureSet, Search, EmbedStatus, BranchMerge/Diff, DurabilityCounters, state family) are otherwise untracked.

**NODE — Node SDK parity:** Enumerated all 8 namespaces + ~60 flat methods of stratadb.d.ts (published contract) and index.d.ts (NAPI layer), mapped each to the 103 Command variants, and cross-checked dispositions against docs/architecture/v1-removed-surfaces.md, docs/executor/executor-command-migration-checklist.md (148-command legacy ledger), executor-minimal-surface-implementation-plan.md (EX4/EX5 slots), and engine-next contracts (primitive-implementation, temporal-context, entity-ref). Filtered as V1-removed: manual transactions, follower/refresh, branch bundles, flush/compact/retentionApply, streaming, branch metadata. Not deeply audited: output payload field-by-field parity for every command (spot-checked KV/branch/vector/batch/inference), executor facade module internals, and Arrow command behavior. Did not run builds; all claims from source reads.

**MCP — MCP server parity:** Enumerated all 63 registered tools in ~/Documents/GitHub/strata-mcp/src/tools (the brief's '61' undercounts by 2; per-file ToolDef counts in the inventory file) and mapped each to the 103 executor-next Command variants — full table in the inventory file. Filtered per instructions: txn (5 tools), bundles (3), retention, db_flush, db_compact are V1-removed and not reported; branch merge/diff ARE reported because V1 keeps merge (strict refusal) and M6 plans compare/promote. Verified the escape-hatch mechanics directly: Command derives Deserialize with serde(tag="type", snake_case, deny_unknown_fields) (command.rs:19-21), cli-next run --command-json deserializes it generically (lib.rs:866-885), and IDL defaults.yaml derives mcp_name as strata_{family}_{op_slug} — so name+args dispatch is proven, with the gaps being catalog coverage (32/103), missing input schemas, and unstructured decode errors. Confirmed old-MCP session switch tools are subsumed by per-command branch/space Options. Checked engine-next data capabilities (no state primitive) and v1-removed-surfaces.md (state absent from it). Did not run cargo; all claims are from source/doc reads. Not covered: behavioral testing of the run --command-json path, and the strata-python/strata-nodesdk repos beyond memory-level knowledge (other units own those).

**CLI — CLI parity and thinness:** Read all 9 cli-next source files in full and the executor surfaces they consume (command.rs serde attrs, dispatch.rs arms for all 103 variants, output.rs tagging, error.rs ErrorStatus, executor/mod.rs session model, cli_metadata.rs + generated cli-command-index.json). Verified reachability per variant against lib.rs mapping functions and the dispatch table; did not run cargo. Did not exhaustively audit human/raw rendering fidelity per Output variant (presentation-only). Sibling SDK/MCP repos were not deep-read for this unit; the inference-verb requirement is inferred from the executor shipping the commands plus the charter listing inference as a public command family. Deferred/removed verbs (txn, flush, compact, tags, notes, search, recipe) were checked against the V1-removed list and are correctly refused, not reported as gaps.

**ENGINE — Engine capability reachability:** Verified all 103 dispatch arms (dispatch.rs:8-769): zero todo!/unimplemented!/stub errors, so category (b) is clean - every command reaches implemented engine or inference behavior, including the suspect VectorIndexQuery which correctly carries as_of and VectorIndexDiagnostics into Output. Enumerated every pub fn on Database, BranchService, SpaceService, AdminService, and the five capability services and mapped each to command reachability (full table in inventory file). Parity confirmed with no finding: KV/JSON/event/vector as_of coverage matches engine _at variants 1:1; branch outputs carry full BranchSummary fidelity (generations, parent fork version/timestamp, cleanup facts); space mutations carry CommitReceipt/MutationEffect; event append-only design correctly has no history command; test-only engine methods are properly testkit-gated. Excluded per instructions: persistence/commit internals, removed V1 surfaces (bundles, tags, manual transactions, maintenance commands), Database open/close (session protocol, called at executor/mod.rs:223). Not deeply audited here (other units own them): Arrow import/export implementation quality, error-code registry conformance, sibling-repo per-method requirement diffs.

**DSGN — Design consistency (Stripe-grade):** Covered all five matrix axes across all 103 variants: verb conventions (section 1 of inventory), batch coverage incl. behavioral divergence (section 2), pagination/limits for every enumerator with engine-level verification of unbounded paths (section 3; engine-next kv/event service code read to confirm limit=None semantics), addressing (section 4), and output envelope (section 5, full output.rs read). Cross-checked consumer requirements in strata-python (__init__.py), strata-nodesdk (index.d.ts), strata-mcp (tools/) and the in-repo cli-next renderer. Verified prior-pass claims by direct file reads (graph batch op names, MCP kv_list doc string, CLI bytes heuristic, JSON batch duplicate handling, EventRangeDirection default). Not covered: error envelope consistency (error.rs/error_registry.rs — assumed owned by another unit), inference-family internals beyond noting nested foreign DTOs (inventory §4), and runtime behavior verification via cargo test (static analysis only; all behavioral claims traced through executor→engine source). V1-removed features excluded per instructions; search/RAG absence not reported except the JSON index-query orphan, which is engine-level (M6), not intelligence-level.

**EVOL — Wire evolution and versioning:** Read command.rs (full attrs + name()/is_write()), output.rs (all 100+ variants), types/common.rs, types/admin.rs, error.rs, error_registry.rs (grep), idl_tooling.rs, cli_metadata.rs, idl/v1/ (manifest, defaults, commands/, generated), tests/command_contract/{roundtrip,shapes,goldens}.rs, fixtures tree, cli-next raw command-json path, and all five docs/executor/ documents. Did not run cargo (unknown-field rejection is already asserted by checked-in tests). Did not re-audit sibling-repo requirement sets (other units); did not deep-read graph/vector/event type files beyond enum/attr scans — their serde attr patterns match the audited files. The executor-imports-inference dependency question (rule 5) is noted only for its wire-ownership consequence, not adjudicated here.

**ERR — Error boundary:** Covered all six asks: (1) grepped every error construction site in src/executor/, src/arrow/, and helpers (invalid_input/not_found/ExecutorError::new/render_status plus all literal <class>.<area>.<detail> strings) and cross-checked each against error_registry.rs + engine registry — zero unregistered codes; verified the three-layer enforcement (source-scan tests in executor/engine/inference, debug_assert at engine construction, runtime normalization fallback). (2) Taxonomy assessed against contract §Stable Concepts: class + per-code retry_policy + retryable + commit_outcome all serialized; retryability explicitly signaled. (3) Redaction: grepped engine/executor message templates for storage wording (none) and interpolated user content (only allowed facts: branch names, labels, limits); inference secret-leak test confirmed. (4) Compared registry to first-run §7.2 hint+ref requirements including CLI rendering in cli-next/src/render.rs. (5) Checked non_exhaustive on all five public error enums and audited test assertions in error_and_guards.rs plus behavior suites for prose assertions (none). (6) Traced From<EngineError>/From<InferenceError> and all dispatch.rs map_err arms for lossiness. Not run: cargo test (grep/read sufficient; registry tests read in full). Not deeply audited: engine-internal storage→engine mapping (out of unit scope), IPC wire encoding of ErrorStatus beyond serde shape, and the command_contract fixture files' error JSON beyond spot checks.

**THIN — Charter conformance (thinness / one model):** Read in full: executor-charter.md, executor/mod.rs, dispatch.rs, batch.rs, services.rs, kv.rs, json.rs (batch+list), vector.rs, event.rs, arrow_commands.rs, admin_branch.rs, input_common.rs, kv_json_convert.rs, vector_convert.rs, event_convert.rs, all six facade files, lib.rs, arrow/import.rs, plus targeted reads of error.rs, error_registry.rs, command.rs/output.rs inference sections, types/common.rs BatchMode, engine-next api/options.rs+database.rs, and cli-next open.rs/lib.rs consumption. graph.rs, graph_convert.rs, admin_convert.rs, cli_metadata.rs, arrow/export.rs and arrow/schema.rs were scanned via grep/head rather than read line-by-line; a policy issue hiding deep in arrow/schema.rs value-mapping (888 LOC) or arrow/export.rs (823 LOC) could have been missed. Did not run cargo; all claims are from source reading. Facade losslessness verified for every method body (all match the build-one-Command/call-execute pattern). IPC/session layer not present in this crate, so charter responsibility 2/5 conformance was assessed only as absence.

**META — Machine-readable metadata readiness:** Reviewed both source files in full, both named test files in full, the authored IDL tree (idl/v1/*.yaml, commands/, prose/), generated command-index.json and cli-command-index.json (parsed and diffed inputs against the 103-variant list), CI workflow, cli-next crate (Cargo.toml, options.rs, repl.rs, lib.rs, grep for consumption), the IDL overlay implementation plan, first-run-experience.md §7/§11, the engine error registry entry shape, and strata-mcp's inputSchema requirement. Not run: cargo build/test (grep + generated-artifact inspection sufficed; CI config confirms the idl-tooling suite runs on PRs). Not reviewed in depth: strata-python/strata-nodesdk (their arg-schema needs are inferred from the MCP inputSchema requirement and the agents-guide spec rather than a per-method audit — other units cover those repos). The V1-removed feature list was respected: none of the 71 uncovered variants are removed features; kv.history/vector.history (Getv) are covered. Findings META-1/2/4/6/9 carry planned_milestone M9 where the roadmap has a credible slot (first-run-experience delivery map D3/D8/D9 and the plan's own deferred list); META-3/5/7/8 are gaps in the shipped mechanism itself with no plan reference found.

**SESS — Session and lifecycle model:** Covered all six questions with file-level evidence: (1) lifecycle — constructors, builders, close/reopen/double-close, execute-after-close error contract, open-options gap; (2) concurrency — Send+Sync proven by a standalone compile check (scratchpad/sendcheck) rather than inference, &mut receivers counted, storage single-writer lock confirmed, test absence verified; (3) branch/space defaulting traced through every family (services.rs, admin_branch.rs, arrow re-dispatch, inference exemption) — resolution is uniform across all 103 variants; (4) access modes — none exist; durable-vs-cache gating is vacuous because no durable-only command exists (vector artifacts get a memory store in cache mode), verified in engine source; (5) txn/session grep over the whole crate = zero hits, clean; (6) both config handlers and their engine backing read, plus migration-checklist archaeology explaining the Configure prefix. Not exercised: runtime behavior of the inference-feature build (Send/Sync check ran with default-features=false, so InferenceRuntime's auto-traits are unverified); IPC/multi-process transport (charter section 5) has no implementation to review. Sibling-repo review was targeted (python __init__.py, mcp session.rs/txn.rs) rather than exhaustive.

**DTO — Protocol DTO quality:** Read all 11 types/ files, output.rs, command.rs (spot), error.rs, executor conversion/validation modules (input_common, batch, kv, vector, vector_convert, kv_json_convert), engine-next value newtypes (KvKey/KvValue/JsonValue/VectorEmbedding), storage-next table_manifest bounds, cli-next render.rs, and Node SDK index.d.ts for consumer expectations. Static analysis only — the >4KB-key durability wedge (DTO-1) was traced through code, not reproduced at runtime; recommend one integration test to confirm. Not covered in depth: arrow/ import/export internals, idl_tooling fixture completeness, python/mcp repos beyond spot checks. Minor items folded into the file rather than findings: AdminVectorCollection.dimension is the only usize on the wire, VectorScalar f64 precision >2^53, redundant version/timestamp fields duplicating CommitReceipt on several Output variants, admin.rs pub-field convention diverging from getter convention elsewhere, GraphNeighborHit denormalization.

---

## Appendix: full inventories

Each auditor wrote its complete surface inventory (full method→command mapping tables, consistency matrices) to a session scratchpad file (`execreview_<unit>.md`). Those files are session-temporary; the findings above capture every judgment they produced. If the raw mapping tables are wanted as durable artifacts, they should be regenerated as part of the M9 SDK-cutover design docs.
