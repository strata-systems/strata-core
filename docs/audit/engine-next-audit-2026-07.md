# engine-next — Correctness / Completeness / Conformance / Gap Audit

**Date:** 2026-07-06  ·  **Crate:** `strata-engine-next` (~30.6K LOC, 66 files, 453 public items)  ·  **Branch:** `v1`
**Method:** multi-agent exhaustive audit — 15 units read against their governing contracts + the CLAUDE.md hard-rule invariants + the M5/M6 milestone plans, across four dimensions (correctness, completeness, conformance, product-gap). Every critical/high finding was then adversarially verified by an independent skeptic agent (default-reject) before being retained. 33 agents, ~3.46M tokens.

> **Status: documentation only — not being fixed yet.** This records all findings for a later remediation pass.

## Scorecard

- **Raw findings:** 88  →  **retained 82**  (rejected as false-positives by the adversarial pass: 6)
- **Confirmed (adversarially verified):** 12  —  0 critical, 9 high, 3 medium(downgraded)
- **Unverified (medium/low, reported by the auditor but not adversarially re-checked):** 70
- **Plausible:** 0

| Dimension | critical | high | medium | low |
|---|---|---|---|---|
| correctness | 0 | 1 | 7 | 8 |
| completeness | 0 | 4 | 16 | 0 |
| conformance | 0 | 3 | 15 | 5 |
| product-gap | 0 | 1 | 13 | 9 |

### Verdict

engine-next is **solid where it is built** — the persistence adapter, control plane, and the capability read/write paths (KV/JSON/event/graph/vector) are implemented and largely conformant. The gaps concentrate in two places:

1. **M6 product semantics are substantially unbuilt** — the entire branch-workspace surface (compare/diff/promote/merge/copy/restore/revert/cherry-pick + the capability-branch-adapter seam), graph binding policy/target-status, and the production trigger that seals the vector index. These are contract-mandated but not yet implemented (M6 is a Planned milestone).
2. **A handful of real bugs exist in shipped code** — time-travel `as_of` read semantics (silent clamp / collapse-to-absent), graph accepting cross-branch bindings (violates Hard Rule #18), and a crash window during branch create/fork/delete that can permanently brick a durable database.

No confirmed finding is a happy-path data-loss/corruption bug; the crash-brick (E1) is the most operationally severe. Several 'high' findings are unbuilt-milestone scope rather than regressions — noted per finding.

## Part 1 — Confirmed findings (adversarially verified)

Each was independently re-checked against the code by a default-reject skeptic; the verification reasoning is folded under each.

### 1. 🟠 HIGH · `api-surface` · completeness

**Branch compare/diff, promote/merge, copy, restore/revert, cherry-pick are entirely missing**

- **Location:** `crates/engine-next/src/branch/service.rs:234`
- **Governing spec:** public-api-and-cli-surface-cleanup-checklist.md "Branching And Time Travel" (compare/diff, promote/merge with explicit conflict strategy, copy, restore/revert); m6-m6t-implementation-plan.md M6D; CLAUDE Hard Rule 20 (branch merge strict refusal on divergent history)

**Issue.** BranchService exposes only create, create_from_head/fork_current, fork_at_version, fork_at_timestamp, delete, list, and get. A grep for merge/diff/compare/promote/revert/restore/cherry across all of crates/engine-next/src finds no branch mutation operations (only unrelated vector-candidate 'merge'). The cleanup checklist lists compare/diff, promote/merge, copy, and restore/revert as required V1 branch surface, and M6D scopes them explicitly. CLAUDE invariant 20 mandates a strict-refusal merge behavior that cannot exist because merge is unimplemented. This is the core write-side of the branch workspace product and is unavailable to CLI/SDK/MCP.

**Evidence.** service.rs public methods end at `delete` (L234); grep `fn (merge|diff|compare|promote|revert|restore|cherry)` over engine-next/src returns only vector index helpers; checklist L250-256; m6 plan L33 lists "compare, promote, copy, restore, revert, cherry-pick".

**Failure scenario.** A user forks `feature` from `main`, edits JSON docs on `feature`, and wants to promote the change back: `strata branch promote feature` (or an SDK `merge`). The engine exposes no such method, so the entire fork-edit-merge product loop is impossible; likewise `strata branch compare feature main` cannot be served.

<details><summary>Adversarial verification (CONFIRMED)</summary>

Read crates/engine-next/src/branch/service.rs in full: BranchService's public surface is list (L36), get (L47), create (L59), create_from_head (L89), fork_current (L98), fork_at_version (L202), fork_at_timestamp (L218), delete (L234) — everything else is a private helper. No compare/diff/promote/merge/copy/restore/revert/cherry-pick. A broad grep across all of crates/engine-next/src for promote|cherry-pick|branch-merge|branch-diff|branch-compare|revert|restore returned nothing, and `fn (merge|diff|compare|promote|revert|restore|cherry|copy)` matched only the unrelated vector helper merge_and_rerank_candidates (data/vector/index.rs:1614) plus its tests. api/branch.rs is DTO-only; persistence/control and executor-next/cli-next also have no such branch ops. The contract claims verify: public-api-and-cli-surface-cleanup-checklist.md L249-252 lists Compare/diff, Promote/merge with explicit conflict strategy, Copy, and Restore/revert in the V1 Keep set; m6-m6t-implementation-plan.md L33 (M6D) explicitly scopes compare, promote, copy, restore, revert, cherry-pick. Only the create/fork/delete/list/get half of M6D is implemented; the mutation/comparison half is entirely absent, so CLAUDE Hard Rule 20's strict-refusal merge behavior has no implementation to exist in. The failure scenario holds exactly: a forked branch cannot be promoted/merged back and `branch compare` cannot be served through any layer. This is a real, required-by-contract completeness gap, not a misread, guard, or out-of-scope item.

</details>

---

### 2. 🟠 HIGH · `branch-commit` · correctness

**Crash between the pending-intent commit and the catalog-activation commit permanently bricks a durable database (no resume/cleanup)**

- **Location:** `crates/engine-next/src/control/bootstrap.rs:358`
- **Governing spec:** branch-operation-and-capability-adapter-contract.md — Branch Creation / Publication rule (2) and Common Workflow Shape rule 5; Conformance Tests 10 & 16

**Issue.** Every mutating branch op (create/fork/delete) runs as TWO separate durable commits: `ControlPlane::begin_branch_operation` writes a pending-index row + pending record (service.rs:65,255,304; bootstrap.rs:145-165), then a later `persist_catalog_record` / `persist_branch_record` commit activates the catalog and clears the pending rows (bootstrap.rs:185-223). If the process crashes in the window between these two commits, the pending row survives. On reopen, `load_existing_database` reads the pending index (bootstrap.rs:352-371), decodes the pending record for validation, then UNCONDITIONALLY returns `EngineError::corruption("data_loss.engine.branch_create_pending", ...)`. There is no resume-forward and no cleanup/quarantine path anywhere (grep for pending/resume/recover confirms none). The entire database — every branch, all data — becomes unopenable, and there is no repair tooling (CLAUDE.md forbids migration/repair machinery). The contract's Publication rule requires recovery to either finish publishing the branch-control rows or clean up/quarantine the destination storage state; the implementation does neither.

**Evidence.** bootstrap.rs:358-371: `if !pending_names.is_empty() { if let Some(name) = pending_names.first() { let row = read_required(...)?; let _ = decode_pending_branch_record(&row)?; } return Err(EngineError::corruption("data_loss.engine.branch_create_pending", "branch catalog contains an unfinished branch create operation")); }` — the decoded pending record is discarded (`let _ =`) and load always fails.

**Failure scenario.** Open a durable DB, call branches().create("feature"). `begin_branch_operation` fsyncs the pending rows; `persistence.create_branch` fsyncs the storage branch; process is killed before `persist_catalog_record` fsyncs. Reopen the DB: `load_existing_database` returns `data_loss.engine.branch_create_pending` and Database::open_local fails forever. All pre-existing branches (default, plus any others) are now inaccessible with no recovery path.

**Suggested fix.** Implement the contract's recovery: on load, use the persisted pending record to either (a) roll the storage branch forward by re-running catalog activation, or (b) delete/quarantine the half-created storage branch and clear the pending rows, so a crash mid-op leaves the DB openable. At minimum this must land before any durable-mode GA (M6D/M10).

<details><summary>Adversarial verification (CONFIRMED)</summary>

The code confirms every load-bearing claim.

Two separate durable commits: In `create` (branch/service.rs:59-86) the order is `ControlPlane::begin_branch_operation` (line 65) -> `persistence.create_branch` (storage fork, line 68) -> `persist_catalog_record` (line 82). `begin_branch_operation` (bootstrap.rs:145-165) does its own `persistence.commit(...)` writing the pending-index row + pending record; `persist_branch_record` (bootstrap.rs:185-223) is a distinct `persistence.commit(...)` that writes the catalog row/index AND clears the pending rows. Fork (service.rs:122, 304) and delete (service.rs:255) follow the same begin->storage->persist_catalog_record shape. So the crash window between the two fsync'd commits genuinely exists for a durable backend.

Unconditional hard fail on reopen: `load_existing_database` (bootstrap.rs:352-371) reads the pending index, and if non-empty decodes the pending record only to discard it (`let _ = decode_pending_branch_record(&row)?;`, line 365) and then UNCONDITIONALLY returns `EngineError::corruption("data_loss.engine.branch_create_pending", ...)`. This check sits at lines 352-371, BEFORE the branch-index/branch-record loading at lines 373-412, so the entire load aborts and no branches are ever loaded — every pre-existing branch and all data become unopenable.

No resume/cleanup/quarantine: grep across engine-next/src shows the only pending-clearing paths are `clear_pending_branch_operation` (bootstrap.rs:167-183) and `persist_branch_record`. Both are invoked only from the live in-process handlers `clear_pending_after_storage_error` (service.rs:363-376, runs only when create_branch returns Err in-process) and `persist_catalog_record`. Neither runs on a hard process crash, and neither is reachable from the load/recovery path. The test `pending_branch_create_fails_closed_on_load` (bootstrap.rs:473-487) codifies the brick-on-reopen behavior as intended. CLAUDE.md forbids repair/migration tooling, so there is no escape hatch — the brick is permanent.

Contract violation: Publication rule (2) (contract lines 358-361) requires that when storage fork succeeds but branch-control publication fails, recovery must EITHER finish publishing the branch-control rows OR clean up/quarantine the destination storage state before it appears as an ordinary branch. The implementation does neither — it refuses to open the whole database. Conformance Test 10 (line 820-821) is only the negative half ("cannot produce an ordinary visible branch"), which the fail-closed refusal technically satisfies, but the affirmative recovery obligation in the Publication rule is unimplemented. The pending intent is written but never consumed by recovery.

Severity: high is correct. When triggered, a single crash (or even an in-process catalog-commit failure via `fail_closed_after_branch_operation_error`, which also leaves the pending row set) during a routine branch create/fork/delete renders the entire durable database permanently unopenable with a data_loss-class error and no recovery path. Not critical because it is crash-timing-dependent rather than occurring on every run and it fails closed (refuses to open) rather than silently serving corrupt data; not medium because the blast radius is total loss of availability for the whole durable database, which is exactly the durability guarantee the store exists to provide, and the contract explicitly mandates the missing recovery path.

</details>

---

### 3. 🟠 HIGH · `branch-commit` · completeness

**Entire promote/compare/preview/selected-copy/restore surface and the capability-branch-adapter abstraction are absent**

- **Location:** `crates/engine-next/src/branch/service.rs:36`
- **Governing spec:** branch-operation-and-capability-adapter-contract.md — V1 Minimum items 3-10 and the Capability Branch Adapter Requirements section; m6-m6t-implementation-plan.md epic M6D

**Issue.** The BranchService public surface is only list/get/create/create_from_head/fork_current/fork_at_version/fork_at_timestamp/delete (service.rs:36-234). The governing contract's V1 Minimum mandates: compare branches by capability/space (3), preview promotion from lineage (4), promote with strict + explicit source-wins strategies (5), selected copy distinguishing current-record copy from selected-change apply (6), restore/undo via compensating commits (7), and a capability branch adapter for KV/JSON/event/vector/graph (9). A workspace-wide grep confirms NONE of promote/merge/compare/preview/restore/cherry-pick/revert exist, and there is no `CapabilityBranchAdapter`/`ConflictStrategy`/three-way-diff type anywhere in engine-next. This is the majority of the branch contract and is the M6D exit gate. Because the capability-adapter seam does not exist, conflict policy, strict-refusal merge, source-wins reporting, and derived-state disposition-on-promotion are all unimplemented.

**Evidence.** service.rs pub fns: list(36), get(47), create(59), create_from_head(89), fork_current(98), fork_at_version(202), fork_at_timestamp(218), delete(234). Grep `pub fn merge|compare|promote|preview|restore|diff|cherry|revert` across crates/engine-next/src returns nothing; grep for `CapabilityBranchAdapter|ConflictStrategy|three_way` returns no trait/impl.

**Failure scenario.** A downstream consumer (CLI/SDK/Strata AI) that follows the branching pathway — fork a branch, make edits, preview promotion conflicts, then promote with strict-refusal or source-wins — has no engine surface to call. `db.branches()` exposes no compare/promote/restore method, so the core 'branches are workspaces you merge back' product story is not reachable.

**Suggested fix.** Land M6D: define the capability branch adapter trait (row classification, entity identity, conflict rules, strict/source-wins, restore scopes, delete cleanup, derived-state disposition) and the coordinating compare/preview/promote/selected-copy/restore workflows over persistence, with KV as the reference adapter per the contract's Capability-Specific Minimums.

<details><summary>Adversarial verification (CONFIRMED)</summary>

Verified directly in code and contract. crates/engine-next/src/branch/service.rs exposes only list(36)/get(47)/create(59)/create_from_head(89)/fork_current(98)/fork_at_version(202)/fork_at_timestamp(218)/delete(234) — no compare/preview/promote/selected-copy/restore. The public db.branches() surface in api/branch.rs is DTO-only (BranchSummary/BranchCreateOutcome/BranchDeleteOutcome/BranchCleanupSummary) with no such methods. The branch/ module has only catalog.rs, mod.rs, name.rs, service.rs. Grep `pub fn (merge|compare|promote|preview|restore|diff|cherry|revert)` over crates/engine-next/src returns nothing; grep for CapabilityBranchAdapter|ConflictStrategy|three_way|source_wins|compensating|selected_copy returns nothing (the only `adapter` files are the unrelated persistence adapter). The governing contract's V1 Minimum (branch-operation-and-capability-adapter-contract.md line 884) makes items 3-10 MUST requirements — compare by capability/space (3), preview promotion from lineage reporting conflicts (4), promote strict+source-wins (5), selected copy distinguishing current-record vs selected-change (6), restore/undo via compensating commits (7), KV/JSON/event/vector/graph capability branch adapters (9), explicit derived-state disposition (10) — and the Capability Branch Adapter Requirements section (line 600) defines the adapter seam. m6-m6t-implementation-plan.md epic M6D scopes exactly "compare, promote, copy, restore, revert, cherry-pick" with a product-pathway conformance exit gate. The failure scenario genuinely holds: the fork->edit->preview->promote(strict|source-wins) pathway has no engine surface to call. Only imprecision is a cosmetic paraphrase of the V1-Minimum item numbering, which does not affect correctness. Severity high is well-calibrated: it is the majority of a contract-mandated, product-central surface plus the entire capability-adapter seam, absent, blocking the M6D exit gate. Not critical since it is missing functionality, not data-loss/corruption/security. (Note: the v1-progress-tracker lists M6 as Planned, so this is contract-mandated work that is genuinely unbuilt on this branch, not a subtle regression — but a completeness-dimension audit against the binding contract legitimately flags a mandated-but-absent surface.)

</details>

---

### 4. 🟠 HIGH · `event` · product-gap

**get_by_type and range_by_time/list/len_at load the entire event log into memory; the per-append type index is written on every append but never read**

- **Location:** `crates/engine-next/src/data/event/service.rs:401`
- **Governing spec:** primitive-implementation-contract.md §Event 'Query by sequence, type, range, and time'; §5 Key Encoding rule 3 (keys must support prefix/range queries needed by product APIs)

**Issue.** Every append writes a secondary type-index row (service.rs:133-136, RowClass::EventIndex, key = space|type|be(sequence)) on top of the event row and metadata row. That index is never consulted by any query — the only non-test readers of EventIndex are the space-delete cleanup path (api/space.rs:517). get_by_type_with_selector (line 390-410) answers type queries by calling event_rows -> event_raw_rows -> scan_prefix(..., None) (line 431-438), an UNBOUNDED scan of every event row in the space, then `retain`s by type. range_by_time (line 293), list_page/list (line 356), len_at (line 187), and list_types_at (line 320) do the same full-log scan (scan_prefix with limit None => read_limit maps None to unbounded, adapter.rs:895). range_by_time additionally sorts the entire log on every call (line 299). On this billion-scale branch, the core documented event operations (facade: 'append, range, range by time, list types') cannot scale, and the index that exists precisely to serve get_by_type is pure write amplification.

**Evidence.** service.rs:431 `self.persistence.scan_prefix(record.storage_branch_id(), RowClass::Event, encode_event_space_prefix(&self.space), selector, limit)` called with limit None from event_rows; service.rs:133-136 writes the EventIndex row that no query reads.

**Failure scenario.** A log with 1,000,000,000 events. `get_by_type(&EventType::new("user.created"), None, Some(10))` reads all 1e9 event rows into a Vec<EventVersionedRecord> and only then truncates to 10 — O(N) I/O and memory for a bounded query, despite an existing (be(sequence)-ordered) type index that could serve it in O(matches). `range_by_time(...)` on the same log allocates and sorts all 1e9 records per call.

**Suggested fix.** Serve get_by_type/get_by_type_at from the EventIndex via scan_prefix over encode_event_type_index_prefix(space,type) with the limit pushed down; add early-termination (stop after limit+1) to the sequence-window scan; answer range_by_time by binary-searching the monotonic append-time -> sequence relationship instead of full-scan+sort, or delete the unused index if these paths will not use it.

<details><summary>Adversarial verification (CONFIRMED)</summary>

Every load-bearing claim is confirmed against the code. (1) Type index written per append: service.rs:133-136 (RowClass::EventIndex via type_index_address 534-545); key encoding at key.rs:279-288 is space|len_prefixed(type)|be(sequence), i.e. prefix/range-queryable by type. (2) Index never read by any query: non-test references to RowClass::EventIndex/encode_event_type_index_key are only the writer (service.rs:542) and the space-delete cleanup path (api/space.rs:511-519); no read path consults it — pure write amplification, as claimed. (3) get_by_type unbounded scan: get_by_type_with_selector (390-410) calls event_rows(record, selector, None) at line 401 -> event_raw_rows (425) -> scan_prefix(..., None) (431-438); read_limit(None)=>Ok(None) (adapter.rs:895-900) = unbounded scan; retain (402-405) and truncate (406-408) run only after full materialization, so a bounded query reads O(N). (4) range_by_time (293) + full sort (299), list_page (356), len_at (187-188), list_types_at (320) all pass None limit = full-log scans. Failure scenario holds: get_by_type(...,Some(10)) on 1e9 events materializes the entire log into a Vec before truncating to 10 (would OOM); range_by_time sorts the whole log per call. Contract §Event item 5 requires query by type/range/time and §5 rule 3 requires keys support the prefix/range queries product APIs need — the key encoding satisfies rule 3, the defect is that get_by_type never uses the index it writes. Minor overstatement: the finding lumps in `range` by sequence, which actually uses bounded windowed scans (scan_sequence_window 440-476), and latest len/list_types use metadata — but the finding's named victims (get_by_type, range_by_time, list, len_at, list_types_at) are all accurately described. On the v1-billion-scale-perf branch this is a genuine scalability wall plus per-append write amplification for an unused index. Severity high is correct: results are correct (not critical/data-loss), but multiple core event query APIs cannot meet the branch's billion-scale mandate and the remediating index is already paid for on every write.

</details>

---

### 5. 🟠 HIGH · `graph` · conformance

**Cross-branch relationship bindings are accepted instead of rejected**

- **Location:** `crates/engine-next/src/data/graph/service.rs:275`
- **Governing spec:** entity-ref-and-relationship-layer-contract.md — Branch Scope rule 4 / Binding Decision 6 / conformance test 9; CLAUDE.md Hard Rule 18

**Issue.** `upsert_node` (and the `UpsertNode` arm of `batch_write`) stores `binding.clone()` and its binding-index row verbatim with no check on `binding.target().branch()`. `GraphBindingTarget::new` (types.rs:233) accepts an arbitrary `Option<BranchName>` with no validation. Nothing in the graph service, the type constructor, the record codec, or the key codec constrains the binding branch to the graph node's own branch. The contract is explicit: 'Graph relationship bindings reject cross-branch targets for V1' (Branch Scope rule 4) and 'V1 APIs reject cross-branch relationship bindings with a structured unsupported or failed-precondition status' (Decision 6). CLAUDE.md Hard Rule 18 states 'Cross-branch references are rejected.' A grep for cross_branch/failed_precondition/unsupported in the graph module returns nothing.

**Evidence.** service.rs:275 `if let Some(binding) = new_record.data().binding() { ... encode_graph_binding_record(&binding_record) }` — no branch guard; types.rs:233 `pub fn new(primitive, branch: Option<BranchName>, space, key) -> EngineResult<Self> { ... Ok(Self { primitive, branch, space, key }) }` — branch stored unvalidated.

**Failure scenario.** Open a graph service on branch `default`, then call `upsert_node("deps", "n", GraphNodeData::new(props, Some(GraphEntityBinding::new(GraphBindingTarget::new(GraphBindingPrimitive::Kv, Some(BranchName::new("secret")), space, "k").unwrap()))))`. The call returns Ok and persists a node + binding-index row whose relationship target points into branch `secret`. Conformance test 9 requires this to fail; instead it silently succeeds, and any future resolution of that binding crosses branch isolation.

**Suggested fix.** In upsert_node and batch_write's UpsertNode, reject when `target.branch()` is Some and != self.branch (or forbid Some entirely for stored bindings) with a structured `failed_precondition`/`unsupported` code before mutating; mirror the check in the record decoder to surface legacy cross-branch rows as unsupported.

<details><summary>Adversarial verification (CONFIRMED)</summary>

The defect is real and the failure scenario holds in the code as written.

Contract (all three cited clauses verified in entity-ref-and-relationship-layer-contract.md):
- Branch Scope rule 4, line 285: "Graph relationship bindings reject cross-branch targets for V1."
- Binding Decision 6, lines 199-204: "V1 APIs reject cross-branch relationship bindings with a structured unsupported or failed-precondition status."
- Relationship-layer conformance rule 9, line 858: "Cross-branch relationship bindings are rejected for V1."
- CLAUDE.md Hard Rule 18: "Cross-branch references are rejected."

Code confirms no such rejection exists:
- GraphBindingTarget::new (types.rs:233-252) validates only the key text via validate_text_component; the branch: Option<BranchName> is stored verbatim (line 246-251) with zero cross-branch check.
- GraphEntityBinding::new (types.rs:308) and GraphNodeData::new (types.rs:396) are const constructors that store the binding with no validation.
- upsert_node (service.rs:249-290): line 261-282 stores the node record and, when a binding is present, writes GraphBindingRecord::new(...binding.clone()) and its binding-index row via encode_graph_binding_record. binding.target().branch() is never inspected against self.branch. The only branch used for addressing is record.storage_branch_id() (the node's own branch), so a target branch of "secret" is persisted inside the payload untouched.
- batch_write UpsertNode arm (service.rs:785-795) has the identical gap.
- commit_batch (service.rs:1443-1478) only checks non-empty and computes counts; no branch validation.
- Record codec (record.rs:472) explicitly encodes target.branch(), so the cross-branch target survives round-trip persistence.
- Grep for cross_branch/failed_precondition/unsupported in crates/engine-next/src/data/graph returns only an unrelated "unsupported control byte" message (types.rs:569); no cross-branch guard and no cross-branch test exists anywhere in engine-next.

The public API (api/graph.rs) merely re-exports GraphService, so there is no interposed validation layer. Calling upsert_node on branch "default" with a binding target whose branch = Some("secret") returns Ok and persists both a node record and a binding-index row whose relationship target points into branch "secret", exactly as the finding describes. This is a genuine conformance violation of a hard branch-isolation invariant.

</details>

---

### 6. 🟠 HIGH · `graph` · completeness

**No binding policy and no dangling/deleted/missing target status on bindings or traversal**

- **Location:** `crates/engine-next/src/data/graph/outcome.rs:379`
- **Governing spec:** entity-ref-and-relationship-layer-contract.md — V1 Minimum item 6, Reverse Maps rule 3, Dangling policy matrix, delete-policy vocabulary; primitive-implementation-contract.md §11; m6 plan M6C ('delete policies')

**Issue.** The relationship layer's central V1 promise — explicit dangling/deleted/missing/history-trimmed target status and a binding policy (strict-live / allow-dangling / pinned-provenance) — is entirely absent. `GraphEntityBinding` (types.rs:300) carries only a target, with no policy field. `GraphBinding` (outcome.rs:379) and `GraphNeighbor` (outcome.rs:303) carry no target-resolution-status field, so traversal and reverse-map lookups cannot report whether the bound entity exists. Interactive creation never validates that the target exists (no strict-live-target default). A grep across engine-next for any resolution-status/Dangling consumer of graph bindings finds none, so this is not implemented at a higher layer either. The contract makes target status a MUST ('Dangling/deleted/history-trimmed target status is explicit'), and M6C explicitly lists 'delete policies' as in-scope for this slice.

**Evidence.** outcome.rs:379-434 `struct GraphBinding { graph, node_id, binding, version, timestamp }` — no target status; outcome.rs:303-336 `struct GraphNeighbor { node, edge, direction }` — no target status; types.rs:300-317 `GraphEntityBinding { target }` — no policy.

**Failure scenario.** A graph node binds to JSON document `orders/ord_1`. The JSON document is later deleted. `bindings_for_entity(target)` and `neighbors(...)` still return the node with no indication the target is Deleted/Missing/Dangling, and there was never a policy recorded to say the binding was strict-live. Downstream (RAG/search/CLI) cannot filter or flag broken bindings as the contract's policy matrix requires.

**Suggested fix.** Add a `BindingPolicy` enum (StrictLive/AllowDangling/PinnedProvenance) to GraphEntityBinding and a target-resolution-status slot to GraphBinding/GraphNeighbor; wire interactive creation to validate targets by default through the persistence/orchestration resolver (graph must not fetch siblings directly).

<details><summary>Adversarial verification (CONFIRMED)</summary>

Every claim in the finding checks out against the code and the governing contract.

STRUCTURE: GraphEntityBinding (types.rs:300-317) has a single field `target` — no policy field. GraphBinding (outcome.rs:380-434) carries graph/node_id/binding/version/timestamp — no target-resolution-status. GraphNeighbor (outcome.rs:304-336) carries node/edge/direction — no target status.

BEHAVIOR: bindings_for_entity_with_selector (service.rs:709-743) scans reverse-map binding index rows and maps each via binding_from_row without ever resolving the bound target entity, so it cannot report Deleted/Missing/Dangling. upsert_node (service.rs:249-290) writes the binding row with no target-existence validation — no strict-live-target default. delete_node (service.rs:335-360) only cleans the graph node's own binding row; there is no hook from bound-target deletion into the reverse map, and no cascade/detach/keep-dangling vocabulary anywhere. A grep over the entire crates/engine-next/src/ for ResolutionStatus/Dangling/HistoryTrimmed/MalformedTarget/DerivedStateStale returns 0 hits, so no resolution-status model exists at any layer. The finding's failure scenario (bind node to JSON doc, delete the doc, traversal/bindings_for_entity still return the node with no status) genuinely holds.

CONFORMANCE: The contract makes this a MUST at V1-Minimum item 6 (line 951, "Dangling/deleted/history-trimmed target status is explicit"; Must = "V1 relationship semantics are incomplete without it," line 44), Binding Decision 12 (line 230), "Traversal APIs must expose target status" (line 595), "Delete policies must be explicit" (line 598), the policy matrix (lines 604-613), the delete-policy vocabulary (lines 615-620), the optional-binding-policy field (line 454), and default strict-live for interactive relationships (line 470). None of this is in the contract's Deferred/Open-Questions list (lines 903-937). M6C scope (m6 plan line 32) explicitly names "graph relationship bindings, reverse maps, and delete policies." The binding/reverse-map/traversal machinery of M6C is implemented and shipped, but the target-status/policy/delete-policy dimension the same clause requires those exact surfaces to carry is entirely absent — a genuine completeness gap in implemented code, not premature complaint about unstarted work.

SEVERITY: high is correct. It is a V1-Minimum MUST, in-scope for an epic whose sibling surfaces are already built, and it yields silently-misleading traversal/binding results (dangling/deleted targets returned as if live) that downstream RAG/search/CLI cannot filter or flag as the policy matrix requires.

</details>

---

### 7. 🟠 HIGH · `temporal-timeline` · conformance

**as_of timestamp after the latest retained commit silently clamps to current state**

- **Location:** `crates/engine-next/src/persistence/adapter.rs:914`
- **Governing spec:** temporal-context-and-timeline-resolver-contract.md Binding Decision 6 / Resolved Frontier / Conformance #7

**Issue.** `should_fall_back_to_latest` matches an after-latest timestamp error from storage and re-issues the read/scan with `ReadSelector::Latest`, returning current state. It is applied in `read_row` (adapter.rs:508-511), `scan_prefix_inner` (597-608), `scan_range` (641-651) and `scan_immutable_sources` (681-691), so every capability's `get_at`/`get_versioned_at`/`list_at`/scan-at-timestamp inherits it (kv/service.rs:117-124, json/service.rs:208, vector/service.rs:326). The returned value carries the *latest* commit version/timestamp with no marker that the requested point was in the future. This is a direct violation of the governing contract: Binding Decision 6 ('the resolver returns an after-latest diagnostic rather than silently clamping to current state'), the Resolved Frontier section ('After-latest timestamp requests ... produce a typed out-of-range diagnostic so users choose current intentionally instead of accidentally reading current state through a future timestamp'), and Conformance test #7 ('after-latest does not silently clamp to current state').

**Evidence.** fn should_fall_back_to_latest(selector, error) -> bool { matches!(selector, ReadSelector::AtTimestamp(_)) && matches!(error, StorageApiError::TimestampHistoryUnavailable { reason, .. } if *reason == "timestamp is after latest retained timeline history") }  // then: read_point(..ReadSelector::Latest..)

**Failure scenario.** Branch head is at commit ts=1000. User calls `kv.get_versioned_at(key, Timestamp::from_micros(2000))` (a future/after-latest timestamp). Instead of an after-latest diagnostic, the engine returns the current value tagged with the current commit version/timestamp, so the caller believes the key had that value at t=2000 and cannot tell it accidentally read live state through a future timestamp.

**Suggested fix.** Remove the fall-back-to-latest path; map the storage after-latest condition to a distinct typed diagnostic (e.g. an after-latest / out-of-retained-range error class carrying timeline bounds) so the read fails instead of clamping. Reserve latest-lookup for an explicit internal/maintenance API as the contract allows.

<details><summary>Adversarial verification (CONFIRMED)</summary>

Traced the full path and cross-checked the governing contract. (1) Storage emits a distinct after-latest diagnostic: storage-next/src/api/runtime/data.rs:333-338 maps CommitTimelineMiss::AfterLatestRetained to TimestampHistoryUnavailable{reason:"timestamp is after latest retained timeline history"}, kept separate from the before-history case (line 329-332). (2) The engine silently swallows exactly that: should_fall_back_to_latest (adapter.rs:914-921) matches AtTimestamp selectors against that exact reason string, and read_row (508-511) plus scan_prefix_inner (597-608), scan_range (641-651), scan_immutable_sources (681-691) re-issue with ReadSelector::Latest and return the current row. is_outside_retained_history (902-912) deliberately excludes the after-latest string, so it is not even turned into not-found. (3) Product surface reaches it with no marker: data/kv/service.rs get_versioned_at (110-123) calls read_row(...,AtTimestamp(ts)) and builds KvVersionedValue from the returned row (versioned_value_from_row), which on fallback is the latest commit with latest version/timestamp; get_at delegates to it; list_at (217-228) uses the scan fallback. The caller cannot distinguish a future-timestamp read from a real read at that time. (4) This directly violates the contract: Binding Decision 6 (contract lines 335-337: after-latest returns a diagnostic "rather than silently clamping to current state"), Resolved Frontier (188-191: "typed out-of-range diagnostic so users choose current intentionally instead of accidentally reading current state through a future timestamp"), and Conformance #7 (735-736: "after-latest does not silently clamp to current state"). The contract's only exception (339-341) is for an internal maintenance/diagnostic API that reports the after-latest condition; this is the V1 product read path and reports nothing. The failure scenario holds exactly as described. Severity high is correct: a silent, caller-undetectable temporal-correctness violation on the core time-travel surface across kv/json/vector.

</details>

---

### 8. 🟠 HIGH · `temporal-timeline` · conformance

**Before-history timestamps and non-retained versions collapse into ordinary absence (Ok(None))**

- **Location:** `crates/engine-next/src/persistence/adapter.rs:902`
- **Governing spec:** temporal-context-and-timeline-resolver-contract.md Binding Decisions 6/7/8, Engine-must-not list, Conformance #8

**Issue.** `is_outside_retained_history` catches storage `TimestampHistoryUnavailable`/`RetainedHistoryUnavailable` whose reason contains 'before retained' or 'outside retained' and returns `Ok(None)` for point reads (adapter.rs:507) and empty for scans (596, 640). This maps three distinct contract cases to plain not-found: (a) a timestamp before the oldest retained commit, (b) a commit version below the retained floor (trimmed history), and (c) a version above the latest retained commit — all become `None`, indistinguishable from 'the key never existed'. Since after-latest is separately clamped (see other finding), the engine's temporal read surface can NEVER surface `history_unavailable.engine.persistence_history` for a timestamp/version selector; the only history code in the registry is effectively dead on the read path. This violates Binding Decision 6 ('If the timestamp is before the oldest retained commit, the resolver returns a retained-history diagnostic rather than reporting ordinary not-found'), Binding Decision 7 (non-retained version must 'fail with a typed diagnostic'), Binding Decision 8 ('Missing History Is Not Ordinary Absence ... must not be indistinguishable from the key never existed'), the explicit Engine-must-not rule ('Convert retained-history misses into ordinary not-found'), and Conformance test #8. It also blocks Strata AI/debugging 'history explanations' the contract calls out as required.

**Evidence.** Err(error) if is_outside_retained_history(&error) => return Ok(None),  // read_row; and `return Ok(Vec::new())` in scan_prefix_inner/scan_range. is_outside_retained_history matches reason.contains("outside retained") || reason.contains("before retained").

**Failure scenario.** Retention trims commits older than version 50. A key last written at version 10 still logically existed at ts=5. `kv.get_at(key, ts=5)` returns `None` — identical to a key that was never written. `kv.get_at_version(key, CommitVersion::new(10))` (now-trimmed) also returns `None`. The caller cannot distinguish 'history was trimmed' from 'absent' and cannot learn the retained timeline bounds.

**Suggested fix.** Propagate before-history and non-retained-version conditions as distinct typed diagnostics (before-history vs after-latest vs pruned-gap vs version-not-retained) carrying `RetainedTimelineBounds`, instead of swallowing them to `Ok(None)`/empty. Keep genuine absence (key not present at a valid resolved frontier) as `None`.

<details><summary>Adversarial verification (CONFIRMED)</summary>

The defect is real and the failure scenario holds in the code as written. In adapter.rs, read_row (line 507), scan_prefix_inner (596), and scan_range (640) intercept is_outside_retained_history(&error) and return Ok(None)/Ok(Vec::new()) BEFORE map_storage_error is reached. is_outside_retained_history (902-912) matches reason.contains("outside retained") || reason.contains("before retained") on both RetainedHistoryUnavailable and TimestampHistoryUnavailable. Cross-checking storage-next reason strings (data.rs:270-342): version-below-floor = "commit version is outside retained history", version-not-in-timeline/above-latest = "commit version is outside retained timeline history", before-history timestamp = "timestamp is before retained timeline history" — all three contain the matched substrings, so all collapse to plain not-found. The after-latest timestamp case is separately re-read at Latest via should_fall_back_to_latest (914-921). Consequently the only history code in the registry, history_unavailable.engine.persistence_history (produced only inside map_storage_error at 1040-1047), is unreachable for these ordinary product cases on the read surface. The public engine methods call read_row directly with no resolver preflight (kv/service.rs:86-124 get_at_version/get_versioned_at; json/service.rs:180-208), and a grep confirmed no timeline-resolver/bounds machinery exists in engine-next/src/persistence/. This directly violates Binding Decision 6 (before-history must return a retained-history diagnostic, not not-found), Binding Decision 7 (non-retained version must fail with a typed diagnostic), Binding Decision 8 / Conformance #8 (history-trimmed must be distinguishable from absent), and the explicit Engine-must-not #4 "Convert retained-history misses into ordinary not-found." Severity remains high: it is a broad, cross-capability (KV/JSON/event) conformance violation on the primary temporal read surface that renders a registry error code dead and defeats the required absent-vs-trimmed distinction, but it is graceful degradation (returns None, no crash/corruption/security impact), so not critical.

</details>

---

### 9. 🟠 HIGH · `vector-service` · completeness

**Automatic index sealing is unwired: every production vector query is an exact full-collection scan**

- **Location:** `crates/engine-next/src/data/vector/service.rs:2012`
- **Governing spec:** primitive-implementation-contract.md §12 Derived-State Hooks ("How rebuild and repair happen", "Users should not have to run manual maintenance in normal use"); retrieval-and-derived-state-contract.md V1 Families 0x42 Vector index; Vector §3 "ANN indexes must be rebuildable or clearly authoritative"

**Issue.** The entire flat/HNSW/manifest acceleration path exists but no production code ever seals an index. `seal_index_artifacts_from_snapshot` and all `seal_*`/`store_*_artifact_ref` helpers are `#[allow(dead_code)]` and are reached only through the testkit-gated `seal_index_artifacts_for_test` (service.rs:1059). The seal-decision surface is likewise dead: `VectorIndexPolicy::should_seal_active_delta` (index.rs:226) is never called anywhere, and `should_seal_hnsw_source` is called only inside the dead seal fns (service.rs:2037,2096). Because nothing writes a `VectorIndexManifest` in production, `index_manifest_resolution` always returns `Missing`, `load_index_artifact_sources_for_query` returns `unavailable`, and `query_vector_sources_with_index_artifacts` falls to `ExactVectorSource` — a full scan that JSON-decodes and scores every visible row. The bounded active-delta fast path (service.rs:1489) can never trigger. Results stay correct; scalability does not exist on the branch named v1-billion-scale-perf. Independently flagged by docs/architecture/engine/engine-test-plan.md:450-453.

**Evidence.** service.rs:2012 `#[allow(dead_code)] fn seal_index_artifacts_from_snapshot(...)`; only caller service.rs:1059 `pub fn seal_index_artifacts_for_test` under `#[cfg(any(test, feature = "testkit"))]`; index.rs:226 `pub(crate) const fn should_seal_active_delta` has zero call sites.

**Failure scenario.** Create a collection, batch_upsert 1,000,000 vectors, call `query(&c, &q, 10, None)`. No manifest is ever written, so the query scans all 1,000,000 source rows, JSON-decodes each embedding, and scores it — O(collection) latency and memory per query. The HNSW/flat ANN index never engages.

**Suggested fix.** Wire an engine-owned seal/rebuild trigger (post-commit maintenance hook or write-path threshold using the already-present `should_seal_active_delta`/`active_delta_seal_threshold`) so the manifest and artifacts are built automatically, and remove the `#[allow(dead_code)]` markers once reachable. If deferral is intentional, record the scope decision and a deletion/enable condition per the test-plan §10 gap.

<details><summary>Adversarial verification (CONFIRMED)</summary>

Every link in the finding's chain holds in the code as written.

Dead seal surface: `seal_index_artifacts_from_snapshot` is `#[allow(dead_code)]` (service.rs:2012-2013). Its only caller is `seal_index_artifacts_for_test` (service.rs:1071), which is `#[cfg(any(test, feature = "testkit"))]` (service.rs:1057-1059). That test fn is invoked only from test files (index_hnsw.rs:396, index_sources.rs:65/457/829/871). `should_seal_active_delta` (index.rs:226) has zero call sites — grep returns only its definition. `should_seal_hnsw_source` (index.rs:232) is `#[allow(dead_code)]` and called only at service.rs:2037 and 2096, both inside the dead seal fns.

No production manifest write: all three callers of `put_index_manifest_bytes` are test/dead — service.rs:1026 (`seed_index_manifest_for_test`, cfg test/testkit), service.rs:1220 (`put_raw_index_manifest_for_test`, cfg test/testkit), service.rs:2062 (inside dead `seal_index_artifacts_from_snapshot`). No derived-state/maintenance hook triggers sealing; `active_delta_seal_threshold` and the seal fns are referenced nowhere outside vector/ and the dead chain.

Query falls to full scan: with no manifest row, `index_manifest_resolution` returns `Missing` with empty `artifact_refs` (service.rs:1712-1716). The bounded active-delta fast path requires `!manifest_resolution.artifact_refs.is_empty()` (service.rs:1491), so it is skipped and control reaches the full-snapshot path (service.rs:1543) which reads the entire `vector_query_snapshot`. `load_index_artifact_sources_for_query` returns `unavailable` on a non-Loaded lookup (service.rs:1780-1786), so flat/hnsw artifact vecs are empty. In `query_vector_sources_with_index_artifacts`, `use_flat`/`use_hnsw` both require non-empty artifact vecs (index.rs:1261-1267), so both are false and execution falls to `ExactVectorSource::new(entries)` scoring every entry (index.rs:1344). The failure scenario (1M vectors → O(collection) exact scan, ANN never engaged) genuinely holds.

Independently corroborated by engine-test-plan.md:450-453, which flags the seal fns as `#[allow(dead_code)]` with all sealing manual and raises "verify whether sealing is wired into engine maintenance at all" as an open scope decision.

Severity: correctly rated high, not critical. Results remain correct (no data loss, crash, or security impact) — this is purely a completeness/scalability gap. But it is a severe one: a fully built and tested flat/HNSW/manifest acceleration subsystem is entirely unreachable in production, so the vector index's core value proposition is inert on a branch named v1-billion-scale-perf. The test-plan's framing of this as an open scope decision slightly tempers it toward "known-incomplete slice" rather than "shipped regression," but the described code state is exact. High stands.

</details>

---

### 10. 🟡 MEDIUM · `invariants-crate-shape` · completeness

**Crate-shape invariant guards depend on an external `rg` binary; 12 of 26 guard tests fail (panic) without ripgrep**

- **Location:** `crates/engine-next/tests/dependency_guards.rs:852`
- **Governing spec:** target-crate-shape-and-test-harness.md Crate Shape Principles #10 ("Test harnesses should be reusable, deterministic, and explicitly invoked")
- **Severity:** 🟡 MEDIUM (auditor reported high; corrected on verification)

**Issue.** The guards that enforce the most important crate-shape invariants of this area all scan directory trees via a shell-out to ripgrep, which panics via `.expect()` when `rg` is absent. Confirmed empirically: `which rg` -> not found on this machine; `cargo test -p strata-engine-next --test dependency_guards` reported `14 passed; 12 failed`, and every failing test panicked with `run rg source file listing: Os { code: 2, kind: NotFound }`. The 12 failing guards include storage_crate_imports_stay_inside_persistence_adapter (the storage-boundary invariant), vector_indexing_does_not_reintroduce_process_global_backend_state (process-global-state), product_scope_stays_limited_to_current_primitives, planning_vocabulary_stays_out_of_sources_and_tests, executor_crate_does_not_import_storage_crates, and executor_facing_api_does_not_expose_storage_types. The 14 that pass are the single-file `fs::read_to_string` guards that never call rust_files(). The underlying code invariants are all intact (verified independently by grep), but their automated enforcement does not run on a stock Rust toolchain.

**Evidence.** rust_files() shells out: `Command::new("rg").arg("--files").arg(root).output().expect("run rg source file listing")` (line 852-856). Every directory-scanning guard routes through this helper (13 call-sites). `rg` is not a declared dev-dependency nor a documented prerequisite anywhere.

**Failure scenario.** A contributor clones the repo on a machine with only rustup+cargo (no ripgrep) and runs `cargo test -p strata-engine-next`. Twelve dependency-guard tests fail with a cryptic `rg ... NotFound` panic that looks like an invariant violation but is a missing external tool. Worse: if any CI image lacks ripgrep, the entire crate-shape guard suite goes red for a tooling reason; the natural remediation (make rust_files() skip gracefully when rg is missing) would silently stop enforcing storage-boundary, process-global-state, and product-scope invariants that a Stripe-grade engine relies on.

**Suggested fix.** Replace the `rg --files` shell-out with a self-contained `std::fs::read_dir` recursion (or add `walkdir`/`ignore` as a declared dev-dependency) so the guard suite runs deterministically in any environment.

<details><summary>Adversarial verification (CONFIRMED)</summary>

The defect is real and reproduced empirically. crates/engine-next/tests/dependency_guards.rs:852-856 defines rust_files() as Command::new("rg").arg("--files").arg(root).output().expect("run rg source file listing"); the .expect() at line 856 panics when the rg binary is absent. On this machine `which rg` returns exit 1 (not installed). Running `cargo test -p strata-engine-next --test dependency_guards` produced exactly "14 passed; 12 failed", and every one of the 12 failures panicked at dependency_guards.rs:856 with `run rg source file listing: Os { code: 2, kind: NotFound }`. The 12 failing tests are all the directory-scanning guards that route through rust_files() (12 verified call sites at lines 10, 57, 108, 292, 357, 548, 580, 644, 683, 730, 776, 815), and the actual failing set includes every guard the finding named (storage_crate_imports_stay_inside_persistence_adapter, vector_indexing_does_not_reintroduce_process_global_backend_state, product_scope_stays_limited_to_current_primitives, planning_vocabulary_stays_out_of_sources_and_tests, executor_crate_does_not_import_storage_crates, executor_facing_api_does_not_expose_storage_types). The 14 passing guards are the single-file fs::read_to_string checks (e.g. open_options_remain_explicit at line 832) that never call rust_files(). ripgrep is not a declared dev-dependency (engine-next [dev-dependencies] lists only proptest and tempfile) nor a documented prerequisite. The failure scenario genuinely holds on a stock rustup+cargo machine. This maps directly against target-crate-shape-and-test-harness contract clause #10 (harnesses should be reusable/deterministic): a harness that shells out to an undeclared external binary is neither. I lower the severity from high to medium: the impact is confined to test tooling (the underlying product invariants are intact and the finding concedes this), the failure is a loud panic rather than a silent pass so it cannot masquerade a real violation as green, and the shipping CI path (cargo test --workspace on GitHub ubuntu-latest per .github/workflows/ci.yml, whose standard runner image ships ripgrep) is not red today — so real-world blast radius is local contributors and non-GitHub CI/dev images without rg. The finding's escalating "worse case" (a naive fix silently disabling enforcement) describes a hypothetical future remediation, not the defect as written. Real and worth fixing (walk the tree with std::fs/walkdir instead of shelling to rg), but medium.

</details>

---

### 11. 🟡 MEDIUM · `persistence` · conformance

**Adapter enforces no scoped control-plane write authority and no read-only/write-disabled write guard**

- **Location:** `crates/engine-next/src/persistence/adapter.rs:463`
- **Governing spec:** persistence-adapter-contract.md — Binding Decision 5 ('The adapter is the final write-access guard ... the adapter must also reject write commit plans for read-only, closed, closing, or otherwise write-disabled handles before storage mutation'); Write Path rule 7 ('Data capability write paths may not write Registry, Dataset, Recipe, Branch, or Space control rows by selecting those symbolic assignments directly'); Control Plane section ('The adapter rejects control-plane writes made under a data-capability scope'); V1 Minimum 11; Conformance tests 15 and 18
- **Severity:** 🟡 MEDIUM (auditor reported high; corrected on verification)

**Issue.** The contract designates the adapter as the final write-access guard and requires it to (a) reject writes under a data-capability scope that target control-plane row families and (b) reject writes for read-only/write-disabled handles before storage mutation. Neither guard exists. There is no scope concept in CommitPlan and no read-only database mode anywhere in engine-next; closed-handle rejection is enforced only above the adapter (Database::kv returns ClosedRuntime), not at the adapter as the contract requires. This is both a corruption vector (a data path or bug that constructs a control-plane RowAddress silently overwrites registry/branch/dataset rows) and a missing V1-minimum capability (read-only open) for downstream SDK/CLI consumers.

**Evidence.** StoragePersistence::commit (adapter.rs:463-483) builds mutations and commits with zero authority/mode checks: `let batch = CommitBatch::new(plan.branch_id(), mutations, options)...; let summary = self.runtime.commit(&batch)...`. CommitPlan (plan.rs:8-12) carries only `branch_id`, `mutations`, `expected_generation` — no write-scope and no classification. RowMutation/RowAddress (row.rs) let any caller pick any RowClass, including RowClass::Registry/DatasetIdentity/BranchControl/SpaceControl. A workspace-wide grep for read_only/ReadOnly/AccessMode/WriteAuthority/ControlScope in engine-next/src returns nothing, and the public open options (CacheOpenOptions, DurableLocalOpenOptions) expose no read-only mode.

**Failure scenario.** A capability service (or a copy-paste bug) constructs `RowAddress::new(branch, RowClass::Registry, storage_registry_key())` inside a normal KV/JSON write path and passes it to `persistence.commit`. The adapter maps it to storage-space 0x32, commits it, and corrupts the durable storage-space registry with no authority error. Separately, a caller that wants to open a database read-only cannot: there is no read-only mode, so the 'reject writes in read-only handles' guarantee is structurally impossible to satisfy.

**Suggested fix.** Add a control-plane write scope to CommitPlan (or a per-call authority token) and have commit() reject mutations whose RowClass is a control family unless the matching scope is present; add a read-only/write-disabled flag to StoragePersistence checked before any commit, and surface a read-only open target.

<details><summary>Adversarial verification (CONFIRMED)</summary>

Both contract-mandated guards are genuinely absent, and the failure scenario holds in the code as written.

Scoped control-plane write authority — MISSING. `StoragePersistence::commit` (adapter.rs:463-483) builds mutations and commits with zero authority/scope/classification checks. `to_storage_mutation` (adapter.rs:781-794) → `storage_space_for_class` (adapter.rs:800-802) maps ANY `RowClass` — including `Registry` (0x32), `DatasetIdentity` (0x34), `BranchControl` (0x30), `SpaceControl` (0x31) per space.rs:35-38 — straight to a storage space with no guard. `CommitPlan` (plan.rs:8-12) carries only `branch_id`, `mutations`, `expected_generation`; it lacks the "Write authority and access-mode facts inherited from runtime" the contract's Commit Plan definition (line 201) requires. `RowClass` and `RowAddress::new` are `pub(crate)`, so any engine-next module can construct `RowAddress::new(branch, RowClass::Registry, storage_registry_key())` (both exported from persistence/mod.rs) and commit it — the adapter maps it to 0x32 and overwrites the registry with no authority error. This is already exercised in practice: the vector data-capability service routes through `RowClass::SpaceControl` via the same unguarded path (data/vector/service.rs:1625-1629), showing the row-class boundary is porous by design and unenforced. Contract clauses confirmed verbatim: Binding Decision 5 (lines 240-243), Write Path rules 6-7 (lines 339-344), Control Plane section (lines 492-500), V1 Minimum 11 (lines 683-684), Conformance tests 15/18 (lines 621, 626).

Read-only / write-disabled handle rejection — MISSING. No read-only mode exists anywhere: a workspace grep for read_only/AccessMode/WriteAuthority/ControlScope in engine-next/src returns only a doc comment on admin.rs:203. `CacheOpenOptions`/`DurableLocalOpenOptions` (options.rs) expose only `default_branch` and `memory_budget_bytes` — no read-only. The only operation gate, `Database::require_open` (database.rs:425-433), lives ABOVE the adapter, checks only `self.open`, and returns `closed_runtime`; the adapter itself performs no closed/closing/read-only check before storage mutation, exactly as the finding states. The contract designates the adapter as "the final write-access guard" (line 240) and requires this rejection "before storage mutation" (line 339) — that guard is entirely absent.

Severity corrected high → medium. The gap is real and contract-mandated (V1 Minimum "must", two dedicated conformance tests), but neither concrete harm is user- or SDK-reachable: the corruption vector requires an internal engine-next bug because control `RowClass` values are `pub(crate)`-only and never selectable from external input, making this a defense-in-depth / internal-invariant gap rather than a user-triggerable corruption; and read-only open is a missing V1 feature, not an active data-loss defect, on an in-progress rewrite line. That places it below the user-reachable-corruption bar that "high" denotes, but well above trivial — it is a complete absence of a designated last-line-of-defense boundary.

</details>

---

### 12. 🟡 MEDIUM · `retrieval-derived-state` · product-gap

**Vector index derived state has no production build/seal trigger; production vector search always does exact O(n) scans**

- **Location:** `crates/engine-next/src/data/vector/service.rs:2013`
- **Governing spec:** retrieval-and-derived-state-contract.md Derived-State Families (vector index 0x42; rebuild behavior); Runtime Resource Policy (prefer indexes when profile allows)
- **Severity:** 🟡 MEDIUM (auditor reported high; corrected on verification)

**Issue.** The vector index derived-state family (flat + HNSW artifacts, manifest, active-delta watermark, runtime graph cache) has a complete read/plan path and on-disk format, but every function that BUILDS/seals it is `#[allow(dead_code)]` and the only entry point, `seal_index_artifacts_for_test` (service.rs:1058), is `#[cfg(any(test, feature = "testkit"))]`. There is no production caller: `seal_index_artifacts_from_snapshot` (2013), `seal_whole_collection_flat_artifact`, `seal_*_for_source`, `store_flat_artifact_ref`, and `put_index_manifest_bytes` are reachable only from testkit seed/seal helpers. `upsert`/`batch_upsert`/`commit_batch` never seal, and admin.rs only calls `list_collections()`. Consequently, in a real (non-testkit) database the index manifest row is never written, so `index_manifest_resolution` returns `VectorIndexManifestLookup::Missing`, `load_index_artifact_sources_for_query` returns `unavailable("missing")`, and `query_with_selector_and_policy` falls through to `ExactVectorSource` over the full snapshot. The engine_vector.rs test `assert_default_exact_policy_diagnostics` codifies this: it asserts `diagnostics.manifest_status() == "missing"` for a default query. On a branch explicitly targeting billion-scale vector performance, the acceleration structure cannot be populated outside tests, so shipping vector search is O(collection) per query.

**Evidence.** service.rs:2012-2013 `#[allow(dead_code)] fn seal_index_artifacts_from_snapshot(...)`; service.rs:1057-1058 `#[cfg(any(test, feature = "testkit"))] pub fn seal_index_artifacts_for_test`; tests/engine_vector.rs:1353 `assert_eq!(diagnostics.manifest_status(), "missing");` for the default policy path. grep confirms no non-test caller of any seal_* function.

**Failure scenario.** Open a durable database, create a collection, upsert 10M vectors, then call `query(collection, q, 10, None)`. No seal ever runs, so the manifest is Missing and the planner scans and scores all 10M rows on every query — the flat/HNSW artifact path built and tested in index.rs/artifact.rs is never exercised in production.

**Suggested fix.** Wire a production seal trigger (commit-time threshold on active-delta size via `should_seal_active_delta`, and/or a background maintenance pass) that calls the existing seal path and persists the manifest, so the vector index family is actually built and consulted outside testkit.

<details><summary>Adversarial verification (CONFIRMED)</summary>

Every factual claim verifies against the code. seal_index_artifacts_from_snapshot (service.rs:2012-2013) is #[allow(dead_code)]; the entire seal chain (seal_index_artifacts_from_immutable_sources 2065, seal_flat/hnsw_artifact_for_source, store_flat_artifact_ref 2261) is dead-code annotated. The sole entry, seal_index_artifacts_for_test (1057-1059), is #[cfg(any(test, feature=\"testkit\"))], and its line-1071 call is the only caller of seal_index_artifacts_from_snapshot (grep-confirmed). put_index_manifest_bytes (1223) is reachable only from testkit seeders (1026, 1220) and the dead-code seal chain (2062). Write paths upsert (237) and batch_upsert (475) call only commit_batch (2683) = persistence.commit, never sealing. index_manifest_resolution (1698) returns VectorIndexManifestLookup::Missing with empty artifact_refs when no manifest row exists (1712-1716); in query_with_selector_and_policy (1446) empty refs force bounded_watermark=None (1489-1496), skipping the fast path and running vector_query_snapshot over the whole collection with exact scoring (1543-1573), while load_index_artifact_sources_for_query returns unavailable for Missing (1780-1786). engine_vector.rs:1353 codifies manifest_status()==\"missing\" as the default. A workspace sweep found no non-test seal trigger anywhere (api/, persistence/adapter.rs clean). So in a non-testkit database the acceleration structure is never built and every query is O(collection) — the failure scenario holds exactly. Downgrading high->medium: the uniform #[allow(dead_code)] markers on a complete, tested seal path indicate intentional staging (machinery built, trigger not yet wired) on an active development branch, query correctness is fully preserved, and exact-scan fallback for Missing derived state is explicitly contract-permitted (contract line 972). It is a real and important product gap against the branch's billion-scale goal, but a pending-wiring intermediate state rather than a shipped or correctness-affecting defect.

</details>

---

## Part 2 — Unverified findings (medium / low)

Reported by the unit auditors but **not** adversarially re-verified (verification budget was spent on critical/high). Treat as leads to confirm during remediation, not established defects.

### `api-surface`

**U1. 🟡 MEDIUM · completeness — No dataset clone / export-import artifact surface**  
`crates/engine-next/src/api/database.rs:146` · public-api-and-cli-surface-cleanup-checklist.md "Clone And Data Movement", "CLI Shape" (`strata clone`), and "V1 Minimum" item 7; m6-m6t-implementation-plan.md M6G

There is no clone, export, import, or .strata artifact surface anywhere in engine-next (grep for clone_artifact/.strata/export_database/import_database = 0 hits). The cleanup checklist makes `strata clone <source> <destination>`, an SDK clone API, and `.strata` artifact validation required V1 surface and names clone as the single cold-start data-movement story in the V1 Minimum. The engine layer that would own the clone substrate provides nothing for a downstream CLI/SDK to call.

*Failure scenario:* A user wants to copy a database for CI or to hand a dataset to a teammate (`strata clone ./db ./db-copy`). There is no engine surface to build this on, so the only documented V1 data-movement path cannot be implemented; integrations must reach around the public API to move data.


**U2. 🟡 MEDIUM · conformance — Two identical public branch-from-head methods violate one-canonical-path**  
`crates/engine-next/src/branch/service.rs:89` · CLAUDE Hard Rule 8 "One canonical path per operation. No two public surfaces expose the same behavior."; PR discipline "Never add a second public way to do the same operation"; cleanup checklist Integration Boundary Rule (stable command names)

BranchService::create_from_head (service.rs:89-95) is a one-line delegate whose entire body is `self.fork_current(source, name)`, and BranchService::fork_current (service.rs:98) is the real implementation. Both are `pub fn` on the public `BranchService` (re-exported via api::BranchService). This is exactly the prohibited second public path for the same operation. The cleanup checklist even notes that fork naming may survive only as an alias led by product wording, but exposing both as first-class public methods lets them drift and forces downstream CLI/SDK/MCP to choose arbitrarily.

*Failure scenario:* An MCP/ORM adapter author binding branch operations sees two public methods (`create_from_head` and `fork_current`) that appear distinct but are byte-for-byte equivalent; two integrations bind different names for the same op, and a future behavior change applied to only one method silently diverges the two paths.


**U3. 🟡 MEDIUM · correctness — Space delete is hard-capped at 10,000 mutations with no paged path — large spaces become permanently undeletable**  
`crates/engine-next/src/api/space.rs:355` · public-api-and-cli-surface-cleanup-checklist.md "Data Capabilities" (Spaces: "define delete safety") and Product Surface Rule; CLAUDE unified-scale / billion-key positioning

SpaceService::delete computes tombstone mutations for every visible data row plus catalog rows, then rejects the whole operation when data_mutations.len()+catalog_mutations.len() exceeds SPACE_DELETE_MUTATION_LIMIT (=10_000) with invalid_argument.engine.space_delete_too_large. delete() is the only deletion path; there is no chunked/looped/paged deletion. Consequently a space that has accumulated more than ~10k visible rows can never be deleted through the API — the error is returned deterministically on every attempt. For a multi-tenant, billion-scale engine this is an operational dead end (a tenant space cannot be reclaimed).

*Failure scenario:* Tenant space `tenant_a` holds 15,000 visible KV/JSON/vector/graph rows. Operator runs `spaces.delete(&space("tenant_a"), true)` to offboard the tenant. Every call returns invalid_argument.engine.space_delete_too_large; the tenant's data and space entry can never be removed via the product API, leaving orphaned storage.


**U4. 🟡 MEDIUM · product-gap — describe() reports primitive counts only for the default space, silently under-reporting non-default-space data**  
`crates/engine-next/src/api/admin.rs:293` · public-api-and-cli-surface-cleanup-checklist.md "Inspection And Diagnostics" + Integration Boundary Rule (bounded, honest inspection through describe for MCP/agents)

AdminService::describe lists all registered spaces on the branch (spaces field) but always builds primitive counts from `ProductSpace::new(control_space::DEFAULT_SPACE)` (admin.rs:293) via primitive_summary(&branch, &space). The AdminDescribeSummary.primitives doc even says "Primitive summaries for the default space." describe is explicitly "a structured database snapshot for agent/CLI introspection", so an agent inspecting a database whose data lives in a non-default space is told the spaces exist but sees zero KV/JSON/event/vector/graph counts, i.e. the database looks empty.

*Failure scenario:* A tenant-scoped app stores everything in space `tenant_a` and nothing in `default`. An MCP agent calls describe to understand the database: it receives spaces=[default, tenant_a] but primitives.kv_count/json_count/... all 0, and reports to the user that the database contains no data.


**U5. 🟡 MEDIUM · conformance — Open surface produces no locked/IPC-fallback classification for downstream transport**  
`crates/engine-next/src/api/database.rs:29` · ipc-and-command-boundary-contract.md Binding Decision 4 ("Engine classifies open outcome; executor owns transport") and Target Flow (Client Open While Server Runs → ProductOpenOutcome::Ipc; Locked Without Server → locked-without-socket diagnostic)

DatabaseOpenTarget has only Cache and DurableLocal, and DatabaseOpenSummary reports only {target, created, durable}. open_local surfaces no lock-conflict / IPC-fallback outcome; a concurrent durable open is left to bubble up as a generic mapped storage error. The IPC contract makes engine responsible for classifying the open outcome (local handle vs IPC fallback vs locked-without-socket) so executor can route transport; that classification does not exist at this surface. (This is scoped to M6F IPC semantics and is future work, but it is a named engine responsibility in the governing IPC contract and there is currently no seam for it.)

*Failure scenario:* `strata up` owns a durable database and another process opens the same path. The engine cannot report a structured 'locked, socket present → use IPC' vs 'locked, no socket → run strata up' outcome, so the executor has nothing to route on and cannot implement the contract's transparent IPC fallback or the locked-without-socket diagnostic.


**U6. ⚪ LOW · correctness — describe capability flags arrow/inference hardcoded false (and vector_index true) regardless of build**  
`crates/engine-next/src/api/admin.rs:319` · public-api-and-cli-surface-cleanup-checklist.md "Explicit feature availability and unsupported-feature errors" (Integration Boundary Rule); Serializable Command Boundary Checklist (feature availability must be accurate)

AdminService::describe always emits AdminCapabilitySummary { arrow: false, inference: false, vector_index: true, ... } with constant literals. The field docs claim arrow/inference are "available in executor builds that enable it", but the engine hardcodes them false and cannot see executor feature flags. A downstream consumer that gates behavior on describe().capabilities will read false even in a build where the feature is present. These flags either belong to the executor DTO (which knows its features) or should be sourced from real capability state rather than constants.

*Failure scenario:* An executor build compiled with the inference/arrow feature exposes describe().capabilities.inference == false to an MCP client; the client concludes generation/arrow is unsupported and disables working functionality, or vice-versa a consumer trusts vector_index=true on a runtime profile that cannot serve it.


### `branch-commit`

**U7. 🟡 MEDIUM · conformance — Branch workflow decodes/encodes vector capability rows inline instead of through a capability adapter**  
`crates/engine-next/src/branch/service.rs:427` · branch-operation-and-capability-adapter-contract.md — Binding Decision 3 ("Capabilities interpret their own rows") and Forbidden Dependencies #4 ("Decode capability values without going through the owning capability adapter")

`materialize_vector_index_manifests_for_fork` / `vector_index_manifest_fork_mutations` (service.rs:389-479) is the one place branch code already touches capability data, and it does so by directly importing and calling vector-capability codecs — `decode_vector_index_manifest`, `encode_vector_index_manifest` (imported at service.rs:6-7), plus `manifest.matches_branch_key(...)` and `manifest.materialize_for_child_fork(...)`. The branch workflow is decoding, filtering, transforming, and re-encoding vector-capability value bytes itself. The contract is explicit that capability rows must be interpreted by the owning capability's branch adapter, not by branch workflow orchestration; this is exactly the 'one-off primitive-specific decoding hidden in an unrelated module' anti-pattern the contract's Current Code Evidence calls out for removal.

*Failure scenario:* When JSON/event/graph fork-time derived-state handling is added, the pattern here forces each capability's row-format knowledge back into service.rs, re-creating the coupling the contract forbids; and a malformed vector manifest is silently skipped here (row loop uses `let Ok(...) = decode... else { continue; }`, service.rs:452-453) rather than surfacing the structured diagnostic the adapter contract requires (Adapter rule 5).

*Fix:* Move fork-time vector manifest interpretation behind the capability branch adapter seam (once introduced in M6D), so the branch service coordinates and the vector capability owns decode/transform/encode and malformed-byte diagnostics.

**U8. 🟡 MEDIUM · correctness — A UUID-form branch name can alias the v5-derived id of an ordinary branch name; nothing rejects the collision**  
`crates/engine-next/src/branch/catalog.rs:256` · CLAUDE.md Hard Rules 16 & 18 (generations monotonic scoped per branch name; cross-branch references rejected); branch contract Binding Decision 11 (same-name recreation must not inherit stale state)

`derive_branch_id_text` returns the literal parsed UUID for any name that parses as a UUID (catalog.rs:260-262), while ordinary names hash to a UUIDv5. `aliases_reserved_branch_identity` only guards the three RESERVED ids (default, default-storage, system) — it does NOT check collisions with the derived ids of other user branch names (catalog.rs:242-254). So two distinct, valid branch names can map to the SAME branch_id and therefore the SAME storage_branch_id. Example: `derive("main") == 1f64c067-acf2-5034-a5d0-92d5764138ec` (proven by the locked anchor test at catalog.rs:275-280), and `derive("1f64c067-acf2-5034-a5d0-92d5764138ec")` returns those same bytes. The catalog is keyed by NAME and the generation counter is name-scoped (`next_generation_for_name`, bootstrap.rs:136-139), but the storage branch is id-scoped, so the two views diverge. Storage's per-id generation guard (branch_registry.rs:218-239 requires generation > current) prevents actual stale-row resurrection, but it converts the aliasing into spurious, confusing, order-dependent failures.

*Failure scenario:* User creates branch `1f64c067-acf2-5034-a5d0-92d5764138ec` (valid, storage branch created at gen 1), then deletes it. Later the user creates branch `main` — a name they have never used. `next_generation_for_name("main")` returns 1 (name absent from catalog), but storage branch 1f64c067 is a deleted gen-1 branch, so `create_branch(1f64c067, gen=1)` is rejected by storage's recreate guard with a generation-regress error. The user gets a permanent-looking failure creating a fresh branch name, with an error referencing storage state they never knowingly touched.

*Fix:* Either scope generation/lifecycle by storage_branch_id rather than name, or reject a branch name whose parsed-UUID id collides with the derivable id of any other reachable name (or drop the raw-UUID-name shortcut for user-created branches). At minimum, detect storage_branch_id collisions at create time and return a structured `already_exists.engine.branch`-class error naming the aliasing branch instead of a raw storage generation error.

**U9. 🟡 MEDIUM · conformance — Historical fork falls back to copying LATEST vector manifests when the fork frontier has none — current derived state used for a historical point**  
`crates/engine-next/src/branch/service.rs:408` · temporal-context-and-timeline-resolver-contract.md — Binding Decision 10 ("Current derived state must never be used for historical correctness unless classified exact or source-filtered"); branch contract Binding Decision 10 (derived state explicit)

`materialize_vector_index_manifests_for_fork` first scans source vector-index manifests at the fork frontier (AtVersion(fork_version) or AtTimestamp). If that scan returns no rows, it retries the scan at `ReadSelector::Latest` (service.rs:408-415) and materializes the child's manifests from the source's CURRENT state. For a fork pinned to a retained historical version/timestamp, this copies vector-index manifests that describe the source's index as of NOW, not as of the fork point — i.e., it derives historical branch state from current derived state, which the temporal contract forbids. Vector manifests are derived/acceleration rows, so the blast radius is search correctness on the forked branch (index metadata inconsistent with the child's actually-visible historical vectors), not source-row loss — but the manifest can claim vectors/segments that are not visible at the child's frontier.

*Failure scenario:* Source branch has vectors; the vector index manifest for a collection is (re)written at commit V+5, and more vectors are added through commit V+20. User forks at retained version V (before the manifest existed at that key). The frontier scan returns empty, so the code copies the LATEST manifest (reflecting the V+20 index) onto the child branch forked at V. The child's vector search now consults a manifest built over vectors that are invisible at its frontier.

*Fix:* Remove the Latest fallback for historical forks; if no manifest exists at the fork frontier, leave the child manifest absent (to be rebuilt) or mark derived state stale/rebuild-required per the derived-state contract, rather than importing current-frontier manifests.

**U10. ⚪ LOW · conformance — Fork-from-timestamp records the requested timestamp but drops the resolved commit timestamp and resolution kind; no engine timeline-resolver abstraction**  
`crates/engine-next/src/branch/service.rs:218` · temporal-context-and-timeline-resolver-contract.md — Binding Decision 13 (branch workflows reuse the resolver), Resolved Frontier / ResolutionKind, and Output Metadata (selected vs observed timestamp)

fork_at_version/fork_at_timestamp forward the raw selector to storage (service.rs:202-231) and record only `fork_version` (resolved) and `fork_timestamp` (the caller's REQUESTED timestamp, echoed back by storage — see storage test api/tests/branch.rs:334 where a request for ts=20 yields fork_timestamp=20 while resolving to the commit at ts=10). The engine keeps no ResolvedFrontier with a ResolutionKind (exact vs rounded-down) and does not record the resolved commit's actual timestamp. So `BranchParentSummary.fork_timestamp()` returns the user's requested instant, indistinguishable from an exact hit, and the commit timestamp the branch is actually pinned to is lost. There is no engine-owned temporal resolver; resolution is delegated wholesale to storage, and after-latest/pruned-gap fork diagnostics are neither surfaced distinctly nor covered by the branch tests (branch_semantics.rs only exercises an exact retained timestamp).

*Failure scenario:* User forks branch A at time:T where the nearest retained commit is at time:T-5m. The returned parent reports fork_timestamp=T with no signal it was rounded down, and the resolved commit timestamp (T-5m) is unavailable to the caller — so reproducible/auditable fork provenance (required for Strata AI and history explanations per the contract's Output Metadata) cannot be reconstructed.

*Fix:* Introduce the engine ResolvedFrontier used by all branch-from-history workflows, and record both the requested selector and the resolved commit version+timestamp+resolution kind in branch lineage; add fork conformance tests for before-history, after-latest (must not clamp), and rounded-down cases.

**U11. ⚪ LOW · product-gap — BranchSummary omits contract-required inspection fields (current visible version, derived-state health)**  
`crates/engine-next/src/api/branch.rs:73` · branch-operation-and-capability-adapter-contract.md — Branch Inspection (fields 4 "current visible version" and 7 "derived-state health summary")

BranchSummary exposes name, branch_id, generation, status, parent, created_at, deleted_at, state_revision (api/branch.rs:73-146). The contract's Branch Inspection list requires, among these, the branch's current visible version and a derived-state health summary. Neither is present, so a CLI/SDK `branch get` cannot show where a branch's head is or whether its derived indexes are healthy/stale/rebuilding — capabilities a world-class engine's branch inspection needs for its downstream consumers.

*Failure scenario:* A CLI `strata branch show feature` cannot report the branch's current head commit version or flag that its vector/search derived state is stale after a fork, because the engine summary carries neither field.

*Fix:* Extend BranchSummary (or a richer inspection DTO) with the resolved current visible commit version and a derived-state health summary once the temporal resolver and derived-state manifests land in M6.

### `control-plane`

**U12. 🟡 MEDIUM · correctness — Fork clears the pending marker after the storage branch is already durably forked, orphaning the branch and permanently poisoning the name**  
`crates/engine-next/src/branch/service.rs:191` · control-plane-layout-contract.md — Bootstrap/pending-marker fail-closed protocol; Branch Behavior > Create/Fork

The control-plane pending-marker protocol (control/bootstrap.rs begin_branch_operation / clear_pending_branch_operation / persist_branch_record) exists so an interrupted branch op fails closed on reopen. In fork_current (service.rs:186-193) and fork_with (service.rs:339-346) the storage branch is ALREADY durably forked (fork_branch_current returned Ok and describe_branch succeeded) before materialize_vector_index_manifests_for_fork runs. If that manifest step fails, the code calls clear_pending_after_storage_error, which CLEARS the durable pending marker (control/bootstrap.rs:167-183) rather than failing closed. Because persist_catalog_record is never reached, the child is never inserted into ControlPlane.branches, so next_generation_for_name(name) stays at the same value. The forked storage branch (storage_branch_id = derive(name), generation N) is left orphaned with no catalog row and is undetectable on reopen (load_existing_database only enumerates the catalog index, never storage branches). Note the inconsistency: the immediately-preceding describe_branch failure path (service.rs:156-163) correctly calls fail_closed_after_branch_operation_error and leaves the marker, so two adjacent post-fork failure handlers make opposite durability choices.

*Failure scenario:* Fork 'child' from 'default' where 'default' has committed data (fork_version>0) and at least one vector index manifest. fork_branch_current creates storage branch X at generation 1 and succeeds; the manifest-copy commit then fails transiently while the subsequent clear-pending commit succeeds. Result: X is an orphaned active storage branch, the pending marker is gone, and 'child' is absent from the catalog. Retrying fork 'child' recomputes next_generation_for_name('child')=1 and calls fork_branch_current(X, default, gen=1); storage sees X already active and returns already_exists.engine.persistence. The name 'child' can never be created again (permanent denial) and X leaks storage.

*Fix:* On any failure that occurs AFTER the storage branch is durably created/forked, fail closed (leave the pending marker, mint terminal_error) instead of clearing the marker, or roll back by deleting the just-created storage branch before clearing. Make the post-storage-creation failure handling consistent between the describe path and the manifest path.

**U13. 🟡 MEDIUM · conformance — Capability registry hardcodes only "kv" despite JSON/event/vector/graph being implemented, and decode hard-rejects any other value**  
`crates/engine-next/src/control/records.rs:101` · control-plane-layout-contract.md — Target Layout > Global Scope: "Capability registry | 0x32 | Source | Enabled capabilities, capability schema versions, required migrations"; Binding Decision 5

encode_capability_registry() writes exactly one capability token, b"kv", and is only ever emitted once during bootstrap_new_database (bootstrap.rs:278-281); it is never re-encoded when other capabilities are used. decode_capability_registry() (records.rs:108-119) requires capability == b"kv" and errors otherwise. The engine, however, fully implements and vends JSON, event, vector, and graph services (api/database.rs). The required global source row therefore permanently misrepresents the database's enabled capabilities, violating the contract's capability-registry family definition ("Enabled capabilities").

*Failure scenario:* Open a DB, write vector and graph data, close, reopen: the on-disk capability registry still advertises only 'kv'. Any consumer that relies on the registry to enumerate capabilities (e.g., a clone/export manifest per dataset-clone-artifact-contract manifest item 16 'Included data capabilities', or a future capability gate) sees only KV and would omit or reject the vector/graph capabilities the database actually contains.

*Fix:* Record the actual set of enabled data capabilities (kv/json/event/vector/graph) in the registry (versioned), and update decode to accept the enumerated set; keep the registry authoritative for clone/export and open-time capability validation.

**U14. 🟡 MEDIUM · completeness — Dataset clone artifact substrate and its supporting control rows are entirely absent**  
`crates/engine-next/src/control/bootstrap.rs:249` · dataset-clone-artifact-contract.md — V1 Minimum (1-10); control-plane-layout-contract.md — Global families "Dataset provenance" and "Remote refs" (0x34)

There is no clone/export/import/validation substrate anywhere in engine-next (confirmed by grep across src and tests), and none of the control rows the clone flow depends on exist: the only 0x34 rows are database_identity and local_instance_identity (persistence/key.rs). There is no dataset-provenance/clone-source row, no remote-refs row, and no artifact manifest/validation path. The dataset-clone-artifact-contract V1 minimum (clone materializes a normal DB, validates before promotion, mints local identity while preserving source provenance, omits/rebuild-marks derived state, excludes secrets, declares branch/version scope) is wholly unimplemented, and the control-plane-layout global families 'Dataset provenance' and 'Remote refs' are missing. This is the M6G deliverable; it appears not yet started on this branch.

*Failure scenario:* `strata clone <source> <dest>` (the stated V1 product path) has no engine substrate to call: an artifact cannot be validated, its provenance cannot be recorded, and a cloned database cannot mint local identity while preserving source provenance. Any downstream CLI/SDK/Hub clone/info feature has no engine surface to build on.

*Fix:* Implement the M6G clone artifact substrate: dataset-provenance/clone-source control rows (0x34), remote-refs rows, artifact manifest read/validate-before-promotion, staged import with incomplete-marker, identity minting, and derived-state omit/rebuild-marking, per the clone contract.

**U15. 🟡 MEDIUM · product-gap — Space registration re-reads and re-decodes the space index plus a catalog row on every write batch instead of caching the registered-space set**  
`crates/engine-next/src/control/space.rs:35` · engine-architecture / V1 engineering standards — hot-path efficiency; billion-scale perf mandate

registration_mutations() is invoked from every capability write batch (e.g., kv/service.rs:532-533, and identically in json/event/vector/graph services). In the common steady-state case where the target space is already registered, it still performs read_required_space_index() (one point read + decode of the space index) and then validate_space_catalog_row() (a second point read + decode of the space catalog row), before returning an empty mutation set. The registered-space set is not cached in the in-memory ControlPlane, even though branch records are already held in memory. This adds two point reads, two payload decodes, and several Vec allocations to every write batch on the hottest path.

*Failure scenario:* A high-throughput KV/vector write loop against the default space performs, per batch, two extra storage reads and decodes solely to re-confirm the default space is registered. At billion-scale write volume this is pure per-batch overhead that scales with write count; it is not amortized or cached.

*Fix:* Cache the per-branch registered-space set (and reserved-space validity) in the in-memory control plane, invalidating on space create/delete, so steady-state writes skip storage reads; validate the catalog row lazily only on registration changes or during diagnostics/open.

**U16. 🟡 MEDIUM · product-gap — Populated product spaces above 10,000 rows cannot be deleted (no scalable/chunked drop path)**  
`crates/engine-next/src/api/space.rs:355` · control-plane-layout-contract.md — Branch Behavior > Delete; downstream CLI/SDK space management

SpaceService::delete() collects all data-row delete mutations up front (delete_mutations_for_space) and, if the total exceeds SPACE_DELETE_MUTATION_LIMIT (10_000), rejects with invalid_argument.engine.space_delete_too_large before deleting anything. force=true does not bypass the cap; force=false rejects a non-empty space. There is no chunked, background, or streaming drop-space path. A space that has accumulated more than ~10k visible rows therefore cannot be dropped in a single operation; the only workaround is to first delete all underlying data rows through the capability APIs and then drop the now-empty space.

*Failure scenario:* A tenant space accrues 50,000 KV/JSON/vector rows. `space delete tenant --force` returns space_delete_too_large and deletes nothing. The CLI/SDK user has no engine-provided way to drop the populated space; they must manually enumerate and delete 50k rows across five capabilities first.

*Fix:* Provide a scalable drop-space path (chunked multi-commit or background reclamation) so a populated space can be deleted regardless of row count, or document a supported bounded-batch drain-and-drop operation in the engine surface.

**U17. ⚪ LOW · correctness — Interrupted branch delete/fork reopens with a create-specific corruption code and message**  
`crates/engine-next/src/control/bootstrap.rs:367` · v1-error-and-diagnostics-contract — accurate typed diagnostics

begin_branch_operation writes the same pending marker for create, fork, and delete (it is called from all of them in branch/service.rs). On reopen, a leftover marker always yields corruption code data_loss.engine.branch_create_pending with message 'branch catalog contains an unfinished branch create operation' (bootstrap.rs:367-370), even when the interrupted operation was a delete or a fork. The behavior (fail closed) is correct, but the diagnostic mislabels the operation. The pending record decoded at bootstrap.rs:359-365 is discarded (`let _ = ...`) and could carry the operation kind.

*Failure scenario:* A branch delete is interrupted after its durable pending marker is written but before catalog activation. Reopen fails closed reporting 'unfinished branch create operation' for a delete, misleading an operator diagnosing the halted database.

*Fix:* Encode the operation kind (create/fork/delete) in the pending record and surface an accurate code/message (e.g., branch_operation_pending with the operation kind), rather than always reporting a create.

### `diagnostics`

**U18. 🟡 MEDIUM · conformance — `data_loss` code prefix is not a V1 error class (33 codes)**  
`crates/engine-next/src/diagnostics/registry.rs:137` · error-and-diagnostics-contract.md / v1-error-and-diagnostics-contract.md

CORRUPTION_CODES (registry.rs:135-172) registers 33 codes with the prefix `data_loss.engine.*` (e.g. `data_loss.engine.kv_key`, `data_loss.engine.kv_value`, `data_loss.engine.json_document`). `data_loss` is NOT one of the 15 V1 error classes; the V1 class enum (`ErrorClass`) has no `DataLoss` variant. `public_class_for_code`/`public_class_for_legacy` collapse `Some("corruption" | "data_loss") => ErrorClass::Corruption` (registry.rs:316, error.rs:478), so the rendered status carries `class = corruption` while the stable code string begins with `data_loss`. The cross-layer contract states engine-owned code families 'still use the V1 class' and 'Error codes must belong to exactly one class'; the engine contract itself prescribes `corruption.capability_value` for malformed capability bytes (line 291), not a `data_loss.*` code. The code prefix therefore disagrees with the class it maps to.

*Failure scenario:* An SDK or docs generator calls the exported `error_code_registry_entry("data_loss.engine.kv_value")`; it returns `class = corruption` but the code string starts with `data_loss`. A consumer that indexes codes by their leading class segment (a natural, documented format `<class>.<area>.<detail>`) sees `data_loss`, which is not in the 15-class vocabulary and cannot be mapped; the doc anchor `/errors/data_loss.engine.kv_value` sits under a class the token does not name. These are stable, frozen-at-V1 public codes.

*Fix:* Rename the `data_loss.engine.*` codes to `corruption.engine.*` (aligning with the contract's `corruption.capability_value`), or add `data_loss` as an explicit 16th V1 `ErrorClass` variant and update both contracts. Do not leave a code prefix that is not a class token.

**U19. 🟡 MEDIUM · correctness — Registry misclassifies not_found/already_exists JSON and vector codes as invalid-argument in message_template and suggested_fix**  
`crates/engine-next/src/diagnostics/registry.rs:385` · error-and-diagnostics-contract.md (registry is single source of truth for public error metadata)

`message_template_for_code` (registry.rs:372-425) and `suggested_fix_for_code` (registry.rs:427-496) dispatch on capability keyword substrings BEFORE considering class, and only added not_found/already_exists special cases for `branch` and (message-only) `vector_collection`. As a result four registered codes whose class is not-found or already-exists receive invalid-argument guidance: `not_found.engine.json_document`, `already_exists.engine.json_document`, `already_exists.engine.json_index`, and `already_exists.engine.vector_collection`. For `not_found.engine.json_document` the `.json_document` arm yields message 'The JSON document request is invalid.' and fix 'Use a valid JSON document id and a JSON value within the configured size and depth limits.'; `already_exists.engine.vector_collection` hits the `.vector_` arm ('The vector request is invalid.'). These entries are returned by the exported public API `error_code_registry_entry`/`error_code_registry_entries`.

*Failure scenario:* A user requests a JSON document that does not exist; a CLI/SDK error-catalog page or `strata errors explain not_found.engine.json_document` renders the registry entry and tells the user their request is malformed and to shrink their JSON value within size/depth limits — actively wrong remediation for a not-found failure. Correct guidance is 'the document was not found; create it or list documents.' Live error responses keep the engine's generic class default, so live and registry metadata also silently diverge for these codes.

*Fix:* Reorder the dispatch to key on the code's class prefix first (not_found/already_exists) and only fall to capability-keyword phrasing for invalid_argument/corruption codes; add explicit not_found/already_exists arms for json_document, json_index, and vector_collection.

**U20. 🟡 MEDIUM · product-gap — No engine-owned redaction primitive; `ErrorDetail`/status accept unredacted free text and the 'redacted' naming is unenforced**  
`crates/engine-next/src/diagnostics/error.rs:95` · error-and-diagnostics-contract.md 'Redaction' section

The contract makes redaction a MUST (provider keys, hub credentials, signed URLs, bearer tokens, access keys, full env values, secret config values, user data values) across 'display strings, structured details, logs, and IPC/CLI/SDK JSON responses.' The diagnostics vocabulary provides no redaction mechanism: `ErrorDetail::new` is documented 'Creates a redacted structured detail' / 'Returns the redacted detail value' (error.rs:95,110) but stores the value verbatim, and `EngineErrorStatus::new` (error.rs:130-154) accepts an arbitrary `message`/`details`/`hints` with no scrubbing. Redaction relies entirely on every call site never inserting a secret. The sole redaction test (`error_redaction.rs`) is a negative substring allowlist over today's exercised paths (`assert_no_secret_leak` checks for 'bearer ', 'api_key', 'x-amz-', etc.) and never injects a secret to prove it is scrubbed, so a leak on any unexercised path would pass.

*Failure scenario:* When provider/clone/StrataHub work lands (or if any capability builds a message with `format!("... {value}")` embedding user content or a signed URL), the string flows verbatim through `EngineErrorStatus` to the CLI/SDK/IPC JSON. Nothing redacts it and the substring-based test would not catch a token format it does not already enumerate. The 'redacted' doc comments imply an enforcement guarantee that does not exist.

*Fix:* Introduce a centralized redaction pass (deny-list + allow-list) applied when constructing `EngineErrorStatus`/`ErrorDetail`, or fix the doc comments to say redaction is a call-site responsibility and add a positive injection test that plants each secret category and asserts it is scrubbed from message, details, hints, and the full source chain.

**U21. 🟡 MEDIUM · conformance — Commit-path storage faults get a phase-blind `commit_outcome` via blanket `map_storage_error`**  
`crates/engine-next/src/persistence/adapter.rs:475` · error-and-diagnostics-contract.md 'Commit, Durability, And Storage Mapping' / 'Persistence Adapter To Engine Parent Error'

The commit path `self.runtime.commit(&batch).map_err(map_storage_error)` (adapter.rs:475) uses the same class-derived mapper as reads. `map_storage_error` assigns `commit_outcome` purely from the storage error class with no knowledge of commit phase: `StorageApiError::LowerLayer` -> `unavailable.engine.persistence` with `CommitOutcomeStatus::NotApplicable` (adapter.rs:980-994), and the generic table maps `Internal`/`NotFound`/`Unsupported` to `NotApplicable` as well (adapter.rs:998-1088). For a failed commit, `NotApplicable` (contract: 'The failed operation did not attempt a commit') is semantically wrong — the commit WAS attempted. The contract explicitly forbids this: 'Write-path storage errors must not use blanket conversion' and 'keep phase and commit outcome together so a future maintainer cannot map a write-path storage error with a blanket conversion.'

*Failure scenario:* `db.kv(branch, space).put(k, v)` while the storage lower layer is unavailable during commit returns `unavailable.engine.persistence` with `commit_outcome = not_applicable`. A CLI/SDK caller inspecting `commit_outcome` to decide whether the write may have landed sees 'no commit attempted' rather than the correct write-path outcome (`not_started`/`definitely_not_committed`, or `maybe_committed` if the storage window is ambiguous), defeating the purpose of the commit-outcome field on the write path.

*Fix:* Thread the operation phase into the mapper (a write-path variant of `map_storage_error` or a phase parameter) so commit-time failures resolve to `not_started`/`definitely_not_committed`/`maybe_committed` per the contract table, and keep blanket conversion for the read path only.

**U22. ⚪ LOW · conformance — Two public error-class enums exposed on the engine error (legacy `EngineErrorClass` + V1 `ErrorClass`)**  
`crates/engine-next/src/diagnostics/error.rs:368` · error-and-diagnostics-contract.md 'Target Error Shape' / 'Vocabulary Discipline'

The contract targets one product class. The engine exports both `ErrorClass` (V1, 15 variants) and `EngineErrorClass` (legacy, 9 variants) from `diagnostics/mod.rs`, and the primary accessor `EngineError::class()` returns the legacy `EngineErrorClass` (error.rs:368) while `public_class()` returns the V1 `ErrorClass` (error.rs:395). The legacy enum is coarser and lossy: `unsupported`, `resource_exhausted` collapse to `EngineErrorClass::Unavailable` and `history_unavailable` to `EngineErrorClass::NotFound`. Both are `#[non_exhaustive]` and public. This is acknowledged transitional in code ('until EngineErrorClass is retired as the public surface', registry.rs:643) but currently presents two class vocabularies to downstream consumers.

*Failure scenario:* An SDK branching on `error.class()` (the ergonomic accessor) gets the legacy vocabulary and cannot distinguish `unsupported` from `resource_exhausted` from `unavailable` (all `EngineErrorClass::Unavailable`); an SDK using `public_class()` gets a different, finer vocabulary. The two disagree, and executor adds yet a third (`ExecutorErrorClass`).

*Fix:* Make `class()` return the V1 `ErrorClass` and demote/hide `EngineErrorClass` to `pub(crate)` (or delete it) per the cutover plan, so there is a single public class surface.

### `event`

**U23. 🟡 MEDIUM · completeness — Event capability exposes no version read and no history read, both required by the capability matrix**  
`crates/engine-next/src/data/event/service.rs:158` · engine-architecture.md 'Required Data Capability Semantics' (Event: Version read = Yes, History = Yes); primitive-implementation-contract.md §7 Read Contract items 2 and 4

The public EventService surface (append, batch_append, get, get_at, exists, len, len_at, get_by_type, get_by_type_at, range, range_by_time, list_types, list_types_at, list, list_page, verify_chain) contains no version read (nothing analogous to KV's `get_at_version`/JSON's `get_at_version` using ReadSelector::AtVersion) and no history read (nothing analogous to KV/JSON `get_versions`). The architecture matrix requires both 'Yes' for Event, and Binding Decision §4 states an unsupported operation 'must be explicit and diagnosed. It must not silently fall back to current-state-only behavior.' Here there is no version-read entry point at all, and the as-of reads silently fall back to Latest + append-time filtering (see the temporal-read finding).

*Failure scenario:* A downstream consumer (CLI `getv`/history pathway, M6D) asks for an event log's state at a specific commit version, or the version history of a given sequence, the way it can for KV and JSON. There is no engine API to serve it, so the event capability cannot participate in the version/history product pathways the matrix marks required.

*Fix:* Add a commit-version read (ReadSelector::AtVersion) and a history/version read for events (append-only means at most one version per sequence, so history can still enumerate the log at a version or report the single record + trimmed/missing states) to match the KV/JSON reference shape.

**U24. 🟡 MEDIUM · correctness — EventRangePage.cursor has inconsistent consume-semantics between range() (inclusive last-returned, non-resumable) and list_page() (exclusive after_sequence)**  
`crates/engine-next/src/data/event/service.rs:682` · primitive-implementation-contract.md §7 Read Contract rule 5 (list/scan ordering deterministic enough for automation); §8 pagination determinism

page_from_events sets `cursor = events.last().sequence()` — the sequence of the LAST returned event (line 682). list_page consumes its cursor as an exclusive lower bound via `after_sequence.is_none_or(|after| event.sequence() > after)` (line 359), so list_page pagination is correct. But range() takes no cursor/after parameter — only start_seq/end_seq — and its start bound is INCLUSIVE. The same EventRangePage type and the same `.cursor()` accessor are returned from both methods, so a consumer that pages range() by feeding the returned cursor back as the next start_seq re-reads the boundary event. There is in fact no parameter on range() to resume from its own cursor at all.

*Failure scenario:* Events 0..9. `let p = range(EventSequence::new(0), None, Some(3), Forward, None)` returns [0,1,2], cursor=Some(2). To fetch the next page the caller reuses the cursor the same way list_page does — as the next start: `range(EventSequence::new(2), None, Some(3), Forward, None)` returns [2,3,4], duplicating event 2. The identical call shape on list_page (`list_page(None, Some(cursor), Some(3), None)`) correctly returns [3,4,5]. The shared cursor thus means 'exclusive next' for one method and 'inclusive last' for the other.

*Fix:* Either give range()/range_by_time() an explicit after-cursor input and make the cursor the exclusive continuation token (matching list_page), or document/type the cursor distinctly so callers cannot cross-feed it; at minimum make range's forward/reverse cursor point at the next start, not the last returned element.

**U25. ⚪ LOW · conformance — append() collapses precise validation error codes into a generic invalid_argument.engine.event_append, unlike batch_append**  
`crates/engine-next/src/data/event/service.rs:63` · primitive-implementation-contract.md §13 Diagnostics rule 1 (capability errors must map into stable product error codes); CLAUDE.md Hard Rule 27-29

append() delegates to batch_append, then if the single item failed it re-wraps the item's message under a fixed code `invalid_argument.engine.event_append` (line 64-67), discarding the precise code the batch item carries (`event.error_status().code()` is `invalid_argument.engine.event_type` or `invalid_argument.engine.event_payload`). The batch path preserves the precise code (the test at engine_event.rs:331-337 asserts `mixed.items()[1].error_status().code() == "invalid_argument.engine.event_type"`), so the same invalid input yields different, less-specific error codes depending on whether it went through append() vs batch_append(). No test covers append()'s error code, so this is unverified behavior.

*Failure scenario:* `append(EventType` is pre-validated so the realistic trigger is a payload that fails deep validation, e.g. a non-object or non-finite float assembled via a path that reaches batch validation: append() surfaces `invalid_argument.engine.event_append` while the exact same entry in batch_append() surfaces `invalid_argument.engine.event_payload`. A client keying off the stable code gets an inconsistent classification for identical input.

*Fix:* Propagate the item's EngineErrorStatus (code + class + message) unchanged from append() instead of substituting a generic event_append code.

### `graph`

**U26. 🟡 MEDIUM · conformance — Graph capability provides no History read form for nodes or edges**  
`crates/engine-next/src/data/graph/service.rs:293` · primitive-implementation-contract.md §7 (read forms, item 4 History) and §14 shared conformance test 5 (History read)

The primitive contract requires every capability to define a History read that 'distinguishes missing records, deleted records, tombstones, trimmed history, malformed historical values, and unsupported history,' and lists it in the shared conformance suite. Graph nodes and edges are mutable (upsert overwrites), but the graph service exposes only point-in-time reads (get_node_at_version / get_edge_at_version) — there is no enumerating version-history read. Peer mutable capabilities implement it (KV `get_versions` at service.rs:127, JSON `get_versions` at service.rs:216); graph is the outlier with no equivalent.

*Failure scenario:* A user upserts node `doc` three times, then wants to audit its change history (values, versions, tombstone/trimmed markers). There is no API to enumerate a node's or edge's versions, so the required History read form and its shared conformance test cannot be satisfied for graph.

*Fix:* Add `get_node_versions` and `get_edge_versions` returning history rows (value/version/timestamp/tombstone/trimmed) mirroring KvHistory/JsonHistory.

**U27. 🟡 MEDIUM · product-gap — Neighbor/list queries load all matching rows and resolve every neighbor node before paginating**  
`crates/engine-next/src/data/graph/service.rs:1219` · primitive-implementation-contract.md §7 (list/scan) and Graph direction §9 (traversal/neighbor query behavior)

`outgoing_neighbors`/`incoming_neighbors` call `scan_prefix(..., None)` (no storage-side limit) and, for every non-tombstone edge row, perform a point read to resolve the neighbor node via `visible_node_or_corruption` — before the caller's `limit` is applied. `neighbors_with_selector` then collects all hits, sorts in memory, and truncates. `list_nodes`/`list_graphs`/`bindings_for_entity` follow the same scan-everything-then-truncate shape. There is no pushdown of the pagination bound to storage. On the billion-scale target this makes supernode traversal and large-graph listing O(edges) I/O + memory per page.

*Failure scenario:* A node with 10M outgoing edges: `neighbors(node, Outgoing, limit=10)` scans and decodes all 10M edge rows AND issues ~10M point reads to resolve destination nodes, sorts 10M hits in memory, then returns 10. Each subsequent page repeats the full scan. Latency and memory are unbounded regardless of the requested page size.

*Fix:* Push a bounded range scan keyed by the same order used for the neighbor cursor (fetch limit+1 rows), and resolve only the surviving page's destination nodes.

**U28. ⚪ LOW · correctness — Commit put/delete counts include derived reverse-edge and binding-index rows**  
`crates/engine-next/src/data/graph/service.rs:1455` · primitive-implementation-contract.md — Binding Decision 7 (authored vs derived separation) and anti-pattern 7

`commit_batch` computes `user_put_count`/`user_delete_count` over the full mutation vector, which includes derived reverse-edge rows and binding-index rows, then attaches them to CommitOutcome via `with_counts`. A single logical edge upsert emits a forward + reverse row put, so the user-facing count is 2 for one edge; a node re-bind emits an extra derived put+delete for the binding index. This conflates authored and derived rows in the counts surfaced to callers.

*Failure scenario:* A caller does one `upsert_edge` and reads the returned CommitOutcome to report 'rows written'; it shows 2 (forward+reverse) rather than 1 authored edge. A node whose binding changes reports an extra derived put and delete. SDK/CLI surfacing these counts as user row activity is misleading.

*Fix:* Count only authored (metadata/node/edge) mutations for the user-facing counts, or expose authored vs derived counts separately.

### `invariants-crate-shape`

**U29. 🟡 MEDIUM · completeness — "No process-global semantic state" is guarded only inside data/vector, not crate-wide**  
`crates/engine-next/tests/dependency_guards.rs:575` · CLAUDE.md Hard Rule #9 ("No process-global semantic state. Per-database state only."); target-crate-shape-and-test-harness.md Crate-Level Policy

CLAUDE Hard Rule #9 forbids process-global semantic state anywhere in the engine, but the automated guard only covers src/data/vector. The rest of the crate has no static-scan enforcement. Current code is compliant (independent grep across all of src/ found zero `static`/`OnceLock`/`OnceCell`/`lazy_static`/`thread_local`), so this is an enforcement-coverage gap, not a present violation.

*Failure scenario:* A future change adds e.g. `static CATALOG: OnceLock<...>` in src/runtime/ or `static mut` in src/control/ (process-global semantic state, violating rule #9). All 26 guard tests still pass because no guard scans outside src/data/vector, so per-database-state isolation regresses undetected — exactly the class of bug (a shared global holding one database's state) that corrupts multi-database processes.

*Fix:* Widen the forbidden-token static scan to root at `src/` (the whole crate) with the same token set, rather than restricting it to the vector capability.

**U30. ⚪ LOW · conformance — `config` module is empty dead scaffolding while its responsibilities live in api/options.rs**  
`crates/engine-next/src/config/mod.rs:1` · target-crate-shape-and-test-harness.md Module Ownership -> `config` ("Owns user-facing config DTOs, runtime-resource profile inputs, defaults, capability enablement flags, and conversion into lower-layer storage/runtime budgets")

The spec assigns `config` an ownership charter, but the module is an empty placeholder that exports nothing and is used by nothing, while its documented responsibilities have been implemented in the `api` bucket instead. This is dead scaffolding that diverges from the documented crate shape.

*Failure scenario:* A maintainer following the documented module map looks in `config/` for open options / defaults / capability-enablement flags and finds an empty module, then has to discover the real config surface lives in api/options.rs — a navigability/shape drift, not a runtime bug.

*Fix:* Either relocate the config DTOs/defaults from api/options.rs into config/ per the ownership charter, or delete the empty module until it is populated.

**U31. ⚪ LOW · product-gap — executor-next imports inference-next directly with no dependency guard covering the boundary**  
`crates/executor-next/src/lib.rs:36` · CLAUDE.md Hard Rule #5 ("Executor and CLI consume intelligence; never import inference directly"); V1 Stack

Executor imports inference directly, which the dependency direction rule forbids (executor should consume intelligence, which wraps inference). This is transitional and defensible today because intelligence-next (M8) is not yet a workspace crate, and the import is feature-gated. Engine-next itself is clean (its Cargo.toml declares neither inference nor intelligence, so the compiler already forbids such imports there). The gap is that no guard exists to flag the executor->inference edge once intelligence-next lands. Reported at low severity and scoped: executor is a separate crate from the engine-next unit under audit, but the enforcing guard lives in engine-next/tests and the focus covers dependency direction.

*Failure scenario:* When intelligence-next is introduced (M8), executor is supposed to re-route through it and drop the direct inference dependency. Because no guard covers this edge (unlike the storage edge, which is guarded), executor can retain its direct `strata_inference_next` import indefinitely and the Hard-Rule-#5 violation persists silently past cutover.

*Fix:* Add a dependency guard mirroring executor_crate_does_not_import_storage_crates that forbids `strata_inference_next` / `strata-inference-next` in executor once intelligence-next exists, so the transitional exception cannot outlive its milestone.

### `json`

**U32. 🟡 MEDIUM · product-gap — JSON secondary index is write-only — no query/lookup surface consumes it**  
`crates/engine-next/src/data/json/service.rs:541` · primitive-implementation-contract.md §JSON(7), §10 Search/Text adapter, §12 Derived-State Hooks

create_index/drop_index/list_indexes exist and index entries are diffed and written on every document mutation (index_mutations_for_change, service.rs:869) plus a full-scan backfill on creation (service.rs:571-583). But there is NO method on JsonService (or anywhere in engine-next) that reads documents by an index. A repo-wide search for encode_json_index_entry_prefix / encode_json_index_entry_key / RowClass::JsonIndex outside the json module finds only json_index_entry_count_for_test (test/testkit-only, api/database.rs:290) and space-deletion cleanup (api/space.rs:501-504). The index feature therefore imposes write amplification and backfill cost with zero query benefit.

*Failure scenario:* A user creates a Tag index `by_name` and writes 100k documents. Every set/delete pays extra index-entry row writes and create_index backfills all 100k docs, yet no API can return the documents where name == 'alice' — the index can never serve or accelerate a read. Cost is paid, benefit is unreachable.

*Fix:* Either expose an index-backed query/lookup method on JsonService (feeding retrieval), or defer index maintenance until its consumer (M6E retrieval) lands so writers do not pay for an unusable index.

**U33. 🟡 MEDIUM · product-gap — count() and sample() materialize every document (full values) into memory**  
`crates/engine-next/src/data/json/service.rs:489` · primitive-implementation-contract.md §13 Diagnostics(11) resource exhaustion; §Read(7) bounded summary

count(prefix) calls persistence.scan_prefix(..., None) with no limit, pulls every matching JSON row (including full value bytes) into a Vec via into_iter(), then filters and counts (service.rs:491-503). sample(prefix, n) does the same full materialization and then strides over the in-memory Vec (service.rs:513-537). Neither needs the row values (count needs none; sample needs at most n). On a large space this is an O(dataset) memory spike, directly at odds with the repo's unified/billion-scale positioning (current branch v1-billion-scale-perf).

*Failure scenario:* A space holds 5,000,000 JSON documents. Calling count(None) loads all 5M full document values into a Vec purely to return an integer, risking OOM/latency; sample(None, 10) loads all 5M rows to return 10.

*Fix:* Add a value-free / count-only scan path in the persistence adapter (keys or count only), and for sample use a bounded reservoir/streaming approach instead of materializing the whole prefix.

**U34. 🟡 MEDIUM · completeness — No JSON branch adapter — document-level merge V1 invariant is unimplemented and unenforceable**  
`crates/engine-next/src/data/json/service.rs:34` · CLAUDE.md Hard Rule 20/21; primitive-implementation-contract.md §9 Branch Adapter, §JSON V1 branch baseline

The contract requires every capability to provide a branch adapter (diff, three-way compare, promote/merge, source-wins, strict-conflict, copy, restore). Rule 21 states 'JSON merge is document-level' and 'disjoint path edits still conflict if source and target changed the same document differently since the branch point.' None of this exists: JsonService has no diff/merge/promote/compare, and a workspace search for fn merge/fn promote/BranchAdapter/source_wins/three_way across engine-next returns nothing (only vector rerank 'merge'). BranchService supports fork/create/delete but not merge. The named V1 invariant cannot be exercised or enforced because the operation does not exist.

*Failure scenario:* A user forks `feature` from `default`, edits document `profile.name` on feature, then wants to promote/merge feature back into default. There is no engine surface to do so, and the document-level conflict rule (Rule 21) has no code path to enforce it.

*Fix:* Implement the JSON branch adapter under M6D with strict document-level conflict as the safe baseline, per the contract; this is cross-cutting (all capabilities lack it) and should land with the M6D branch/temporal epic.

**U35. ⚪ LOW · conformance — Trimmed-history timestamp/version reads are swallowed to None, losing the trimmed vs never-existed distinction**  
`crates/engine-next/src/persistence/adapter.rs:507` · primitive-implementation-contract.md §Read(7) Rule 4 ('History must distinguish ... trimmed history'), §13 Diagnostics(6)

JSON get_at / get_at_version / list_at delegate to the shared adapter, which on RetainedHistoryUnavailable / TimestampHistoryUnavailable returns Ok(None)/empty (adapter.rs:507 for read_row, :596 for scan_prefix, :640 for scan_range via is_outside_retained_history). As a result a JSON point-in-time read against a point that has been trimmed is indistinguishable from a document that never existed at that point, contrary to the contract's requirement that history distinguish trimmed history. (Shared adapter behavior; surfaced through the JSON read API.)

*Failure scenario:* History is trimmed past timestamp T. get_at(doc, root, T) returns Ok(None), identical to a document that never existed at T, so the caller cannot tell 'no longer retained' from 'absent'.

*Fix:* Distinguish outside-retained-history from absent at the read boundary (a typed 'history trimmed/unavailable' diagnostic) rather than collapsing both to None.

**U36. ⚪ LOW · correctness — Document validation recurses to full depth before enforcing the nesting-depth bound**  
`crates/engine-next/src/data/json/types.rs:148` · primitive-implementation-contract.md §Value Encoding rules; §Validation limits (MAX_NESTING_DEPTH)

JsonValue::validate first serializes the whole value with serde_json::to_vec (types.rs:149), then computes nesting_depth (types.rs:756) and max_array_size (types.rs:764) — all fully recursive with no depth guard — and only afterward compares depth against MAX_NESTING_DEPTH=100 (types.rs:163). A pathologically deep value therefore drives unbounded recursion (in to_vec and nesting_depth) before the limit can reject it. Wire input is capped by serde_json's ~128-deep parse limit, so this is reachable only via programmatic Value construction inside engine/SDK code, but the depth bound is enforced too late to protect against stack overflow.

*Failure scenario:* Trusted code builds a serde_json::Value ~100k levels deep and calls JsonValue::new; serde_json::to_vec / nesting_depth recurse to full depth and overflow the stack (process abort) before MAX_NESTING_DEPTH rejects it.

*Fix:* Enforce the depth bound with an iterative or depth-limited traversal before any full-tree serialization/recursion, aborting as soon as depth exceeds MAX_NESTING_DEPTH.

**U37. ⚪ LOW · product-gap — Path key validation rejects control bytes, making some legal JSON object keys unaddressable by path**  
`crates/engine-next/src/data/json/types.rs:244` · primitive-implementation-contract.md §JSON(2) optional path identity, §Read/Write path behavior

JsonValue::new validates only size/depth/array-width, not object-key contents, so a document containing a key with a newline or NUL byte (e.g. {"a\nb": 1}) is accepted and stored. But JsonPath::validate runs validate_component on every Key segment (types.rs:251-260), which rejects any byte == 0 or == '\n' (types.rs:747). Such a field can be written at the document root but can never be read, set, or deleted at path granularity because no JsonPath naming it can be constructed.

*Failure scenario:* create(id, {"a\nb": 1}) succeeds; get(id, path("a\nb")) is impossible — JsonPath rejects the key — so the field is permanently unreadable and undeletable at path level.

*Fix:* Either forbid such keys at document-value validation time for consistency, or relax path-key validation (control bytes are addressable only via the segment/bracket form) so any storable key is addressable.

### `kv`

**U38. 🟡 MEDIUM · product-gap — count/list/sample perform an unbounded full scan that materializes every row (with value bytes) in memory**  
`crates/engine-next/src/data/kv/service.rs:288` · primitive-implementation-contract.md §7 (List/Count) + branch v1-billion-scale-perf intent

count() (line 288-303), list(None) (line 175-190), and sample() (line 306-333) all call persistence.scan_prefix(..., limit=None) and collect the entire result into a Vec<PersistenceReadRow> before filtering/counting/sampling. PersistenceReadRow::from_storage clones the full value bytes for every row, even for count() which never needs the value. There is no streaming/keys-only/count-only path and no upper bound.

*Failure scenario:* On a KV space with 100M keys, AdminService::describe() -> primitive_summary() calls kv.count(None); the engine pulls all 100M rows (key + cloned value bytes) into one Vec purely to count them, causing an O(N) memory spike / potential OOM and multi-second latency. list(None) and sample(prefix, k) have the same unbounded-buffer behavior. This directly undercuts the billion-scale target.

*Fix:* Add a storage/persistence count-only (keys-only, no value materialization) path for count(); require or internally chunk an upper bound for list(None)/sample so they cannot buffer an entire space.

**U39. 🟡 MEDIUM · completeness — get_versions returns unbounded full history and cannot distinguish complete vs. trimmed history**  
`crates/engine-next/src/data/kv/service.rs:127` · primitive-implementation-contract.md §7 rule 4 ("History must distinguish ... trimmed history ...") and §13 rule 6

get_versions() loads the entire version chain via read_history(address, true) with no limit/pagination and returns Some(KvHistory) or None. Neither KvHistory nor KvHistoryRow (outcome.rs) carries any watermark/‘truncated’ flag, and the persistence read_history path (adapter.rs:517) discards any retention watermark — it only maps a wholesale RetainedHistoryUnavailable to an error, never a partial-history marker. So a history that storage has trimmed is returned as if it were the key's complete history.

*Failure scenario:* A key is written 10M times, then storage retention/compaction trims the oldest versions. get_versions() returns a partial chain with no trimmed indicator, so a downstream audit/time-travel consumer treats the oldest returned row as the key's creation — a silently wrong result. Separately, for a hot key with millions of live versions, get_versions buffers the entire chain in memory (no limit).

*Fix:* Thread a retention watermark from storage's history outcome into KvHistory (e.g. a `trimmed`/`oldest_retained_version` field) and add a bounded/paginated history read form.

**U40. 🟡 MEDIUM · conformance — KV provides no branch adapter — cannot participate in diff/promote/merge/copy/restore**  
`crates/engine-next/src/data/kv/service.rs:1` · primitive-implementation-contract.md §9 (Branch Adapter) & §14 shared-conformance item 9; CLAUDE.md Hard Rule #20 (branch merge strict-refusal in V1)

The contract requires every capability to provide a branch adapter defining diff, three-way comparison, promote/merge (strict-conflict baseline), copy-selected-record, and restore/revert, and requires KV to pass the shared 'branch copy/promote/restore participation' conformance. No such KV adapter exists, and the branch service (branch/service.rs) exposes only list/get/create/create_from_head/fork_current/fork_at_version/fork_at_timestamp/delete — there is no promote, merge, diff, copy, or restore anywhere in engine-next.

*Failure scenario:* A user forks 'feature' from 'default', edits key 'k' differently on each, then attempts to promote/merge 'feature' into 'default' (a stated V1 capability). No API exists to diff or promote branches, and KV defines no strict-conflict behavior for the same-key-changed-differently case the contract's KV section explicitly calls out, so branch workflows cannot be completed.

*Fix:* If deferred to a later milestone, record the deferral explicitly; otherwise implement a KV branch adapter (strict-conflict promote as the safe baseline) plus the branch-service promote/diff/restore surface.

**U41. ⚪ LOW · product-gap — KV exposes no TTL/expiry even though the storage commit path plumbs a per-put TTL**  
`crates/engine-next/src/persistence/adapter.rs:787` · primitive-implementation-contract.md §6 (value encoding: "TTL metadata where supported")

CommitMutation::Put carries a `ttl` field, but to_storage_mutation() hardcodes `ttl: None`, and neither KvValue nor put/put_batch accept an expiry. TTL is a near-universal KV expectation (session tokens, caches, idempotency keys) and the storage plumbing already exists, so the gap is a facade omission rather than a substrate limitation.

*Failure scenario:* A consumer needs a KV entry that expires after one hour; the KV facade offers no way to express expiry, so every put is permanent and the caller must build and enforce expiry externally.

*Fix:* Add an optional TTL to the KV write DTO and thread it through RowMutation::Put -> CommitMutation::Put, or explicitly document TTL as out-of-scope for V1.

**U42. ⚪ LOW · product-gap — No per-key conditional/compare-and-swap write for optimistic concurrency**  
`crates/engine-next/src/data/kv/service.rs:49` · primitive-implementation-contract.md §8 rule 10 ("Write conflicts must produce structured errors")

put/put_batch/delete unconditionally overwrite. The only conflict detection is branch-generation-level (CommitPlan expected_generation), not per-key. There is no put-if-absent or put-if-version primitive for downstream consumers that need idempotent/optimistic writes.

*Failure scenario:* An SDK/MCP caller wants an idempotency-key write that must fail if the key already exists (or if its version changed). The KV surface offers only blind overwrite plus a read-then-write pattern that carries a race window across the branch generation, and cannot return a structured per-key conflict.

*Fix:* Add a conditional-write form (put-if-absent / put-if-version) that maps a failed precondition to a structured conflict error; low priority given the embedded single-writer model.

### `persistence`

**U43. 🟡 MEDIUM · conformance — RowClass storage-space IDs diverge from the governing registry table and do not separate derived rows from source rows**  
`crates/engine-next/src/persistence/space.rs:32` · storage-space-id-registry.md — 'V1 Engine Assignments' normative table (JSON 0x21, Event 0x22, Vector 0x23, Graph 0x24; Search 0x40, Shadow-vector 0x41, Vector-index 0x42, Graph-index 0x43, Projection 0x44, Derived-state 0x45); 'Separate Source Rows From Derived Rows'; 'Use Few IDs'; 'How To Choose An ID'

The engine's compiled storage-space assignments contradict the authoritative registry contract table (per CLAUDE.md, architecture contracts win). Beyond the numeric mismatch, the scheme violates the contract's structural 'separate source from derived' rule: index/derived families (JsonIndex, EventIndex, GraphReverseEdge, GraphBindingIndex) get bespoke source-range IDs instead of the mandated 0x40-0x45 derived range or a shared source ID via key prefix. The contract states the source/derived split exists precisely so branch fork, clone/export, repair, and diagnostics can treat derived rows differently — with derived rows interleaved into the source range, those lifecycle operations cannot distinguish rebuildable derived state from authoritative data. An implementation plan (storage-row-family-naming-implementation-plan.md) documents the current bytes, so this is an unreconciled divergence rather than an accident, but the governing registry doc has not been updated to match.

*Failure scenario:* Clone/export is told to omit derived rows so they can be rebuilt on import (registry Snapshot/Clone rule 3). Because GraphReverseEdge (0x3c) and GraphBindingIndex (0x3e) live in the source range instead of 0x43, an export that filters by 'derived storage-space IDs (0x40-0x45)' will carry these rebuildable index rows as if they were authoritative source data, or a source-only clone will treat them as source — defeating the rebuild-on-import contract and risking stale indexes surviving a clone.

*Fix:* Reconcile space.rs with the registry table: move JSON/Event/Vector/Graph source rows onto 0x21/0x22/0x23/0x24 (using key-prefix discrimination for their transactional secondary rows), and relocate rebuildable index families (JsonIndex, EventIndex, GraphReverseEdge, GraphBindingIndex) into the 0x40-0x45 derived range — or, if the even-offset scheme is intentional, update storage-space-id-registry.md and its conformance tests to make the code the source of truth before freeze.

**U44. 🟡 MEDIUM · conformance — Adapter makes temporal product decisions via exact prose matching on storage error text and collapses before-history into absent**  
`crates/engine-next/src/persistence/adapter.rs:914` · persistence-adapter-contract.md — Temporal Context ('The adapter must not decide product explanations such as "nearest available time"'; 'should preserve ... Whether the failure is before-history, after-latest, pruned gap, corruption, or unsupported backend mode'); Read Path rule 3 ('History reads must distinguish absent, deleted, malformed historical value, and history-trimmed states')

The adapter decides 'an as_of after the latest retained time returns the latest row' — a nearest-available-time product decision the contract explicitly reserves for the temporal-context/product layer — and implements the decision by exact/substring matching on storage's human-readable reason prose (contrast CLAUDE.md invariant that behavior must not depend on display text). It also collapses 'before retained history' into Ok(None), which is indistinguishable from 'the row did not exist at that time', discarding the before/after/pruned/corruption distinction the contract says the adapter must preserve for the temporal layer.

*Failure scenario:* 1) storage-next changes the reason wording (e.g. capitalization or phrasing) — a purely cosmetic change with no compiler coupling — so `should_fall_back_to_latest` returns false; every `as_of(future_timestamp)` read that previously returned the latest row now surfaces a HistoryUnavailable error to the user. 2) A point read `as_of(t)` where t precedes the row's retained history returns Ok(None); the temporal layer renders 'not found' and cannot tell the user 'that time is before retained history', so a time-travel query silently reports absence instead of a trimmed-history boundary.

*Fix:* Have storage-next expose a structured temporal-boundary reason (before-history / after-latest / pruned-gap / corruption) instead of prose, and map that structured discriminant here; return a typed 'before/after/pruned' fact to the caller rather than Ok(None), and move the after-latest→latest coercion up into the temporal-context layer.

**U45. 🟡 MEDIUM · completeness — No shared frontier or retention pin for multi-row timestamp-bounded operations**  
`crates/engine-next/src/persistence/adapter.rs:496` · persistence-adapter-contract.md — Read Path ('Timestamp-bounded multi-row reads must be snapshot-consistent ... the adapter must resolve the requested timestamp once per branch into a retained version frontier before issuing row reads or scans. The same frontier must be used for every row ... The adapter must also hold a storage read view, retention pin, snapshot guard, or equivalent guarantee for the lifetime of any multi-row temporal operation'); V1 Minimum 12; Conformance test 16

The contract requires that any logical operation touching more than one row at a timestamp (relationship resolution, retrieval, compare, branch-from-time diff/copy) resolve the timestamp to a single retained frontier once and reuse it for every row, and hold a retention pin for the operation's lifetime. The adapter provides no such mechanism, so any higher-layer temporal operation composed of multiple adapter calls re-resolves the timestamp on each call and pins nothing between calls. This capability is unscheduled in the M5/M6 plans (grep for 'frontier'/'snapshot-consistent' returns 0), so it appears deferred, but it is a stated V1-minimum of the governing adapter contract.

*Failure scenario:* A graph traversal `as_of(t)` scans nodes (call 1) then issues point reads for edges (calls 2..n). A concurrent commit advances the branch timeline between call 1 and call 2; because each call independently resolves t and nothing is pinned, the edge reads can observe a different retained frontier than the node scan (or retention/compaction can reclaim a row between calls), yielding an internally inconsistent as_of snapshot.

*Fix:* Add a frontier-resolution + read-view API to the adapter (resolve timestamp→retained version once per branch, return a pinned read view/guard) and route multi-row temporal operations through it, or document the deferral with a removal gate if storage-next will provide an atomic timestamp selector.

**U46. 🟡 MEDIUM · completeness — Per-key compare-and-set is available in storage-next but not plumbed through the commit plan**  
`crates/engine-next/src/persistence/adapter.rs:469` · persistence-adapter-contract.md — Commit Plan definition ('Optional compare/CAS facts when a product operation requires them')

The commit plan can express only branch-level optimistic concurrency (expected_generation). Storage-next already supports per-key compare-and-set conditions (must-be-absent, must-be-exact-version), which the contract lists as a commit-plan capability, but the adapter never surfaces or forwards them. As a result conditional writes — put-if-absent, compare-and-set, optimistic single-row updates — cannot be expressed by any engine capability through the canonical write path, even though the substrate supports them.

*Failure scenario:* A KV capability needs put-if-absent (create-only) semantics or a JSON capability needs optimistic 'update only if version == N'. There is no way to express the per-key precondition through CommitPlan, so the capability must fall back to a non-atomic read-then-write, which races under concurrency and can silently clobber a concurrent writer's row.

*Fix:* Add optional per-row compare/CAS facts to RowMutation/CommitPlan and translate them into CommitCondition in to_storage_mutation/commit, surfacing storage CAS-failure as the existing Conflict mapping.

**U47. ⚪ LOW · product-gap — Row mutations cannot carry TTL / expiry metadata**  
`crates/engine-next/src/persistence/adapter.rs:787` · persistence-adapter-contract.md — Row Mutation definition ('Optional TTL or expiry metadata')

The storage put mutation accepts a ttl field but the engine row-mutation type has no way to set it, so TTL/expiry-based data (session rows, caches, ephemeral KV) cannot be expressed through the persistence boundary. The contract lists TTL/expiry as optional, so this is a product-capability gap for downstream KV/SDK consumers rather than a contract violation.

*Failure scenario:* A downstream KV/SDK user wants keys that auto-expire (session tokens, rate-limit counters). There is no engine surface to request expiry, so the capability must implement manual sweep-and-delete, which is more code and leaves stale rows visible until swept.

*Fix:* If TTL is a V1 product goal, add an optional expiry field to RowMutation::Put and forward it as the storage ttl; otherwise record the deferral explicitly.

### `product-pathway-conformance`

**U48. 🟡 MEDIUM · completeness — Graph relationship history is missing while KV/JSON/vector expose full history**  
`crates/engine-next/src/data/graph/service.rs:293` · product-pathway-conformance-plan.md pathway 26 (Required)

KvService has get_versions, JsonService has get_versions, VectorService has history — each returns the full version list with values/tombstones/timestamps via persistence.read_history. GraphService exposes only point-in-time reads (get_node_at_version/get_node_at, get_edge_at_version/get_edge_at) and has no method returning the full version history of a node or edge. Pathway 26 explicitly requires graph relationship history.

*Failure scenario:* A user inspecting how a node/edge changed over time (the required 'Inspect record history' pathway for graph relationships) has no engine call; they can only guess-and-probe individual versions via get_node_at_version, with no way to enumerate the retained history or see tombstones.

*Fix:* Add node/edge history methods on GraphService that call persistence.read_history and return versioned rows including tombstones, matching the KvHistory/JsonHistory/VectorHistory shape.

**U49. 🟡 MEDIUM · completeness — Bounded multi-hop BFS graph traversal is missing; only single-hop neighbors is implemented**  
`crates/engine-next/src/data/graph/service.rs:565` · product-pathway-conformance-plan.md pathway 13 (Required)

GraphService offers only single-hop `neighbors` (with direction/edge-type filter, cursor pagination, and time-travel). There is no bounded breadth-first traversal. Pathway 13 ('Traverse and query graph neighborhoods' — Required) explicitly requires 'Neighbor and bounded BFS queries respect direction, edge type, branch, space, time context, result bounds, and missing-node errors.'

*Failure scenario:* A consumer wants nodes within N hops of a start node (a bounded neighborhood). They must issue O(frontier) manual `neighbors` calls and reimplement BFS with depth/result bounds in the client, without engine-enforced result bounds — defeating the point of an engine-owned bounded traversal.

*Fix:* Add a bounded BFS traversal method (max depth + result cap, direction/edge-type filter, branch/space/time context, missing-node error) to the graph capability.

**U50. 🟡 MEDIUM · completeness — Event type index is written on every append but never read; type queries do a full log scan**  
`crates/engine-next/src/data/event/service.rs:390` · product-pathway-conformance-plan.md pathway 9 (Required, bounded pagination); billion-scale performance goals

batch_append writes an EventIndex row per event (type_index_address, service.rs:133-136), but no query path reads it: get_by_type_with_selector calls event_rows(...) which scans ALL events (encode_event_space_prefix, unbounded) and filters by type in memory. `grep` confirms encode_event_type_index_key/prefix is used only for the write and in key.rs unit tests — never for a read. So the index is pure write amplification, and type-filtered queries are O(total events), not O(matching events). range_by_time, list_page, len_at, list_types_at, and get_by_type_at similarly load the entire log into memory before filtering. On the billion-scale-perf line this defeats pathway 9's 'bounded pagination behavior' for type/time queries.

*Failure scenario:* A branch/space with 10M events and a rare type queried via get_by_type(limit=10) loads all 10M event rows into memory and filters, returning correct results but with unbounded memory/latency — and the per-append EventIndex write cost bought nothing.

*Fix:* Either read the EventIndex to answer get_by_type in O(matching) with a bounded sequence-window scan (like range's scan_sequence_window), or remove the unused index. Make the time/type query paths bounded rather than full-log materializations.

**U51. ⚪ LOW · correctness — Event single-record reads decode tombstones and raise a spurious corruption error instead of returning None**  
`crates/engine-next/src/data/event/service.rs:384` · product-pathway-conformance-plan.md pathway 9 / v1-error-and-diagnostics-contract (correct error classification); consistency with other capabilities

get_with_selector reads a row and passes it straight to event_from_row without checking is_tombstone(); event_from_row (service.rs:585) requires row.value() to be Some and otherwise returns EngineError::corruption('data_loss.engine.event_record'). A tombstoned event row (produced by force space-delete, which tombstones event rows) therefore yields a spurious data-loss corruption error from get/get_at/exists, instead of Ok(None). Every other capability's single read (KV get_versioned service.rs:79-82, JSON versioned_value_from_row, vector versioned_entry_from_row) checks is_tombstone first.

*Failure scenario:* Force-delete space `alt` (which tombstones its event rows and metadata), then call db.event(branch, 'alt').get(EventSequence(3)). Instead of Ok(None), the caller receives a Corruption/data_loss error, misreporting a deleted event as database corruption.

*Fix:* In get_with_selector, filter tombstones before decoding (`.filter(|row| !row.is_tombstone())` -> Ok(None)), matching KV/JSON/vector single-read semantics.

### `retrieval-derived-state`

**U52. 🟡 MEDIUM · conformance — Vector index derived-state manifest is stored in the SpaceControl (0x31) space, not the contract-mandated 0x45 derived-metadata family**  
`crates/engine-next/src/data/vector/service.rs:1627` · retrieval-and-derived-state-contract.md Binding Decision 5; V1 Families table; Manifest And Watermark Rows; Implementation Guidance 5

The contract binds derived-state manifests/health to storage-space 0x45 (Binding Decision 5: "Derived rows use 0x40..=0x45 ... Derived-state health and manifests use 0x45"; V1 Families table: Health/manifests = 0x45). The vector index manifest is instead addressed with `RowClass::SpaceControl` (which resolves to 0x31 in space.rs) plus a hand-rolled control-plane key `\x01vector:index-manifest:...` (key.rs:921 `vector_index_manifest_key`). No RowClass maps to any 0x40..=0x45 id (space.rs:26-45), and space.rs:88 asserts those ids stay unused. So the one derived-state family that exists does not live in its designated storage space, and there is no shared derived-state manifest/health abstraction as Implementation Guidance item 5 requires ("Derived-state services should use shared manifest/watermark patterns instead of custom health structs per subsystem").

*Failure scenario:* A clone/export or diagnostic routine that enumerates derived-state rows by the contract's 0x40..=0x45 family ids (e.g. to decide what is safe to omit from an artifact) will not find the vector index manifest, because it is filed under the branch/space control-plane namespace instead of 0x45.

*Fix:* When M6E lands, move the vector index manifest to the 0x45 health/manifest family (and index artifacts to 0x42) behind a shared derived-state manifest/watermark type, and drop the ad-hoc SpaceControl key.

**U53. 🟡 MEDIUM · conformance — Vector index manifest omits required derived-state manifest facts (model spec hash, build status, validation/failure, clone-omit flag)**  
`crates/engine-next/src/data/vector/manifest.rs:376` · retrieval-and-derived-state-contract.md Manifest And Watermark Rows (minimum facts 7,9,11,12,13,14); Vector Stage req 7; Autoembedding req 5

The contract's "Manifest And Watermark Rows" section lists minimum facts every derived-state manifest must carry, including: source capability registry version, model spec hash (where the family depends on model output), build status, last successful validation, last failure code + redacted message, and whether rows/files are safe to omit from clone/export. `StoredManifestBody`/`StoredArtifactRef` (manifest.rs:376-404) carry only branch/space/collection identity, per-ref covered_commit_version, dimension, metric, vector_count, derived_bytes, checksum, and fork_version_cap. There is no build status, no health/validation record, no last-failure field, no clone-omit flag, and no model spec hash. The missing model-spec-hash is the specific field the future embedding-model-mismatch check depends on (Vector Stage req 7 compares "the recipe-selected embedding model spec hash ... against the shadow-vector manifest"), so the manifest schema as-is cannot support `failed_precondition.embedding_model_mismatch` when shadow vectors arrive. Freshness is instead recomputed indirectly per query (manifest branch/collection/config match -> Stale, else artifact byte/checksum/count match at load -> Stale), which works for the vector index today but is not the durable manifest/health record the contract requires.

*Failure scenario:* Once shadow vectors exist, a query whose embedding model differs from the model that produced the stored vectors cannot be detected at planning time, because the manifest records no model spec hash to compare against; the mismatch would silently rank against embeddings from the wrong model instead of returning `failed_precondition.embedding_model_mismatch`.

*Fix:* Extend the derived-state manifest schema (bumping MANIFEST_POLICY_VERSION) to record source capability-registry version, build/validation/failure status, clone-omit disposition, and—for model-dependent families—the embedding model spec hash, before the shadow-vector/autoembedding slice consumes it.

**U54. ⚪ LOW · correctness — as_of-after-latest read fallback pivots on an exact storage error prose string, risking silent breakage**  
`crates/engine-next/src/persistence/adapter.rs:919` · retrieval-and-derived-state-contract.md Binding Decision 7 (historical retrieval must be honest); Time Basis; Hard Rules 29-30

Temporal reads (`get_at`, `query_at`, timestamp snapshots) rely on `should_fall_back_to_latest` to correctly answer an `as_of` timestamp that is after the latest retained commit by returning the latest state (an honest interpretation of "state as of a future instant"). But the trigger matches the storage error's human-readable `reason` field by exact string equality: `*reason == "timestamp is after latest retained timeline history"`. This couples a correctness-bearing engine decision to another layer's prose, which the error contract explicitly discourages (Hard Rules 29/30: assert on class/code not display text; storage errors carry no product wording). If storage-next ever reworks that message, the branch silently stops matching and a legitimate `as_of`-after-latest read errors out instead of returning latest — a behavior change with no compile-time or test signal tying the two strings together. `is_outside_retained_history` (adapter.rs:902-912) has the same brittleness (`reason.contains("outside retained")`).

*Failure scenario:* storage-next changes the `TimestampHistoryUnavailable` reason string (e.g. capitalization or wording). `should_fall_back_to_latest` now returns false for an `as_of` timestamp just past the latest commit, so a read that should return the current state instead propagates a RetainedHistory/Timestamp error to the caller.

*Fix:* Have storage-next expose a typed discriminant (e.g. an enum variant/kind meaning "after latest" vs "before retained") and match on that instead of the prose reason; add a cross-layer test asserting the exact condition triggers latest-fallback.

### `temporal-timeline`

**U55. 🟡 MEDIUM · correctness — Temporal read correctness is coupled to unversioned storage error prose strings**  
`crates/engine-next/src/persistence/adapter.rs:919` · temporal-context-and-timeline-resolver-contract.md Storage Responsibilities #3-#5 (typed facts); CLAUDE.md Hard Rules #29/#30

`should_fall_back_to_latest` compares the storage error reason to the EXACT string "timestamp is after latest retained timeline history", and `is_outside_retained_history` does substring matching on 'outside retained'/'before retained'. These `reason` fields are `&'static str` message text in storage-next (crates/storage-next/src/api/runtime/data.rs:328-337), not a stable typed contract. A wording change on the storage side silently flips engine behavior: an after-latest read would stop clamping and instead propagate an error, and a before-history read would stop returning `None` and instead surface an error — a behavior change with no compile-time or test signal. This contradicts the project rule that behavior must key off error class/code, not display text (CLAUDE.md Hard Rules #29/#30), and the engine-test-plan itself (§5.3, §9) flags this exact string-coupling as a fragility risk. Because storage and engine are separate crates with an intended typed boundary, the coupling is a latent correctness hazard.

*Failure scenario:* A storage-next refactor renames the reason to 'timestamp exceeds latest retained commit'. `should_fall_back_to_latest` now returns false, so `get_at(key, future_ts)` starts returning a history_unavailable error instead of latest; and if the before-history wording changes, `get_at(key, old_ts)` starts erroring instead of returning None. No engine test fails at the unit boundary because the coupling is on prose, so the regression ships.

*Fix:* Have storage-next expose the retained-history miss kind as a typed discriminant (before-history / after-latest / pruned-gap / not-retained) on the error enum, and match on that variant in the engine instead of on `reason` text.

**U56. 🟡 MEDIUM · conformance — Event capability conflates event-domain time with commit-timeline time for as_of reads**  
`crates/engine-next/src/data/event/service.rs:169` · temporal-context-and-timeline-resolver-contract.md Binding Decision 11, Event capability requirement, Conformance #12

All event temporal reads read at `ReadSelector::Latest` and then filter on the engine-generated event timestamp, never resolving a commit frontier: `get_at` (169-170), `len_at` (185-192), `get_by_type_at` (213-220), `range_by_time` (293-299), `list_types_at` (318-329), and `list`/`list_page` with `as_of` (336-360). Event timestamps come from `SystemTime::now()` monotonicized per-log (service.rs:701-712, `next_event_timestamp` -> `unix_timestamp`), which is a different clock from storage's commit timeline. The governing contract Binding Decision 11 requires 'Temporal context uses commit timeline time for visibility. Event-domain time filters are event capability semantics layered on top of the resolved commit frontier ... The two timestamps must not be conflated', the Event capability requirement ('Event temporal reads must first select visible commits, then apply event-domain filters'), and Conformance test #12 ('Event-domain time filters do not replace commit-time visibility'). Here the event-domain timestamp REPLACES commit-time visibility.

*Failure scenario:* Event A committed with commit ts=110 but engine event-timestamp=90 (wall-clock skew or per-log monotonic bump), Event B commit ts=100 / event ts=100. `events.list(as_of = 95)` returns A (event ts 90 <= 95) even though at commit-frontier time 95 no commit with ts<=95 exists, so a true commit-frontier read would return nothing/before-history. The visibility set diverges from the branch commit timeline the contract mandates.

*Fix:* Resolve the branch commit frontier at-or-before the as_of timestamp (via the same timeline resolver used by KV), read events visible at that frontier with an AtTimestamp/AtVersion selector, then apply event-domain filters (type, sequence, event timestamp) on top.

**U57. 🟡 MEDIUM · completeness — No retention-safe read view / pinned frontier for multi-row temporal operations**  
`crates/engine-next/src/data/kv/service.rs:463` · temporal-context-and-timeline-resolver-contract.md Binding Decisions 3 & 4, Conformance #3 & #9, V1 Minimum #4 & #11

Multi-row operations issue independent per-page storage calls with a fresh selector each time and no shared, pinned read view. `scan_range_limited` (463-498) and `scan_keys_after_cursor` (405-461) loop calling `persistence.scan_range(.. ReadSelector::Latest ..)` once per page, re-deriving `start` from the previous page's last key; there is no `open_read_view`/retention-pin/snapshot handle held across the operation (the persistence adapter exposes none — every `read`/`scan_*` is a standalone `StorageRuntime` call, and storage opens a fresh `read_view_for_branch` internally per call). The governing contract requires Binding Decision 3 ('resolve current state once per branch at operation start and use that fixed frontier'), Binding Decision 4 ('Resolving a temporal selector must also establish a read view, retention pin, snapshot guard, or equivalent'), Conformance #3 and #9, and V1 Minimum #4/#11. None of that mechanism exists at the engine boundary. This is latent in cache mode (no retention trimming, single &mut handle) but is a real torn-read/moving-frontier risk in durable mode where background flush/compaction/retention can run between pages.

*Failure scenario:* In durable mode a large `list_page`/`scan` spans several storage calls; a concurrent commit or a retention/compaction pass runs between page 1 and page 2. Page 2 resolves a newer 'Latest' frontier (or loses rows to reclamation), so the returned list reflects two different points in time — a torn result the contract's one-frontier / retention-safe-view rule is meant to prevent.

*Fix:* Resolve a single frontier (commit version) once at operation start and pass an explicit AtVersion selector to every page, and/or hold a storage read view / retention pin for the duration of multi-row operations so required rows cannot be reclaimed mid-scan.

**U58. 🟡 MEDIUM · completeness — Structured temporal outputs cannot carry selected frontier, resolution kind, or timeline/record-history bounds**  
`crates/engine-next/src/data/kv/outcome.rs:11` · temporal-context-and-timeline-resolver-contract.md Output Metadata, History Reads, V1 Minimum #7

Temporal read outputs expose only the observed record version+timestamp: `KvVersionedValue` (outcome.rs:11-43) and `KvScanRow` carry version/timestamp; `KvHistory`/`KvHistoryRow` (130-195) carry per-row version/timestamp/tombstone but no completeness or bounds. There is no field anywhere for selected frontier, selected timestamp, resolution kind (exact vs rounded-down), `RetainedTimelineBounds`, or `RetainedRecordHistoryBounds`. The governing contract Output Metadata section states machine-readable output 'must be able to carry selected frontier, selected timestamp, observed version/timestamp, resolution facts, timeline bounds, and record-history bounds for temporal reads ... required for Strata AI, debugging, history explanations, and reproducible retrieval', History Reads requires history output to include record-history/timeline bounds 'when the history may be incomplete', and V1 Minimum #7 requires these. The absence also removes the surface on which before-history/after-latest/trimmed diagnostics would report bounds, compounding the two diagnostic findings above.

*Failure scenario:* Strata AI asks for a history explanation of a key whose early versions were trimmed. `kv.get_versions(key)` returns rows with no completeness flag or record-history bounds, so the consumer cannot tell the history is partial or state the retained window — the contract's stated required use case cannot be satisfied.

*Fix:* Add a shared temporal-metadata carrier (selected frontier/timestamp, resolution kind, timeline bounds, record-history-completeness/bounds) to versioned reads, scans, and history outputs across capabilities, populated from a real timeline-resolution step.

### `vector-index-artifact`

**U59. 🟡 MEDIUM · completeness — Vector index is never built in production — all seal/build paths are dead code outside testkit**  
`crates/engine-next/src/data/vector/service.rs:2013` · engine-vector-indexing-implementation-plan.md — Implementation Order step 7 (unmarked) and Definition of Done item 3; vector-indexing-design.md §2.5/§3 compaction hook

Every index-build entry point (seal_index_artifacts_from_snapshot, seal_whole_collection_flat_artifact, seal_whole_collection_hnsw_artifact, seal_*_for_source, store_flat_artifact_ref, store_hnsw_artifact_ref) is marked #[allow(dead_code)] and the only caller of seal_index_artifacts_from_snapshot is seal_index_artifacts_for_test (service.rs:1059), which is #[cfg(any(test, feature = "testkit"))]. There is no compaction/flush derived-state hook (design §2.5/§3) and no threshold-driven auto-seal (impl-plan step 7 'Active Delta and Sealing Policy', which is the only implementation-order step NOT marked 'implemented as the current scaffold'). Consequently, in a real (non-testkit) build no VectorIndexManifest ref is ever written, the manifest is always Missing, and every query resolves to a full exact scan. The entire flat/HNSW artifact + manifest machinery (index.rs, manifest.rs, artifact.rs) is inert for production workloads.

*Failure scenario:* Open a durable database (no testkit), create a 1,000,000-vector collection, and issue a query. resolve_kind selects Hnsw/Flat by threshold, but index_manifest_resolution returns Missing (nothing ever sealed), so load_index_artifact_sources_for_query reports 'missing' and the planner falls to ExactVectorSource, scanning all 1M rows every query. The HNSW/flat acceleration the design/plan promises (Definition of Done item 3: 'flat indexing is available for qualifying collections or sources') is unreachable.

*Fix:* Wire step 7: trigger seal_index_artifacts_from_snapshot from the engine's write/maintenance path (best-effort, per decision 12/step 7) when the active-delta crosses active_delta_seal_threshold, or subscribe to a storage compaction hook (Level 3). Until wired, document explicitly that production always uses exact scan so the threshold diagnostics are not mistaken for an active index.

**U60. 🟡 MEDIUM · correctness — Fork-cap blind spot: inherited artifact ref keeps uncapped covered_commit_version, so a fork-point-visible key can be surfaced by neither the artifact nor the bounded active delta**  
`crates/engine-next/src/data/vector/manifest.rs:305` · engine-vector-indexing-implementation-plan.md — Current Correctness Contract items 1 and 7 (committed rows are source of truth; flat results must match exact byte-for-byte) and Branch/Timestamp Semantics item 5-6

materialize_for_child_fork -> with_fork_version_cap clamps fork_version_cap (min of existing and fork version) but preserves covered_commit_version unchanged. A sealed flat/HNSW artifact stores only the newest version per key (FlatVectorArtifact::new/from_visible_entries dedups via the BTreeMap in visible_entries and sorts by key), so if key K was written at v30 then overwritten at v60 and sealed at v60, the artifact contains only K@v60 (covered=60). If a child forks at v50 and (because no manifest existed at v50) inherits this ref via the fallback-to-Latest path (branch/service.rs:408-414), the child ref has covered=60, fork_version_cap=50. On a Latest query, the bounded fast path uses active_delta_watermark = max(covered) = 60 (service.rs:60-65), so the active-delta snapshot reads only child rows with version > 60 and skips K@v30 (30 <= 60). Meanwhile artifact_row_visible hides K@v60 (60 > cap 50), and the artifact has no K@v30 row. The child therefore sees NO value for K even though at fork point v50 K was legitimately visible as K@v30. The underfill fallback does not rescue this when >= k other keys fill the result: the query returns k results with wrong contents (true-best K missing).

*Failure scenario:* Parent: upsert K@v30 (embedding E30), overwrite K@v60 (E60), seal at v60 (artifact holds only K→E60, covered=60). Create child branch forked at v50 where the collection's first manifest was written at v60 (so the at-version(50) read is empty and materialize_vector_index_manifests_for_fork falls back to Latest, capping the covered=60 ref at 50). Child query for a vector nearest E30 at k=10 over a collection with >=10 other keys: bounded watermark=60 -> active delta = rows>60 (empty) -> K@v30 never read; HNSW/flat with fork_cap_excludes_rows=true hides K@v60 -> K omitted from results even though it should rank #1. Result is silently wrong (no error, full k).

*Fix:* When capping an inherited ref, clamp the effective read watermark to min(covered_commit_version, fork_version_cap) so the bounded active-delta read still covers keys whose fork-visible version is <= cap but whose sealed version was collapsed above the cap; or, on fork, if no manifest exists at the fork selector, inherit an empty manifest (rely on exact committed-row search) instead of capping a later Latest manifest that cannot faithfully represent pre-seal per-key state.

**U61. ⚪ LOW · correctness — Stored active_delta_count uses min(covered) while the read watermark uses max(covered), inflating the diagnostic right after a multi-source seal**  
`crates/engine-next/src/data/vector/service.rs:2738` · engine-vector-indexing-implementation-plan.md — Diagnostics (active_delta_count must reflect unsealed rows)

active_delta_count_for_refs computes the stored manifest active_delta_count against the MINIMUM covered_commit_version across refs (.min()), but the actual read boundary active_delta_watermark uses the MAXIMUM (.max(), service.rs:64, with an in-code comment explaining why min is wrong there). For a multi-source seal where sources have different covered versions (e.g. 50 and 100), the manifest records active_delta_count = count of snapshot rows with version > 50, which includes rows in (50,100] that are actually sealed. Immediately after a whole-snapshot seal, active_delta_count should be ~0 but is reported as nonzero. This value flows only to VectorIndexManifestLookup::Loaded.active_delta_count and diagnostics (manifest_active_delta_count), so it is a diagnostic/observability defect, not a result-correctness defect.

*Failure scenario:* Seal a collection whose immutable sources have covered versions 50 (keys a-m) and 100 (keys n-z). active_delta_count_for_refs uses min=50 and counts all snapshot rows with version > 50 (the n-z rows sealed up to 100) as 'active delta'. The freshly-sealed manifest reports a large active_delta_count although nothing is unsealed; a tuning/telemetry consumer reading manifest_active_delta_count would over-estimate unsealed work and could mis-trigger a re-seal.

*Fix:* Use max(covered_commit_version) consistently (matching active_delta_watermark) when computing the stored active_delta_count so the diagnostic counts only genuinely-unsealed rows.

**U62. ⚪ LOW · product-gap — Filtered queries against a sealed HNSW artifact brute-force the entire artifact, losing HNSW acceleration**  
`crates/engine-next/src/data/vector/index.rs:1160` · vector-indexing-design.md §6 (selectivity-aware filtered search); engine-vector-indexing-implementation-plan.md — Metadata Filters (bitmap deferred)

HnswArtifactVectorSource::search routes any query with filter.is_some() (also non-Latest selector or fork-cap exclusion) to score_hnsw_artifact_exact, which iterates EVERY row of the artifact (index.rs:1532-1567). So a metadata-filtered vector query over a large sealed HNSW segment is O(N), not O(log N) graph traversal — the exact case the design's §6 selectivity-aware filtered search (roaring-bitmap pre-filter + high/low-selectivity strategy) is meant to accelerate. The implementation plan explicitly defers metadata bitmap indexing ('Do not add a metadata bitmap index in this slice') and mandates exact filtered correctness with overfetch/fallback, so this is a deferred product capability rather than a plan violation, but it is a real downstream performance gap for filtered ANN at scale (CLI/SDK filtered search).

*Failure scenario:* A 5M-vector sealed HNSW collection receives query(filter=kind=doc, k=10). Because filter.is_some(), the graph is bypassed and all 5M rows are scored and sorted, giving exact results but full-scan latency — no better than exact search, defeating the point of maintaining the HNSW graph for filtered workloads.

*Fix:* Implement the design §6 strategy in a later slice: for low-selectivity filters run the graph and post-filter with higher overfetch; for high-selectivity filters brute-force only the pre-filtered subset. Until then, note in diagnostics that filtered HNSW queries fall to full-artifact scan.

### `vector-service`

**U63. 🟡 MEDIUM · completeness — delete_collection leaks the vector index manifest row and sealed artifact payloads**  
`crates/engine-next/src/data/vector/service.rs:178` · retrieval-and-derived-state-contract.md §Branch Behavior ("Branch delete should drop branch-local derived rows, sidecars, and health rows"); primitive-implementation-contract.md §12 ("How branch copy/delete/promote affects it")

`delete_collection` deletes the collection config row and every currently-visible vector row, but never touches `index_manifest_address(...)` (the `SpaceControl` manifest row) or the `VectorArtifactStore` payloads. After a delete-then-recreate with the same name, the stale manifest survives; it is defused only because `matches_branch_collection`/`artifact_refs_match_config` detect a generation/dimension mismatch and downgrade the lookup to `Stale` (service.rs:1729-1746). So there is no wrong-result bug, but derived manifest rows and on-disk flat/HNSW artifact files accumulate unbounded across create/delete cycles, and health/manifest state for a defunct collection is never cleaned.

*Failure scenario:* Repeatedly create a collection `docs`, seal an index (once sealing is wired), and `delete_collection("docs")` in a loop. Each iteration leaves a `0x45`-class manifest row plus flat/HNSW artifact files on disk that are never reclaimed; artifact storage grows without bound.

*Fix:* In `delete_collection`, also delete the index manifest row and remove referenced artifact payloads (reuse the resolution + artifact-id enumeration already present in the test helpers), and drop derived-state health rows for the collection generation.

**U64. 🟡 MEDIUM · product-gap — list_collections / admin describe compute exact per-collection counts by full row scan**  
`crates/engine-next/src/data/vector/service.rs:199` · primitive-implementation-contract.md §7 Read Contract ("Count or bounded summary where product APIs expose it"); Runtime resource policy (retrieval contract) — scalable summaries

`list_collections` maps every collection row through `collection_info_from_row`, which calls `count_with_record` — a full `scan_prefix` over all vector rows of that collection filtered for non-tombstones (service.rs:2421-2432). Listing N collections with M vectors each is O(N·M): naming the collections requires reading every vector in the database. The engine admin `describe` path calls `list_collections()` directly (api/admin.rs:385-397), so a metadata/stats request full-scans the entire vector keyspace. `count()` itself is likewise O(collection).

*Failure scenario:* A DB with 50 vector collections of 20M rows each. `db.admin().describe()` (or any `list_collections()`) scans 1B rows just to return 50 collection names, dimensions, and counts.

*Fix:* Maintain a per-collection live-count in control-plane metadata updated at commit time, or add a `list_collections` variant that omits counts / returns an approximate/cached count, so listing is O(collections) not O(total vectors).

**U65. 🟡 MEDIUM · product-gap — Bulk deletes full-scan, decode every embedding, and emit one unbounded commit**  
`crates/engine-next/src/data/vector/service.rs:1399` · primitive-implementation-contract.md §8 Write Contract (batch atomicity/partial-failure must be explicit); Runtime resource policy — low-resource profiles must not weaken correctness but should bound work

`delete_all`, `delete_by_filter`, and `delete_collection` call `visible_vector_entries` (full scan that `decode_vector_record`s the embedding + metadata of every row via `entry_from_row`) and then build a single `RowMutation` vector of every match committed in one `commit_batch`. `delete_all` (predicate `|_| true`) decodes all embeddings it never inspects, and there is no chunking: a billion-row `delete_all` materializes a billion mutations and reads a billion embeddings in memory before a single giant commit.

*Failure scenario:* `delete_all(&big_collection)` on a 100M-row collection allocates 100M decoded `VectorEntry`s (each holding a Vec<f32> embedding) plus 100M `RowMutation`s and attempts one commit — OOM / oversized WAL record long before completion.

*Fix:* Stream deletes in bounded chunks (scan keys only, not decoded embeddings), and for `delete_all`/`delete_by_filter` decode only metadata when the predicate needs it; commit in batches with explicit progress/atomicity semantics.

**U66. 🟡 MEDIUM · product-gap — Metadata filtering supports only top-level Eq on scalars**  
`crates/engine-next/src/data/vector/types.rs:398` · primitive-implementation-contract.md §Vector item 6 ("Metadata filtering"); retrieval-and-derived-state-contract.md Hard Filters ("user-provided field or tag filters supported by a capability adapter")

`VectorFilterOp` has a single variant `Eq`, and conditions match only a top-level field against a scalar (`VectorFilterCondition::matches`, types.rs:467). There is no range (`lt`/`gte`), set membership (`in`), inequality (`ne`), existence, prefix, OR-composition (filters are AND-only), or nested/array matching (nested fields are rejected at validation, types.rs:688). For a world-class engine feeding CLI/SDK/MCP retrieval, equality-only metadata filtering is a thin surface that forces callers to post-filter.

*Failure scenario:* A caller wants "vectors where rank >= 3 and kind in (doc,note)". The API cannot express it; they must fetch a large k and filter client-side, defeating server-side pruning and the underfill fallback.

*Fix:* Extend `VectorFilterOp` with range/in/ne/exists operators (and optionally OR groups), routing them through the same adapter so both the exact path and artifact path apply them; keep top-level scalar scope if nested paths remain out of V1 scope.

**U67. 🟡 MEDIUM · product-gap — Source vector records store the embedding as JSON text**  
`crates/engine-next/src/data/vector/record.rs:135` · primitive-implementation-contract.md §6 Value Encoding (deterministic, efficient authored value bytes); Runtime resource policy

`encode_vector_record` serializes `StoredVectorRecord` (including `embedding: Vec<f32>`) with `serde_json::to_vec`. A 1536-dim f32 embedding needs 6 KiB packed but serializes to ~15-25 KiB of JSON text, a 3-4x on-disk/WAL bloat, and every read must run a serde_json float parse. Combined with finding #1 (production always full-scans source rows), this means each query JSON-parses every embedding in the collection. At billion scale this dominates both storage footprint and query CPU.

*Failure scenario:* 1B rows × 1536-dim: JSON encoding stores multiple TB more than a packed layout, and each `query` (exact scan) parses ~1B JSON float arrays.

*Fix:* Encode the embedding as a length-prefixed little-endian f32 block (keep JSON only for the small metadata/identity envelope) behind the versioned record format; this is a format decision to make before the M3 freeze locks it.

**U68. ⚪ LOW · correctness — active_delta watermark/count use inconsistent max vs min aggregation**  
`crates/engine-next/src/data/vector/service.rs:60` · retrieval-and-derived-state-contract.md §Provenance And Stats (derived-family facts must be accurate); §Watermark semantics

`VectorIndexManifestResolution::active_delta_watermark` aggregates covered commit versions with `.max()` (correct for the bounded read: only rows past the newest sealed source are active). But `active_delta_count_for_refs`, which computes the `active_delta_count` persisted into the manifest and surfaced via `VectorIndexDiagnostics::manifest_active_delta_count`, aggregates with `.min()`. For a multi-source seal the stored/reported active-delta count over-counts (it includes rows already sealed by the higher-watermark source). This is a diagnostics/provenance inaccuracy only — the per-key `covered_versions_by_key` dedup governs actual query correctness — and it currently manifests only through testkit seals. Flagged in engine-test-plan.md:454-456.

*Failure scenario:* Seal two sources covering v50 and v100, then leave 3 rows past v100. The manifest records `active_delta_count` counting all rows past v50 (e.g. dozens), while the bounded read (max=v100) processes only the 3 real active-delta rows; diagnostics report an inflated active-delta size.

*Fix:* Compute `active_delta_count_for_refs` against the same `.max()` watermark used by `active_delta_watermark`, and add the multi-source seal test the plan calls for.

**U69. ⚪ LOW · product-gap — No public version-pinned vector search (query_at is timestamp-only)**  
`crates/engine-next/src/data/vector/service.rs:596` · primitive-implementation-contract.md §7 Read Contract (Version read must be supported); §14 ("Temporal vector query behavior")

Point reads expose both `get_at_version` and `get_at`, but ranked search exposes only `query` (Latest) and `query_at` (timestamp). There is no public `query_at_version`, even though the internal selector machinery (`ReadSelector::AtVersion`) and the testkit `query_*` helpers fully support it. SDK/CLI callers can pin a vector search to a timestamp but not to a specific commit version, an asymmetry with the point-read surface and with KV/JSON version reads.

*Failure scenario:* A user reproducing a search result at commit version 42 (e.g. for an audit/repro flow) can pin `get_at_version(42)` but cannot `query_at_version(42)`; they must resolve a timestamp for that version first, which is lossy when multiple commits share a timestamp.

*Fix:* Add `query_at_version(collection, query, k, filter, version)` mirroring `query_at`, delegating to `query_with_selector(ReadSelector::AtVersion(version))`.

**U70. ⚪ LOW · product-gap — VectorConfig has no dtype/quantization; all embeddings are stored full f32**  
`crates/engine-next/src/data/vector/types.rs:256` · primitive-implementation-contract.md §Vector item 3 ("Collection config: dimension, metric, dtype, metadata schema where present")

`VectorConfig` carries only `dimension` and `metric`; the `dtype`/storage-precision axis named in the contract is absent. There is no way to declare int8/f16/product-quantized storage, so every collection pays 4 bytes/dim at rest and in the (full-scan) query path. The contract lists dtype as "where present" (a should), so this is a deliberate simplification, but for the billion-scale target it forecloses the primary lever for cutting vector footprint and bandwidth.

*Failure scenario:* 1B × 1536-dim f32 = ~6 TB of raw embeddings with no supported way to opt into int8 (~1.5 TB) or f16 storage, even when the workload tolerates it.

*Fix:* Reserve a `storage_dtype` field on `VectorConfig` (default f32) before the format freeze so quantized storage can be added without a config/format break.

## Part 3 — Rejected findings (false-positives caught by verification)

The adversarial pass rejected these; recorded for completeness.

- **R1. `event` — Temporal reads (get_at/len_at/list_at/range_by_time/get_by_type_at) filter by engine-fabricated append timestamps over ReadSelector::Latest instead of the storage commit timeline**  
  _Why rejected:_ The finding's mechanical description is accurate — event temporal reads (get_at service.rs:168-170, len_at:190, get_by_type_at:219, list_types_at:323, list_page as_of:360, range_by_time:295) all use ReadSelector::Latest + a filter on event.timestamp(), which is the engine-fabricated wall-clock append timestamp (outcome.rs:124/435; normalized_event_timestamp service.rs:705-712 = observed.max(last+1) from SystemTime::now()), and the event code never uses AtTimestamp/AtVersion while KV/JSON do (kv/service.rs:119). However, the claimed FAILURE SCENARIO does not hold in the code as written. It rests on the premise that the 5 events 'commit at storage-commit-time T' (a wall-clock value) and that a KV row read via get_at(key, T) with that same T is meaningfully visible — i.e., that event append time and the storage commit timeline share a domain. They do not: the engine's CommitPlan carries no timestamp and commit() uses CommitOptions::default() (adapter.rs:468-476), so storage stamps commits via next_commit_timestamp() = last_allocated + 1µs seeded at DEFAULT_TIMESTAMP = Timestamp::from_micros(1) (runtime/mod.rs:127, 3212-3234). The storage commit timeline is thus a purely logical +1µs counter starting at 1, completely divorced from the wall-clock (~1.75e15 µs) event append timestamp. Consequences: (1) 'all 5 commit at storage-commit-time T' is false — they receive logical counter values, not wall-clock T; (2) AtTimestamp resolves commit_timestamp <= T (runtime/data.rs:222, api/read.rs:1166), so kv.get_at(key, wall_clock_T) returns the row only because every logical commit ts is trivially < any wall clock — a degenerate always-true comparison, not a real timeline placement; (3) there is no single T that is comparable across the two capabilities, so the described cross-capability 'as of T snapshot inconsistency' is a timestamp-domain mismatch, not a data-visibility bug. Within its own (wall-clock) domain the event capability's temporal reads are self-consistent — the natural, user-visible semantic for an event log; using AtTimestamp against the logical commit counter would in fact make it impossible for users to query by the timestamps events actually expose. The only residual kernel of truth is that max(last+1) can push append times a few µs ahead of true wall clock under high append rate or clock stall, causing a sub-microsecond boundary imprecision entirely inside the event's own time domain — an intended monotonicity trade-off, not the commit-timeline correctness defect claimed, and low severity at most. The architecture doc also explicitly disclaims the semantics table as aspirational, not a per-path guarantee.
- **R2. `api-surface` — No access mode / read-only open in the engine-next public surface — writes are never gated**  
  _Why rejected:_ The finding's factual observations are accurate: crates/engine-next/src/api/options.rs:8-11 and 51-53 show CacheOpenOptions/DurableLocalOpenOptions carry only default_branch + memory_budget_bytes; a grep for access_mode/read_only over engine-next/src returns 0 hits (AccessMode does not exist anywhere in crates/*/src); and database.rs:148-170 always opens read-write with no mode. However, this is not a defect at the current state.

(1) The cited contracts are explicitly TARGET-STATE, not current requirements. The cleanup checklist is "Status: V1 architecture draft" (L3), describes cleanup "required before engine-next becomes the V1 product surface" (L7-8), and its Test-and-Guard item 7 is gated "Before V1 surface freeze" (L779-781). The IPC contract clauses cited are under "Target rules" (L440); its "Current access mode facts" describe the OLD crates/engine crate being replaced. The -next suffix is still present, and per CLAUDE.md that is shed only at M9B before V1 promotion, so the freeze these checklists gate has not arrived.

(2) Read-only open is scheduled, unbuilt future work. The roadmap product-coverage matrix (L133) lists it as an engine-next item whose dependency (open/access-mode tests) is still pending. cli-next-implementation-plan.md:102 says read-only open "can be added if executor-next exposes a durable read-only mode" (conditional/future). Read-only client and pathway validation are scheduled at M10 (M10TB) and M11 (M11TB). The current branch v1-billion-scale-perf (BS4.x storage-perf tranche) is far from that window.

(3) The concrete failure scenario cannot occur as written. It requires `strata --db ./prod --read-only info` to be accepted then silently ignore a write, but cli-next options.rs (Cli struct, lines 11-39) defines no --read-only flag; clap rejects the unknown argument and exits non-zero. There is no accept-but-ignore path, so the "operator believes protection is active while it is silently dropped" danger is unreachable.

The finding correctly names a gap between the in-progress state and the V1 target surface, but that gap is planned-not-yet-implemented work owned by M9-M11, and the illustrative failure scenario does not hold. Out of scope, so REJECTED. Reported severity high is wrong; as a tracking note against future milestones it is at most low/informational.
- **R3. `retrieval-derived-state` — Retrieval substrate, recipes, shadow vectors, and embedding_model_mismatch detection are entirely absent**  
  _Why rejected:_ Every factual claim checks out, but this is intentional milestone-staged deferral, not a defect. Verified: space.rs:87-88 asserts !unique.contains(&0x33) and all(|id| !(0x40..=0x45).contains(id)) exactly as quoted; RowClass (space.rs:4-22) defines only capability + control classes, no derived-state/recipe families; grep for recipe|retrieval|provenance|shadow|freshness|embedding_model_mismatch across crates/engine-next/src returns 0 hits (the only rerank hits are merge_and_rerank_candidates in data/vector/index.rs:1614, internal per-query vector candidate dedup, not a retrieval substrate); the contract (retrieval-and-derived-state-contract.md:344) does reserve 0x33 for recipes and 0x40..=0x45 for derived rows and names shadow vectors/freshness/StageOutcome/embedding_model_mismatch; M6 plan rows M6C and M6E are the responsible-but-unlanded slices. However, the cited assertions are the codebase deliberately RESERVING those IDs: the space.rs test is named core_control_row_ids_are_locked_and_leave_deferred_gaps_unused, and dependency_guards.rs:372-409 (core_control_does_not_define_deferred_row_families) plus control_plane.rs:264-283 actively FORBID adding Recipe/Retrieval/Shadow/Derived families prematurely, calling them deferred systems. No M6C/M6E commits have landed and this is a storage-perf branch predating the M6 retrieval gate. The claimed failure scenario does not genuinely hold — there is no caller attempting recipe/hybrid retrieval and failing at runtime; the surfaces are simply unbuilt by design, so nothing misbehaves. This falls squarely under the guarded / out-of-scope / failure-scenario-does-not-hold rejection grounds, and the finding itself admits it is a milestone-staged gap, not a regression. High severity is unwarranted; at most this is a low-severity tracking note.
- **R4. `product-pathway-conformance` — Branch compare/diff, promote/merge, copy/cherry-pick, and restore/revert are entirely absent from the engine surface**  
  _Why rejected:_ The finding's factual observations are correct: BranchService (crates/engine-next/src/branch/service.rs:24-480) exposes only list/get/create/create_from_head/fork_current/fork_at_version/fork_at_timestamp/delete, and a workspace grep finds no compare/diff/merge/promote/copy/cherry-pick/restore/revert (only the vector-index internal merge_and_rerank_candidates at data/vector/index.rs:1614). Pathway 30 is indeed "Required" (product-pathway-conformance-plan.md:199) and M6D scopes these operations (m6-m6t-implementation-plan.md:33). However, the characterization as a high-severity defect is wrong, for two decisive reasons. (1) The absence is INTENTIONAL and TEST-ENFORCED — not an accidental gap: tests/dependency_guards.rs:414-433 (branch_service_does_not_expose_deferred_workflows) asserts service.rs must NOT contain `pub fn merge`, `pub fn cherry`, `pub fn revert`, `pub fn restore` etc., and tests/dependency_guards.rs:800-829 (product_scope_stays_limited_to_current_primitives) forbids Merge/Diff/Restore/Revert/CherryPick anywhere in engine-next src. Adding these operations now would fail the build. This also directly refutes the finding's claim that no test target references these operations. (2) It is OUT OF SCOPE for the current state: the progress tracker (v1-progress-tracker.md:3,74-76) shows M3H complete with M4, M5, and M6 all "Planned"/not-started; pathway 30's conformance gate is M6D+M6TC (m6 plan line 53), which has not been reached, so there is no conformance obligation being violated. The finding audits deliberately-deferred future milestone work as a shipped product gap. The failure scenario (downstream consumer cannot call compare/promote/restore) is literally true but describes unbuilt-by-design functionality guarded against premature introduction, not a defect in the code as written. This squarely matches the REJECTED criteria: guarded and out of scope.
- **R5. `product-pathway-conformance` — Keyword/semantic/hybrid search, BM25, and recipes have no engine surface at all**  
  _Why rejected:_ The finding's description of the current code is factually accurate: crates/engine-next/src/data/mod.rs:1-6 exposes only five capability modules (event, graph, json, kv, vector); there is no retrieval/search directory; and grep for recipe|retrieval|bm25|keyword|semantic|hybrid|fusion across src/ returns only vector nearest-neighbor rerank code (data/vector/index.rs:1614+), not indexed-text search. The api surface (src/api/mod.rs) likewise has no search/recipe method. Pathways 16-19 are indeed Required (product-pathway-conformance-plan.md:180-183) and the Retrieval Bucket with BM25/recipes/fusion is a real design target (engine-architecture.md:810-826), owned by epic M6E.

However, this is not a defect — it is intentionally-deferred, guard-enforced future scope. (1) crates/engine-next/tests/dependency_guards.rs:799-814 (`product_scope_stays_limited_to_current_primitives`) scans all of engine-next/src and actively ASSERTS that "Retrieval" (alongside Ipc, Export, Merge, Diff, Restore, Revert, CherryPick) must NOT appear — the absence is deliberately enforced, so adding retrieval prematurely would fail CI. (2) crates/engine-next/tests/control_plane.rs:264-282 forbids recipe/search/retrieval/shadow/derived from leaking into diagnostics as explicitly "deferred" detail. (3) M6E ("Retrieval and derived state") is a separate, not-yet-reached epic sequenced after the data-capability epics (M6A-C) and branch/temporal (M6D) within M6 (m6-m6t-implementation-plan.md:34; roadmap:541-542). (4) intelligence-next does not yet exist as a crate (absent from crates/), so no downstream consumer is wired to invoke a retrieval surface — the failure scenario's MCP/agent/CLI/SDK/Strata-AI consumers are not built against this surface, so the scenario does not hold as a live defect.

This maps to REJECTED: guarded and out of scope for the current milestone state; a mid-flight V1 rewrite's not-yet-built planned epic mischaracterized as a high-severity conformance defect. At most it is a roadmap-tracking observation (low), not an actionable code defect.
- **R6. `product-pathway-conformance` — Graph ontology (object/link type define/get/list/delete/freeze/status) is entirely missing**  
  _Why rejected:_ The finding's factual observations are all accurate, but its characterization as a defect is wrong — the absence is intentional, documented, and guarded.

Facts confirmed:
- crates/engine-next/src/data/graph/service.rs exposes exactly the methods the finding lists (create_graph/delete_graph/list_graphs/graph_info/upsert_node/get_node/delete_node/list_nodes/upsert_edge/get_edge/delete_edge/neighbors/bindings_for_entity/batch_write). No ontology define/get/list/delete or freeze/status methods exist.
- grep for ontology/object_type/link_type/define_type across crates/engine-next/src returns 0 hits.
- GraphEdgeType (crates/engine-next/src/data/graph/types.rs:129) is a free-form validated String newtype, not a declared/freezable type — as the finding states.
- product-pathway-conformance-plan.md:170 does list pathway 12 "Define graph ontology" as Required.

Why this is REJECTED (out of scope + guarded, not a defect):
- The ontology surface is intentionally deferred, not missing by accident. The current slice is the graph-core slice: docs/architecture/archive/implementation-plans/engine-graph-core-implementation-plan.md:12-14 states "Ontology is intentionally a separate follow-up." Lines 63-67 and 185-204 explicitly place "ontology object and link types," "ontology draft/frozen lifecycle," and "ontology validation" under "Old Behavior Not To Carry Into This Slice," and line 190 says "Do not include ontology status in this slice." The slice exit criteria (line 350) require that "No ontology... are introduced."
- The absence is actively enforced by a guard test. crates/engine-next/tests/dependency_guards.rs:271-296 (graph_core_does_not_expose_deferred_graph_surface) asserts the token "Ontology" (alongside Wcc/Cdlp/Pagerank/Lcc/Sssp/Bfs/SemanticMerge/BranchDag/Boost) must NOT appear in the graph core surface. So ontology's absence is a deliberate, test-enforced design decision at this milestone, not a gap in claimed-complete functionality.
- The conformance plan is a V1 acceptance target, not a claim that all Required pathways are already built (plan lines 16-18, 277-299 describe an acceptance bar; line 299: "the pathway remains absent from V1 default" until conditions are met). Engine-next is mid-rebuild; ontology is scheduled as a distinct future "ontology slice" referenced by the graph-core plan.

This falls squarely under the REJECT criteria "guarded" and "out of scope / deferred future work." The failure scenario (a consumer can't declare/freeze ontology types today) is literally true but is intended, tracked behavior for this milestone, not a defect. If treated as a backlog gap rather than a bug, its severity as a "defect" is at most low.

## Part 4 — Contract conformance matrix

Across the 18 governing contracts: **2 conforms · 22 partial · 3 violates · 1 n/a**.

| Unit | Contract | Status | Notes |
|---|---|---|---|
| `control-plane` | dataset-clone-artifact-contract | **violates** | No clone/export/import/artifact/provenance substrate exists anywhere in engine-next (verified by exhaustive grep of src and tests). None of the V1 Minimum requirements are implemen |
| `persistence` | storage-space-id-registry | **violates** | Raw numeric IDs are correctly centralized behind RowClass (space.rs) and appear nowhere in capability code, and storage is kept opaque (no engine-byte→product-name mapping). Howeve |
| `temporal-timeline` | temporal-context-and-timeline-resolver-contract | **violates** | The area implements point/version reads and fork-at-version/timestamp equivalence correctly within the retained window (the timeline_model proptest confirms KV as-of-version, as-of |
| `api-surface` | ipc-and-command-boundary-contract | **partial** | Positives: the api is a typed method surface using product vocabulary (branch/space/key/document/vector/graph) with no storage keys, WAL, manifest, or table internals leaked in nor |
| `api-surface` | public-api-and-cli-surface-cleanup-checklist | **partial** | Positives (real conformance): the removed surfaces are genuinely absent from the public api — no follower, no public transaction/begin/commit/rollback, no tags/notes, no branch bun |
| `branch-commit` | branch-operation-and-capability-adapter-contract | **partial** | Implemented and conforming: engine-owned create-empty (V1 Minimum item covering empty-branch, contract Create Empty Branch — required and distinct from fork), create-from-current/f |
| `branch-commit` | temporal-context-and-timeline-resolver-contract | **partial** | For the branch-commit scope: fork-from-version and fork-from-time are implemented and record fork_version/fork_timestamp, and storage does at-or-before resolution with an unretaine |
| `control-plane` | control-plane-layout-contract | **partial** | Conformant core: global control rows (database identity, local instance identity, storage registry, capability registry, migration registry, branch catalog/index/default/pending) l |
| `diagnostics` | error-and-diagnostics-contract | **partial** | Strongly satisfied: one product error parent (EngineError) with serializable EngineErrorStatus (class/code/retry_policy/commit_outcome/message/suggested_fix/details/hints); public  |
| `diagnostics` | v1-error-and-diagnostics-contract (cross-layer vocabulary, applied by the engine contract) | **partial** | Conforms on: the 15-class ErrorClass enum matches the class table; RetryPolicy and CommitOutcomeStatus match the defined vocabularies; codes are lowercase-ASCII, snake_case, URL-pa |
| `event` | primitive-implementation-contract | **partial** | Strong on the write/encoding/validation and integrity axes: append/batch_append map to single atomic commits (event rows + type index + metadata in one CommitPlan, service.rs:129-1 |
| `event` | engine-architecture (Required Data Capability Semantics — Event column) | **partial** | Latest read, Entity-addressable, and text/search-projectable payload shape are met. Version read (matrix: Yes) is not implemented at all. Timestamp read (matrix: Yes) is implemente |
| `graph` | entity-ref-and-relationship-layer-contract | **partial** | Conforming: bindings are structured/typed (GraphBindingTarget with primitive/branch/space/key), not opaque URI strings (satisfies EntityRef & URI Encoding rules 1,4,5); resolved re |
| `graph` | primitive-implementation-contract | **partial** | Conforming: clean capability anatomy (facade GraphService, semantic types, entity addressing, deterministic length-prefixed key encoding that is prefix-safe, versioned value codecs |
| `invariants-crate-shape` | target-crate-shape-and-test-harness | **partial** | Conforms on all hard crate-level policies: #![deny(unsafe_code)] present (lib.rs:3); storage-next imported only under persistence/ (grep-verified, zero escapes); no subsystem-insta |
| `json` | primitive-implementation-contract | **partial** | Strong on facade, semantic types, deterministic key/value encoding, and read/write forms: latest/version/timestamp/history/exists/list/count/sample reads and create/set/set_or_crea |
| `kv` | primitive-implementation-contract | **partial** | CONFORMS: §5/§6 key & value encoding (deterministic, versioned KEY_VERSION=1, length-prefixed product space giving true branch/space isolation and prefix-scannability); §7 read for |
| `persistence` | persistence-adapter-contract | **partial** | Implemented well: single storage-facing adapter as the only storage import point (mod.rs imports strata_storage_next only here); capability code emits RowAddress/RowMutation/Commit |
| `product-pathway-conformance` | product-pathway-conformance-plan | **partial** | Data-capability pathways 7-11, 15, 31 (KV/JSON/event/vector/graph CRUD, EntityRef bindings, spaces) and pathways 25/27/29 (branch create, branch-from-version/time, as_of/versioned  |
| `product-pathway-conformance` | engine-architecture | **partial** | The implemented data capabilities and control plane match the declared bucket structure and dependency direction (only engine imports storage, under persistence/). But the architec |
| `product-pathway-conformance` | m6-m6t-implementation-plan | **partial** | M6A-M6C (data capability pattern; KV/JSON/events; vector/graph relationships) appear substantially complete and match M6TB coverage. M6D (branch/temporal) is only partially deliver |
| `retrieval-derived-state` | retrieval-and-derived-state-contract | **partial** | The retrieval/derived-state substrate this contract defines is largely a future milestone (M6E) not yet on this branch: no recipes (0x33 unused), no text/BM25/fusion/rerank/RAG/gra |
| `vector-index-artifact` | vector-indexing-design | **partial** | The engine implements the design's core query contract correctly: per-source flat/HNSW artifacts, MVCC + branch fork-version caps applied post-retrieval (artifact_row_visible), ove |
| `vector-service` | primitive-implementation-contract | **partial** | STRONG on the core capability shape: all required read forms exist and are correct — latest (get/get_versioned), version (get_at_version), timestamp (get_at), history (newest-first |
| `vector-service` | retrieval-and-derived-state-contract | **partial** | The vector-as-a-retrieval-source substrate is largely conformant where implemented: §Vector-stage 1 dimension/metric validation is enforced before search (query.validate_dimension) |
| `vector-index-artifact` | dataset-clone-artifact-contract | **not-applicable** | The in-scope files implement the engine-internal VECTOR INDEX derived-state artifacts and their branch-local manifest — not the .strata portable clone/export artifact this contract |
| `invariants-crate-shape` | CLAUDE Hard Rules (dependency direction, authority, storage substrate, public surface, quality) | **conforms** | For the engine-next crate itself: Rule #1 (storage only in persistence/) holds; Rule #2 (engine never imports intelligence/inference) holds and is compiler-enforced (neither is a d |
| `invariants-crate-shape` | engine-architecture (Target Dependency Rules, What Engine-Next Must Exclude) | **conforms** | Target Dependency Rule #1 (API must not call storage-next) and #9 (persistence is the only storage-facing bucket) hold. Exclusions #4/#5 (no hidden model-provider calls, no inferen |

## Part 5 — Coverage notes (per unit)

- **`kv`** — Read fully: data/kv/{mod,service,outcome,types}.rs; persistence/{mod,row,adapter}.rs and the KV codec in persistence/key.rs (encode_kv_key/encode_kv_key_bytes/encode_kv_space_prefix/decode_kv_key, encode_user_key, next_prefix, exclusive_after_key); control/{bootstrap,space}.rs for space auto-registration folded into the KV commit batch; commit/mod.rs with_counts; branch/service.rs public surface; api/database.rs kv() wiring and api/admin.rs Kv read reuse. Read both test targets (branch_and_kv.rs, kv_pagination.rs) and the governing contract clause-by-clause, plus M5/M6 plans for scope.

Verified correct (no findings): key encoding gives sound branch+space isolation because the space is length-prefixed (a space name that is a byte-prefix of another cannot collide); scan_range/scan default upper bound via next_prefix(space_prefix) is a correct exclusive bound and never leaks adjacent spaces; cursor pagination (scan_keys_after_cursor) returns every key exactly once with an accurate has_more (it keeps scanning past tombstones so has_more is precise, not merely 'raw rows remain'); exclusive_after_key(k)=k||0x00 is the correct minimal successor so no rows are skipped or duplicated across the 64-row raw-page boundary; scan_range_limited never under-returns below limit when more live rows exist; empty-value put stores a non-tombstone empty value that round-trips distinctly from a miss; batch dedup is on the encoded key; empty put/delete batches and all-missing delete batches return the correct kv_batch / no-commit outcomes; timestamp reads fall back to latest at future timestamps for both point and list reads; all .expect() calls are guarded by preceding emptiness/has_more checks; count()'s usize->u64 conversion is lossless on 64-bit. No panics or unwrap_or_default footguns in the module.

Scope notes: The branch-adapter/promote gap (finding 3) is cross-cutting — the whole branch subsystem lacks promote/diff/merge/restore, not just KV's adapter — and may be a later-milestone item; reported at medium because the contract and CLAUDE.md #20 make it a V1 requirement KV must eventually satisfy. Read-only-mode write rejection (§8.7/§14.10) is not enforced in KV, but engine-next currently has no read-only Database mode to enforce against (AdminService is a separate read-only-by-construction surface), so this is a global deferral rather than a KV defect and is not filed as a finding. Test quality was not audited per instructions.
- **`json`** — Scope read in full: crates/engine-next/src/data/json/{mod,document,types,service,outcome}.rs, plus the persistence dependencies it calls (persistence/key.rs JSON encoders/decoders and encode_user_key/decode_user_key/encode_length_prefixed_text; persistence/adapter.rs scan_prefix/scan_range/read_row/read_history/commit and request builders; storage-next api/atoms.rs ScanRange bound semantics), branch/service.rs (to confirm merge/promote absence), diagnostics/error.rs+registry.rs (error class/code mapping), and api/{json,database,space}.rs (facade + index-entry consumers). Governing contract primitive-implementation-contract.md read clause-by-clause; CLAUDE.md Hard Rules and m6 plan consulted for 'complete' calibration. Test target crates/engine-next/tests/engine_json.rs read in full to judge coverage (not audited for its own quality). Coverage is strong for single-doc CRUD, path type/not-found errors, array-shift-on-delete, batch set/delete/delete_entries positional semantics, list/count/sample boundaries, history/version/timestamp reads, null-document liveness, space/branch isolation, durable reopen, and index-entry maintenance (testkit). Coverage gaps observed (not counted as implementation findings): no test for list/count where a prefix exactly equals a document id (start-inclusive boundary — verified correct by code/ScanRange), no test for setting a path through an existing JSON null intermediate (silently replaced by object), and no negative test asserting index-backed query is unavailable. Verified NOT bugs during the pass: list pagination cursor/prefix filtering and exclusive_after_key advancement (no missed/duplicated rows), numeric index order-preserving f64 encoding and fixed-width entry-key layout (no separator ambiguity/collision), sample stride formula (strictly increasing, no duplicates), batch atomicity (validation before commit; no partial visible state), and Display<->parse path round-trip for keys/indices."
- **`event`** — Read exhaustively: data/event/{mod,service,types,record,outcome,hash}.rs and their inline unit tests, plus the integration suite tests/engine_event.rs. Followed call paths into persistence/key.rs (encode/decode_event_key, type-index key, be(sequence) ordering — confirmed lexicographic == numeric sequence order), persistence/adapter.rs (read_row/scan_prefix/scan_range, ReadSelector::{Latest,AtVersion,AtTimestamp}, read_limit None => unbounded), persistence/space.rs (RowClass ids), and api/space.rs (EventIndex only touched by delete cleanup). Cross-checked sibling capabilities data/kv/service.rs and data/json/service.rs to calibrate required read forms (both expose get_at_version via AtVersion and get_at via AtTimestamp; events expose neither). Reviewed governing contract primitive-implementation-contract.md clause-by-clause, engine-architecture.md capability matrix, and m6-m6t-implementation-plan.md. Concurrency race on sequence allocation was considered and dismissed: EventService borrows &mut StoragePersistence/&mut ControlPlane, so intra-process appends to the same branch/space are serialized by the borrow checker; cross-process is lock-guarded. No critical (data-loss/corruption) issue found — commits are atomic and the codec self-verifies the hash chain on decode. Test suite covers append/batch/range(fwd+rev)/range_by_time/list/list_page pagination/verify_chain/branch+space isolation/durable reopen well, but has NO coverage of: a true commit-version read, same-microsecond-commit as-of behavior (the temporal-read finding), append()'s error code, or range() cursor round-trip resumption."

- **`graph`** — Read exhaustively: crates/engine-next/src/data/graph/{mod,types,record,service,outcome}.rs in full; the graph key encode/decode it depends on in crates/engine-next/src/persistence/key.rs (including encode_user_key/encode_length_prefixed_text prefix-safety and the binding-target suffix codec); the RowClass storage-space ids in persistence/space.rs; the full test target crates/engine-next/tests/engine_graph.rs; both governing contracts clause-by-clause; the m6-m6t plan (M6C scope). Cross-checked peer capabilities (KV service.rs:127, JSON service.rs:216) to confirm the History-read norm graph omits, and grepped the whole crate to confirm no higher layer resolves graph bindings to target status and no cross-branch/failed_precondition guard exists. Verified NON-issues so as not to over-report: length-prefixed keys make all prefix scans (node/edge/binding/metadata) prefix-safe against name-prefix aliasing; neighbor and binding cursors are collision-free because node/edge/graph identifiers reject control bytes (validate_text_component) so the U+001F delimiter is unambiguous; forward+reverse edge rows and node/binding rows are always written and cascaded within a single atomic commit, so the 'edge visible but endpoint missing' corruption in visible_node_or_corruption is a genuine corruption signal and not reachable via normal single-threaded time-travel (the dedicated test graph_historical_edge_reads_never_dangle_or_corrupt confirms this); batch binding cleanup correctly deletes stale binding-index rows on rebind and on binding removal; MutationMap dedup gives last-writer-wins per (RowClass,key); ProductSpace::new already rejects `_system_`/`_`-prefixed spaces so a binding target cannot leak into a reserved space via the public API. No engine 'export'/clone path exists for any capability yet (the 'export split paths' focus corresponds to unimplemented functionality, not shipped graph code). Did not assess test quality per instructions.
- **`vector-service`** — Read fully: service.rs (all 2797 lines), outcome.rs, types.rs, record.rs, distance.rs, mod.rs, and the load-bearing helpers in index.rs (query_vector_sources_with_index_artifacts, query_vector_exact, ActiveDeltaVectorSource/Exact/Flat/Hnsw sources, merge_and_rerank_candidates, sort_candidates, artifact_row_visible, covered_versions_by_key) plus persistence/adapter.rs (read_history, scan_prefix/after_version/range, prefix_scan_request) to confirm tombstone visibility and selector semantics. Compared clause-by-clause against primitive-implementation-contract.md and retrieval-and-derived-state-contract.md, and cross-checked scope against m6-m6t plan and engine-test-plan.md (which independently flags the auto-seal and watermark items). Test targets engine_vector.rs and vector_filter_scalars.rs were read for coverage judgment (not audited for their own quality): they cover lifecycle/count, metadata patch, batch upsert/get/delete positional semantics, bulk delete/filter, the three metrics + tie/zero/opposite ordering, k=0, dimension-mismatch errors, timestamp reads + history depth, and the full Eq scalar matrix (null/int-vs-float/bit-exact -0.0/type-mismatch/bool). CONFIRMED-CORRECT (no finding) after tracing: (a) Latest prefix scans DO return tombstone rows, so active-delta tombstone suppression of sealed artifact rows is sound; (b) latest_vector_revision returns the newest revision (read_history is newest-first, include_tombstones=false); (c) filter is deferred past newest-wins dedup so a re-upsert that no longer matches a filter cannot resurface an older matching version; (d) filtered/HNSW paths apply the filter before truncation and the underfill fallback re-runs exact only over a complete snapshot (entries_are_complete guard). NOT independently verified (out of area): read-only-mode write rejection and cache-mode derived-state suppression are assumed enforced at the persistence/control layer; storage L9 tombstone-in-scan behavior was inferred from the adapter and the engine's pervasive is_tombstone filtering rather than read in storage-next. Minor items folded into findings rather than reported separately: list_keys prefix listing scans the whole collection prefix and post-filters with starts_with (O(collection) per prefix page; correct but unindexed), and f32 (not f64) accumulation in cosine/dot/euclidean loses precision at very high dimension.</coverage_notes>
</invoke>

- **`vector-index-artifact`** — Read in full: index.rs (2091 lines), manifest.rs (679 lines), artifact.rs (2393 lines), both governing specs, the dedicated engine-vector-indexing-implementation-plan.md (authoritative for 'complete'), tests/engine_vector.rs (harness + recall gates), and the wiring in data/vector/service.rs (query path, manifest resolution, artifact loading, sealing) and branch/service.rs (fork manifest materialization). Verified end-to-end call paths for build (seal), query (bounded active-delta + full-snapshot), fork-cap handling, and durable/memory artifact load+recovery. Confirmed via grep that no production caller triggers sealing. Correctness of the common (no-fork and fork-from-HEAD) query paths was analyzed and found sound: MVCC/fork visibility is applied post-candidate-generation, active-delta/artifact version ranges are disjoint per key (covered_versions_by_key), tombstones always propagate to the merge for suppression, index-unavailable in the bounded path correctly triggers needs_full_snapshot re-run, and artifact decode has bounds/overflow/row-count guards with checksum+identity validation. The two correctness findings are latent (reachable today only via testkit seeding + historical fork) but are real logic flaws that will bite once production sealing (impl-plan step 7) is wired. Did not audit test quality (out of scope). Did not deep-read distance.rs/types.rs/record.rs/outcome.rs beyond their interfaces used by the in-scope code.</coverage_notes>
</invoke>

- **`persistence`** — Read in full: persistence/{mod,adapter,plan,row,space,fault,key}.rs and both test targets (persistence_adapter.rs, persistence_faults.rs, which use tests/common). Followed call paths into control/bootstrap.rs and control/records.rs (registry read/validate on open), the capability commit callers (data/{kv,json,event,graph,vector}/service.rs, api/space.rs, branch/service.rs), storage-next api/{commit,error}.rs, and the data-type length validators (graph/vector/event types.rs). Checked and dismissed a potential encode panic: all length-prefixed key fields go through validators capped well under u16::MAX (graph name/edge type 256, node id 1024, collection name 256, event type 256), so encode_length_prefixed_text's u16::try_from().expect() is unreachable via validated types. key.rs encoding/decoding is dense but solid: version+discriminator+length-prefixed product space, round-trip and cross-family rejection are well covered, corruption maps to data_loss.engine.* codes, trailing-byte and truncation cases are rejected. Error mapping is the strongest part of the area — comprehensive class/code/retry/commit-outcome coverage with redaction assertions. Milestone calibration: M5/M6 plans contain zero mentions of read-only, write-authority, control-plane scope, frontier, or snapshot-consistency, so Findings B and D correspond to governing-contract V1-minimums that are currently unscheduled (likely deferred) rather than regressions. Test coverage gaps observed (not audited for quality, per instructions): no test exercises a data-capability attempting a control-plane write, no read-only-rejects-write test, no multi-row as_of snapshot-consistency test, no CAS/conditional-write path — consistent with those capabilities being absent. Did not deeply audit control/records.rs registry-persistence codes since that file is outside the persistence/ directory, but flagged the relevant deviations in the registry conformance verdict."

- **`api-surface`** — Scope covered exhaustively: all 12 files under crates/engine-next/src/api/ (admin.rs, branch.rs, control.rs, database.rs, options.rs, space.rs, and the event/graph/json/kv/vector re-export shims + mod.rs). Followed call paths into the re-exported logic in src/branch/service.rs (branch operations), src/diagnostics/error.rs + registry.rs (error-code/class conformance), and src/persistence/adapter.rs open path. Read both governing contracts clause-by-clause, plus CLAUDE.md Hard Rules, and the M5/M6 implementation plans for the definition of 'complete'.

Note on execution state: docs/architecture/v1-progress-tracker.md is stale (declares M3 complete and M4/M5/M6 'Planned'), but the code is clearly mid-M6 — a full engine-next api surface with capabilities, branches, spaces, admin, and a V1 response/error contract exists. Findings #1/#2/#3/#7 map to work the milestone plans place in M5/M6D/M6F/M6G; they are reported because both governing contracts require these surfaces for V1 and downstream CLI/SDK/MCP, and there is currently no seam for them — severity reflects product impact, not an assumption that they were due in a shipped slice.

Verified NON-issues (checked and cleared to avoid false positives): the two-level error model (internal EngineErrorClass vs wire ErrorClass) is intentional and consistent — public_class_for_legacy derives the wire class from the code-string prefix, all api-used codes (space_not_empty, space_delete_*, branch_*, runtime_closed, config_key) are registered, and admin.rs correctly treats an unavailable control plane as Degraded (not Unhealthy). space delete is atomic (catalog + data committed in one CommitPlan) with correct non-empty gating and a working force path. The capability_conformance.rs and admin_and_space.rs suites exercise write-visibility, fork inherit/isolate, space isolation, missing-branch, closed-runtime, durable reopen, forced space delete, and the config allowlist; they do not cover access-mode, branch compare/merge/restore, clone, large-space delete (>10k), or non-default-space describe — the gaps behind findings #1/#2/#3/#5/#6.

Test-quality was intentionally not audited (separate workstream).
- **`control-plane`** — Read in full: control/mod.rs, control/bootstrap.rs (incl. tests), control/records.rs (incl. tests), control/space.rs (incl. tests); both governing contracts clause-by-clause; both test targets (control_plane.rs, control_plane_lifecycle.rs); branch/service.rs and branch/catalog.rs and branch/name.rs (lifecycle transitions); api/database.rs (health gating), api/space.rs (space delete orchestration), persistence/adapter.rs branch mechanics, persistence/key.rs control-row key families, and kv/service.rs write-path use of space_registration_mutations. Milestone plans m5/m6 read for scope. Note: the v1-progress-tracker.md marks M4-M6 'Planned', which is stale on this v1-billion-scale-perf branch — the code clearly implements M5 (persistence/control plane) and M6A-M6D (KV/JSON/event/vector/graph, branch create/fork/fork-at-version/fork-at-timestamp/delete, space create/list/delete/usage), so I treated the code as source of truth and judged completeness against it. Not fully verifiable without deeper storage-layer reading: whether delete+recreate of a same-name branch (same derived storage_branch_id, bumped generation) fully isolates the old generation's user/space rows from the recreated branch — flagged conceptually but not reported as a finding because it depends on storage-next generation-guarded read semantics I did not audit. The branch-service orphan-fork finding (service.rs) is reported because 'lifecycle transitions' is in-scope for this area and the defect is in the control-owned pending-marker protocol's usage; the core-plane files themselves (bootstrap/records/space) are otherwise solid, well-tested, and fail-closed. Tests were read only to judge coverage, not audited for their own quality.
- **`branch-commit`** — Scope read in full: crates/engine-next/src/branch/{mod,name,catalog,service}.rs and src/commit/mod.rs. Followed call paths into src/control/{mod,bootstrap,space}.rs (ControlPlane, pending-intent lifecycle, space seeding), src/api/branch.rs (DTOs), and src/persistence/adapter.rs (create/fork/delete/describe/branch_exists + PersistenceBranchOutcome mapping). Read both governing contracts fully and compared clause-by-clause, plus CLAUDE.md Hard Rules, engine-architecture context, and m5/m6 implementation plans to calibrate 'complete'. Read the two named test targets (branch_semantics.rs, branch_dag_model.rs) for coverage only (not auditing test quality). Cross-checked storage-next generation semantics (commit/branch_registry.rs recreate_active guard, api/tests/branch.rs fork-at-timestamp behavior) to confirm/downgrade the generation-reuse aspect of the aliasing finding and to verify the fork-timestamp echo behavior. Confirmed via workspace grep that no merge/compare/promote/preview/restore/cherry-pick/revert or capability-branch-adapter trait exists anywhere in engine-next, and that no pending-branch resume/recovery path exists. commit/mod.rs (CommitOutcome) is a thin value type with no defects found. Coverage gaps in the existing tests (durable crash/pending-recovery, after-latest/before-history/pruned-gap forks, historical-fork derived-state consistency, and branch-id aliasing) map onto findings 1, 5, and 6; the DAG property test and branch_semantics cover create/fork/delete/generation/precedence well in cache mode. Did not run the crate build (heavy) — findings are grounded in code reading plus targeted cross-referencing; the two items with a residual storage-behavior dependency (finding 4 generation-regress error, finding 6 after-latest) are framed as confirmed engine behavior with the storage dependency stated."
}
- **`diagnostics`** — Read in full: diagnostics/mod.rs, diagnostics/error.rs, diagnostics/registry.rs (incl. its test module), tests/error_redaction.rs and tests/common/mod.rs. Followed call paths into the helper that implements the contract's storage-mapping section (persistence/adapter.rs map_storage_error, storage_error_details, persistence_suggested_fix) and the executor boundary that consumes the exported registry (executor-next/src/error.rs render_status/normalize_explicit_status, error_registry.rs) to determine the real blast radius of registry metadata. Cross-checked both governing contracts and the M5/M6 milestone plans to calibrate 'complete'. Verified runtime-vs-registry consistency for class/retry/commit across all registered codes (they share class-based defaults; the 7 override codes are adapter-only and agree), and confirmed the message/suggested_fix divergence is the only place runtime and registry can disagree. Did NOT audit test quality per instructions. Test coverage observations (for the completeness picture, not scored as defects): (a) no test asserts a runtime-constructed EngineError's status equals its registry entry, so future drift on message/suggested_fix would be silent; (b) no test asserts commit_outcome for write-path storage faults — the redaction test injects a commit fault but only checks retryable(); (c) redaction coverage is a negative substring guard with no secret injected; (d) the reverse dead-code registry test counts codes referenced only in #[cfg(test)] assertions as 'used', so a registered-but-never-emitted code could pass. Domains legitimately absent at this milestone (retrieval/recipe/search/model, clone/import/export, IPC/protocol, graph-relationship, read-only/closing access-mode, wrong_type, dangling_reference) are deferred to M6E/M8/M9 and were not scored as gaps. ErrorClass::Io / Serialization / AccessDenied are defined but unreached by any engine code today (forward-looking, not a defect).
- **`retrieval-derived-state`** — Read in full: the governing contract; both persistence files fully relevant here (space.rs, key.rs) and the temporal/selector portion of adapter.rs; the entire vector derived-state path (service.rs ~2797 lines, index.rs ~2091 lines, manifest.rs, and the artifact format/store in artifact.rs); and the M5/M6 milestone plans. Skimmed the other data capability services (kv/json/event/graph) and their key encodings — they are the source-of-truth KV rows this area consumes and showed no retrieval/derived-state logic. Assessed coverage via tests/engine_vector.rs and its engine_vector/*.rs submodules (core CRUD, timestamp reads, distance metrics, HNSW recall gates, manifest lookup states, artifact stale/corrupt/missing/over-budget diagnostics, policy planner) — coverage of the implemented vector index read path is strong, but every index-build path is exercised only through testkit seed/seal helpers, and there is no test (and no code) for recipes, shadow vectors, freshness classes, provenance, or embedding_model_mismatch because those are unimplemented. Did not audit test quality (out of scope). Confidence is high on Findings 1-3 (structurally confirmed by grep + the space.rs id-reservation test + dead_code gating) and on the vector temporal-correctness assessment; Findings 4-5 are confirmed by direct code reading. No correctness/data-loss bug was found in the shipping vector read path itself.
- **`temporal-timeline`** — Scope read exhaustively: crates/engine-next/src/persistence/{adapter.rs,mod.rs,row.rs,plan.rs} (the temporal read/scan/fork delegation core), branch/service.rs (fork_at_version/at_timestamp, vector-manifest fork materialization), and the temporal read surfaces of data/{kv,json,event,vector,graph}/service.rs plus kv outcome/types. Cross-checked engine behavior against storage-next resolve_read_bound/require_version_retained (api/runtime/data.rs) and its exact reason strings. Read the governing contract clause-by-clause and the M5/M6 plans (M6D 'branch-from-version, branch-from-time, ... getv, history, as_of'; M6TC 'timestamp resolution ... matches temporal contracts'), plus the engine-test-plan §5.3 which documents the after-latest/before-history divergence as intentional. Judged completeness against tests/temporal_timeline_model.rs (KV as-of-version/timestamp oracle + fork-at-version equivalence) — note this test encodes the non-conformant behaviors (before-history -> None at version 0/EPOCH; after-latest -> latest at CommitVersion::MAX/Timestamp::MAX) and does not cover after-latest-diagnostic, before-history-diagnostic, trimmed-history distinguishability, event/relationship temporal frontier resolution, or retention-safe multi-row reads. Did not deep-audit vector-search internals (separate area); temporal vector query does read a frontier-scoped snapshot (query_with_selector -> vector_query_snapshot(selector)) so temporal vector reads use the resolved selector, but source-filtering exactness is out of this unit's scope. One inspected-but-not-reported item: branch/service.rs materialize_vector_index_manifests_for_fork falls back to reading source manifests at ReadSelector::Latest when none are visible at the fork point — temporally suspect, but the child collection-config gate makes any copied dangling manifest inert, so no confirmed user-visible failure.
- **`invariants-crate-shape`** — Read fully: engine-next/src/lib.rs, Cargo.toml, api/mod.rs, config/mod.rs, test_support/mod.rs, testkit/mod.rs; governing spec target-crate-shape-and-test-harness.md (clause-by-clause); engine-architecture.md (Target Dependency Rules, Bucket Placement, What Engine-Next Must Exclude, Module Ownership); both in-scope test targets (dependency_guards.rs, runtime_lifecycle.rs); root workspace Cargo.toml lint config; CLAUDE.md Hard Rules. Verified invariants directly by grep + a clean warning-free `cargo build -p strata-engine-next`: (1) zero `strata_storage_next` imports outside persistence/; (2) zero intelligence/inference references in engine src (and neither declared as a Cargo dependency); (3) `#![deny(unsafe_code)]` at lib.rs:3 with the only `unsafe` token in the crate being that attribute; (4) zero process-global statics crate-wide (no static/OnceLock/OnceCell/lazy_static/thread_local); (5) workspace lints inherited via `[lints] workspace=true` with the single local relaxation carrying a written reason; (6) public surface intentionally re-exported (no unreachable_pub warnings). Ran the in-scope guard tests: 14 pass, 12 fail — every failure is the ripgrep-absence tooling panic (Finding 1), NOT an invariant violation. Net assessment: the engine-next crate-shape code is genuinely clean; the four findings concern enforcement completeness/portability (guard suite requires an undeclared external binary; process-global guard is vector-scoped; empty config bucket; unguarded executor->inference edge), not code-level invariant breaches. Out of strict scope but noted: executor-next's direct inference dependency. Did not audit test assertion quality per instructions.
- **`product-pathway-conformance`** — Scope read exhaustively: crates/engine-next/src/api/{mod,database,kv,json,event,graph,vector,branch,space,admin,control,options}.rs and crates/engine-next/src/data/{mod, kv/service, json/service, event/service, graph/service, vector/service (first ~1570 of 2797 lines; remainder is index-artifact query plumbing and testkit helpers)}.rs, plus the re-exported crate::branch::service.rs which api/ surfaces. Also read the governing product-pathway-conformance-plan.md in full, engine-architecture.md, the m5/m6 milestone plans, and the conformance test target capability_conformance.rs (which only exercises the shared data skeleton: write/read, fork inherit+isolate, space isolation, missing-branch, closed-runtime, durable reopen — it does NOT and cannot cover the missing branch-ops/ontology/BFS/history/search surfaces).

Verification method: read call paths into helpers; grep across src/ for fn (compare|diff|merge|promote|restore|revert|cherry|import|export|clone_artifact|ipc|recipe|bm25|keyword|ontology|freeze|bfs|traverse) and for EventIndex read usage. All findings are confirmed in code, not hypothesized.

Additional gaps folded here to keep the findings list high-signal (each is a whole not-yet-landed epic rather than a defect in built code): (a) Pathway 4 IPC/command-boundary DTOs — no surface in api/data (M6F). (b) Pathways 5-6 clone artifact validate/materialize/provenance — no `.strata` artifact surface (M6G); note VectorService has extensive artifact plumbing but that is the vector index, not dataset clone. (c) Pathway 32 import/export — absent (optional). (d) Pathway 35 config load/set/get with secret redaction and hidden-network policy — the config module compiles to no public surface (grep of src/config/*.rs finds no pub items); admin exposes only a small read-only allowlist (target/created/durable/default_branch), so there is no engine path to set/validate config keys or redact secrets. (e) Pathway 33 durability counters — AdminMetricsSummary reports target/durable/open/branch_count/space_count/control_status but no durability/commit/WAL counters. (f) Pathway 11 dangling-reference diagnostics — bindings_for_entity returns bindings but does not validate that the bound EntityRef target still exists, so a binding to a deleted JSON/KV/vector record surfaces no 'dangling reference' diagnostic; unconfirmed whether this is intended to live at a higher layer, so not filed as a hard finding.

Not audited (out of scope or separate workstream): storage-next internals below the persistence boundary; the quality of the tests themselves; the latter ~1200 lines of vector/service.rs beyond confirming the query/index paths (skimmed structurally, no correctness claim made about them).

---

*Generated from the multi-agent audit run `wf_ff6eed6c-5f8`. Full structured results retained in the session workflow journal.*
