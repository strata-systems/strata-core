# V1 Progress Tracker

> **Superseded — historical record (#3135).** The V1 rewrite is finished and
> shipped; the product is on the 1.2.x line. This tracker was frozen at "M3H
> complete" with M4–M11 marked `Planned`, which is a year out of date and was
> being read as a statement of current work.
>
> It is kept for the label vocabulary and update protocol below, which the repo
> still uses, and as a record of how the rewrite was tracked. **The milestone
> status table is not current and should not be read as such.**
>
> For what is true now: the root `CHANGELOG.md` (or `strata changelog`), and the
> GitHub Projects boards for what is next.

## Purpose

This document *was* the single progress ledger for the V1 rewrite. It recorded
milestone status, the issue/PR label vocabulary, and the update protocol for
milestone, epic, slice, and test-track work. Only the vocabulary and protocol
remain live.

The architecture and implementation-plan documents remain the source of truth
for scope and decisions. This tracker only records execution state.

## Tracking Rules

1. Track work with roadmap labels in issues, PR titles, branch names, and commit
   messages only.
2. Do not use roadmap labels in production crate names, module names, file names,
   type names, function names, test names, feature flags, error codes, metrics,
   CLI commands, config keys, or user-facing text.
3. Every implementation PR should name exactly one implementation slice or one
   milestone-level planning task.
4. Every test PR should name exactly one test slice or one milestone-level test
   task.
5. A milestone closes only after both its implementation track and test track
   close.
6. If a slice grows beyond one focused pass, split the slice before adding
   temporary compatibility code.
7. Update this tracker when a milestone, epic, or slice changes status.

## Status Vocabulary

| Status | Meaning |
|---|---|
| `Complete` | Scope is implemented, reviewed, fixed, and verified for the slice or milestone gate. |
| `Ready` | Inputs are present and the work may start. |
| `Planned` | Scope exists but an upstream milestone or decision still gates execution. |
| `In Progress` | Active implementation or review is underway. |
| `Review` | Implementation is complete and waiting on review or review response. |
| `Fix` | Review found issues and fixes are underway. |
| `Blocked` | Work cannot proceed until a named blocker closes. |
| `Deferred` | Intentionally outside the current V1 execution path. |

## Label Vocabulary

Use these labels for issues and PRs in any external tracker.

| Label kind | Shape | Example |
|---|---|---|
| V1 effort | `v1` | `v1` |
| Track | `track:implementation`, `track:test`, `track:docs` | `track:test` |
| Milestone | `milestone:M{n}` | `milestone:M3` |
| Epic | `epic:M{n}{letter}` or `epic:M{n}T{letter}` | `epic:M6E`, `epic:M6TD` |
| Slice | `slice:M{n}{letter}{number}` or `slice:M{n}T{letter}{number}` | `slice:M3C1`, `slice:M3TA1` |
| Status | `status:planned`, `status:ready`, `status:in-progress`, `status:review`, `status:fix`, `status:complete`, `status:blocked`, `status:deferred` | `status:review` |
| Risk | `risk:format`, `risk:recovery`, `risk:boundary`, `risk:performance`, `risk:security` | `risk:recovery` |

PR and issue titles should start with the slice or epic code followed by a short
domain title, for example:

```text
M3C1: Add storage row golden vectors
M6TD2: Cover recipe freshness degradation
```

## Current Milestone Status

| Milestone | Title | Implementation status | Test status | Gate status | Next action |
|---|---|---|---|---|---|
| `M0` | Architecture freeze and tracking | Complete | Complete | Complete | M0 closed. |
| `M1` | Core-next | Complete | Complete | Complete | M1 closed; start `M2A` and `M2TA` when storage implementation begins. |
| `M2` | Storage-next testkit and crate skeleton | Complete | Complete | Complete | M2 closed. |
| `M3` | Storage-next backend, layout, format, and durable services | Complete | Complete | Complete | M3 closed. M3TF1 reran lower-layer backend conformance across memory and local filesystem backends after durable services landed. |
| `M4` | Storage-next table, branch, commit, recovery, and L9 API | Planned | Planned | Planned | Start M4 storage table, branch, commit, lifecycle recovery, and L9 API work. |
| `M5` | Engine-next persistence adapter and control plane | Planned | Planned | Planned | Start after L9 is consumable. |
| `M6` | Engine-next product semantics | Planned | Planned | Planned | Start after engine persistence/control plane are stable. |
| `M7` | Inference-next hardening | Planned | Planned | Planned | May start now if inference should run parallel with storage path. |
| `M8` | Intelligence-next orchestration | Planned | Planned | Planned | Start after engine surfaces and inference task contracts are ready. |
| `M9` | StrataHub V1 integration | Planned | Planned | Planned | Start after engine clone substrate and CLI conventions are stable. |
| `M10` | Executor, CLI, SDK, tests, benches, and docs cutover | Planned | Planned | Planned | Start after product and StrataHub surfaces stabilize. |
| `M11` | V1 readiness hardening | Planned | Planned | Planned | Start after cutover. |

## Current Epic Status

| Epic | Title | Track | Status | Blocks | Next action |
|---|---|---|---|---|---|
| `M0D` | Tracking setup | Implementation | Complete | none | M0 can close and M1 can start. |
| `M1A` | Core-next crate skeleton | Implementation | Complete | none | `M1A1` complete; continue M1 atom work. |
| `M1TA` | Core atom unit tests | Test | Complete | none | Atom tests cover construction, validation, ordering, parsing, display, and serde behavior through `M1C`. |
| `M1B` | Core atoms | Implementation | Complete | none | `M1B1` through `M1B4` complete. |
| `M1C` | Core atom encodings | Implementation | Complete | none | Parse, display, and serde behavior complete. |
| `M1TB` | Core atom property tests | Test | Complete | none | Generated ordering, boundary, invalid-input, and parse/display/serde round-trip coverage complete. |
| `M1TC` | Core-next dependency guard | Test | Complete | none | Guard fails if core declares or resolves a dependency on any other Strata crate. |
| `M1D` | Core-next boundary documentation | Implementation | Complete | none | Implemented public exports and rejected candidates are documented. |
| `M1TD` | Core-next API audit | Test | Complete | none | Public API snapshot guard is checked in and runs with the core test suite. |
| `M2A` | Storage-next crate skeleton | Implementation | Complete | none | `M2A1` complete. |
| `M2B` | Backend contract shell | Implementation | Complete | none | `M2B1` complete. |
| `M2C` | Memory and local backend shells | Implementation | Complete | none | `M2C1` complete. |
| `M2TA` | Backend conformance smoke | Test | Complete | none | Private backend conformance smoke covers memory and local filesystem shells. |
| `M2D` | Testkit foundation | Implementation | Complete | none | `M2D1` complete; boundary guards closed under `M2TD`. |
| `M2E` | Harness scaffolding | Implementation | Complete | none | Harness entry points and invocation notes are in place. |
| `M2TB` | Feature matrix | Test | Complete | none | `cargo-hack` feature powerset gate passes for storage. |
| `M2TC` | WASM cache compile | Test | Complete | none | wasm32 memory/cache compile gate passes with localfs excluded. |
| `M2TD` | Testkit boundary guards | Test | Complete | none | Compile probes prove testkit and fault APIs stay feature-gated. |
| `M3A` | Backend operations | Implementation | Complete | none | `M3A1` complete; backend mode capability validation is in place. |
| `M3B` | Object layout | Implementation | Complete | none | `M3B1` and `M3B2` complete; continue with format codec work. |
| `M3C` | Format codec | Implementation | Complete | none | `M3C1` through `M3C5`, `M3F`, and `M3G1` through `M3G4` close the implemented durable format codecs, goldens, fuzz routing, source guards, and table integration harness replacement. |
| `M3D` | Durable publisher | Implementation | Complete | none | Durable publisher implementation and publish fault-window tests are complete. |
| `M3E` | Durable services | Implementation | Complete | none | `M3E1` adds manifest services, `M3E2` adds WAL service mechanics, `M3E3A-D` add snapshot/checkpoint/optional-sidecar mechanics, and `M3E4A-D` add quarantine inventory, mutation, purge, and recovery-classification mechanics. |
| `M3F` | WAL commit payload format | Implementation | Complete | none | `M3F1` through `M3F4` replace `WalRecord` opaque commit payload bytes with a bounded storage-row commit payload, update WAL goldens/spec/fuzz routing, and preserve L4 WAL service mechanics. |
| `M3G` | Immutable table format | Implementation | Complete | none | `M3G1` adds strict table header, footer, and block-frame bytes with uncompressed/zstd support. `M3G2` adds data entries over `StorageRow`, monolithic index payloads, and properties payloads. `M3G3` adds whole-table artifact helpers and cross-block validation. `M3G4` pins table goldens, fuzz routing, source guards, and generated integration coverage. |
| `M3H` | Table object publication | Implementation | Complete | none | `M3H1` adds the L4 table object publication service over layout-owned table object names, V1 immutable-table byte validation, create-only durable publish, publish metadata verification, and cache-mode durable absence coverage. |
| `M3TA` | Format golden tests | Test | Complete | none | `M3C1` adds internal-key and storage-row goldens; `M3C2` adds manifest, watermark, and segment metadata goldens; `M3C3` adds WAL segment, envelope, and record goldens; `M3C4` adds snapshot header, section, and container goldens; `M3E4A` adds quarantine inventory goldens; `M3F` adds row-native WAL commit payload goldens and refreshes WAL record/envelope vectors; `M3G4` adds table frame, payload, and whole-artifact goldens. |
| `M3TB` | Format fuzz tests | Test | Complete | none | `M3C5` adds the first cargo-fuzz package and initial parser targets for manifest, snapshot envelope, storage row, and WAL record bytes. `M3E4A` adds the quarantine inventory decoder target. `M3F4` adds the direct WAL commit payload decoder target and corpus seeds. `M3G4` adds table block and whole-table artifact fuzz targets with golden-seeded corpora. |
| `M3TC` | Durable fault-window tests | Test | Complete | none | `M3TC1` covers durable-publish fault windows; `M3TC2A-G` close the WAL durable-service matrix; `M3TC3A-E` close the snapshot, checkpoint, and sidecar durable-service matrix; `M3TC4A-D` close quarantine inventory, mutation, purge, reconciliation property, and service-fuzz coverage. |
| `M3TD` | Cache-mode durable absence | Test | Complete | none | `M3TD1` proves cache mode does not require durable primitives, writes only caller-requested non-durable cache objects, and creates no durable manifest, table, WAL, sidecar, snapshot, checkpoint, quarantine, lock, or temporary object families. Lifecycle-level cache open/commit/checkpoint assertions are explicitly deferred until storage exposes those entry points. |
| `M3TE` | Manifest service tests | Test | Complete | none | `M3TE1A` through `M3TE1E` closed; manifest services have reference-grade load, update, publish-failure, opacity, and error-precision coverage. |
| `M3TF` | Backend conformance | Test | Complete | none | `M3TF1` reran private backend conformance across memory and local filesystem and external testkit backend selection for both backends. The run fixed a `testkit,localfs` cfg import gap in the integration harness. |
| `M3TG` | Durable integration harnesses | Test | Complete | none | `M3TG1` replaces the crash-recovery, service fault-window, and stress placeholders with storage service-path harnesses through hidden testkit entry points. |
| `M7A` | Inference task traits | Implementation | Planned | none | May start now if parallel inference work is scheduled. |

## M0 Closure Record

M0 is closed by the following artifacts and recorded verification gates:

| Code | Status | Artifact |
|---|---|---|
| `M0TA` | Complete | Transient verification gate; see the M0TA link check record below. |
| `M0TB` | Complete | Transient verification gate; see the M0TB terminology scan record below. |
| `M0TC` | Complete | `docs/architecture/v1-boundary-baseline.md` |
| `M0A` | Complete | `docs/architecture/v1-document-inventory.md` |
| `M0B` | Complete | `docs/architecture/v1-open-question-register.md` |
| `M0C` | Complete | Roadmap and target crate-shape standards alignment. |
| `M0TD` | Complete | `docs/architecture/v1-engineering-standards-baseline.md` |
| `M0TE` | Complete | `docs/architecture/v1-test-inventory.md` |
| `M0D` | Complete | `docs/architecture/v1-progress-tracker.md` |

## M0TA Link Check Record

`M0TA` is a transient verification gate with no separate artifact file. Its
scope was active V1 architecture, product, and spec documents outside archive
directories.

Closure required local markdown links and backticked local document paths to
resolve, except for classified future references to not-yet-created `*-next`
crates and explicit negative examples. During M0, no real broken required
document links were found.

## M0TB Terminology Scan Record

`M0TB` is a transient verification gate with no separate artifact file. Its
scope was active V1 documents, target crate-shape documents, and implementation
plans.

Closure required cleanup-era milestone vocabulary to be removed, moved to
historical context, or classified as intentional evidence. During M0, the only
domain anti-pattern hits in target crate-shape documents were explicit negative
examples that say not to create production types or modules named `Helper`.

## M1 Closure Record

M1 is closed by the following artifacts and verification gates:

| Code | Status | Artifact |
|---|---|---|
| `M1A` | Complete | `crates/core` crate skeleton and workspace membership. |
| `M1TA` | Complete | `crates/core/tests/atom_contract.rs` plus module-local atom tests. |
| `M1B` | Complete | `crates/core/src/branch.rs`, `crates/core/src/version.rs`, and `crates/core/src/time.rs`. |
| `M1C` | Complete | Core atom parse, display, serde, and durable representation tests. |
| `M1TB` | Complete | `crates/core/tests/atom_properties.rs`. |
| `M1TC` | Complete | `crates/core/tests/dependency_guard.rs`. |
| `M1D` | Complete | `docs/architecture/core-architecture.md` Implemented M1 Boundary section. |
| `M1TD` | Complete | `crates/core/tests/public_api_snapshot.rs` and `crates/core/tests/snapshots/public_api.txt`. |

## M2 Closure Record

M2 is closed by the following artifacts and verification gates:

| Code | Status | Artifact |
|---|---|---|
| `M2A` | Complete | `crates/storage` crate skeleton, workspace membership, crate-level unsafe policy, feature declarations, and initial module tree. |
| `M2B` | Complete | Backend capability vocabulary, storage-mode requirements, memory backend capability facts, object name/prefix validation, backend metadata/fence/range types, backend error categories, and minimal synchronous backend trait. |
| `M2C` | Complete | Memory backend implementation and local filesystem backend shell with basic object operations, capability declarations, range reads, metadata, deterministic list/delete behavior, local object-file encoding for prefix-collision safety, symlink rejection on localfs paths, and current-code cross-check against existing cache/local filesystem behavior. |
| `M2TA` | Complete | Private backend conformance smoke in `crates/storage/src/backend/conformance.rs`, covering claimed basic object capabilities, missing-object behavior, range boundaries, overwrite semantics, prefix listing, delete behavior, and unsupported conditional operations for memory and local filesystem backends. |
| `M2D` | Complete | Crate-private `test_support`, feature-gated doc-hidden `testkit`, backend selector parsing, deterministic faulting backend wrapper with a public operation gate, fault script/rule/call types, and coverage across default and no-default testkit/fault-injection builds. |
| `M2TD` | Complete | `crates/storage/tests/testkit_boundary.rs`, with external compile probes proving `testkit` is unavailable without its feature, selector APIs are available with `testkit`, fault APIs require `fault-injection`, and source guards keep testkit exports doc-hidden. |
| `M2E` | Complete | Storage harness scaffolding under `crates/storage/tests/`, `tests/common/mod.rs`, `tests/README.md`, `testdata/goldens/storage-format-v1/README.md`, and `fuzz/`, with smoke entry points for conformance, properties, goldens, fault windows, crash recovery, stress, and API harnesses without durable-behavior claims. |
| `M2TB` | Complete | `cargo hack -p strata-storage --feature-powerset --depth 2 --locked check --all-targets` covers no-default, default, each feature, and pairwise combinations. `cargo test -p strata-storage --no-default-features --features testkit --test backend_conformance --locked` proves unavailable backend selections report a targeted `localfs` feature requirement. |
| `M2TC` | Complete | `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked` proves the memory/cache build compiles on wasm with `localfs` excluded. `cargo test -p strata-storage --test testkit_boundary --locked localfs_feature_is_rejected_for_wasm_builds` proves default-feature wasm builds fail with a targeted `localfs` unsupported message. |

## M3 Execution Record

M3 is active. Closed work is recorded here until the full milestone gate can
move to Complete.

| Code | Status | Artifact |
|---|---|---|
| `M3A1` | Complete | `crates/storage/src/config/mode.rs` adds cache, durable-local standard, durable-local always, and object-durable-candidate backend capability validation. `crates/storage/src/backend/conformance.rs` proves current memory and local filesystem basic object backends accept cache mode and reject durable modes until durable publish, sync, writer-lock, and object-fencing capabilities are implemented. `docs/architecture/archive/implementation-plans/m3-porting-log.md` records the source-map and retirement disposition. |
| `M3B1` | Complete | `crates/storage/src/layout/mod.rs` adds reserved object families and constructors for manifest, WAL segment, table, snapshot, temporary, quarantine, lock, and metadata object names and prefixes. Layout tests prove fixed-width ordered IDs, prefix ownership, component validation, quarantine separation, and follower-state path exclusion. `docs/architecture/archive/implementation-plans/m3-porting-log.md` records the source-map and retirement disposition. |
| `M3B2` | Complete | `crates/storage/src/layout/tests.rs` adds property-style tests for WAL/snapshot ordering, table/temporary/quarantine prefix ownership, and invalid component rejection while keeping the production layout module under the V1 file-size threshold. `crates/storage/tests/object_layout_properties.rs` adds a source guard proving reserved durable layout names stay owned by layout/object/local-fs boundary code. `docs/architecture/archive/implementation-plans/m3-porting-log.md` records the source-map and retirement disposition. |
| `M3C1` | Complete | `crates/storage/src/row/mod.rs` adds storage-space id, physical key, internal key, and storage row records. `crates/storage/src/format/key.rs` and `crates/storage/src/format/storage_row.rs` add strict V1 internal-key and storage-row encoders/decoders. `crates/storage/testdata/goldens/storage-format-v1/` adds golden vectors for ordinary internal keys, zero-byte escaped internal keys, put rows, and tombstone rows. `docs/spec/strata-storage-format-v1.md` records the frozen row format and internal-key ordering exception. |
| `M3C2` | Complete | `crates/storage/src/format/manifest.rs`, `crates/storage/src/format/watermark.rs`, and `crates/storage/src/format/segment_metadata.rs` add strict V1 encoders/decoders for database manifest bytes, snapshot watermark bytes, and WAL segment metadata sidecars, including invalid recovery-fact rejection. `crates/storage/testdata/goldens/storage-format-v1/` adds identity-codec manifest, empty/present watermark, and segment metadata golden vectors. `docs/spec/strata-storage-format-v1.md` records the exact V1 manifest, watermark, and sidecar byte formats. |
| `M3C3` | Complete | `crates/storage/src/format/wal.rs` adds strict V1 encoders/decoders for WAL segment headers, codec-aware outer record envelopes, and inner WAL records. `crates/storage/testdata/goldens/storage-format-v1/` adds segment header, empty-payload record, non-empty-payload record, and envelope golden vectors. `docs/spec/strata-storage-format-v1.md` records the exact V1 WAL header, envelope, and record byte formats. |
| `M3C4` | Complete | `crates/storage/src/format/snapshot.rs` adds strict V1 encoders/decoders for snapshot headers, section envelopes, and whole containers with footer CRC validation. `crates/storage/testdata/goldens/storage-format-v1/` adds identity-codec header, empty-section envelope, and single-section container golden vectors. `docs/spec/strata-storage-format-v1.md` records the exact V1 snapshot container and section envelope byte formats. |
| `M3C5` | Complete | `crates/storage/fuzz/` is now a cargo-fuzz package with initial targets for manifest, snapshot envelope, storage row, and WAL record decoder families. `crates/storage/src/testkit/format_fuzz.rs` exposes a hidden testkit routing surface for fuzz targets without making format internals production API. |
| `M3D1` | Complete | `crates/storage/src/backend/publish.rs` adds the backend publish contract and classified publish-failure windows. `crates/storage/src/backend/local_fs.rs` implements Unix durable publication with unique temporary files, temp file sync, atomic no-clobber create, atomic replace, and parent-directory sync; non-Unix local filesystem builds do not advertise durable publish/sync yet. `crates/storage/src/backend/memory.rs` implements explicit non-durable cache publication. `crates/storage/src/service/publish.rs` exposes the thin L4 publisher wrapper over layout-owned object names. `crates/storage/src/testkit/mod.rs` can now observe and fault publish operations. |
| `M3TC1` | Complete | `crates/storage/src/backend/local_fs.rs` adds backend-local Unix fault hooks and tests for temporary creation, temporary write, temporary sync, final publish, and parent-directory sync. The tests assert classified publish errors, final object visibility, and generated temporary object cleanup without exposing the hooks through the public testkit. |
| `M3E1` | Complete | `crates/storage/src/service/manifest.rs` adds database manifest service mechanics over V1 manifest bytes and payload-opaque table manifest publication over layout-owned table manifest object names. The service distinguishes missing, corrupt, codec-mismatch, invalid-recovery-fact, and publish-window failures without importing branch/table semantics. `crates/storage/src/format/manifest.rs` now rejects present-but-zero snapshot recovery facts before bytes can be persisted. `docs/architecture/archive/implementation-plans/m3e1-manifest-test-suite-plan.md` defines the follow-up `M3TE1` reference-grade manifest test suite. |
| `M3E2` | Complete | `crates/storage/src/backend/mod.rs` adds object-name based append/sync backend primitives and `crates/storage/src/backend/local_fs.rs` implements them for local durable WAL segments. `crates/storage/src/service/wal.rs` adds the durable-local WAL service: segment create/open, append, standard/always durability policy, rotation, strict reads, partial-tail facts, and covered-segment deletion. `crates/storage/src/service/wal/tests.rs` covers cache backend rejection, header validation, append/read roundtrip, policy sync behavior, rotation, corruption, partial tails, and active segment protection. `docs/architecture/archive/implementation-plans/m3e2-wal-test-suite-plan.md` defines the follow-up `M3TC2` reference-grade WAL test suite. |
| `M3E3A` | Complete | `crates/storage/src/service/snapshot.rs` adds snapshot publish/load basics over V1 snapshot container bytes: create-only durable publish, optional and required load, codec and database identity validation, zero snapshot-fact rejection before backend access and after decode, header/object id mismatch rejection, corrupt-byte fail-closed decode routing, publish-failure kind propagation, and `visit_sections` for borrowed section inspection without materializing payloads. `crates/storage/src/format/mod.rs` exposes snapshot container and borrowed visitor format functions to L4, and `crates/storage/src/service/mod.rs` wires the service for later lifecycle/recovery consumers. `docs/architecture/archive/implementation-plans/m3-porting-log.md` records the source-map and retirement disposition. |
| `M3E3B` | Complete | `crates/storage/src/service/snapshot/listing.rs` adds snapshot listing over `ObjectLayout::snapshot_prefix()`, exact lowercase `snapshots/<16-hex-id>` parsing, latest-snapshot selection by highest listed id, and caller-driven pruning with live-snapshot and newest-retained protection. `crates/storage/src/service/snapshot/listing_tests.rs` covers numeric ordering, malformed snapshot-family fail-closed behavior, weak-prefix family ignores, list failure routing, malformed prune listings before any delete, live/newest protection, retain count clamping, delete-failure reports, and delete-capability preflight. `crates/storage/src/service/snapshot.rs` and `crates/storage/src/service/mod.rs` wire the snapshot object/delete report facts for later lifecycle/recovery consumers. |
| `M3E3C` | Complete | `crates/storage/src/service/checkpoint.rs` adds mechanical checkpoint sequencing over database MANIFEST and snapshot services. It validates existing MANIFEST identity, reuses that loaded MANIFEST for active WAL persistence, publishes immutable snapshot bytes, persists snapshot id plus watermark only after snapshot publish success, returns typed orphan snapshot facts for final MANIFEST no-visible failures, and returns typed final-MANIFEST uncertainty for `VisibilityUnknown` / `VisibleDurabilityUnconfirmed`. `crates/storage/src/service/checkpoint/tests.rs` and `tests/support.rs` cover success order, single-load active-WAL update, invalid input facts before mutation, missing/corrupt/mismatched MANIFEST failures before snapshot publish, active-WAL MANIFEST failure, every snapshot publish `PublishFailureKind`, final-update orphan facts, final-MANIFEST uncertainty, direct snapshot loadability, and previous MANIFEST fact preservation. Verification: `cargo test -p strata-storage --locked service::checkpoint`, `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo test -p strata-storage --all-features --locked`, `cargo test -p strata-storage --features testkit,fault-injection --locked`, `cargo test -p strata-storage --no-default-features --features testkit,fault-injection --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage --no-deps --locked`, and `cargo fmt --package strata-storage --check`. Engineering scan: files stay below review thresholds after splitting checkpoint support; no forbidden naming hits except `from_slice` false positive. |
| `M3E3D` | Complete | `crates/storage/src/layout/mod.rs` adds `meta/wal/<16-hex-segment-id>` object naming for optional WAL segment metadata sidecars. `crates/storage/src/service/sidecar.rs` adds `WalSegmentMetadataSidecarService` over the M3C2 segment metadata codec: durable replace publish, load as present/missing/corrupt fallback facts, typed backend read failures, publish failure kind preservation, and non-fatal delete reports. Missing or corrupt sidecars do not affect authoritative WAL or snapshot facts. `crates/storage/src/service/sidecar/tests.rs` covers zero-id rejection across load/publish/delete, memory-backend durable rejection, and local filesystem durable publication. `docs/architecture/storage/l2-object-layout.md` records the implemented sidecar object namespace, and `docs/architecture/archive/implementation-plans/m3-porting-log.md` records the source-map and retirement disposition. Verification: `cargo test -p strata-storage --locked service::sidecar`, `cargo test -p strata-storage --locked wal_segment_metadata`, `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo test -p strata-storage --all-features --locked`, `cargo test -p strata-storage --features testkit,fault-injection --locked`, `cargo test -p strata-storage --no-default-features --features testkit,fault-injection --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage --no-deps --locked`, and `cargo fmt --package strata-storage --check`. |
| `M3TC2A` | Complete | `crates/storage/src/service/wal/tests.rs` adds WAL construction, capability, and listing hardening: exact segment-id and segment-size rejection, exact per-capability open rejection across all seven WAL-required backend capabilities, exact memory-backend unsupported capability routing, missing-segment header creation, existing-segment no-rewrite behavior, malformed header and wrong-database routing, invalid listed WAL object matrix including zero segment id as a list-context invalid object, backend-order-independent segment reads with accepted gaps, adjacent-prefix filtering, and max-segment rotation overflow. The initial full-suite run caught a reserved layout string in the test itself; the test now derives the WAL prefix through `ObjectLayout`. Sensitivity probes: temporarily removed `AppendObject` from `WAL_REQUIRED_CAPABILITIES`, returned `WalServiceError::List` for invalid listed WAL components, and removed segment sorting so listed segments followed backend order; the targeted tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage --locked service::wal` and `cargo test -p strata-storage --no-default-features --locked service::wal`. Broad verification: `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo fmt --package strata-storage --check`, and `git diff --check -- crates/storage/src/service/wal/tests.rs docs/architecture/v1-progress-tracker.md docs/architecture/archive/implementation-plans/m3tc2-wal-test-implementation-plan.md docs/architecture/archive/implementation-plans/m3e2-wal-test-suite-plan.md`. Engineering scan: no forbidden naming hits; roadmap-label scan hits only `from_slice` false positives. |
| `M3TC2B` | Complete | `crates/storage/src/service/wal/tests/append.rs` adds WAL append and rotation hardening: exact-fill records stay in the current segment, one-byte-over records rotate before append, oversized records fail before backend mutation, active metadata mismatch is rejected before append both before and after rotation, backend append failure and wrong append reports leave service state clean, and a proptest append model checks 1 to 128 records across 1 KiB to 8 KiB segment sizes against segment ids, offsets, dirty facts, active metadata, reopened reads, and object-size bounds. `crates/storage/src/service/wal/tests/localfs.rs` holds localfs-only WAL behavior after the split from the root test module, keeping each unit-test file below the engineering-standard split threshold. Sensitivity probes: temporarily changed rotation from `>` to `>=`, bypassed record-too-large preflight, and disabled active metadata tracking; targeted tests or the append model failed under each mutation and passed after revert. Narrow verification: `PROPTEST_CASES=2048 cargo test -p strata-storage --locked wal_append_model`, `cargo test -p strata-storage --locked service::wal`, and `cargo test -p strata-storage --no-default-features --locked service::wal`. Broad verification: `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo test -p strata-storage --all-features --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage --no-deps --locked`, `cargo fmt --package strata-storage --check`, and `git diff --check -- crates/storage/src/service/wal.rs crates/storage/src/service/wal/tests.rs crates/storage/src/service/wal/tests/append.rs crates/storage/src/service/wal/tests/localfs.rs`. Engineering scan: no forbidden naming hits; roadmap-label scan hits only Rust `slice` API false positives. |
| `M3TC2C` | Complete | `crates/storage/src/service/wal/tests/durability.rs` adds WAL durability-policy and sync-failure hardening over a sync-fault backend. `standard` tests prove append does not force sync, dirty bytes and records are tracked, `force_durable` and `close` sync the active segment and clear counters on success, and both force/close sync failures return typed sync errors while preserving dirty facts. `always` tests prove append syncs before success, successful appends return `forced_durable = true` with clean counters, and failed sync after a successful backend append leaves the record visible with durability unconfirmed: active segment id and object size advanced, active metadata updated, dirty counters non-zero, `read_all` returns the record without truncation, and a later successful append resumes at the visible offset. Sync error routing preserves `NotFound`, `Interrupted`, and `Unavailable` as backend sync failures. Sensitivity probes: temporarily cleared dirty counters on `force_durable` failure and temporarily removed active metadata tracking before sync; targeted durability tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage --locked service::wal::tests::durability` and `cargo test -p strata-storage --locked service::wal`. Broad verification: `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo test -p strata-storage --all-features --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage --no-deps --locked`, `cargo fmt --package strata-storage --check`, and `git diff --check -- crates/storage/src/service/wal.rs crates/storage/src/service/wal/tests.rs crates/storage/src/service/wal/tests/durability.rs docs/architecture/v1-progress-tracker.md docs/architecture/archive/implementation-plans/m3tc2-wal-test-implementation-plan.md docs/architecture/archive/implementation-plans/m3e2-wal-test-suite-plan.md`. |
| `M3TC2D` | Complete | `crates/storage/src/service/wal/tests/corruption.rs` adds the WAL corruption matrix with named byte-boundary mutations and exact inner `FormatError` assertions for the load-bearing source kind. Coverage includes segment header magic/version/checksum/segment-id/database-id cases, envelope length and checksum corruption, final-envelope truncation, non-latest truncation, encoded-record byte mutation, inner record length and length-CRC corruption, payload CRC and payload-byte corruption, branch-id byte and truncation corruption, timestamp truncation, and latest versus non-latest trailing garbage. The tests keep the header-id asymmetry explicit: segment-id mismatch is `Format`, while a valid header checksum for another database id is `DatabaseMismatch`. `crates/storage/src/service/wal/tests/support.rs` now owns shared WAL test builders and fake backends so WAL test files stay below the engineering-standard review threshold. Sensitivity probes: temporarily treated all partial tails as recoverable, temporarily mapped database-id mismatch to `Format`, temporarily mapped segment-id mismatch to `DatabaseMismatch`, and temporarily skipped payload CRC verification; targeted corruption tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage --locked service::wal::tests::corruption` and `cargo test -p strata-storage --locked service::wal`. Broad verification: `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo test -p strata-storage --all-features --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage --no-deps --locked`, `cargo fmt --package strata-storage --check`, and `git diff --check -- crates/storage/src/service/wal.rs crates/storage/src/service/wal/tests.rs crates/storage/src/service/wal/tests/support.rs crates/storage/src/service/wal/tests/corruption.rs docs/architecture/v1-progress-tracker.md docs/architecture/archive/implementation-plans/m3tc2-wal-test-implementation-plan.md docs/architecture/archive/implementation-plans/m3e2-wal-test-suite-plan.md`. |
| `M3TC2E` | Complete | `crates/storage/src/service/wal/tests/fault_windows.rs` adds WAL backend fault-window and partial-visibility hardening. Coverage includes active-segment create publish failures, metadata failure before append, list and read failures during replay, delete failure reporting without hiding successful deletes or protected segments, visible partial append failures, visible partial append with misleading metadata, latest partial-tail reopen facts, and non-latest partial-tail corruption. Sensitivity probes: temporarily advanced dirty state before append backend errors, temporarily ignored delete failures, and temporarily treated non-latest partial tails as recoverable; targeted fault-window tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage --locked service::wal::tests::fault_windows` and `cargo test -p strata-storage --locked service::wal`. Broad verification: `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo test -p strata-storage --all-features --locked`, `cargo test -p strata-storage --features testkit,fault-injection --locked`, `cargo test -p strata-storage --no-default-features --features testkit,fault-injection --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage --no-deps --locked`, `cargo fmt --package strata-storage --check`, and `git diff --check -- crates/storage/src/service/wal.rs crates/storage/src/service/wal/tests.rs crates/storage/src/service/wal/tests/support.rs crates/storage/src/service/wal/tests/fault_windows.rs docs/architecture/v1-progress-tracker.md docs/architecture/archive/implementation-plans/m3tc2-wal-test-implementation-plan.md docs/architecture/archive/implementation-plans/m3e2-wal-test-suite-plan.md`. |
| `M3TC2F` | Complete | `crates/storage/src/service/wal/tests/retention_reopen.rs` adds WAL retention/deletion and deterministic reopen hardening. Retention coverage includes covered old segment deletion, explicit `DeleteObject` capability refusal, active and newer segment protection, above-watermark segment protection, delete failure reporting, non-WAL object exclusion, no-covered-segment reports, and deterministic sorted reports under unsorted backend listings. Reopen coverage includes clean standard close, dirty standard append, always append, rotation, latest partial tail with same-segment and proven would-rotate append refusal, non-latest partial tail strict failure, and corrupt header strict failure. `crates/storage/src/service/wal/tests/support.rs` now advertises `DeleteObject` for the stored-byte WAL backend because it implements delete and is the module-local retention backend. Process-kill windows remain deferred to L7/L8. Sensitivity probes: temporarily allowed active segment deletion, temporarily deleted segments when any record was below the retention watermark, and temporarily reopened latest partial tails at the physical object size instead of the valid end offset; targeted retention/reopen tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage --locked service::wal::tests::retention_reopen`, `cargo test -p strata-storage --locked service::wal`, and `cargo test -p strata-storage --no-default-features --locked service::wal`. Broad verification: `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo test -p strata-storage --all-features --locked`, `cargo test -p strata-storage --features testkit,fault-injection --locked`, `cargo test -p strata-storage --no-default-features --features testkit,fault-injection --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage --no-deps --locked`, `cargo fmt --package strata-storage --check`, `cargo hack -p strata-storage --feature-powerset --depth 2 --locked check --all-targets`, and `git diff --check -- crates/storage/src/service/wal.rs crates/storage/src/service/wal/tests.rs crates/storage/src/service/wal/tests/support.rs crates/storage/src/service/wal/tests/retention_reopen.rs docs/architecture/v1-progress-tracker.md docs/architecture/archive/implementation-plans/m3e2-wal-test-suite-plan.md`. |
| `M3TC2G` | Complete | `crates/storage/src/service/wal/tests/read.rs` adds explicit read/watermark hardening for zero and max watermarks, duplicate commit versions, out-of-order commit versions, and mixed branch ids. `crates/storage/src/service/wal/tests/corruption.rs` adds the shortened-envelope-length mutation with a refreshed envelope CRC. `crates/storage/src/service/wal/tests.rs` and `durability.rs` add review-gap assertions for empty existing segment objects, open-time metadata failure routing, and clean close without sync. The append property model now includes larger payloads to exercise rotation-edge paths more often. Sensitivity probes: temporarily changed `read_after_commit_version` from `>` to `>=`, temporarily sorted reads by commit version before filtering, and temporarily made `close` sync unconditionally; targeted tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage --locked service::wal::tests::read`, `cargo test -p strata-storage --locked service::wal::tests::corruption::corrupt_envelope_shortened_length_is_inner_record_format_error`, `cargo test -p strata-storage --locked service::wal`, and `PROPTEST_CASES=2048 cargo test -p strata-storage --locked wal_append_model`. Broad verification: see final implementation note. |
| `M3TC3A` | Complete | `crates/storage/src/service/snapshot/publish_load_tests.rs` adds snapshot construction, publish, load, validation, and capability hardening. Suite cases covered: `ReadObject` capability preflight for load, durable publish/sync capability preflight for snapshot publication, invalid codec id rejection before backend publish, section-kind-zero construction rejection, exact layout object use, publish outcome metadata and durability facts, multi-section roundtrip ordering and opaque payload preservation, backend read failure routing, service-level future/pre-V1/checksum/truncation/invalid-section decode routing, and snapshot load validation through `load_required_for_codec`. `crates/storage/src/service/snapshot.rs` now checks `ReadObject` capability before reading a snapshot object. Sensitivity probe: temporarily bypassed the `ReadObject` preflight; `snapshot_load_requires_read_capability_before_backend_access` failed by reaching the backend panic, then passed after revert. Narrow verification: `cargo test -p strata-storage --locked snapshot_load_requires_read_capability_before_backend_access`, `cargo test -p strata-storage --locked service::snapshot`, and `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`. Broad verification: `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo test -p strata-storage --all-features --locked`, `cargo fmt --package strata-storage --check`, `cargo doc -p strata-storage --no-deps --locked`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo hack -p strata-storage --feature-powerset --depth 2 --locked check --all-targets`, and `git diff --check`. |
| `M3TC3B` | Complete | `crates/storage/src/service/snapshot/listing_tests.rs` and `crates/storage/src/service/snapshot/listing_property_tests.rs` add snapshot listing, latest, prune, and model hardening. Suite cases covered: empty and ordered listing, exact lowercase snapshot-family parsing, `ListPrefix` capability preflight before parsing, malformed snapshot-family fail-closed behavior, weak-prefix adjacent-family ignores, typed list failures without reads, empty prune reports, retain-count no-op behavior, retain-zero clamping, live MANIFEST snapshot protection, newest-retained protection, sorted deleted/protected/failed reports under unsorted backend listings, per-object delete failure reporting, non-snapshot-family delete exclusion, zero-live rejection before listing, delete-capability preflight before listing, and a host-only `proptest` model over 1 to 96 create/list/latest/prune operations with adjacent-family noise. The property model was later expanded during `M3TC3E` closeout to use real snapshot publish/load service calls and sidecar publish/corrupt/load service calls, closing the full M3TC3 state-machine requirement. Sensitivity probe: temporarily disabled live-snapshot protection during prune; `prune_snapshots_protects_live_snapshot_and_newest_retained` failed by attempting to delete the live snapshot, then passed after revert. Narrow verification: `PROPTEST_CASES=2048 cargo test -p strata-storage --locked snapshot_service_state_machine_matches_model`, `cargo test -p strata-storage --locked prune_snapshots_protects_live_snapshot_and_newest_retained`, and `cargo test -p strata-storage --locked service::snapshot`. Broad verification: `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo test -p strata-storage --all-features --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo doc -p strata-storage --no-deps --locked`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo hack -p strata-storage --feature-powerset --depth 2 --locked check --all-targets`, `cargo fmt --package strata-storage --check`, `git diff --check`, and engineering scans for forbidden names, roadmap labels, production panics, and file-size thresholds. |
| `M3TC3C` | Complete | `crates/storage/src/service/checkpoint/tests/sequencing.rs` adds checkpoint sequencing hardening without growing the existing checkpoint test file beyond the engineering review threshold. Suite cases covered: every active-WAL MANIFEST publish-failure kind stops before snapshot publish, manifest codec/database identity failures leave checkpoint work unstarted, final-MANIFEST uncertainty preserves the exact operation order and leaves the already published snapshot directly loadable, and existing checkpoint tests continue to cover success ordering, invalid input facts before mutation, snapshot publish failures before final facts, orphan-snapshot classification, final-MANIFEST uncertainty classification, and previous MANIFEST fact preservation. Sensitivity probe: temporarily persisted snapshot facts before snapshot publish; `snapshot_publish_failures_do_not_persist_snapshot_manifest_facts` failed with an extra `SnapshotFactsManifest` event before the snapshot publish, then passed after revert. Narrow verification: `cargo test -p strata-storage --locked service::checkpoint` and `cargo test -p strata-storage --locked snapshot_publish_failures_do_not_persist_snapshot_manifest_facts`. Broad verification: `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo test -p strata-storage --all-features --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo doc -p strata-storage --no-deps --locked`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo fmt --package strata-storage --check`, `git diff --check`, and engineering scans for forbidden names, roadmap labels, production panics, and file-size thresholds. |
| `M3TC3D` | Complete | `crates/storage/src/service/sidecar/tests/optional_fallback.rs` adds sidecar optional-fallback hardening without expanding the existing sidecar test module further. Suite cases covered: sidecar object construction delegates to the layout namespace, segment id `0` is rejected before backend access, corrupt sidecar fallback preserves the specific decode reason for invalid magic, future version, pre-V1 version, CRC mismatch, and trailing bytes, missing and corrupt sidecars leave authoritative WAL bytes untouched, and both uncertain sidecar publish kinds leave WAL and manifest recovery facts unchanged. `VisibilityUnknown` remains missing in the test harness; `VisibleDurabilityUnconfirmed` leaves the sidecar observable as an optional cache fact. `crates/storage/src/service/sidecar/tests.rs` now models `VisibleDurabilityUnconfirmed` by making sidecar bytes visible before returning the publish error, so fault-window tests exercise the post-visibility path. Sensitivity probes: temporarily downgraded sidecar segment-id mismatch from corrupt to missing; `sidecar_segment_id_mismatch_returns_recoverable_corrupt_fact` failed. Temporarily stopped the test backend from making `VisibleDurabilityUnconfirmed` sidecar bytes visible; `sidecar_publish_uncertainty_does_not_mutate_authoritative_recovery_facts` failed. Both probes passed after revert. Broad verification also caught and fixed an object-layout source-guard violation from uppercase reserved layout vocabulary in a checkpoint test comment. Narrow verification: `cargo test -p strata-storage --locked service::sidecar`, `cargo test -p strata-storage --locked sidecar_segment_id_mismatch_returns_recoverable_corrupt_fact`, and `cargo test -p strata-storage --locked sidecar_publish_uncertainty_does_not_mutate_authoritative_recovery_facts`. Broad verification: `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo test -p strata-storage --all-features --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo doc -p strata-storage --no-deps --locked`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo fmt --package strata-storage --check`, `git diff --check`, and engineering scans for forbidden names, roadmap labels, production panics, and file-size thresholds. |
| `M3TC3E` | Complete | `crates/storage/src/service/snapshot/publish_fault_tests.rs` adds the service-boundary snapshot publish fault-window matrix. Suite cases covered: temporary write and temporary sync failures return `FailedBeforeVisibility` with no visible snapshot object, duplicate immutable snapshot create returns `PreconditionFailed` and preserves prior bytes, backend-reported `VisibilityUnknown` is preserved at the snapshot service boundary, and local filesystem parent-directory sync failure returns `VisibleDurabilityUnconfirmed` while leaving the snapshot directly loadable. Existing checkpoint and sidecar suites cover the remaining M3TC3E matrix rows: snapshot publish failures stop before final MANIFEST snapshot facts, final MANIFEST no-visible failures return orphan snapshot facts, final MANIFEST uncertainty is not reported as an orphan, and sidecar publish uncertainty does not mutate authoritative WAL or snapshot facts. `crates/storage/src/backend/local_fs.rs` exposes test-only publish fault injectors for snapshot service tests without adding public testkit API. `crates/storage/src/service/snapshot/listing_property_tests.rs` now closes the full M3TC3 service state-machine requirement with generated create snapshot, load snapshot, list, latest, prune, publish sidecar, corrupt sidecar, and load sidecar operations. `crates/storage/src/testkit/service_fuzz.rs` and `crates/storage/fuzz/fuzz_targets/service_snapshot.rs` pull service fuzzing forward through a hidden bounded operation script over a durable in-memory backend; the existing M3C5 `format_snapshot_envelope` target remains the byte-decoder fuzz target. Sensitivity probe: temporarily collapsed parent-directory sync failure to `FailedBeforeVisibility`; `snapshot_publish_parent_sync_fault_leaves_snapshot_visible_but_unconfirmed` failed, then passed after revert. Narrow verification: `cargo test -p strata-storage --locked service::snapshot::publish_fault_tests`, `PROPTEST_CASES=2048 cargo test -p strata-storage --locked snapshot_service_state_machine_matches_model`, `cargo test -p strata-storage --locked service_fuzz`, `cd crates/storage && cargo +nightly fuzz run service_snapshot -- -runs=1024`, `cargo test -p strata-storage --locked service::snapshot`, `cargo test -p strata-storage --locked service::checkpoint`, and `cargo test -p strata-storage --locked service::sidecar`. Broad verification: `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo test -p strata-storage --all-features --locked`, `cargo test -p strata-storage --features testkit,fault-injection --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo doc -p strata-storage --no-deps --locked`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo hack -p strata-storage --feature-powerset --depth 2 --locked check --all-targets`, `cargo fmt --package strata-storage --check`, and `git diff --check`. |
| `M3E4A` | Complete | `crates/storage/src/format/quarantine.rs` adds the V1 quarantine inventory codec over `STQI` bytes with database id, raw `BranchId`, codec id, canonical inventory entries, object id/source object validation, duplicate rejection, CRC32 protection, pre-V1/future-version rejection, and old `STRAQRTN` rejection. `docs/spec/strata-storage-format-v1.md` specifies the durable byte shape, `crates/storage/testdata/goldens/storage-format-v1/quarantine-inventory-empty.hex` and `quarantine-inventory-multi-entry.hex` lock the empty and multi-entry vectors, and `crates/storage/fuzz/fuzz_targets/format_quarantine.rs` adds the cargo-fuzz decoder target through hidden testkit wiring. `docs/architecture/archive/implementation-plans/m3-porting-log.md` records the old quarantine source map and V1 retirement decisions. Sensitivity probe: temporarily bypassed duplicate object-id validation; `inventory_rejects_duplicate_object_ids` failed by accepting two entries with the same object id, then passed after revert. Narrow verification: `cargo test -p strata-storage --locked format::quarantine`, `cargo test -p strata-storage --locked quarantine_inventory`, `cargo test -p strata-storage --features testkit --locked format_fuzz_decoders_reject_empty_input_without_panicking`, `cargo test -p strata-storage --locked reserved_layout_names_stay_owned_by_layout_module`, and `cd crates/storage && cargo +nightly fuzz run format_quarantine -- -runs=4096`. Broad verification: `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo test -p strata-storage --all-features --locked`, `cargo test -p strata-storage --features testkit,fault-injection --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo doc -p strata-storage --no-deps --locked`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo hack -p strata-storage --feature-powerset --depth 2 --locked check --all-targets`, `cargo fmt --package strata-storage --check`, and `git diff --check`. |
| `M3E4B` | Complete | `crates/storage/src/service/quarantine.rs` adds quarantine inventory service mechanics over storage backends and the M3E4A inventory codec. The service supports absent-as-empty optional loads, required loads that distinguish missing from corrupt inventory bytes, read-capability preflight, typed backend read errors, database/branch/codec mismatch errors, durable replace publication over missing or existing inventory objects, explicit empty-inventory publication as a drained state, and result facts for object name, branch id, entry count, byte count, and publish outcome. `crates/storage/src/service/quarantine/tests.rs` covers the inventory load/publish matrix, all five publish failure kinds, no-visible-replacement byte preservation, memory-backend durable rejection, explicit `DurablePublish`-without-`DurableSync` preflight rejection before backend publish, visible-but-unconfirmed replacement bytes, and reserved-layout source guard compatibility by deriving source objects through `ObjectLayout`. Sensitivity probe: temporarily bypassed codec mismatch validation; `inventory_load_rejects_identity_mismatches` failed by accepting an inventory with the wrong codec id, then passed after revert. Narrow verification: `cargo test -p strata-storage --locked service::quarantine`, `cargo test -p strata-storage --no-default-features --locked service::quarantine`, `cargo test -p strata-storage --all-features --locked service::quarantine`, and `cargo test -p strata-storage --locked reserved_layout_names_stay_owned_by_layout_module`. Broad verification: `cargo test -p strata-storage --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo fmt --package strata-storage --check`, `git diff --check`, and engineering scans for forbidden names, roadmap labels, raw reserved path literals, production panics, and file-size thresholds. |
| `M3E4C` | Complete | `crates/storage/src/service/quarantine/mutation.rs` and `mutation/types.rs` add gate-checked quarantine copy and purge mechanics over the quarantine inventory service. The quarantine path validates safe-gate and request facts before backend mutation, rejects inventory/source drift, verifies source metadata byte counts, publishes inventory before durable quarantine object create, deletes the source only after copy success, and returns typed reports for success, already-quarantined state, source-delete retry, inventory publish failure or uncertainty, quarantine publish failure or uncertainty, and delete failure or missing-source races. The purge path deletes only inventory-listed quarantine objects, preserves unlisted adjacent objects, separates deleted/already-missing/failed outcomes, retains failed entries for retry, rewrites the inventory to retained entries, and preserves delete reports even when the inventory rewrite publish fails. `crates/storage/src/service/quarantine/tests/mutation.rs` and `tests/mutation/purge.rs` cover success ordering, unsafe gates, source read/metadata/missing failures, corrupt inventory, metadata size mismatch, all inventory-publish failure kinds, quarantine-publish uncertainty, existing-entry idempotency and drift checks, source-delete failure/missing races, purge gate preflight, empty purge without delete capability, delete failure retention, missing object removal, and rewrite failure reporting. Sensitivity probe: temporarily bypassed `validate_gate`; `quarantine_unsafe_gate_fails_before_backend_access` and `purge_unsafe_gate_fails_before_delete_or_rewrite` failed by performing mutations, then passed after revert. Narrow verification: `cargo test -p strata-storage --locked service::quarantine`, `cargo test -p strata-storage --no-default-features --locked service::quarantine`, `cargo test -p strata-storage --all-features --locked service::quarantine`, and `cargo test -p strata-storage --locked reserved_layout_names_stay_owned_by_layout_module`. Broad verification: `cargo test -p strata-storage --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage --no-deps --locked`, `cargo fmt --package strata-storage --check`, `git diff --check`, and engineering scans for forbidden names, roadmap labels, raw reserved path literals, production panics, and file-size thresholds. |
| `M3E4D` | Complete | `crates/storage/src/service/quarantine/reconcile.rs` adds read-only quarantine reconciliation and recovery classifications over branch-local and whole-family quarantine listings. Branch reconciliation now distinguishes clean absent state, explicit clean inventory, corrupt inventory bytes or identity mismatches, missing inventory-listed quarantine objects, unlisted quarantine objects, malformed branch-local object ids, inventory read failures, and branch-list failures. Family reconciliation parses the global quarantine namespace, reports malformed branch path text that branch-local scans cannot discover, routes malformed object ids with valid branch text into that branch report, and ignores adjacent-family weak-prefix matches. The report DTOs classify recovery state as healthy, policy-downgraded, or unavailable without string matching and carry branch id, inventory object, listed/missing/unlisted object facts, malformed object facts, corrupt inventory source facts, and backend operation/source facts. Comments document the empty-vs-orphan distinction, corrupt-inventory precedence, identity-mismatch recovery treatment, and family-vs-branch malformed-name split. Review follow-up added absent-inventory-with-orphan coverage, branch and codec mismatch recovery facts, corrupt-inventory visible-object retention, and family-level policy severity precedence. `docs/architecture/archive/implementation-plans/m3-porting-log.md` records the old-to-new source map and retirement notes. Sensitivity probe: temporarily ignored unlisted quarantine objects during classification; `reconcile_unlisted_quarantine_object_is_policy_downgrade` failed by returning `CleanInventory`, then passed after revert. Narrow verification: `cargo test -p strata-storage --locked service::quarantine`, `cargo test -p strata-storage --locked reconcile_unlisted_quarantine_object_is_policy_downgrade`, and `cargo test -p strata-storage --locked reserved_layout_names_stay_owned_by_layout_module`. Broad verification: `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo test -p strata-storage --all-features --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage --no-deps --locked`, `cargo fmt --package strata-storage --check`, `git diff --check`, and engineering scans for forbidden names, roadmap labels, raw reserved path literals, production panics, and file-size thresholds. |
| `M3F1` | Complete | `crates/storage/src/format/wal/commit_payload.rs` adds the `STCP` WAL commit payload codec and `WalCommitPayload` type over bounded batches of `StorageRow` values. The codec rejects empty row lists, bad magic, pre-V1 and future versions, oversized row counts including `u32::MAX`, zero and oversized row lengths, truncated row frames, nested storage-row version/flag/tombstone-payload failures, and trailing bytes, while preserving row order and exact row facts. The payload validator checks row commit version, branch id, and commit timestamp against the outer WAL record facts without interpreting product semantics. Review follow-up added the missing 1-to-64 row proptest model with put/tombstone rows, duplicate physical-key generation, values up to 512 bytes, and regression persistence at `proptest-regressions/wal_commit_payload.txt`; the former `wal_commit_payload_model` command now runs a real test instead of matching zero tests. Sensitivity probes: temporarily allowed zero row count, skipped branch-id outer-fact validation, and ignored trailing payload bytes; `wal_commit_payload_rejects_empty_rows`, `wal_commit_payload_validates_rows_against_outer_wal_facts`, and `wal_commit_payload_rejects_trailing_bytes` failed under those mutations and passed after revert. Narrow verification: `cargo test -p strata-storage --locked --lib wal_commit_payload`, `cargo test -p strata-storage --no-default-features --locked --lib wal_commit_payload_model`, and `cargo test -p strata-storage --locked --lib format::wal`. |
| `M3F2` | Complete | `crates/storage/src/format/wal.rs` now stores a typed `WalCommitPayload` inside `WalRecord` instead of arbitrary `Vec<u8>` commit bytes. `WalRecord::new` validates payload rows against outer WAL facts, WAL record encode nests the row-native payload bytes, WAL record decode validates the decoded payload after CRC checks, and tests cover constructor mismatches, decode-time nested fact mismatches, old empty-payload rejection, pre-V1/future-version routing, length/payload CRC boundaries, exact consumed byte counts, duplicate row preservation, and valid put/tombstone payload records. Review follow-up added WAL-record-level put-plus-tombstone and duplicate-physical-key round trips, not only payload-level coverage. `docs/spec/strata-storage-format-v1.md`, `crates/storage/testdata/goldens/storage-format-v1/`, `crates/storage/src/format/tests.rs`, and `crates/storage/tests/format_golden.rs` now treat old empty WAL payload bytes as a historical malformed fixture and lock the row-native payload, WAL record, and envelope vectors. Narrow verification: `cargo test -p strata-storage --locked --lib wal_record`, `cargo test -p strata-storage --locked --lib wal_`, and `cargo test -p strata-storage --locked format_fuzz_corpus_matches_golden_vectors`. |
| `M3F3` | Complete | `crates/storage/src/service/wal/tests/support.rs`, `read.rs`, `append.rs`, `localfs.rs`, `retention_reopen.rs`, and cache-mode support now construct valid WAL service records through `StorageRow` values and typed `WalCommitPayload`. Service tests prove single-row and multi-row payloads survive append/read/reopen, row order is preserved, branch-id facts remain uninterpreted by L4, duplicate and out-of-order commit-version filtering still depends on the outer WAL commit version, and record-too-large/rotation checks account for row-native payload overhead. Review follow-up added service corruption tests that refresh the inner record CRC so malformed commit-payload magic and nested row versions are reached as row-payload validation errors, plus latest and non-latest truncation tests that cut physically inside commit-payload row bytes. Earlier review found a real size-bound fallout: old 900-byte opaque fixtures no longer fit because row framing adds overhead, so exact-frame helpers and a narrower property payload range now exercise the intended segment-boundary cases without blessing invalid opaque payloads. Narrow verification: `cargo test -p strata-storage --locked --lib service::wal::tests::corruption`, `cargo test -p strata-storage --locked --lib service::wal`, and `cargo test -p strata-storage --no-default-features --locked --lib service::wal`. |
| `M3F4` | Complete | `crates/storage/src/format/fuzzing.rs`, `src/testkit/format_fuzz.rs`, and `fuzz/fuzz_targets/format_wal_commit_payload.rs` add direct fuzz routing for the row-native WAL commit payload decoder. `crates/storage/fuzz/Cargo.toml`, fuzz README files, `.gitignore`, and `fuzz/corpus/format_wal_commit_payload/` register the target and seed it from the checked-in one-put and put-plus-tombstone golden bytes; `format_golden` now verifies fuzz corpus seeds do not drift from their golden vectors. This keeps `format_wal_record` as the outer-record route while adding direct coverage for row-count and row-length guardrails. Narrow verification: `cargo test -p strata-storage --features testkit --locked format_fuzz_decoders_reject_empty_input_without_panicking`, `cargo test -p strata-storage --locked format_fuzz_corpus_matches_golden_vectors`, and `cd crates/storage && cargo +nightly fuzz run format_wal_commit_payload -- -runs=1024`. Broad verification: `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo fmt --package strata-storage --check`, and `git diff --check`. |
| `M3F5` | Complete | `crates/storage/tests/wal_commit_payload_source_guard.rs` closes the M3F test-plan source-scan requirement. The guard scans WAL format, WAL service tests, WAL fuzz targets, format-golden registration, and format-fuzz routing for legacy valid-payload vocabulary such as `EntityRef`, MessagePack, primitive, transaction, JSON, graph, engine-payload, vector-payload, and search-payload wording. A second guard checks the storage WAL format crate files and manifest do not import or depend on engine payload code. The scan intentionally allows `StorageSpaceId::engine` because that names the storage-space atom used inside `StorageRow`, not an engine-owned WAL payload. Review sensitivity: a literal `MessagePack` valid-payload comment in a scanned WAL file fails `wal_commit_payload_surfaces_do_not_use_legacy_payload_vocabulary`; adding a `strata-engine` manifest dependency fails `wal_format_source_does_not_import_engine_crates`. Narrow verification: `cargo test -p strata-storage --locked --test wal_commit_payload_source_guard`. |
| `M3G1` | Complete | `crates/storage/src/format/table/mod.rs` and `table/tests.rs` add the first immutable table byte layer: strict 64-byte `STTB` headers, strict 64-byte `STTF` footers, and table block frames with uncompressed and zstd payload support. Header tests cover magic, pre-V1/future version routing, header size, flags, reserved bytes, target block size, bounded data-block count, bounded row count, commit-range facts, truncation, and old `STRAKV` rejection. Footer tests cover absent-filter enforcement, footer CRC-before-offset trust, index/properties offsets and lengths, overlap, overflow, hidden bytes between index/properties or before the footer, reserved bytes, magic, fixed-field truncation boundaries, and truncation. Block-frame tests cover uncompressed and zstd round trips, nonzero decoded payloads for all M3G block kinds, reserved filter block rejection, unknown block type/compression, flags, length mismatch, zstd decode failures, encoded/decoded allocation bounds, checksum failure, truncation, exact consumed byte count, and expected-kind rejection. `docs/spec/strata-storage-format-v1.md` now records the stable M3G1 table envelope, header, footer, and block-frame facts instead of old v7 table evidence. Native builds use `zstd`; wasm builds use `zstd-pure-rs` because the C-backed zstd crate cannot target `wasm32-unknown-unknown`. Sensitivity probe: temporarily accepted reserved block type `3` as a properties block; `table_block_frame_rejects_reserved_or_unknown_block_type` failed by decoding the frame, then passed after revert. Narrow verification: `cargo test -p strata-storage --locked format::table`, `cargo test -p strata-storage --no-default-features --locked format::table`, and `cargo test -p strata-storage --lib table_block_frame_rejects_reserved_or_unknown_block_type`. Broad verification: `cargo test -p strata-storage --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo fmt --package strata-storage --check`, and `git diff --check`. |
| `M3G2` | Complete | `crates/storage/src/format/table/data.rs`, `index.rs`, and `properties.rs` add strict V1 table payload codecs on top of the M3G1 frame layer. Data block payloads store sorted `InternalKey` bytes plus nested `StorageRow` bytes, reject empty or oversized entry counts and lengths, propagate nested key/row decode failures including storage-row flags and tombstone payloads, reject key/row fact drift, reject duplicate or unsorted internal keys, allow duplicate physical keys at different versions, preserve row order, and reject trailing bytes. Index block payloads carry a versioned monolithic index with sorted non-overlapping key ranges, absolute data-block offsets, frame lengths, and row counts; payload-local validation rejects pre-V1/future versions, invalid keys, zero frame lengths, zero row counts, malformed counts/lengths, overlap, unsorted entries, and trailing bytes, while whole-table frame validation remains M3G3. Data and index decoders now avoid preallocating vectors from attacker-controlled max-valid entry counts; max-count/truncated payload tests prove they fail without large allocation. Properties payloads carry versioned row/block/commit/key-range facts, derive facts from ordered data blocks, reject invalid counts, commit-range and key-range drift, malformed keys, trailing bytes, and impossible block-range overlap. `docs/spec/strata-storage-format-v1.md` now specifies the stable V1 data/index/properties payload bytes and validation rules. Sensitivity probes: review found and fixed a real helper gap where `TableProperties::from_data_blocks` could derive properties from overlapping caller-supplied blocks; `table_properties_from_data_blocks_rejects_overlapping_block_ranges` now fails if block-overlap validation is bypassed. Review follow-up removed `Vec::with_capacity(entry_count)` from data/index durable decoders; max-count truncation tests would expose reintroducing large preallocation before byte feasibility is known. Narrow verification: `cargo test -p strata-storage --locked --lib format::table`, `cargo test -p strata-storage --no-default-features --locked --lib format::table`, `cargo test -p strata-storage --all-features --locked --lib format::table`, and `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`. Broad verification includes wasm/no-default check, fmt, and diff whitespace gates. |
| `M3G3` | Complete | `crates/storage/src/format/table/artifact.rs` adds whole-table immutable table artifact helpers that compose the `STTB` header, framed data blocks, monolithic index frame, properties frame, and `STTF` footer. Encoding builds table-level properties from `StorageRow` data blocks, writes index entries with absolute data-frame offsets and encoded frame lengths, supports uncompressed/zstd/mixed data-block compression, and preflights empty input, zero rows-per-block, zero target data-block size, oversized row counts, and oversized data-block counts before durable bytes are emitted. Decoding validates the footer CRC before trusting footer offsets, decodes exact index/properties ranges, decodes data frames only within the header-to-index region, rejects hidden bytes between regions, derives row/block/commit/key facts from decoded data blocks, and cross-checks header, properties, index entries, actual data-frame offsets, frame lengths, key ranges, and row counts before returning rows in table order. `crates/storage/src/format/table/artifact_tests.rs` covers one-block, two-block, zstd, and mixed-compression round trips; empty, unsorted, duplicate, and mismatched compression construction rejection; header fact drift; properties row/block/commit/min-key/max-key drift after valid CRCs; footer CRC-before-block-fact trust; wrong index/properties frame types; missing and extra data blocks; index offsets before the header, offset overflow, frame-length drift, row-count drift, first-key drift, last-key drift, and references to non-data frames; hidden bytes between data/index/properties/footer; short tables; impossible footer offsets; mixed header/body facts; and opaque storage-space preservation. `docs/spec/strata-storage-format-v1.md` now records the whole-table contiguous-layout and cross-block validation rules, and `docs/architecture/archive/implementation-plans/m3-porting-log.md` records the source map and retirement disposition. Review follow-up fixed the remaining M3G3 gaps by adding direct tests for index cross-fact drift and properties fact drift, plus early scalar preflight for zero target data-block size. Sensitivity probes: temporarily ignored index entry block-offset drift would fail `immutable_table_rejects_index_entries_that_do_not_match_actual_data_frames`; temporarily skipped data-region hidden-byte validation would fail `immutable_table_rejects_hidden_bytes_between_regions`; temporarily skipped derived-properties comparison would fail `immutable_table_rejects_properties_block_fact_drift_after_valid_crcs`. Narrow verification: `cargo test -p strata-storage --locked --lib format::table`, `cargo test -p strata-storage --no-default-features --locked --lib format::table`, and `cargo test -p strata-storage --all-features --locked --lib format::table`. Broad verification: `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo fmt --package strata-storage --check`, and `git diff --check`. |
| `M3G4` | Complete | `crates/storage/src/format/table/golden_tests.rs`, `tests/format_golden.rs`, and `testdata/goldens/storage-format-v1/` pin seven table vectors: one-put data frame, put-plus-tombstone data frame, zstd data frame, monolithic index payload, properties payload, one-block table artifact, and two-block table artifact. `src/format/fuzzing.rs`, `src/testkit/format_fuzz.rs`, `fuzz/Cargo.toml`, `fuzz/fuzz_targets/format_table_block.rs`, `format_table_artifact.rs`, and checked-in corpus seeds route arbitrary bytes through strict table block and whole-table decoders. `src/format/table/property_tests.rs` and `tests/table_properties.rs` replace the placeholder table harness with generated storage-row-native table artifact properties over uncompressed and zstd paths, including duplicate physical keys at distinct commit versions, independent commit/key-range assertions, and deterministic 128/129-row boundary coverage. `tests/table_format_source_guard.rs` rejects product-payload vocabulary and engine-crate imports on the table format surface while allowing `StorageSpaceId::engine` as an opaque row atom. `docs/spec/strata-storage-format-v1.md`, fuzz READMEs, the golden README, and the porting log now agree on the final M3G table byte shape. Sensitivity probes: accepting partial block-frame trailing bytes would fail `format_table_block` routing and the block consumed-length invariant; skipping table footer CRC coverage would fail complete table golden checks; replacing the generated table property with the old module-existence placeholder would fail `table_property_harness_is_not_a_placeholder`; adding product-payload wording to scanned table format files would fail `table_format_surfaces_do_not_use_product_payload_vocabulary`. Narrow verification: `cargo test -p strata-storage --locked --lib format::table`, `cargo test -p strata-storage --locked --test format_golden`, `cargo test -p strata-storage --locked --test table_format_source_guard`, `cargo test -p strata-storage --features testkit --locked --test table_properties`, `cargo +nightly fuzz run format_table_block -- -runs=4096`, and `cargo +nightly fuzz run format_table_artifact -- -runs=2048`. Broad verification also includes no-default/all-features table suites, clippy, wasm/no-default check, fmt, and diff whitespace gates. |
| `M3TC4A` | Complete | `crates/storage/src/format/quarantine/codec_exhaustive_tests.rs` adds quarantine inventory codec hardening without further expanding the oversized codec module. `crates/storage/fuzz/corpus/format_quarantine/` now seeds the fuzz target with the empty and multi-entry golden inventory bytes. Suite cases covered: durable decode rejects object-id separators, reserved inventory object ids, object ids valid as components but too long after full quarantine path assembly, source objects under quarantine, source objects in unknown families, duplicate object ids, duplicate source objects, and text-branch parsing confusion; durable inventory bytes store `BranchId` as raw 16 bytes, so invalid UUID text remains a request-builder/core atom concern rather than a codec rule. Existing M3E4A codec tests and goldens cover empty/single/multi-entry round trips, canonical ordering, bad magic, future/pre-V1 versions, checksum mismatch, truncation families, trailing bytes, invalid codec ids, empty/invalid/overlong object ids, invalid source objects, oversized counts, old `STRAQRTN` rejection, and the empty and multi-entry golden vectors. Sensitivity probe: temporarily bypassed duplicate object-id validation; `inventory_rejects_duplicate_object_ids`, `inventory_constructor_still_rejects_duplicate_object_ids`, and `decode_rejects_duplicate_object_ids_in_durable_bytes` failed by accepting duplicate entries, then passed after revert. Narrow verification: `cargo test -p strata-storage --locked format::quarantine`, `cargo test -p strata-storage --locked quarantine_inventory`, `cargo test -p strata-storage --locked duplicate_object_ids`, `cargo test -p strata-storage --features testkit --locked format_fuzz_decoders_reject_empty_input_without_panicking`, `cargo test -p strata-storage --locked reserved_layout_names_stay_owned_by_layout_module`, and `cd crates/storage && cargo +nightly fuzz run format_quarantine -- -runs=4096`. Broad verification: `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo test -p strata-storage --all-features --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo doc -p strata-storage --no-deps --locked`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo fmt --package strata-storage --check`, and `git diff --check`. |
| `M3TC4B` | Complete | `crates/storage/src/service/quarantine/tests/inventory.rs` adds adversarial inventory-service coverage without further expanding the oversized parent quarantine test module. Suite cases covered: optional inventory load returns `None` for absence while required load returns `Missing` and default load synthesizes an absent-as-empty inventory; corrupt durable inventory bytes fail closed through optional, default, and required load paths instead of becoming empty state; inventory publication preflights both `DurablePublish` and `DurableSync` before backend publish; and `VisibilityUnknown` publication returns a typed publish error without write facts in both old-inventory-still-visible and replacement-inventory-visible outcomes. Existing M3E4B service tests cover read-capability preflight, backend read failures, database/branch/codec mismatch errors, missing and existing durable replace, explicit empty-inventory publication, memory-backend durable rejection, all five publish-failure kinds, no-visible old-byte preservation, visible-but-unconfirmed replacement bytes, and result facts for object name, branch id, entry count, byte count, and publish outcome. Sensitivity probe: temporarily treated decode errors as `Ok(None)` in optional inventory load; `corrupt_inventory_bytes_are_never_treated_as_empty` failed by accepting corrupt inventory as synthesized empty state, then passed after revert. Narrow verification: `cargo test -p strata-storage --locked service::quarantine::tests::inventory`, `cargo test -p strata-storage --locked service::quarantine`, `cargo test -p strata-storage --no-default-features --locked service::quarantine`, `cargo test -p strata-storage --all-features --locked service::quarantine`, and `cargo test -p strata-storage --locked reserved_layout_names_stay_owned_by_layout_module`. Broad verification: `cargo test -p strata-storage --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo fmt --package strata-storage --check`, `git diff --check`, and engineering scans for forbidden names, roadmap labels, raw reserved path literals, production panics, and file-size thresholds. |
| `M3TC4C` | Complete | `crates/storage/src/service/quarantine/tests/mutation/request.rs`, `mutation.rs`, and `mutation/purge.rs` add adversarial quarantine-object and purge coverage. Suite cases covered: safe gate reaches backend access; non-safe gates, empty object id, malformed object id, reserved inventory object id, quarantine-family source objects, unknown-family source objects, epoch timestamp without explicit permission, existing unlisted quarantine copies, and corrupt current inventory all fail before mutation; source bytes are read once, metadata byte count is persisted, inventory publication precedes quarantine object publication, and source deletion happens only after durable quarantine copy success; inventory publish uncertainty does not copy or delete source even when the inventory object becomes visible; quarantine object publish failures leave source recoverable and reconcile as missing or clean according to copy visibility; source delete failure and `NotFound` races return distinct report states; purge deletes only inventory-listed objects, leaves adjacent/unlisted objects alone, reports deleted/already-missing/failed outcomes deterministically, rewrites empty or retained inventory correctly, and preserves delete facts when the inventory rewrite publish fails. The test plan now records that branch-id request validation is a typed `BranchId` concern; malformed durable branch path text belongs to reconciliation tests. Sensitivity probe: temporarily deleted the source before durable quarantine object publish; `quarantine_publishes_inventory_then_copy_then_deletes_source` failed with `SourceAlreadyMissingAfterPublish`, then passed after revert. Narrow verification: `cargo test -p strata-storage --locked service::quarantine::tests::mutation`, `cargo test -p strata-storage --locked quarantine_publishes_inventory_then_copy_then_deletes_source`, `cargo test -p strata-storage --locked service::quarantine`, `cargo test -p strata-storage --no-default-features --locked service::quarantine`, and `cargo test -p strata-storage --all-features --locked service::quarantine`. Broad verification: `cargo test -p strata-storage --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo fmt --package strata-storage --check`, `git diff --check`, and engineering scans for forbidden names, roadmap labels, raw reserved path literals, production panics, and file-size thresholds. |
| `M3TC4D` | Complete | `crates/storage/src/testkit/quarantine_fuzz.rs`, `testkit/quarantine_fuzz/backend.rs`, and `testkit/quarantine_fuzz/operation.rs` add a shared quarantine service script runner for property tests and cargo-fuzz. The runner decodes bounded 8-byte operations, caps histories at 96 operations and aggregate generated payloads at 64 KiB, models branch ids as canonical bytes for deterministic maps, covers seed source, quarantine, purge, corrupt inventory, insert unlisted object, delete quarantine object, reconcile, and load operations, and asserts backend visibility plus reconciliation kind/recovery class after every operation. `crates/storage/src/service/quarantine/tests/reconcile_property.rs` runs the runner under proptest with checked-in regression persistence only when a failing seed exists, and `crates/storage/fuzz/fuzz_targets/service_quarantine.rs`, `crates/storage/fuzz/Cargo.toml`, and `fuzz/corpus/service_quarantine/basic` add the service fuzz target. The oversized runner was split into backend and bytecode modules, and comments document the recovery model, ambiguous publish visibility, read-only reconciliation invariant, branch-key choice, and severity ordering. Sensitivity probe: temporarily ignored unlisted quarantine objects during production classification; `reconcile_unlisted_quarantine_object_is_policy_downgrade` failed by returning `CleanInventory`, then passed after revert. Narrow verification: `cargo test -p strata-storage --features testkit --locked quarantine_service_script`, `PROPTEST_CASES=2048 cargo test -p strata-storage --locked quarantine_service_state_machine_preserves_recovery_invariants`, `cd crates/storage && cargo +nightly fuzz run service_quarantine -- -runs=2048`, and `cargo test -p strata-storage --locked reconcile_unlisted_quarantine_object_is_policy_downgrade`. Broad verification: `cargo test -p strata-storage --locked service::quarantine`, `cargo test -p strata-storage --features testkit --locked`, `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo test -p strata-storage --all-features --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo doc -p strata-storage --no-deps --locked`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo fmt --package strata-storage --check`, `git diff --check`, and engineering scans for forbidden names, roadmap labels, raw reserved path literals, production panics, and file-size thresholds. |
| `M3TD1` | Complete | `crates/storage/src/service/cache_mode_absence_tests.rs` and `cache_mode_absence_tests/support.rs` add an observing memory/cache backend plus a durable-family scanner that fails on new database manifest, table, WAL, WAL sidecar, snapshot, checkpoint fact, quarantine, lock, metadata, or temporary object families. The suite proves cache mode does not require metadata, append, durable publish, durable sync, or writer-lock capabilities; non-durable publication writes only the caller-requested cache object and returns non-durable facts; direct database/table manifest, WAL, sidecar, snapshot, checkpoint, quarantine mutation, purge, and writer-lock paths reject cache backends before durable mutation; explicit quarantine reconciliation remains read-only; and stale durable temporary objects are not interpreted as cache recovery state. Checkpoint coverage note: the real cache backend rejects the first durable active-WAL manifest update, so the checkpoint-internal snapshot-publish failure branch is intentionally N/A here and remains covered by M3TC3 durable checkpoint sequencing tests. `CACHE_MODE_REQUIREMENTS` now matches the browser-like contract by excluding `ObjectMetadata`. Lifecycle deferral: storage still has no cache open/commit/checkpoint/maintenance entry point, so future L8/L9 lifecycle slices must prove the same durable families absent at that boundary. Sensitivity probe: temporarily bypassed `ObjectPublisher::publish_durable` capability preflight; `cache_backend_rejects_durable_manifest_and_table_publication_before_mutation` failed on a recorded backend publish attempt, then passed after revert. Narrow verification: `cargo test -p strata-storage --locked cache_mode_absence`, `cargo test -p strata-storage --features testkit --locked cache_mode_absence`, and `cargo test -p strata-storage --no-default-features --locked cache_mode_absence`. Broad verification: `cargo test -p strata-storage --locked`, `cargo test -p strata-storage --no-default-features --locked`, `cargo clippy -p strata-storage --all-targets --all-features --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, `cargo doc -p strata-storage --no-deps --locked`, `cargo fmt --package strata-storage --check`, and `git diff --check`. |
| `M3TE1A` | Complete | `crates/storage/src/service/manifest.rs` adds adversarial load/capability hardening for manifest services: non-`NotFound` read failures stay typed as `ManifestServiceError::Read`, codec-validating load preserves backend read failures, memory durable publish failures assert object and source kind, localfs database and table manifest publication assert exact stored bytes, and the publish-fault backend can emit all five `PublishFailureKind` values. Suite cases covered include M3E1 manifest test-suite construction/capability cases, load cases for missing and backend failure, codec-load missing/backend-failure routing, and localfs durable publication byte checks. Sensitivity probe: temporarily routed non-`NotFound` read failures to `Ok(None)`; `database_manifest_load_current_preserves_backend_read_failure` failed, then the mutation was reverted. Narrow verification: `cargo test -p strata-storage --locked database_manifest_load_current_preserves_backend_read_failure` and `cargo test -p strata-storage --locked service::manifest`. Broad verification: `cargo test -p strata-storage --no-default-features --locked service::manifest`, `cargo test -p strata-storage --locked`, and `cargo clippy -p strata-storage --all-targets --locked -- -D warnings`. |
| `M3TE1B` | Complete | `crates/storage/src/service/manifest.rs` adds a durable recording backend for manifest tests, a database manifest model, a host-only `proptest` update-sequence property over 1 to 64 operations, and a raw `publish_current` test proving caller-supplied manifests replace current facts without preservation. The model asserts returned write facts, stored bytes, fresh loads, active WAL segment, snapshot facts, flush watermark, database id, and codec id after every successful update; rejected zero-fact updates assert exact `InvalidRecoveryFact` fields and byte preservation. The `proptest` dev-dependency is gated off wasm so the cache-only wasm compile path remains clean. Sensitivity probe: temporarily changed flush-watermark persistence to drop snapshot facts; `database_manifest_state_machine_preserves_recovery_facts` failed with the minimal sequence snapshot-facts then flush-watermark, then the mutation and probe-only regression seed were removed. Narrow verification: `PROPTEST_CASES=2048 cargo test -p strata-storage --locked database_manifest_state_machine_preserves_recovery_facts`, `cargo test -p strata-storage --locked service::manifest`, and `cargo test -p strata-storage --no-default-features --locked service::manifest`. Broad verification: `cargo test -p strata-storage --locked`, `cargo clippy -p strata-storage --all-targets --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, and `cargo hack -p strata-storage --feature-powerset --depth 2 --locked check --all-targets`. |
| `M3TE1C` | Complete | `crates/storage/src/service/manifest.rs` adds recovery-fact and codec-validation hardening for manifest services. New tests route invalid codec ids through `ManifestServiceError::Encode` before publish, reject malformed recovery fact bytes as `Decode` with the exact `FormatError::InvalidValue` field, accept valid boundary recovery facts at `1` and `u64::MAX`, prove invalid recovery facts fail before backend access, and distinguish matching codec loads, codec mismatches, corrupt bytes, missing manifests, and backend read failures. Sensitivity probes: temporarily mapped invalid codec errors to `InvalidRecoveryFact`, rejected `u64::MAX` active WAL segments, and mapped corrupt codec-validating loads to `CodecMismatch`; the targeted tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage --locked service::manifest` and `cargo test -p strata-storage --no-default-features --locked service::manifest`. Broad verification: `cargo test -p strata-storage --locked`, `cargo clippy -p strata-storage --all-targets --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, and `cargo hack -p strata-storage --feature-powerset --depth 2 --locked check --all-targets`. |
| `M3TE1D` | Complete | `crates/storage/src/service/manifest.rs` adds the manifest publish-failure matrix for database/table roles, create/replace intents, and all five `PublishFailureKind` values. The matrix asserts exact `ManifestServiceError::Publish` role, object name, failure kind, and backend source kind; replace-path cases use a stored-byte fault backend to prove prior bytes remain authoritative for no-visible-replacement failures. Sensitivity probes: temporarily collapsed database publish failures to `Unsupported`, wrote replacement bytes before returning `FailedBeforeVisibility`, and mislabeled table publish failures as database errors; the targeted tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage --locked publish`, `cargo test -p strata-storage --locked service::manifest`, and `cargo test -p strata-storage --no-default-features --locked service::manifest`. Broad verification: `cargo test -p strata-storage --locked`, `cargo clippy -p strata-storage --all-targets --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, and `cargo hack -p strata-storage --feature-powerset --depth 2 --locked check --all-targets`. |
| `M3TE1E` | Complete | `crates/storage/src/service/manifest.rs` adds the table-manifest opacity matrix, create/replace opaque-byte semantics, invalid branch-id layout matrix, and role/object/field precision coverage for reachable manifest service error families. Table payload tests cover empty bytes, arbitrary binary bytes, corrupt database-manifest-looking bytes, WAL-looking bytes, embedded NUL bytes, and large payloads without decoding. Layout tests distinguish component-too-long from assembled-object-too-long and prove invalid branch IDs fail before backend access. Sensitivity probes: temporarily decoded table payloads as database manifests, mislabeled table layout errors as database role, and collapsed assembled object-name overflow into component overflow; the targeted tests failed under each mutation and passed after revert. Narrow verification: `cargo test -p strata-storage --locked table`, `cargo test -p strata-storage --locked service::manifest`, and `cargo test -p strata-storage --no-default-features --locked service::manifest`. Broad verification: `cargo test -p strata-storage --locked`, `cargo clippy -p strata-storage --all-targets --locked -- -D warnings`, `cargo check -p strata-storage --no-default-features --target wasm32-unknown-unknown --all-targets --locked`, and `cargo hack -p strata-storage --feature-powerset --depth 2 --locked check --all-targets`. |
| `M3H1` | Complete | `crates/storage/src/service/table.rs` adds the L4 immutable table object publication service that L5 can call after producing whole-table bytes. The service owns `ObjectLayout::table_object(branch, level, table_id)` naming, preflights durable publish/sync capability before table decode, validates supplied bytes with the V1 immutable-table decoder before durable publication, publishes create-only through `ObjectPublisher::publish_durable_create`, preserves all five `PublishFailureKind` values, verifies returned publish object, size, and durability metadata, and returns storage-mechanical table facts: object name, byte count, row count, data-block count, and commit range. `crates/storage/src/service/mod.rs` exports the service for later table-runtime consumers, and `crates/storage/src/service/cache_mode_absence_tests.rs` now proves cache mode rejects durable table object publication before mutation alongside table manifest publication. Narrow verification: `cargo test -p strata-storage --locked service::table` and `cargo test -p strata-storage --locked cache_mode_absence`. |
| `M3TG1` | Complete | `crates/storage/src/testkit/integration_harness.rs` adds hidden testkit entry points for durable local reopen recovery, bounded generated stress scripts, and L4 service publish-fault windows without making crate-private services public API. `tests/crash_recovery.rs` now runs by default under `testkit,localfs`, publishes snapshot, sidecar, and immutable table bytes to LocalFS, reopens the backend, and reloads/decodes each object. `tests/service_fault_windows.rs` now drives snapshot, table-object, and quarantine service paths through `FaultingBackend` publish failures and asserts no before-visibility bytes/source deletion leak. `tests/stress.rs` now runs deterministic bounded scripts over snapshot service fuzzing, quarantine service fuzzing, and generated table artifacts; the seed and optional duration still come from harness environment variables. `tests/README.md` now documents the non-ignored commands. Narrow verification: `cargo test -p strata-storage --features testkit,fault-injection --locked --test service_fault_windows --test crash_recovery --test stress -- --test-threads=1` and `cargo test -p strata-storage --no-default-features --features testkit --locked --test stress`. |

## Slice Update Protocol

When a slice starts:

1. Add the slice code and title to the owning milestone implementation plan if it
   is not already listed.
2. Add or update a tracker row if the slice changes milestone gate status.
3. Link the slice to any open questions it closes.
4. Link the slice to the test-track work that proves it.

When a slice closes:

1. Record the verification commands in the PR or final implementation note.
2. Mark the slice `Complete` in the issue/PR tracker.
3. Update this file only if the slice changes an epic or milestone status.
4. Do not add completed slice codes to production names or user-facing docs.

## Next Work

The current implementation milestone is `M4`. Start storage table,
branch, commit, lifecycle recovery, maintenance, and L9 API work.

`M7` may also begin now if inference work should run in parallel with the
storage path.
