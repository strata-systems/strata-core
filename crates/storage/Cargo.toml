[package]
name = "strata-storage"
version.workspace = true
description = "Branch-aware MVCC storage substrate for the Strata database"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[features]
default = ["localfs"]
localfs = ["dep:fs2"]
testkit = []
fault-injection = ["testkit"]
perf-trace = []

[dependencies]
arc-swap = { workspace = true }
crc32fast = { workspace = true }
parking_lot = { workspace = true }
serde = { workspace = true }
sha2 = { workspace = true }
strata-core = { path = "../core" }
tracing = { workspace = true }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
fs2 = { workspace = true, optional = true }
zstd = { workspace = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
web-time = { workspace = true }
zstd-pure-rs = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
# mutation_partition_guard.rs parses the cargo-mutants lane configs.
toml = { workspace = true }

[target.'cfg(not(target_arch = "wasm32"))'.dev-dependencies]
proptest = { workspace = true }

# TCP4.3 schedule-exploration lane: only compiled under RUSTFLAGS="--cfg loom".
[target.'cfg(loom)'.dependencies]
loom = "0.7"

[lints]
workspace = true
